---
phase: 01-blindaje-de-datos-y-separaci-n-debug-usuario
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - lib/presentation/pages/home_page_impl.dart
autonomous: true
requirements: [UI-01, DATA-01]

must_haves:
  truths:
    - "5 taps sobre el título 'Cingula' alternan entre modo debug y modo usuario, con un SnackBar de confirmación"
    - "En modo usuario no se ve el DiagnosticsPanel (ni su acceso a DataBrowserPage) ni el banner de grabación de micrófono"
    - "En modo usuario siguen visibles el control de monitoreo, la card de audio activo y el botón flotante Iniciar/Detener"
    - "Si hubo una recuperación de base de datos, la app arranca normalmente y muestra un MaterialBanner no bloqueante que indica dónde quedó la copia"
  artifacts:
    - path: "lib/presentation/pages/home_page_impl.dart"
      provides: "HomePage con rama debug/usuario, gesto oculto y banner de recuperación"
      contains: "AppModeConfig.isDebugMode"
  key_links:
    - from: "lib/presentation/pages/home_page_impl.dart"
      to: "AppModeConfig"
      via: "lectura en build() y escritura desde el callback del gesto"
      pattern: "AppModeConfig\\.(isDebugMode|setDebugMode)"
    - from: "lib/presentation/pages/home_page_impl.dart"
      to: "HiddenTapGesture"
      via: "envuelve el título del AppBar"
      pattern: "HiddenTapGesture\\("
    - from: "lib/presentation/pages/home_page_impl.dart"
      to: "AppDatabase.lastRecoveryEvent"
      via: "getIt<AppDatabase>() leído en addPostFrameCallback"
      pattern: "lastRecoveryEvent"
    - from: "lib/presentation/pages/home_page_impl.dart"
      to: "UserModeView"
      via: "rama else de isDebugMode"
      pattern: "UserModeView\\("
---

<objective>
Cablear en `HomePage` las piezas creadas en los planes 01-01 y 01-02: el gesto oculto de 5 taps sobre el título del AppBar que alterna debug/usuario, la rama condicional que oculta `DiagnosticsPanel` y `_RecordingBanner` en modo usuario, y el `MaterialBanner` no bloqueante que informa una recuperación de base de datos tras el primer frame.

Purpose: es el punto donde UI-01 se vuelve observable (el usuario puede pasar a modo usuario final sin rebuild) y donde DATA-01 se vuelve visible (el aviso de que hubo un problema y dónde quedó la copia).
Output: `home_page_impl.dart` convertido a `StatefulWidget` con las dos ramas de UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-CONTEXT.md
@.planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-UI-SPEC.md
@.planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-01-SUMMARY.md
@.planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-02-SUMMARY.md

<interfaces>
<!-- Todo lo que este plan consume ya existe tras 01-01 y 01-02. -->

```dart
// lib/core/config/app_mode_config.dart  (plan 01-02)
class AppModeConfig {
  static bool isDebugMode;                    // default true
  static Future<void> loadFromPrefs();        // ya llamada desde AppInitializer
  static Future<void> setDebugMode(bool value);
}

// lib/presentation/widgets/hidden_tap_gesture.dart  (plan 01-02)
class HiddenTapGesture extends StatefulWidget {
  const HiddenTapGesture({super.key, required this.child, required this.onActivated});
}

// lib/presentation/pages/home/widgets/user_mode_view.dart  (plan 01-02)
class UserModeView extends StatelessWidget {
  const UserModeView({super.key, required this.isMonitoring, required this.statusMessage,
    required this.currentAsset, required this.onMonitoringChanged});
}

// lib/data/datasources/local/db_recovery.dart  (plan 01-01)
class DatabaseRecoveryEvent {
  final String backupPath;
  final DateTime occurredAt;
  String get backupFileName;   // 'cingula.db.corrupto-20260808-131003'
}

// lib/data/datasources/local/app_database.dart  (plan 01-01)
class AppDatabase {
  DatabaseRecoveryEvent? lastRecoveryEvent;   // mutable: la UI lo limpia tras mostrarlo
}

// Ya existentes
class PlaybackNotifier extends ChangeNotifier {
  AudioAsset? get currentAsset;
  bool get isMonitoring;
  String? get statusMessage;
  Future<void> startMonitoring();
  Future<void> stopMonitoring();
}
T getIt<T extends Object>();                 // lib/core/di/service_locator.dart
class RecorderService { ... }                // lib/core/services/recorder_service.dart
class DiagnosticsPanel extends StatelessWidget/StatefulWidget { const DiagnosticsPanel({super.key}); }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HomePage stateful con gesto oculto y rama debug/usuario</name>

  <files>lib/presentation/pages/home_page_impl.dart</files>

  <read_first>
    - lib/presentation/pages/home_page_impl.dart — archivo completo: el `build()` actual (líneas 17-64) y `_RecordingBanner` (líneas 69-165), que NO se modifica, solo se condiciona
    - lib/presentation/pages/home/widgets/user_mode_view.dart — firma exacta del constructor creado en 01-02
    - lib/presentation/widgets/hidden_tap_gesture.dart — firma exacta del widget creado en 01-02
    - lib/core/config/app_mode_config.dart — API estática creada en 01-02
    - .planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-UI-SPEC.md sección "Copywriting Contract" — copy exacto del SnackBar de modo y tipografía Display 22/700 del título
    - .planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-CONTEXT.md — D-07, D-10, D-11
  </read_first>

  <action>
    Convertir `HomePage` de `StatelessWidget` a `StatefulWidget` (hace falta `setState` tras alternar el modo, y un `initState` para el banner de recuperación de la Task 2).

    1. Agregar imports:
    ```dart
    import '../../core/config/app_mode_config.dart';
    import '../widgets/hidden_tap_gesture.dart';
    import 'home/widgets/user_mode_view.dart';
    ```

    2. Esqueleto:
    ```dart
    class HomePage extends StatefulWidget {
      const HomePage({super.key});

      @override
      State<HomePage> createState() => _HomePageState();
    }

    class _HomePageState extends State<HomePage> {
      Future<void> _toggleMode() async {
        final next = !AppModeConfig.isDebugMode;
        await AppModeConfig.setDebugMode(next);
        if (!mounted) return;
        setState(() {});
        ScaffoldMessenger.of(context).showSnackBar(
          SnackBar(
            content: Text(next ? 'Modo debug activado' : 'Modo usuario activado'),
            duration: const Duration(seconds: 2),
          ),
        );
      }
      ...
    }
    ```
    Las cadenas `'Modo debug activado'` / `'Modo usuario activado'` y los 2 segundos están fijados por `01-UI-SPEC.md` — reproducirlas textualmente.

    3. `build()`:
    ```dart
    @override
    Widget build(BuildContext context) {
      final notifier = context.watch<PlaybackNotifier>();
      final isDebug = AppModeConfig.isDebugMode;
      final currentAsset = notifier.currentAsset;
      final status = notifier.statusMessage;

      return Scaffold(
        appBar: AppBar(
          title: HiddenTapGesture(
            onActivated: _toggleMode,
            child: const Text(
              'Cingula',
              style: TextStyle(fontSize: 22, fontWeight: FontWeight.bold, height: 1.2),
            ),
          ),
        ),
        body: isDebug ? _buildDebugBody(context, notifier) : _buildUserBody(notifier),
        floatingActionButton: FloatingActionButton.extended(
          onPressed: notifier.isMonitoring ? notifier.stopMonitoring : notifier.startMonitoring,
          label: Text(notifier.isMonitoring ? 'Detener' : 'Iniciar'),
          icon: Icon(notifier.isMonitoring ? Icons.stop : Icons.play_arrow),
        ),
      );
    }
    ```
    El FAB queda FUERA de la rama: es visible en los dos modos (D-11). El título usa Display 22/700 del UI-SPEC.

    4. `_buildUserBody` (modo usuario — D-10 oculta `DiagnosticsPanel` y `_RecordingBanner`, D-11 conserva switch + card de audio + FAB):
    ```dart
    Widget _buildUserBody(PlaybackNotifier notifier) {
      return UserModeView(
        isMonitoring: notifier.isMonitoring,
        statusMessage: notifier.statusMessage,
        currentAsset: notifier.currentAsset,
        onMonitoringChanged: (v) =>
            v ? notifier.startMonitoring() : notifier.stopMonitoring(),
      );
    }
    ```

    5. `_buildDebugBody` — mover ahí el `Stack`/`SingleChildScrollView` actual TAL CUAL (mismo `padding: EdgeInsets.fromLTRB(16, 16, 16, 96)`, mismo `SwitchListTile` con su `subtitle`, misma card de audio con `showAudioCard`, mismo `SizedBox(height: 16)` + `const DiagnosticsPanel()`, mismo `_RecordingBanner(recorder: recorder)`). Cambio único: mover `final recorder = getIt<RecorderService>();` desde el `build()` al cuerpo de `_buildDebugBody`, para que el modo usuario no dependa de que `RecorderService` esté registrado en `getIt`.

    No cambiar `_RecordingBanner` ni ninguno de sus estilos (sus `Colors.red.*` hardcodeados son debug-only y están explícitamente fuera del contrato de color del UI-SPEC).
  </action>

  <verify>
    <automated>flutter analyze lib/presentation/pages/home_page_impl.dart</automated>
    <automated>flutter test</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "class HomePage extends StatefulWidget" lib/presentation/pages/home_page_impl.dart` devuelve una línea.
    - `grep -n "HiddenTapGesture(" lib/presentation/pages/home_page_impl.dart` devuelve una línea.
    - `grep -n "AppModeConfig.setDebugMode" lib/presentation/pages/home_page_impl.dart` devuelve una línea.
    - `grep -n "AppModeConfig.isDebugMode" lib/presentation/pages/home_page_impl.dart` devuelve al menos una línea.
    - `grep -n "Modo debug activado" lib/presentation/pages/home_page_impl.dart` y `grep -n "Modo usuario activado" lib/presentation/pages/home_page_impl.dart` devuelven una línea cada uno.
    - `grep -n "UserModeView(" lib/presentation/pages/home_page_impl.dart` devuelve una línea.
    - `grep -n "DiagnosticsPanel()" lib/presentation/pages/home_page_impl.dart` devuelve exactamente una línea, y está dentro de `_buildDebugBody`.
    - `grep -n "_RecordingBanner(recorder" lib/presentation/pages/home_page_impl.dart` devuelve exactamente una línea, y está dentro de `_buildDebugBody`.
    - `grep -n "getIt<RecorderService>()" lib/presentation/pages/home_page_impl.dart` devuelve exactamente una línea, y está dentro de `_buildDebugBody` (no en `build`).
    - `grep -n "FloatingActionButton.extended" lib/presentation/pages/home_page_impl.dart` devuelve una línea, fuera de ambas ramas.
    - `flutter analyze` sin errores; `flutter test` verde.
  </acceptance_criteria>

  <done>5 taps sobre el título alternan modo y muestran el SnackBar correspondiente; en modo usuario se renderiza `UserModeView` y desaparecen `DiagnosticsPanel` y `_RecordingBanner`; el FAB Iniciar/Detener sigue en ambos modos.</done>
</task>

<task type="auto">
  <name>Task 2: Banner no bloqueante de recuperación de base de datos</name>

  <files>lib/presentation/pages/home_page_impl.dart</files>

  <read_first>
    - lib/presentation/pages/home_page_impl.dart — el `_HomePageState` creado en la Task 1
    - lib/data/datasources/local/db_recovery.dart — `DatabaseRecoveryEvent.backupFileName`
    - lib/data/datasources/local/app_database.dart — campo `lastRecoveryEvent`
    - .planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-UI-SPEC.md fila "Error state" del Copywriting Contract — copy exacto del banner
    - .planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-RESEARCH.md "Pitfall 4" — por qué el banner se muestra desde `HomePage` y no desde `AppDatabase`/`AppInitializer`
  </read_first>

  <action>
    Agregar a `_HomePageState`:

    ```dart
    @override
    void initState() {
      super.initState();
      WidgetsBinding.instance.addPostFrameCallback((_) => _showRecoveryBannerIfNeeded());
    }

    void _showRecoveryBannerIfNeeded() {
      final db = getIt<AppDatabase>();
      final event = db.lastRecoveryEvent;
      if (event == null || !mounted) return;
      // One-shot: limpiamos para que no reaparezca al volver a esta pantalla.
      db.lastRecoveryEvent = null;

      final cs = Theme.of(context).colorScheme;
      final messenger = ScaffoldMessenger.of(context);
      messenger.showMaterialBanner(
        MaterialBanner(
          backgroundColor: cs.errorContainer,
          content: Column(
            crossAxisAlignment: CrossAxisAlignment.start,
            mainAxisSize: MainAxisSize.min,
            children: [
              Text(
                'Base de datos restaurada automáticamente',
                style: TextStyle(
                  fontSize: 20,
                  fontWeight: FontWeight.bold,
                  height: 1.2,
                  color: cs.onErrorContainer,
                ),
              ),
              const SizedBox(height: 8),
              Text(
                'Detectamos un problema al abrir tus datos. Guardamos una copia en '
                '${event.backupFileName} y la app sigue funcionando con normalidad.',
                style: TextStyle(fontSize: 16, height: 1.5, color: cs.onErrorContainer),
              ),
            ],
          ),
          actions: [
            TextButton(
              onPressed: messenger.hideCurrentMaterialBanner,
              child: const Text('Entendido'),
            ),
          ],
        ),
      );
    }
    ```

    Agregar el import `import '../../data/datasources/local/app_database.dart';` (el import de `service_locator.dart` para `getIt` ya existe en el archivo).

    Requisitos duros (D-02):
    - Debe ser `showMaterialBanner`, NUNCA `showDialog`: la app tiene que seguir usable si esto ocurre a mitad de una grabación en campo.
    - Se muestra en los dos modos (debug y usuario): es un aviso de integridad de datos, no una herramienta de debug.
    - El copy es el fijado por el UI-SPEC — reproducir textualmente heading, cuerpo y la acción `'Entendido'`, incluidos acentos.
    - `backgroundColor: cs.errorContainer` con texto `cs.onErrorContainer` (rol destructivo del UI-SPEC en su pareja Material 3 correcta). Nada de `Colors.red`.
  </action>

  <verify>
    <automated>flutter analyze lib/presentation/pages/home_page_impl.dart</automated>
    <automated>flutter test</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "addPostFrameCallback" lib/presentation/pages/home_page_impl.dart` devuelve una línea.
    - `grep -n "showMaterialBanner" lib/presentation/pages/home_page_impl.dart` devuelve una línea.
    - `grep -cn "showDialog" lib/presentation/pages/home_page_impl.dart` devuelve `0`.
    - `grep -n "Base de datos restaurada automáticamente" lib/presentation/pages/home_page_impl.dart` devuelve una línea.
    - `grep -n "Detectamos un problema al abrir tus datos" lib/presentation/pages/home_page_impl.dart` devuelve una línea.
    - `grep -n "Entendido" lib/presentation/pages/home_page_impl.dart` devuelve una línea.
    - `grep -n "lastRecoveryEvent = null" lib/presentation/pages/home_page_impl.dart` devuelve una línea (one-shot).
    - `grep -n "errorContainer" lib/presentation/pages/home_page_impl.dart` devuelve al menos una línea.
    - `flutter analyze` sin errores; `flutter test` verde.
  </acceptance_criteria>

  <done>Tras una recuperación de base, la app arranca normal y muestra una sola vez un `MaterialBanner` descartable con el nombre del archivo de respaldo. Sin recuperación, no aparece nada.</done>
</task>

</tasks>

<verification>
- `flutter analyze` sin errores.
- `flutter test` verde (la suite existente debe seguir pasando tras convertir `HomePage` a `StatefulWidget`).
- `grep -n "DiagnosticsPanel()\|_RecordingBanner(recorder" lib/presentation/pages/home_page_impl.dart` → ambas ocurrencias dentro de `_buildDebugBody`.
- `grep -c "showDialog" lib/presentation/pages/home_page_impl.dart` → 0.
</verification>

<success_criteria>
1. El toggle debug/usuario se activa con 5 taps sobre el título del AppBar, sin chrome visible, y confirma con un SnackBar de 2 segundos (UI-01, D-07).
2. En modo usuario no se renderizan `DiagnosticsPanel` (ni su acceso a `DataBrowserPage`) ni `_RecordingBanner`; sí siguen el control de monitoreo, la card de audio y el FAB (UI-01, D-10, D-11).
3. Una recuperación de base de datos se informa con un `MaterialBanner` no bloqueante que nombra el archivo de respaldo (DATA-01, D-02).
4. `flutter analyze` limpio y `flutter test` verde.
</success_criteria>

<output>
Al completar, crear `.planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-03-SUMMARY.md`.
</output>
