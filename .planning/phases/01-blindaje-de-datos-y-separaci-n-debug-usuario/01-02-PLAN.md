---
phase: 01-blindaje-de-datos-y-separaci-n-debug-usuario
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/config/app_mode_config.dart
  - lib/core/app_initializer.dart
  - lib/presentation/widgets/hidden_tap_gesture.dart
  - lib/presentation/pages/home/widgets/user_mode_view.dart
  - test/core/config/app_mode_config_test.dart
  - test/presentation/widgets/hidden_tap_gesture_test.dart
  - test/presentation/pages/home/widgets/user_mode_view_test.dart
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "El modo de la app (debug/usuario) persiste entre reinicios"
    - "En una instalación nueva, o si falla la lectura de preferencias, la app arranca en modo debug"
    - "5 toques dentro de 3 segundos sobre un elemento disparan la activación; toques más espaciados no acumulan"
    - "La vista de modo usuario muestra el control de monitoreo, el estado, y la card de audio solo cuando hay audio activo"
  artifacts:
    - path: "lib/core/config/app_mode_config.dart"
      provides: "Persistencia del toggle debug/usuario vía SharedPreferences"
      exports: ["AppModeConfig"]
      contains: "static bool isDebugMode = true"
    - path: "lib/presentation/widgets/hidden_tap_gesture.dart"
      provides: "Contador de 5 taps en ventana de 3 segundos"
      exports: ["HiddenTapGesture"]
    - path: "lib/presentation/pages/home/widgets/user_mode_view.dart"
      provides: "Cuerpo de la pantalla en modo usuario final, según 01-UI-SPEC.md"
      exports: ["UserModeView"]
    - path: "test/core/config/app_mode_config_test.dart"
      provides: "Prueba del default debug y de la persistencia"
    - path: "test/presentation/widgets/hidden_tap_gesture_test.dart"
      provides: "Prueba del conteo de taps y del vencimiento de la ventana"
    - path: "test/presentation/pages/home/widgets/user_mode_view_test.dart"
      provides: "Prueba del renderizado condicional del modo usuario"
  key_links:
    - from: "lib/core/app_initializer.dart"
      to: "AppModeConfig.loadFromPrefs()"
      via: "llamada en init(), junto a LocationConfig.loadFromPrefs()"
      pattern: "AppModeConfig\\.loadFromPrefs\\(\\)"
---

<objective>
Construir las tres piezas aisladas que necesita el toggle runtime debug/usuario: la persistencia del modo (`AppModeConfig`, espejo de `LocationConfig`), el gesto oculto de 5 taps (`HiddenTapGesture`), y la vista de modo usuario final (`UserModeView`, según el contrato visual de `01-UI-SPEC.md`). Ninguna de las tres se conecta todavía a `HomePage` — ese cableado es el plan 01-03.

Purpose: separar el dashboard de usuario final del panel de debug sin flavors de Flutter (UI-01). Aislando estas tres piezas en archivos propios quedan testeables sin montar el grafo completo de `getIt`/Provider que exige `HomePage`.
Output: 3 archivos nuevos en `lib/` + 3 archivos de test, todos verificables con `flutter test` sin plugins ni emulador.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-CONTEXT.md
@.planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-RESEARCH.md
@.planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-UI-SPEC.md

<interfaces>
<!-- Contratos que este plan crea; el plan 01-03 los consume. -->

```dart
// lib/core/config/app_mode_config.dart
class AppModeConfig {
  static bool isDebugMode;                          // default true (D-08)
  static Future<void> loadFromPrefs();
  static Future<void> setDebugMode(bool value);
}

// lib/presentation/widgets/hidden_tap_gesture.dart
class HiddenTapGesture extends StatefulWidget {
  const HiddenTapGesture({super.key, required this.child, required this.onActivated});
  final Widget child;
  final VoidCallback onActivated;
}

// lib/presentation/pages/home/widgets/user_mode_view.dart
class UserModeView extends StatelessWidget {
  const UserModeView({
    super.key,
    required this.isMonitoring,
    required this.statusMessage,      // String?
    required this.currentAsset,       // AudioAsset?
    required this.onMonitoringChanged, // ValueChanged<bool>
  });
}
```

Contratos ya existentes que se consumen:
```dart
// lib/domain/entities/audio_asset.dart
class AudioAsset {
  const AudioAsset({required this.id, required this.title, required this.artist,
    required this.description, required this.duration, required this.localPath,
    this.remoteUrl, this.uuid, this.updatedAt, this.deletedAt, this.logicalVersion});
  final int id; final String title; final String artist; final String description;
  final Duration duration; final String localPath; final Uri? remoteUrl;
}

// lib/presentation/pages/home/widgets/audio_details.dart
class AudioDetails extends StatelessWidget {
  const AudioDetails({super.key, required this.asset});
  final AudioAsset asset;
}
```

Patrón a espejar (`lib/core/config/location_config.dart`):
```dart
class LocationConfig {
  static const _kActivationRadius = 'loc_activationRadiusMeters';
  static double activationRadiusMeters = 15.0;
  static Future<void> loadFromPrefs() async {
    try {
      final prefs = await SharedPreferences.getInstance();
      activationRadiusMeters = prefs.getDouble(_kActivationRadius) ?? activationRadiusMeters;
    } catch (_) { /* Silenciar errores de preferencias; usar valores por defecto. */ }
  }
  static Future<void> saveActivationRadius(double v) async {
    activationRadiusMeters = v;
    final prefs = await SharedPreferences.getInstance();
    await prefs.setDouble(_kActivationRadius, v);
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AppModeConfig — persistencia del modo con default debug</name>

  <files>
    lib/core/config/app_mode_config.dart (nuevo)
    test/core/config/app_mode_config_test.dart (nuevo)
    lib/core/app_initializer.dart (modificar)
  </files>

  <read_first>
    - lib/core/config/location_config.dart — patrón exacto de clase estática con `loadFromPrefs()` + setters que persisten (D-09 exige reusar este patrón, no inventar un service/notifier)
    - lib/core/app_initializer.dart — dónde se llama hoy `LocationConfig.loadFromPrefs()` (el nuevo load va inmediatamente después)
    - .planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-RESEARCH.md sección "Pitfall 3" — el default DEBE ser debug en los dos lugares (inicializador del campo Y fallback del `??`)
  </read_first>

  <behavior>
    Tests en `test/core/config/app_mode_config_test.dart` (usan `SharedPreferences.setMockInitialValues({...})`, sin dependencias nuevas):
    - `setUp` resetea el estado estático: `AppModeConfig.isDebugMode = true;`
    - Instalación nueva: con `SharedPreferences.setMockInitialValues({})`, tras `await AppModeConfig.loadFromPrefs()` → `AppModeConfig.isDebugMode` es `true` (D-08: el default es modo debug).
    - Valor persistido `false`: con `setMockInitialValues({'app_isDebugMode': false})`, tras `loadFromPrefs()` → `isDebugMode` es `false`.
    - Valor persistido `true`: con `setMockInitialValues({'app_isDebugMode': true})`, tras `loadFromPrefs()` → `isDebugMode` es `true`.
    - Round-trip: partiendo de `setMockInitialValues({})`, `await AppModeConfig.setDebugMode(false)` deja `isDebugMode == false` en memoria, y tras forzar `AppModeConfig.isDebugMode = true` seguido de `await AppModeConfig.loadFromPrefs()` vuelve a leer `false` — o sea, el valor quedó realmente escrito en preferencias (UI-01: persiste entre reinicios).
  </behavior>

  <action>
    1. Crear `lib/core/config/app_mode_config.dart`:

    ```dart
    import 'package:shared_preferences/shared_preferences.dart';

    /// Modo de la app: panel de debug vs. dashboard de usuario final (UI-01).
    /// Persistido con SharedPreferences, mismo patrón que [LocationConfig].
    class AppModeConfig {
      static const _kIsDebugMode = 'app_isDebugMode';

      /// Default: modo debug (D-08). El usuario vive hoy en el panel de debug
      /// para grabar y diagnosticar; pasa a modo usuario solo si lo activa.
      static bool isDebugMode = true;

      static Future<void> loadFromPrefs() async {
        try {
          final prefs = await SharedPreferences.getInstance();
          isDebugMode = prefs.getBool(_kIsDebugMode) ?? true;
        } catch (_) {
          // Silenciar errores de preferencias; usar el default (debug).
        }
      }

      static Future<void> setDebugMode(bool value) async {
        isDebugMode = value;
        try {
          final prefs = await SharedPreferences.getInstance();
          await prefs.setBool(_kIsDebugMode, value);
        } catch (_) {
          // Silenciar: el cambio ya aplicó en memoria para esta sesión.
        }
      }
    }
    ```
    Los dos defaults son `true` a propósito (campo estático y fallback del `??`): si solo uno estuviera mal, una instalación nueva o un fallo de preferencias dejaría al usuario en modo usuario sin acceso al panel de debug.

    2. Escribir `test/core/config/app_mode_config_test.dart` cubriendo exactamente los casos del bloque `<behavior>`. Empezar con `TestWidgetsFlutterBinding.ensureInitialized();` dentro de `main()` (requerido por `shared_preferences` en tests) e importar con `package:cingula_app/core/config/app_mode_config.dart`.

    3. En `lib/core/app_initializer.dart`:
       - Agregar `import 'config/app_mode_config.dart';` junto a `import 'config/location_config.dart';`.
       - Agregar, inmediatamente después de la línea `await LocationConfig.loadFromPrefs();`:
         ```dart
         // Modo de UI persistido (debug vs. usuario final).
         await AppModeConfig.loadFromPrefs();
         ```
  </action>

  <verify>
    <automated>flutter test test/core/config/app_mode_config_test.dart</automated>
  </verify>

  <acceptance_criteria>
    - `flutter test test/core/config/app_mode_config_test.dart` pasa con al menos 4 tests.
    - `grep -n "static bool isDebugMode = true" lib/core/config/app_mode_config.dart` devuelve una línea.
    - `grep -n "getBool(_kIsDebugMode) ?? true" lib/core/config/app_mode_config.dart` devuelve una línea.
    - `grep -c "?? false" lib/core/config/app_mode_config.dart` devuelve `0`.
    - `grep -n "AppModeConfig.loadFromPrefs()" lib/core/app_initializer.dart` devuelve una línea.
    - `flutter analyze` sin errores nuevos.
  </acceptance_criteria>

  <done>El modo persiste entre reinicios y una instalación nueva (o un fallo leyendo preferencias) arranca en modo debug.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HiddenTapGesture — gesto oculto de 5 taps en 3 segundos</name>

  <files>
    lib/presentation/widgets/hidden_tap_gesture.dart (nuevo)
    test/presentation/widgets/hidden_tap_gesture_test.dart (nuevo)
  </files>

  <read_first>
    - .planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-UI-SPEC.md sección "Copywriting Contract", nota final — fija: 5 taps sobre el título del AppBar dentro de 3 segundos
    - lib/presentation/pages/home_page_impl.dart líneas 78-120 — convención del repo para `StatefulWidget` con `Timer` y su `dispose()` (`_RecordingBannerState`)
  </read_first>

  <behavior>
    Tests en `test/presentation/widgets/hidden_tap_gesture_test.dart` (`testWidgets`, sin `getIt` ni Provider — solo `MaterialApp(home: Scaffold(body: HiddenTapGesture(...)))`):
    - 5 taps consecutivos sin avanzar el reloj → `onActivated` se llamó exactamente 1 vez.
    - 4 taps consecutivos → `onActivated` NO se llamó (contador 0 llamadas).
    - 4 taps, luego `await tester.pump(const Duration(seconds: 4))`, luego 1 tap más → `onActivated` NO se llamó (la ventana venció y el contador se reseteó).
    - 5 taps → activa (1 llamada); 4 taps más → sigue en 1 llamada (el contador se resetea tras activar, no queda "cargado").
    - El widget renderiza su `child` tal cual: `expect(find.text('Cingula'), findsOneWidget)` con `child: const Text('Cingula')`.
  </behavior>

  <action>
    Crear `lib/presentation/widgets/hidden_tap_gesture.dart`:

    ```dart
    import 'dart:async';

    import 'package:flutter/material.dart';

    /// Gesto oculto: 5 toques sobre [child] dentro de 3 segundos disparan
    /// [onActivated] (D-07 — sin chrome visible, no ocupa espacio en la UI).
    class HiddenTapGesture extends StatefulWidget {
      const HiddenTapGesture({
        super.key,
        required this.child,
        required this.onActivated,
      });

      final Widget child;
      final VoidCallback onActivated;

      @override
      State<HiddenTapGesture> createState() => _HiddenTapGestureState();
    }

    class _HiddenTapGestureState extends State<HiddenTapGesture> {
      static const _tapsRequired = 5;
      static const _window = Duration(seconds: 3);

      int _tapCount = 0;
      Timer? _resetTimer;

      void _handleTap() {
        _tapCount++;
        _resetTimer?.cancel();

        if (_tapCount >= _tapsRequired) {
          _tapCount = 0;
          widget.onActivated();
          return;
        }

        _resetTimer = Timer(_window, () => _tapCount = 0);
      }

      @override
      void dispose() {
        _resetTimer?.cancel();
        super.dispose();
      }

      @override
      Widget build(BuildContext context) {
        return GestureDetector(
          onTap: _handleTap,
          behavior: HitTestBehavior.opaque,
          child: widget.child,
        );
      }
    }
    ```

    No agregar parámetros configurables para `_tapsRequired`/`_window`: los valores están fijados por `01-UI-SPEC.md` y no hay un segundo consumidor.

    Escribir `test/presentation/widgets/hidden_tap_gesture_test.dart` cubriendo exactamente los casos del bloque `<behavior>`. Usar `await tester.tap(find.text('Cingula')); await tester.pump();` por cada toque, y `await tester.pump(const Duration(seconds: 4));` para vencer la ventana. Importar con `package:cingula_app/presentation/widgets/hidden_tap_gesture.dart`.
  </action>

  <verify>
    <automated>flutter test test/presentation/widgets/hidden_tap_gesture_test.dart</automated>
  </verify>

  <acceptance_criteria>
    - `flutter test test/presentation/widgets/hidden_tap_gesture_test.dart` pasa con al menos 5 tests.
    - `grep -n "_tapsRequired = 5" lib/presentation/widgets/hidden_tap_gesture.dart` devuelve una línea.
    - `grep -n "Duration(seconds: 3)" lib/presentation/widgets/hidden_tap_gesture.dart` devuelve una línea.
    - `grep -n "_resetTimer?.cancel()" lib/presentation/widgets/hidden_tap_gesture.dart` devuelve al menos 2 líneas (en `_handleTap` y en `dispose`).
    - `flutter analyze` sin errores nuevos.
  </acceptance_criteria>

  <done>5 taps rápidos activan el callback una sola vez; taps espaciados más de 3 segundos no acumulan; el timer se cancela en `dispose`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: UserModeView — vista de usuario final según el UI-SPEC</name>

  <files>
    lib/presentation/pages/home/widgets/user_mode_view.dart (nuevo)
    test/presentation/pages/home/widgets/user_mode_view_test.dart (nuevo)
  </files>

  <read_first>
    - .planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-UI-SPEC.md COMPLETO — spacing scale, typography, color roles y copywriting contract son el contrato de esta tarea
    - lib/presentation/pages/home_page_impl.dart líneas 25-64 — el `SwitchListTile` y la card de audio actuales, que esta vista reemplaza con estética propia
    - lib/presentation/pages/home/widgets/audio_details.dart — widget de card de audio que se reutiliza tal cual
    - lib/domain/entities/audio_asset.dart — campos disponibles para construir un `AudioAsset` de prueba
  </read_first>

  <behavior>
    Tests en `test/presentation/pages/home/widgets/user_mode_view_test.dart` (`testWidgets`, envuelto en `MaterialApp(home: Scaffold(body: UserModeView(...)))` — cero `getIt`, cero Provider):
    - `isMonitoring: false, statusMessage: null, currentAsset: null` → `find.text('Listo para iniciar')` encuentra 1 widget.
    - `isMonitoring: true, statusMessage: null, currentAsset: null` → `find.text('Caminando… te avisamos cuando estés cerca de un punto sonoro.')` encuentra 1 widget.
    - `isMonitoring: true, statusMessage: 'Monitoreo activo', currentAsset: null` → `find.text('Monitoreo activo')` encuentra 1 widget (el `statusMessage` explícito gana sobre el copy por defecto).
    - `currentAsset: null` → `find.byType(AudioDetails)` encuentra 0 widgets.
    - `currentAsset:` un `AudioAsset` de prueba con `title: 'Chelenko'` → `find.byType(AudioDetails)` encuentra 1 widget y `find.text('Chelenko')` encuentra 1 widget.
    - Tocar el `SwitchListTile` (`await tester.tap(find.byType(SwitchListTile))`) invoca `onMonitoringChanged` con el valor negado de `isMonitoring`.
  </behavior>

  <action>
    Crear `lib/presentation/pages/home/widgets/user_mode_view.dart` con la firma exacta:

    ```dart
    class UserModeView extends StatelessWidget {
      const UserModeView({
        super.key,
        required this.isMonitoring,
        required this.statusMessage,
        required this.currentAsset,
        required this.onMonitoringChanged,
      });

      final bool isMonitoring;
      final String? statusMessage;
      final AudioAsset? currentAsset;
      final ValueChanged<bool> onMonitoringChanged;
      ...
    }
    ```

    Estructura del `build` (valores tomados literalmente de `01-UI-SPEC.md`, no inventar otros):

    - Raíz: `SingleChildScrollView` con `padding: const EdgeInsets.fromLTRB(16, 16, 16, 96)` (md=16; 96 es clearance del FAB, ya usado en `home_page_impl.dart:30`). `Column(crossAxisAlignment: CrossAxisAlignment.stretch)`.

    - **Hero de estado** (primero, es la jerarquía del modo usuario):
      - `Icon(isMonitoring ? Icons.hearing : Icons.play_circle_outline, size: 48, color: isMonitoring ? cs.primary : cs.onSurfaceVariant)` donde `cs = Theme.of(context).colorScheme`. Accent (`cs.primary`) reservado para el indicador de monitoreo activo — no usarlo en ningún otro elemento de esta vista salvo el switch activo.
      - `const SizedBox(height: 8)` (sm).
      - Texto de estado, Body 16/400: `Text(_statusText, textAlign: TextAlign.center, style: const TextStyle(fontSize: 16, fontWeight: FontWeight.w400, height: 1.5))`, donde:
        ```dart
        String get _statusText =>
            statusMessage ??
            (isMonitoring
                ? 'Caminando… te avisamos cuando estés cerca de un punto sonoro.'
                : 'Listo para iniciar');
        ```
        Esas dos cadenas son copy fijado por el UI-SPEC — reproducirlas carácter por carácter, incluido el `…` (U+2026, no tres puntos) y los acentos.

    - `const SizedBox(height: 32)` (xl — separación entre el hero de estado y el control de monitoreo, fijada por el UI-SPEC).

    - **Control de monitoreo**: `Card(color: cs.surfaceContainer, margin: EdgeInsets.zero, child: SwitchListTile(...))` con
      `title: const Text('Monitorear ubicacion en segundo plano', style: TextStyle(fontSize: 16, height: 1.5))`
      (mismo texto que hoy en `home_page_impl.dart:35` — no renombrar),
      `value: isMonitoring`, `onChanged: onMonitoringChanged`, y sin `subtitle` (el estado ya vive en el hero, no duplicarlo).

    - `const SizedBox(height: 24)` (lg).

    - **Card de audio**: `if (currentAsset != null) AudioDetails(asset: currentAsset!)`. Reutilizar `AudioDetails` tal cual; NO refactorizar su padding de 12px (excepción declarada en el UI-SPEC, el widget no se toca en esta fase).

    Reglas del UI-SPEC a respetar (verificables en revisión):
    - Cero colores hardcodeados (`Colors.red`, `Color(0x...)`, etc.) — todo por rol de `Theme.of(context).colorScheme`.
    - Todo `SizedBox`/`EdgeInsets` usa múltiplos de 4 de la escala declarada (4/8/16/24/32).
    - No importar `DiagnosticsPanel`, `RecorderService`, `getIt` ni `provider` en este archivo: la vista recibe todo por parámetros.

    Escribir `test/presentation/pages/home/widgets/user_mode_view_test.dart` cubriendo exactamente los casos del bloque `<behavior>`. Para el `AudioAsset` de prueba usar:
    ```dart
    const asset = AudioAsset(
      id: 1, title: 'Chelenko', artist: 'Cingula', description: 'Test',
      duration: Duration(minutes: 3), localPath: 'assets/audio/Chelenko.mp3',
    );
    ```
  </action>

  <verify>
    <automated>flutter test test/presentation/pages/home/widgets/user_mode_view_test.dart</automated>
    <automated>flutter analyze lib/presentation/pages/home/widgets/user_mode_view.dart</automated>
  </verify>

  <acceptance_criteria>
    - `flutter test test/presentation/pages/home/widgets/user_mode_view_test.dart` pasa con al menos 6 tests.
    - `grep -n "Caminando… te avisamos cuando estés cerca de un punto sonoro." lib/presentation/pages/home/widgets/user_mode_view.dart` devuelve una línea.
    - `grep -n "Listo para iniciar" lib/presentation/pages/home/widgets/user_mode_view.dart` devuelve una línea.
    - `grep -n "Monitorear ubicacion en segundo plano" lib/presentation/pages/home/widgets/user_mode_view.dart` devuelve una línea.
    - `grep -n "Colors\.\|Color(0x" lib/presentation/pages/home/widgets/user_mode_view.dart` no devuelve ninguna línea.
    - `grep -n "DiagnosticsPanel\|RecorderService\|service_locator\|package:provider" lib/presentation/pages/home/widgets/user_mode_view.dart` no devuelve ninguna línea.
    - `grep -n "SizedBox(height: 32)" lib/presentation/pages/home/widgets/user_mode_view.dart` devuelve una línea.
    - `flutter analyze` sin errores nuevos.
  </acceptance_criteria>

  <done>La vista de modo usuario renderiza estado + control de monitoreo + card de audio condicional, usando solo roles de color del tema y la escala de spacing del UI-SPEC, y es testeable sin DI.</done>
</task>

</tasks>

<verification>
- `flutter test` verde con los 3 archivos de test nuevos.
- `flutter analyze` sin errores.
- `grep -rn "AppModeConfig.loadFromPrefs()" lib/core/app_initializer.dart` → una línea.
- Ninguno de los 3 archivos nuevos de `lib/` importa `getIt`/`provider` salvo `app_initializer.dart` (que ya lo hacía).
</verification>

<success_criteria>
1. `AppModeConfig` persiste el modo entre reinicios y defaultea a debug en instalación nueva o ante fallo de preferencias (UI-01, D-08, D-09).
2. `HiddenTapGesture` dispara su callback exactamente con 5 taps dentro de 3 segundos, y no acumula fuera de la ventana (UI-01, D-07).
3. `UserModeView` cumple el contrato visual de `01-UI-SPEC.md` (copy exacto, roles de color, escala de spacing) y no depende de DI (UI-01, D-11, D-12).
4. `flutter analyze` limpio y `flutter test` verde.
</success_criteria>

<output>
Al completar, crear `.planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-02-SUMMARY.md`.
</output>
