---
phase: 01-blindaje-de-datos-y-separaci-n-debug-usuario
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/data/datasources/local/db_recovery.dart
  - lib/data/datasources/local/app_database.dart
  - lib/presentation/pages/data_browser_page.dart
  - lib/core/di/service_locator.dart
  - pubspec.yaml
  - test/data/datasources/local/db_recovery_test.dart
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "Si la base de datos no abre, el archivo existente queda en disco renombrado con timestamp — nunca se borra"
    - "Después de renombrar, la app abre una base nueva y arranca normalmente"
    - "La app registra el evento de recuperación (ruta del backup + momento) para que la UI lo pueda mostrar"
    - "No existe ninguna acción en la UI de debug capaz de borrar o reemplazar la base de datos"
  artifacts:
    - path: "lib/data/datasources/local/db_recovery.dart"
      provides: "renameCorruptDatabase() + DatabaseRecoveryEvent, sin dependencias de Flutter/sqflite"
      exports: ["renameCorruptDatabase", "DatabaseRecoveryEvent", "formatRecoveryTimestamp"]
    - path: "test/data/datasources/local/db_recovery_test.dart"
      provides: "Prueba de que el rename preserva datos y no borra"
    - path: "lib/data/datasources/local/app_database.dart"
      provides: "init() con recuperación rename-not-delete y campo lastRecoveryEvent"
      contains: "lastRecoveryEvent"
  key_links:
    - from: "lib/data/datasources/local/app_database.dart"
      to: "lib/data/datasources/local/db_recovery.dart"
      via: "import + llamada en el catch de init()"
      pattern: "renameCorruptDatabase\\("
    - from: "lib/presentation/pages/data_browser_page.dart"
      to: "AppDatabase (métodos destructivos)"
      via: "vínculo ELIMINADO — no debe quedar ninguna referencia"
      pattern: "recreateForTesting|importDatabase|file_picker"
---

<objective>
Blindar la integridad de los datos locales: cuando `AppDatabase.init()` no logra abrir `cingula.db`, el archivo existente se renombra con timestamp (nunca se borra) antes de crear una base nueva, y el evento queda registrado para que la UI lo informe. En paralelo, se eliminan por completo las dos acciones destructivas del panel de debug (recrear DB, importar DB) junto con sus métodos en `AppDatabase`.

Purpose: "Cero pérdida de datos ya existentes en el celular" es la restricción de máxima prioridad de este milestone (CLAUDE.md). Hoy el `catch` de `init()` llama a `deleteDatabase(path)` — cualquier fallo transitorio (disco lleno, kill del SO a mitad de la apertura, migración mala) destruye todo lo grabado en campo.
Output: `db_recovery.dart` (nuevo, testeable sin plugins), `app_database.dart` con recuperación no destructiva, `data_browser_page.dart` sin acciones destructivas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-CONTEXT.md
@.planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-RESEARCH.md
@.planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-UI-SPEC.md

<interfaces>
<!-- Contratos que el ejecutor necesita. Extraídos del código actual. -->

Estado ACTUAL de `lib/data/datasources/local/app_database.dart` (fragmento relevante):
```dart
class AppDatabase {
  static const _dbName = 'cingula.db';
  static const _dbVersion = 5;

  Database? _database;

  Future<void> init() async {
    if (_database != null) return;
    final directory = await getApplicationDocumentsDirectory();
    final path = p.join(directory.path, _dbName);
    try {
      _database = await openDatabase(path, version: _dbVersion, onCreate: _onCreate, onUpgrade: _onUpgrade);
    } catch (e) {
      // Si la BD falla al abrir (corrupcion, esquema incompatible), se borra y recrea.
      stderr.writeln('Database init failed: $e. Attempting to delete and recreate...');
      try {
        await deleteDatabase(path);          // <-- ESTE ES EL BUG A ELIMINAR
        _database = await openDatabase(path, version: _dbVersion, onCreate: _onCreate, onUpgrade: _onUpgrade);
      } catch (e2) {
        stderr.writeln('Failed to recreate database: $e2');
        rethrow;
      }
    }
  }
  // ... _onCreate, _onUpgrade, recreateForTesting (líneas 256-273),
  //     exportDatabase (275-308), importDatabase (310-329), getDatabaseStats (331-367)
}
```

Contrato NUEVO que este plan crea (consumido por el plan 01-03):
```dart
// lib/data/datasources/local/db_recovery.dart
class DatabaseRecoveryEvent {
  const DatabaseRecoveryEvent({required this.backupPath, required this.occurredAt});
  final String backupPath;
  final DateTime occurredAt;
  String get backupFileName;   // solo el nombre de archivo, para el banner de UI
}
String formatRecoveryTimestamp(DateTime now);              // 'yyyyMMdd-HHmmss'
Future<String?> renameCorruptDatabase(String dbPath, DateTime now);

// lib/data/datasources/local/app_database.dart
class AppDatabase {
  DatabaseRecoveryEvent? lastRecoveryEvent;   // campo de instancia, null si no hubo recuperación
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recuperación rename-not-delete de la base de datos</name>

  <files>
    lib/data/datasources/local/db_recovery.dart (nuevo)
    test/data/datasources/local/db_recovery_test.dart (nuevo)
    lib/data/datasources/local/app_database.dart (modificar init())
  </files>

  <read_first>
    - lib/data/datasources/local/app_database.dart — estado actual completo de `init()`, `_dbName`, `_dbVersion` y los imports ya presentes (`dart:io`, `package:path/path.dart as p`, `path_provider`, `sqflite`)
    - test/core/background/background_poller_test.dart — convención de tests de este repo (fakes inline, sin fixtures compartidos)
    - .planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-UI-SPEC.md sección "Copywriting Contract" — fija el formato de nombre `cingula.db.corrupto-<yyyyMMdd-HHmmss>` que el banner de la fase menciona textualmente
  </read_first>

  <behavior>
    Tests que deben existir en `test/data/datasources/local/db_recovery_test.dart` (todos usan `Directory.systemTemp.createTemp()` — sin sqflite, sin path_provider, sin plugins):
    - `formatRecoveryTimestamp(DateTime(2026, 8, 8, 13, 10, 3))` devuelve exactamente `'20260808-131003'` (padding a 2 dígitos en mes/día/hora/minuto/segundo, 4 en año).
    - Dado un archivo `<tmp>/cingula.db` con contenido `'DATOS-ORIGINALES'`: `renameCorruptDatabase(path, DateTime(2026,8,8,13,10,3))` devuelve `'<tmp>/cingula.db.corrupto-20260808-131003'`, ese archivo existe y su contenido sigue siendo `'DATOS-ORIGINALES'`, y `File('<tmp>/cingula.db').existsSync()` es `false`. (Prueba de que se renombra, no se copia ni se borra.)
    - Dados también `<tmp>/cingula.db-wal`, `<tmp>/cingula.db-shm` y `<tmp>/cingula.db-journal`: tras el rename, existen `<backupPath>-wal`, `<backupPath>-shm` y `<backupPath>-journal`, y NO existe ninguno de los sidecars originales junto a la ruta limpia. (Un `-wal` huérfano se aplicaría sobre la base nueva y la corrompería.)
    - Si `<tmp>/cingula.db` no existe: `renameCorruptDatabase` devuelve `null` y no lanza.
    - `DatabaseRecoveryEvent(backupPath: '<tmp>/cingula.db.corrupto-20260808-131003', occurredAt: ...).backupFileName` devuelve `'cingula.db.corrupto-20260808-131003'` (sin directorio).
  </behavior>

  <action>
    1. Crear `lib/data/datasources/local/db_recovery.dart` con SOLO `import 'dart:io';` (nada de Flutter, nada de sqflite — así es testeable en host sin plugins):

    ```dart
    import 'dart:io';

    /// Evento one-shot: la base no abría y se recuperó renombrando el archivo.
    class DatabaseRecoveryEvent {
      const DatabaseRecoveryEvent({required this.backupPath, required this.occurredAt});

      final String backupPath;
      final DateTime occurredAt;

      /// Nombre del archivo de respaldo, sin directorio (para mostrar en la UI).
      String get backupFileName =>
          backupPath.split(RegExp(r'[/\\]')).last;
    }

    String _two(int v) => v.toString().padLeft(2, '0');

    /// Formato de timestamp fijado por 01-UI-SPEC.md: yyyyMMdd-HHmmss.
    String formatRecoveryTimestamp(DateTime now) =>
        '${now.year.toString().padLeft(4, '0')}${_two(now.month)}${_two(now.day)}'
        '-${_two(now.hour)}${_two(now.minute)}${_two(now.second)}';

    /// Renombra el archivo de base de datos que no pudo abrirse a
    /// '<dbPath>.corrupto-<yyyyMMdd-HHmmss>', dejándolo intacto y accesible.
    ///
    /// NUNCA borra: si el rename falla, la excepción se propaga para que el
    /// llamador aborte en vez de destruir datos (DATA-01).
    /// Devuelve la ruta del respaldo, o null si no había archivo que respaldar.
    Future<String?> renameCorruptDatabase(String dbPath, DateTime now) async {
      final file = File(dbPath);
      if (!await file.exists()) return null;

      final backupPath = '$dbPath.corrupto-${formatRecoveryTimestamp(now)}';
      await file.rename(backupPath);

      // Los sidecars de SQLite pertenecen al archivo viejo: si quedan junto a la
      // ruta limpia, SQLite los aplicaría sobre la base nueva y la corrompería.
      for (final suffix in const ['-journal', '-wal', '-shm']) {
        final sidecar = File('$dbPath$suffix');
        if (await sidecar.exists()) {
          await sidecar.rename('$backupPath$suffix');
        }
      }

      return backupPath;
    }
    ```

    2. Escribir `test/data/datasources/local/db_recovery_test.dart` cubriendo exactamente los casos del bloque `<behavior>` de arriba. Usar `late Directory tmp;` + `setUp(() async => tmp = await Directory.systemTemp.createTemp('cingula_recovery'))` y `tearDown(() async => tmp.deleteSync(recursive: true))`. Importar con `package:cingula_app/data/datasources/local/db_recovery.dart` (convención del repo: los tests importan por package name, ver `test/widget_test.dart`).

    3. En `lib/data/datasources/local/app_database.dart`:
       - Agregar `import 'db_recovery.dart';` junto a los imports existentes.
       - Agregar el campo público de instancia, justo debajo de `Database? _database;`:
         ```dart
         /// Registro del último evento de recuperación de base (null si no hubo).
         /// La UI lo lee una vez tras el primer frame y lo limpia (ver plan 01-03).
         DatabaseRecoveryEvent? lastRecoveryEvent;
         ```
       - Reemplazar TODO el bloque `catch (e) { ... }` actual de `init()` (el que hoy llama a `deleteDatabase(path)`) por:
         ```dart
         } catch (e) {
           stderr.writeln(
             'Database init failed: $e. Renombrando el archivo existente y arrancando en limpio...',
           );
           // Si el rename falla, la excepción se propaga a propósito: preferimos
           // no arrancar antes que destruir datos del usuario (DATA-01).
           final backupPath = await renameCorruptDatabase(path, DateTime.now());

           _database = await openDatabase(
             path,
             version: _dbVersion,
             onCreate: _onCreate,
             onUpgrade: _onUpgrade,
           );

           if (backupPath != null) {
             lastRecoveryEvent = DatabaseRecoveryEvent(
               backupPath: backupPath,
               occurredAt: DateTime.now(),
             );
           }
         }
         ```
       - NO dejar ningún `deleteDatabase(` en `init()`. NO agregar un `catch` interno que caiga a borrado como fallback.
  </action>

  <verify>
    <automated>flutter test test/data/datasources/local/db_recovery_test.dart</automated>
    <automated>flutter analyze lib/data/datasources/local/db_recovery.dart lib/data/datasources/local/app_database.dart</automated>
  </verify>

  <acceptance_criteria>
    - `flutter test test/data/datasources/local/db_recovery_test.dart` pasa con al menos 5 tests (uno por cada viñeta de `<behavior>`).
    - `grep -c "deleteDatabase(" lib/data/datasources/local/app_database.dart` devuelve como máximo `1` (solo el que queda dentro de `recreateForTesting()`, que la Task 2 elimina); dentro del cuerpo de `init()` no hay ninguna ocurrencia.
    - `grep -n "renameCorruptDatabase(" lib/data/datasources/local/app_database.dart` devuelve al menos una línea.
    - `grep -n "lastRecoveryEvent" lib/data/datasources/local/app_database.dart` devuelve al menos 2 líneas (declaración del campo + asignación en `init()`).
    - `grep -n "^import" lib/data/datasources/local/db_recovery.dart` devuelve exactamente una línea: `import 'dart:io';`.
    - `flutter analyze` no reporta errores ni warnings nuevos en los archivos tocados.
  </acceptance_criteria>

  <done>Un fallo de apertura de la base renombra el archivo existente (más sus sidecars `-wal`/`-shm`/`-journal`) con timestamp, abre una base nueva en la ruta original, y deja `lastRecoveryEvent` poblado. No queda ninguna ruta de código en `init()` que borre datos.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminar las acciones destructivas de la UI de debug y del AppDatabase</name>

  <files>
    lib/presentation/pages/data_browser_page.dart
    lib/data/datasources/local/app_database.dart
    lib/core/di/service_locator.dart
    pubspec.yaml
  </files>

  <read_first>
    - lib/presentation/pages/data_browser_page.dart — métodos `_recreateDb` (líneas 114-136) y `_importDb` (líneas 230-274), el import de `file_picker` (línea 3), y los botones del `Wrap` (líneas 444-454)
    - lib/data/datasources/local/app_database.dart — `recreateForTesting()` (líneas 256-273) e `importDatabase()` (líneas 310-329); `exportDatabase()` (275-308) y `getDatabaseStats()` (331-367) SE CONSERVAN, no son destructivos
    - lib/core/di/service_locator.dart línea 45 — comentario que menciona `recreateForTesting()` y queda obsoleto
    - .planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-CONTEXT.md sección "Acciones destructivas de debug (DATA-02)" — D-03, D-04, D-05
  </read_first>

  <action>
    Per D-03/D-04 y la memoria de feedback del proyecto (`feedback_no_destructive_automation`): estas acciones se ELIMINAN, no se protegen con confirmación. No agregar ningún `AlertDialog` nuevo en esta tarea.

    1. En `lib/presentation/pages/data_browser_page.dart`:
       - Borrar el método completo `Future<void> _recreateDb() async { ... }` (líneas ~114-136, incluye su propio `showDialog`/`AlertDialog` de advertencia).
       - Borrar el método completo `Future<void> _importDb() async { ... }` (líneas ~230-274, incluye el `FilePicker.platform.pickFiles(...)` y su `AlertDialog`).
       - Borrar del `Wrap` (líneas ~444-454) estas dos líneas exactas:
         `OutlinedButton(onPressed: _recreateDb, child: const Text('Recreate DB (debug)')),`
         `ElevatedButton.icon(onPressed: _importDb, icon: const Icon(Icons.download), label: const Text('Importar BD')),`
         CONSERVAR los otros tres botones del `Wrap`: `_exportDb` ('Exportar BD'), `_showStats` ('Ver contenido BD'), `_cleanupOrphanTriggers` ('Limpiar triggers huérfanos').
       - Borrar la línea 3: `import 'package:file_picker/file_picker.dart';`.

    2. En `lib/data/datasources/local/app_database.dart`:
       - Borrar el método completo `Future<void> recreateForTesting() async { ... }` junto con su docstring `/// Para tests: borra el archivo de BD y reinicia con datos seed.` (líneas ~256-273). Este método contiene el último `deleteDatabase(` que queda en el archivo.
       - Borrar el método completo `Future<void> importDatabase(String sourcePath) async { ... }` junto con su docstring `/// Importa una base de datos desde un archivo externo y reinicializa.` (líneas ~310-329).
       - NO tocar `exportDatabase()` ni `getDatabaseStats()`.

    3. En `lib/core/di/service_locator.dart`, reemplazar el comentario obsoleto de la línea 45:
       `  // Use AppDatabase.recreateForTesting() manualmente desde la UI/debug.`
       por:
       `  // No existe ninguna acción de recreado/reemplazo de BD: se eliminaron en la fase 01 (DATA-02).`

    4. En `pubspec.yaml`, borrar estas dos líneas (el comentario y la dependencia), ya que `data_browser_page.dart` era su único consumidor:
       ```
         # Selector de archivos para importar BD
         file_picker: ^8.1.4
       ```
       Luego ejecutar `flutter pub get`. Si `flutter analyze` o `flutter test` fallan por una referencia a `file_picker` que no fue detectada por grep, restaurar esas dos líneas de `pubspec.yaml`, volver a correr `flutter pub get`, y dejar registrada la razón en el SUMMARY — el borrado de las llamadas en `lib/` es lo obligatorio, la limpieza de la dependencia es oportunista.
  </action>

  <verify>
    <automated>flutter analyze</automated>
    <automated>flutter test</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn "recreateForTesting" lib/ test/` no devuelve ninguna línea.
    - `grep -rn "importDatabase" lib/ test/` no devuelve ninguna línea.
    - `grep -rn "file_picker\|FilePicker" lib/ test/` no devuelve ninguna línea.
    - `grep -rn "deleteDatabase(" lib/` no devuelve ninguna línea (cero ocurrencias en todo `lib/`).
    - `grep -n "_recreateDb\|_importDb\|Recreate DB (debug)\|Importar BD" lib/presentation/pages/data_browser_page.dart` no devuelve ninguna línea.
    - `grep -n "exportDatabase\|getDatabaseStats" lib/data/datasources/local/app_database.dart` devuelve al menos 2 líneas (confirma que no se borró de más).
    - `grep -n "Exportar BD" lib/presentation/pages/data_browser_page.dart` devuelve al menos una línea (el botón no destructivo sobrevive).
    - `flutter analyze` termina sin errores.
    - `flutter test` pasa completo.
  </acceptance_criteria>

  <done>No queda ninguna forma de borrar o reemplazar la base de datos desde la UI ni desde el API de `AppDatabase`. `flutter analyze` limpio y suite verde.</done>
</task>

</tasks>

<verification>
- `flutter test` verde (incluye el nuevo `db_recovery_test.dart`).
- `flutter analyze` sin errores.
- `grep -rn "deleteDatabase(" lib/` → cero resultados.
- `grep -rn "recreateForTesting\|importDatabase\|file_picker" lib/ test/` → cero resultados.
- `lib/data/datasources/local/app_database.dart` expone `lastRecoveryEvent` y llama a `renameCorruptDatabase` en el `catch` de `init()`.
</verification>

<success_criteria>
1. Ante un fallo de apertura de la base, el archivo existente queda renombrado como `cingula.db.corrupto-<yyyyMMdd-HHmmss>` en el mismo directorio, con su contenido intacto, y la app abre una base nueva y vacía en la ruta original (DATA-01).
2. Cero llamadas a `deleteDatabase(` en `lib/` (DATA-01).
3. Cero acciones destructivas de base de datos alcanzables desde la UI de debug o desde el API público de `AppDatabase` (DATA-02).
4. `flutter analyze` limpio y `flutter test` verde.
</success_criteria>

<output>
Al completar, crear `.planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-01-SUMMARY.md`.
</output>
