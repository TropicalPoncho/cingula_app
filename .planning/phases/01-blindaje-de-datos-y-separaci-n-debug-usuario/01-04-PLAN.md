---
phase: 01-blindaje-de-datos-y-separaci-n-debug-usuario
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - lib/data/datasources/local/db_recovery.dart
  - lib/core/config/app_mode_config.dart
  - lib/presentation/widgets/hidden_tap_gesture.dart
  - lib/presentation/pages/home/widgets/user_mode_view.dart
  - lib/presentation/pages/home_page_impl.dart
  - .planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-VALIDATION.md
autonomous: false
requirements: [DATA-01, DATA-02, UI-01]

must_haves:
  truths:
    - "El código nuevo de la fase no tiene abstracciones sin uso, dependencias nuevas ni parámetros que nadie configura"
    - "Corromper la base en un dispositivo real y arrancar en frío deja el archivo renombrado en disco y muestra el banner, sin bloquear la app"
    - "5 taps sobre el título alternan el modo, y el modo elegido sobrevive a cerrar y reabrir la app"
  artifacts:
    - path: ".planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-VALIDATION.md"
      provides: "Mapa de verificación con estado final de cada test"
  key_links:
    - from: "auditoría ponytail"
      to: "código nuevo de los planes 01-01 a 01-03"
      via: "revisión + greps de completitud"
      pattern: "deleteDatabase\\(|recreateForTesting|importDatabase|file_picker"
---

<objective>
Cerrar la fase: auditoría ponytail obligatoria sobre el código nuevo (buscar y eliminar sobre-ingeniería antes de dar la fase por terminada) y verificación manual en dispositivo de las dos conductas que no se pueden automatizar con costo razonable — el arranque en frío tras una base corrupta, y la persistencia del toggle entre reinicios.

Purpose: `CLAUDE.md` marca la auditoría ponytail como parte del checklist de CADA fase de ejecución, no solo del cierre de milestone. Y el camino de recuperación de base de datos es la conducta de mayor riesgo del milestone ("cero pérdida de datos"): merece una verificación con ojos humanos sobre hardware real, no solo un test unitario del helper.
Output: código auditado y simplificado, `01-VALIDATION.md` actualizado, aprobación humana registrada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-VALIDATION.md
@.planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-UI-SPEC.md
@.planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-01-SUMMARY.md
@.planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-02-SUMMARY.md
@.planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditoría ponytail del código nuevo de la fase</name>

  <files>
    lib/data/datasources/local/db_recovery.dart
    lib/core/config/app_mode_config.dart
    lib/presentation/widgets/hidden_tap_gesture.dart
    lib/presentation/pages/home/widgets/user_mode_view.dart
    lib/presentation/pages/home_page_impl.dart
    .planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-VALIDATION.md
  </files>

  <read_first>
    - lib/data/datasources/local/db_recovery.dart
    - lib/core/config/app_mode_config.dart
    - lib/presentation/widgets/hidden_tap_gesture.dart
    - lib/presentation/pages/home/widgets/user_mode_view.dart
    - lib/presentation/pages/home_page_impl.dart
    - lib/data/datasources/local/app_database.dart
    - pubspec.yaml — verificar que no se agregó ninguna dependencia de producción en esta fase
    - .planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-VALIDATION.md
  </read_first>

  <action>
    Auditoría ponytail: recorrer los 5 archivos de código de la fase y ELIMINAR (no documentar, no justificar) cualquiera de estos hallazgos:

    a) Parámetros, campos o constantes configurables con un solo valor real de uso (ej.: un `tapsRequired`/`window` expuesto en el constructor de `HiddenTapGesture` cuando el UI-SPEC ya los fija; un `dbName` inyectable en `db_recovery.dart` que nadie pasa distinto).
    b) Clases o interfaces con una sola implementación introducidas en esta fase (ej.: un `AppModeService`/notifier envolviendo `AppModeConfig`, un `RecoveryPresenter` envolviendo el banner).
    c) Helpers/utilidades creadas y usadas una sola vez donde el código inline es más corto.
    d) `try/catch` que capturan y re-lanzan sin agregar nada, o que tragan errores en rutas donde el error importa (excepción legítima: los `catch (_)` de `AppModeConfig`, que replican el patrón deliberado de `LocationConfig`).
    e) Bloques de código muerto o comentado, imports sin usar.
    f) Dependencias nuevas: `git diff pubspec.yaml` no debe mostrar NINGUNA línea agregada bajo `dependencies:` en esta fase (solo la eliminación de `file_picker` del plan 01-01).

    NO simplificar (carve-outs explícitos, dejarlos como están aunque parezcan "de más"):
    - El renombrado de sidecars `-journal`/`-wal`/`-shm` en `renameCorruptDatabase`: previene que la base nueva se corrompa. Es prevención de pérdida de datos, no adorno.
    - El doble default a `true` en `AppModeConfig` (campo estático + fallback del `??`): son dos rutas de código distintas, ambas necesarias.
    - Los 3 archivos de test: son la única red de la fase.

    Correr los greps de completitud y confirmar que dan el resultado indicado:
    - `grep -rn "deleteDatabase(" lib/` → 0 líneas
    - `grep -rn "recreateForTesting\|importDatabase" lib/ test/` → 0 líneas
    - `grep -rn "file_picker\|FilePicker" lib/ test/` → 0 líneas
    - `grep -rn "Colors\." lib/presentation/pages/home/widgets/user_mode_view.dart` → 0 líneas

    Finalmente, actualizar `.planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-VALIDATION.md`:
    - Poner en `✅ green` las filas cuyos tests existen y pasan.
    - Reemplazar la tabla "Per-Task Verification Map" por los archivos de test realmente creados: `test/data/datasources/local/db_recovery_test.dart`, `test/core/config/app_mode_config_test.dart`, `test/presentation/widgets/hidden_tap_gesture_test.dart`, `test/presentation/pages/home/widgets/user_mode_view_test.dart`.
    - Marcar los 6 checkboxes de "Validation Sign-Off" y poner `nyquist_compliant: true` en el frontmatter.
  </action>

  <verify>
    <automated>flutter analyze</automated>
    <automated>flutter test</automated>
  </verify>

  <acceptance_criteria>
    - `flutter analyze` termina sin errores ni warnings.
    - `flutter test` pasa completo, con al menos 4 archivos de test nuevos ejecutándose.
    - `grep -rn "deleteDatabase(" lib/` no devuelve ninguna línea.
    - `grep -rn "recreateForTesting\|importDatabase\|file_picker\|FilePicker" lib/ test/` no devuelve ninguna línea.
    - `grep -rn "Colors\." lib/presentation/pages/home/widgets/user_mode_view.dart` no devuelve ninguna línea.
    - `git diff pubspec.yaml` no muestra ninguna línea agregada bajo `dependencies:`.
    - `grep -n "nyquist_compliant: true" .planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-VALIDATION.md` devuelve una línea.
    - El SUMMARY lista explícitamente qué se eliminó en la auditoría, o la frase "Auditoría ponytail: sin hallazgos" si no hubo nada que sacar.
  </acceptance_criteria>

  <done>Código nuevo auditado y simplificado, cero dependencias de producción agregadas, greps de completitud en cero, `01-VALIDATION.md` actualizado y firmado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificación manual en dispositivo (recuperación de BD + persistencia del toggle)</name>

  <files>ninguno — tarea de verificación, no modifica código</files>

  <read_first>
    - .planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-UI-SPEC.md sección "Copywriting Contract" — el copy exacto que hay que ver en pantalla (banner de recuperación y SnackBar de modo)
    - lib/presentation/pages/home_page_impl.dart — qué se renderiza en cada rama
    - android/app/build.gradle.kts — `applicationId` real, necesario para los comandos `adb run-as`
  </read_first>

  <action>
    Ejecutar la QA manual descrita en `<how-to-verify>` sobre un dispositivo/emulador Android, y presentar al usuario el resultado de los 14 pasos. No modificar código en esta tarea: si un paso falla, registrar el comportamiento observado y esperar la decisión del usuario antes de tocar nada.
  </action>

  <what-built>
    - Recuperación no destructiva de la base: al fallar la apertura, `cingula.db` se renombra a `cingula.db.corrupto-<yyyyMMdd-HHmmss>` (junto con sus sidecars) y la app abre una base nueva.
    - `MaterialBanner` no bloqueante que informa la recuperación y el nombre del archivo de respaldo.
    - Toggle debug/usuario por 5 taps sobre el título "Cingula", persistido en `SharedPreferences`.
    - Eliminación total de "Recreate DB (debug)" e "Importar BD" del panel de datos.
  </what-built>

  <how-to-verify>
    Ejecutar con `flutter run` en un dispositivo o emulador Android real. **Antes de empezar, exportar la base actual desde el panel de debug ("Exportar BD") para tener una copia propia.**

    A. Toggle y modo usuario (UI-01)
    1. Abrir la app. Debe arrancar en modo debug: se ve el `DiagnosticsPanel` bajo el switch de monitoreo.
    2. Tocar el título "Cingula" 5 veces seguidas (menos de 3 segundos). Esperado: SnackBar "Modo usuario activado" (~2 s) y la pantalla pasa a la vista de usuario.
    3. En modo usuario, confirmar que NO se ve el `DiagnosticsPanel` ni el acceso a "Datos de la BD (debug)". Confirmar que SÍ se ven: el control de monitoreo, el texto de estado, y el botón flotante "Iniciar".
    4. Iniciar monitoreo y confirmar que el banner rojo de grabación de micrófono NO aparece en modo usuario.
    5. Cerrar la app por completo (swipe del task switcher) y reabrirla. Esperado: sigue en modo usuario (persistió).
    6. Tocar el título 5 veces de nuevo. Esperado: SnackBar "Modo debug activado" y reaparece el `DiagnosticsPanel`.
    7. Tocar el título 3 veces, esperar 5 segundos, tocar 3 veces más. Esperado: NO cambia de modo (la ventana venció, no acumula).

    B. Acciones destructivas eliminadas (DATA-02)
    8. En modo debug, entrar a "Datos de la BD (debug)". Esperado: NO existen los botones "Recreate DB (debug)" ni "Importar BD". Sí siguen "Exportar BD", "Ver contenido BD" y "Limpiar triggers huérfanos".

    C. Recuperación de base de datos (DATA-01) — el punto crítico de la fase
    9. Cerrar la app por completo.
    10. Corromper el archivo de base con ADB (app en modo debuggable):
        `adb shell "run-as com.example.cingula_app sh -c 'printf BASURA_NO_ES_SQLITE > app_flutter/cingula.db'"`
        (ajustar el applicationId y el subdirectorio si difieren; el archivo vive en el directorio de documentos de la app.)
    11. Abrir la app en frío. Esperado, en este orden:
        - La app arranca y es usable — NO se queda en una pantalla bloqueada ni pide confirmación de nada.
        - Aparece un banner con "Base de datos restaurada automáticamente" y un cuerpo que nombra `cingula.db.corrupto-<fecha>-<hora>`.
        - Tocar "Entendido" lo descarta.
    12. Listar el directorio: `adb shell "run-as com.example.cingula_app ls -l app_flutter/"`. Esperado: existen TANTO `cingula.db` (nueva) COMO `cingula.db.corrupto-<timestamp>` (la vieja, intacta). El archivo corrupto NO fue borrado.
    13. Navegar por la app y volver a Home. Esperado: el banner NO reaparece (es one-shot).
    14. Restaurar los datos de trabajo importando la copia exportada en el paso previo... **NO es posible desde la app** (la importación se eliminó a propósito). Si hacía falta conservar los datos originales, restaurarlos empujando el archivo con `adb push` / `run-as cp` manualmente desde la copia del paso 0.
  </how-to-verify>

  <verify>
    <automated>flutter analyze</automated>
    <automated>flutter test</automated>
  </verify>

  <acceptance_criteria>
    - `flutter analyze` sin errores y `flutter test` verde antes de pedir la verificación al usuario.
    - Los 14 pasos de `<how-to-verify>` fueron ejecutados y su resultado real está registrado paso por paso en el SUMMARY.
    - El usuario respondió "aprobado", o los desvíos quedaron registrados como gaps para `/gsd:plan-phase 01 --gaps`.
  </acceptance_criteria>

  <resume-signal>Escribir "aprobado" si los 14 pasos dan el resultado esperado, o describir qué paso falló y con qué comportamiento real.</resume-signal>

  <done>El usuario confirmó en dispositivo real que la base corrupta se preserva renombrada, que el banner aparece sin bloquear, que el toggle alterna y persiste, y que no quedan botones destructivos.</done>
</task>

</tasks>

<verification>
- `flutter analyze` sin errores, `flutter test` verde.
- Greps de completitud en cero (`deleteDatabase(`, `recreateForTesting`, `importDatabase`, `file_picker`).
- Ninguna dependencia de producción agregada en la fase.
- Verificación manual aprobada por el usuario.
</verification>

<success_criteria>
1. Ante un fallo forzado de apertura, la app genera un respaldo renombrado con timestamp antes de crear la base nueva, y nunca borra sin respaldo (criterio 1 de la fase en ROADMAP.md).
2. No queda ninguna acción de recrear/importar base en el panel de debug (criterio 2 de la fase, resuelto por eliminación per D-03/D-04).
3. El toggle runtime alterna dashboard de usuario y panel de debug sin rebuild, y persiste entre reinicios (criterio 3 de la fase).
4. La auditoría ponytail está hecha y sus hallazgos (o su ausencia) quedan registrados en el SUMMARY.
</success_criteria>

<output>
Al completar, crear `.planning/phases/01-blindaje-de-datos-y-separaci-n-debug-usuario/01-04-SUMMARY.md`.
</output>
