---
phase: 02-backend-real-push-sync
plan: 05
type: execute
wave: 4
depends_on: ["02-02", "02-04"]
files_modified:
  - lib/data/sync/sync_trigger.dart
  - lib/data/sync/sync_api_http.dart
  - lib/data/sync/sync_backoff.dart
  - lib/core/di/service_locator.dart
  - backend/api/_lib/outbox.js
  - backend/api/sync/push.js
  - backend/README.md
  - .planning/phases/02-backend-real-push-sync/02-VALIDATION.md
autonomous: false
requirements: [SYNC-01, SYNC-02, SYNC-03, SYNC-06, SYNC-07, SYNC-08]

must_haves:
  truths:
    - "No quedó código nuevo de esta fase que sea abstracción especulativa, dead code o duplicación"
    - "El backend real está desplegado y la app compilada contra él sincroniza de punta a punta"
    - "Matar la app en medio de un push y reabrirla no produce filas duplicadas en Neon"
    - "Después de varios pushes sobre la misma entidad, Neon guarda exactamente 2 versiones"
  artifacts:
    - path: ".planning/phases/02-backend-real-push-sync/02-VALIDATION.md"
      provides: "tabla de verificación por tarea completada con los IDs reales y su estado"
  key_links:
    - from: "app Flutter compilada con --dart-define"
      to: "Neon synced_entities"
      via: "POST /sync/push contra el deploy real de Vercel"
      pattern: "synced_entities"
---

<objective>
Cerrar la fase: auditoría ponytail sobre todo el código nuevo, setup real de Neon + Vercel, y la
única verificación que los tests mockeados no pueden dar — un push de verdad, contra Postgres de
verdad, sobre HTTP de verdad.

Purpose: PROJECT.md exige la auditoría ponytail como parte del checklist de CADA fase, no sólo del
cierre del milestone. Y el research fue explícito: los tests con `MockClient` y con `node --test`
sin DB no validan ni el `ON CONFLICT` real de Postgres, ni el round-trip HTTP, ni el manejo real
del header de auth.

Output: código auditado, backend desplegado, y los 6 criterios de éxito de la fase confirmados
sobre hardware/infra real.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-real-push-sync/02-CONTEXT.md
@.planning/phases/02-backend-real-push-sync/02-VALIDATION.md
@.planning/phases/02-backend-real-push-sync/02-01-SUMMARY.md
@.planning/phases/02-backend-real-push-sync/02-02-SUMMARY.md
@.planning/phases/02-backend-real-push-sync/02-03-SUMMARY.md
@.planning/phases/02-backend-real-push-sync/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditoría ponytail del código nuevo de la fase</name>
  <files>lib/data/sync/sync_backoff.dart, lib/data/sync/sync_errors.dart, lib/data/sync/sync_api_http.dart, lib/data/sync/sync_trigger.dart, lib/data/sync/sync_client.dart, lib/core/di/service_locator.dart, lib/domain/usecases/run_sync_usecase.dart, backend/api/_lib/auth.js, backend/api/_lib/db.js, backend/api/_lib/outbox.js, backend/api/sync/push.js, backend/api/sync/state.js, backend/package.json</files>

  <read_first>
    - lib/data/sync/sync_backoff.dart
    - lib/data/sync/sync_errors.dart
    - lib/data/sync/sync_api_http.dart
    - lib/data/sync/sync_trigger.dart
    - lib/data/sync/sync_client.dart
    - lib/domain/usecases/run_sync_usecase.dart
    - lib/core/di/service_locator.dart
    - lib/data/datasources/local/sync_local_data_source.dart
    - backend/api/_lib/auth.js
    - backend/api/_lib/db.js
    - backend/api/_lib/outbox.js
    - backend/api/sync/push.js
    - backend/api/sync/state.js
    - CLAUDE.md (sección Constraints: la auditoría ponytail es requisito de fase, no opcional)
  </read_first>

  <action>
    Aplicar la skill `ponytail` (nivel full) sobre TODO el código creado o modificado en los planes
    02-01 a 02-04. Es una auditoría de borrado, no de agregado: el resultado esperado es un diff
    negativo o cero, nunca positivo (salvo comentarios `ponytail:` que documenten un techo conocido).

    Chequeos concretos a correr y resolver uno por uno:

    A. Dead code / código sin llamadores
       - `grep -rn "fetchState" lib/ test/` — si `SyncApiHttp.fetchState()` no lo llama NADIE
         (ni el panel, ni `RunSyncUseCase`, ni un test que no sea el suyo propio), es un endpoint
         y un método construidos para nadie. Decisión: borrar `fetchState` de `SyncApiHttp` y
         `backend/api/sync/state.js` NO se puede borrar sin romper el contrato `SyncApi`, así que
         la salida correcta es dejar `fetchState` implementado (el contrato lo exige) pero
         documentarlo con un comentario `// ponytail: sin llamadores hoy; existe porque SyncApi lo
         declara. Si la Fase 3 no lo usa, sacar el método del contrato.` — NO inventarle un uso.
       - Buscar cualquier otro símbolo público nuevo sin llamadores:
         `for s in SyncTriggerStatus.idle isRunning flush kBackoffBase; do grep -rn "$s" lib/ test/; done`
         Si `isRunning` o `flush` sólo aparecen en su definición y en tests, borrarlos: un getter
         que sólo existe para que un test pueda mirarlo es andamiaje.

    B. Duplicación
       - `_nextDeleteVersion` está duplicado en `geo_trigger_local_data_source.dart` y
         `geo_path_local_data_source.dart` (decisión deliberada del plan 02-01: 3 líneas x2 es más
         barato que un archivo compartido). Confirmar que sigue siendo 3 líneas; si creció,
         extraerlo a `SyncLocalDataSource` como método público.
       - `grep -c "throw SyncTransientException" lib/data/sync/sync_api_http.dart` — hay 4-5
         lanzamientos casi idénticos entre `pushOutbox` y `fetchState`. Si el bloque de
         clasificación de status codes está copiado textualmente, extraerlo a un solo helper
         privado `void _throwForStatus(http.Response r)`.

    C. Abstracciones especulativas
       - Verificar que NO exista ninguna interfaz con una sola implementación creada en esta fase
         (`grep -rn "abstract class" lib/data/sync/` debe devolver sólo `SyncApi`, que ya existía).
       - Verificar que no se haya agregado ninguna clase de configuración, factory, o "strategy"
         para el backoff: `grep -c "class " lib/data/sync/sync_backoff.dart` debe seguir en 0.

    D. Dependencias
       - `git diff main -- pubspec.yaml` debe mostrar exactamente DOS dependencias nuevas:
         `http` y `connectivity_plus`. Cualquier otra es de más y hay que justificarla o sacarla.
       - `backend/package.json` debe tener exactamente UNA dependency (`@neondatabase/serverless`)
         y CERO devDependencies. Si aparecieron `typescript`, `tsx`, `jest`, `vitest` o similares,
         sacarlas.

    E. Comentarios `ponytail:`
       - Cada simplificación con techo conocido debe tener su comentario nombrando el techo y el
         camino de upgrade. Verificar que existan al menos estos tres:
         1. En `sync_client.dart`: el `takeWhile` (head-of-line blocking a propósito)
         2. En `sync_api_http.dart`: 4xx no-401 tratados como transitorios
         3. En `backend/api/sync/push.js`: semántica todo-o-nada de `sql.transaction`
       - Si falta alguno, agregarlo.

    F. Suites verdes después de cualquier borrado
       - `flutter analyze && flutter test`
       - `cd backend && node --test`

    Registrar en el SUMMARY: qué se borró, qué se dejó y por qué. Si la auditoría no borró NADA,
    decirlo explícitamente y justificarlo — "no encontré nada" es una conclusión válida sólo si
    los seis chequeos de arriba se corrieron de verdad.
  </action>

  <verify>
    <automated>flutter analyze && flutter test && cd backend && node --test</automated>
  </verify>

  <acceptance_criteria>
    - `git diff --stat main -- lib/ backend/` muestra que el neto de líneas de esta tarea es <= 0, o el SUMMARY explica cada línea agregada
    - `grep -c "class " lib/data/sync/sync_backoff.dart` devuelve 0
    - `grep -rn "abstract class" lib/data/sync/` devuelve exactamente 1 coincidencia (`SyncApi`, preexistente)
    - `git diff main -- pubspec.yaml | grep "^+" | grep -c ":"` corresponde a exactamente 2 dependencias nuevas (`http`, `connectivity_plus`)
    - `grep -c "devDependencies" backend/package.json` devuelve 0
    - `grep -rc "ponytail:" lib/data/sync/ backend/api/` devuelve 3 o más en total
    - `flutter analyze` 0 errores, `flutter test` verde, `cd backend && node --test` verde
    - El SUMMARY lista explícitamente los 6 chequeos (A-F) con su resultado
  </acceptance_criteria>

  <done>El código nuevo de la fase pasó una auditoría de sobre-ingeniería documentada, con las suites verdes después de cualquier simplificación.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Setup de Neon + Vercel (cuentas y credenciales)</name>
  <files>ninguno en el repo — cuentas externas, env vars en Vercel, backend/schema.sql aplicado en Neon</files>

  <action>
    Tarea de acción humana — crear cuentas y autenticar CLIs requiere navegador y confirmación
    del usuario, Claude no puede hacerlo. Claude DEBE, antes de pausar:
    1. Confirmar que `backend/schema.sql` y `backend/vercel.json` existen y correr
       `cd backend && node --test` para verificar que el código está listo para desplegar.
    2. Presentar los 8 pasos de `<how-to-verify>` como una lista copiable, con los comandos
       exactos ya escritos (el usuario sólo reemplaza `<connection-string>`, `<token>` y
       `<tu-deploy>`).
    3. Recordar explícitamente que el valor de `SYNC_API_KEY` en Vercel y el de
       `--dart-define=CINGULA_SYNC_API_KEY` en el build de Flutter tienen que ser IDÉNTICOS —
       si difieren, todo va a dar 401.
    NO inventar credenciales, no commitear ningún token, no escribir el token en ningún archivo
    versionado (`backend/.env` está gitignoreado; `backend/.env.example` va vacío).
  </action>

  <verify>
    <automated>cd backend && node --test</automated>
    El resto lo verifica el usuario con los dos `curl` del paso 7 (401 sin token, 200 con token)
    y con `DATABASE_URL="&lt;dev&gt;" node --test` del paso 6.
  </verify>

  <done>El deploy responde 401 sin token y 200 con el token correcto; los tests de integración corren contra la branch dev de Neon sin saltearse.</done>

  <what-built>
    El backend en `backend/` está completo y testeado, pero necesita una cuenta de Neon (Postgres) y
    una de Vercel (hosting) para existir en internet. Crear cuentas y autenticar CLIs requiere un
    navegador y confirmación humana — Claude no puede hacerlo.
  </what-built>

  <how-to-verify>
    Correr estos pasos en orden. Cada uno tiene su comando exacto.

    1. Neon: crear cuenta gratis en https://neon.tech y un proyecto (región más cercana a
       Argentina disponible). Copiar el connection string POOLED
       (Console -> Project -> Connection Details -> "Pooled connection").

    2. Neon: crear una branch de desarrollo llamada `dev` (Console -> Branches -> New Branch,
       desde `main`). Copiar TAMBIÉN su connection string — se usa sólo para correr los tests de
       integración, para no ensuciar la branch principal.

    3. Aplicar el schema a las DOS branches. Con `psql` instalado:
       ```
       psql "<connection-string-main>" -f backend/schema.sql
       psql "<connection-string-dev>"  -f backend/schema.sql
       ```
       Sin `psql`: pegar el contenido de `backend/schema.sql` en el SQL Editor de la consola de Neon,
       una vez por branch.

    4. Generar el token compartido:
       ```
       openssl rand -hex 32
       ```
       Guardarlo — se usa en dos lugares (Vercel y el build de Flutter) y tiene que ser el MISMO.

    5. Vercel: crear cuenta gratis en https://vercel.com, luego:
       ```
       npm i -g vercel
       cd backend
       vercel login
       vercel link
       vercel env add DATABASE_URL production     # pegar el connection string de la branch main
       vercel env add SYNC_API_KEY production     # pegar el token del paso 4
       vercel --prod
       ```
       Anotar la URL de producción que imprime el deploy (algo como
       `https://cingula-sync-backend.vercel.app`).

    6. Correr el test de integración contra la branch dev (hasta ahora se salteaba):
       ```
       cd backend
       DATABASE_URL="<connection-string-dev>" node --test
       ```
       Los tests de `push.test.js` deben pasar, no saltearse.

    7. Smoke test del deploy real (verifica auth y routing en un solo comando):
       ```
       curl -i -X POST "https://<tu-deploy>.vercel.app/sync/push" \
         -H "Content-Type: application/json" -d '{"outbox":[]}'
       ```
       Esperado: `HTTP/1.1 401` con body `{"error":"invalid or missing API key"}`.

       ```
       curl -i -X POST "https://<tu-deploy>.vercel.app/sync/push" \
         -H "Authorization: Bearer <token del paso 4>" \
         -H "Content-Type: application/json" -d '{"outbox":[]}'
       ```
       Esperado: `HTTP/1.1 200` con `{"ackedIds":[],"serverCursor":null,"receivedAt":"..."}`.
       Si esto devuelve 404, el rewrite de `vercel.json` no se aplicó — revisar ese archivo.

    8. Pegar acá (o dejar en un archivo local no versionado) la URL del deploy y confirmar que el
       token quedó guardado, para poder compilar la app en la tarea siguiente.
  </how-to-verify>

  <acceptance_criteria>
    - `curl` sin header Authorization contra `/sync/push` del deploy devuelve HTTP 401
    - `curl` con el header correcto devuelve HTTP 200 con `ackedIds: []`
    - `DATABASE_URL="<dev>" node --test` desde `backend/` corre los tests de `push.test.js` sin saltearlos y todos pasan
    - La tabla `synced_entities` existe en las branches `main` y `dev` de Neon
    - El usuario tiene anotados la URL del deploy y el valor de `SYNC_API_KEY`
  </acceptance_criteria>

  <resume-signal>Pegar la URL del deploy (sin el token) y escribir "listo", o describir en qué paso se trabó.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación end-to-end contra el backend real</name>
  <files>ninguno — verificación en dispositivo contra el deploy real</files>

  <action>
    Tarea de verificación humana — requiere un celular real, modo avión, matar la app desde
    recientes, y consultar Neon. Claude no puede ejecutarla. Claude DEBE, antes de pausar:
    1. Armar e imprimir el comando de `flutter run` completo con la URL del deploy y el token que
       el usuario dio en la tarea 2.
    2. Armar e imprimir las consultas SQL exactas de cada caso, con el `record_uuid` ya sustituido
       cuando el usuario lo informe.
    3. Presentar los 7 casos numerados y recolectar el resultado de cada uno.
    Los casos 1-6 corresponden uno a uno con los 6 criterios de éxito de la Fase 2 en ROADMAP.md;
    el caso 7 cubre la constraint más alta del proyecto (integridad de los datos ya en el celular).
    NO marcar la fase como cumplida si algún caso falla — reportarlo y proponer un plan de gap.
  </action>

  <verify>
    <automated>cd backend && node --test</automated>
    Verificación real: las consultas SQL contra Neon de cada caso, ejecutadas por el usuario.
    La consulta clave de no-duplicación es
    `SELECT record_uuid, count(*) FROM synced_entities GROUP BY record_uuid HAVING count(*) > 1;`
    que debe devolver 0 filas.
  </verify>

  <done>El usuario confirmó los 7 casos, o listó cuáles fallaron con lo observado en Neon y en el panel.</done>

  <what-built>
    Toda la cadena de la fase: escritura local -> outbox -> push automático -> HTTP autenticado ->
    upsert versionado en Neon, con backoff en el medio y estado visible en el panel de debug.
  </what-built>

  <how-to-verify>
    Compilar la app apuntando al deploy real (usar la URL y el token de la tarea 2):
    ```
    flutter run \
      --dart-define=CINGULA_API_BASE_URL=https://<tu-deploy>.vercel.app \
      --dart-define=CINGULA_SYNC_API_KEY=<token>
    ```
    Para inspeccionar Neon en cada paso, usar el SQL Editor de la consola o:
    ```
    psql "<connection-string-main>" -c "SELECT table_name, record_uuid, op, current_version, previous_version, deleted_at FROM synced_entities ORDER BY current_updated_at DESC LIMIT 10;"
    ```

    CASO 1 — Criterio de éxito 1 de la fase (SYNC-01)
    Crear o editar un geo_trigger en la app estando online. NO apretar ningún botón de sync.
    Esperar ~5 segundos. Consultar Neon.
    Esperado: aparece una fila con ese `record_uuid` y `current_version` = la versión local.

    CASO 2 — Criterio de éxito 2 de la fase (SYNC-02, kill mid-push)
    Anotar el estado de la fila en Neon (`current_version`, `current_updated_at`).
    Poner el celular en modo avión, editar la misma entidad (queda pendiente en el outbox), sacar
    el modo avión y MATAR la app (swipe desde recientes) dentro del primer segundo del push.
    Reabrir la app, entrar al panel de debug, apretar "Forzar push ahora".
    Consultar Neon.
    Esperado: EXACTAMENTE UNA fila para ese `record_uuid` (no dos), con la versión nueva.
    Correr también:
    `SELECT record_uuid, count(*) FROM synced_entities GROUP BY record_uuid HAVING count(*) > 1;`
    Esperado: 0 filas.

    CASO 3 — Criterio de éxito 3 de la fase (SYNC-03, offline + backoff)
    Modo avión. Editar la entidad 3 veces (3 filas de outbox).
    Panel: "Outbox pendiente" debe ser 3, "Estado" debe indicar error de red.
    Verificar que NO aparece ningún texto sugiriendo que está todo sincronizado (SYNC-08).
    Sacar el modo avión y esperar hasta ~30 segundos sin tocar nada (el backoff puede haber
    agendado el reintento un poco adelante).
    Esperado: "Outbox pendiente: 0" y las versiones reflejadas en Neon.

    CASO 4 — Criterio de éxito 4 de la fase (SYNC-07)
    Ya cubierto por el `curl` sin token de la tarea 2 (401). Confirmar además desde la app:
    recompilar con `--dart-define=CINGULA_SYNC_API_KEY=clave-mala`, editar una entidad.
    Esperado: el panel muestra el error de AUTH específico, la fila queda pendiente, y NO aparece
    una tormenta de requests en los logs de Vercel (`vercel logs <deploy-url>`).

    CASO 5 — Criterio de éxito 5 de la fase (SYNC-08)
    Ya observado en los casos 3 y 4. Confirmar explícitamente: en ningún momento el panel mostró
    "Outbox pendiente" > 0 junto con un estado que dijera que está todo sincronizado.

    CASO 6 — Criterio de éxito 6 de la fase (SYNC-06, ventana de 2 versiones)
    Editar la MISMA entidad 4 veces seguidas, estando online, esperando ~4 segundos entre cada
    edición para que cada una se sincronice por separado. Después consultar Neon:
    ```
    psql "<connection-string-main>" -c "SELECT current_version, previous_version, previous_payload->>'logical_version' AS prev_payload_version FROM synced_entities WHERE record_uuid = '<uuid>';"
    ```
    Esperado: `current_version` = la última versión, `previous_version` = la anteúltima, y NINGÚN
    rastro de las versiones anteriores a esas dos. La tabla tiene una sola fila por entidad, así
    que el historial ilimitado es estructuralmente imposible — confirmar que es así.

    CASO 7 — Regresión de datos (Constraint más alta del proyecto)
    Confirmar que después de todo lo anterior, los recorridos/triggers/audios que ya estaban en el
    celular ANTES de esta fase siguen ahí y se reproducen normalmente. La migración a schema v6 sólo
    agregó una columna, pero esto se verifica, no se asume.
  </how-to-verify>

  <acceptance_criteria>
    - Caso 1: la entidad aparece en `synced_entities` sin haber disparado sync manualmente
    - Caso 2: `SELECT record_uuid, count(*) ... HAVING count(*) > 1` devuelve 0 filas
    - Caso 3: con modo avión el panel muestra 3 pendientes y estado de error; al reconectar drena a 0
    - Caso 4: key inválida produce el mensaje de auth y no un bucle de reintentos en los logs de Vercel
    - Caso 5: el panel nunca declaró sincronización completa con pendientes > 0
    - Caso 6: `current_version` y `previous_version` son las dos últimas y no hay más versiones
    - Caso 7: los datos preexistentes en el celular siguen intactos y reproducibles
  </acceptance_criteria>

  <resume-signal>Escribir "approved" si los 7 casos pasan, o listar cuáles fallaron con lo que se observó en Neon/en el panel.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Cerrar la tabla de validación de la fase</name>
  <files>.planning/phases/02-backend-real-push-sync/02-VALIDATION.md</files>

  <read_first>
    - .planning/phases/02-backend-real-push-sync/02-VALIDATION.md (archivo a modificar: la tabla "Per-Task Verification Map" tiene las columnas Task ID / Plan / Wave con placeholders "TBD" y "02-XX")
    - .planning/phases/02-backend-real-push-sync/02-01-SUMMARY.md
    - .planning/phases/02-backend-real-push-sync/02-02-SUMMARY.md
    - .planning/phases/02-backend-real-push-sync/02-03-SUMMARY.md
    - .planning/phases/02-backend-real-push-sync/02-04-SUMMARY.md
  </read_first>

  <action>
    Actualizar `.planning/phases/02-backend-real-push-sync/02-VALIDATION.md`:

    1. En el frontmatter: `status: draft` -> `status: complete`, `nyquist_compliant: false` -> `true`,
       `wave_0_complete: false` -> `true`.

    2. En la tabla "Per-Task Verification Map", reemplazar cada `02-XX` / `TBD` con los valores
       reales, quedando así (ajustar el estado a lo que efectivamente pasó):

       | Task ID | Plan | Wave | Requirement | Automated Command |
       |---------|------|------|-------------|-------------------|
       | 02-01-T2 | 02-01 | 1 | SYNC-03 | `flutter test test/data/sync/sync_backoff_test.dart` |
       | 02-01-T3 | 02-01 | 1 | SYNC-02 | `flutter test test/data/sync/` |
       | 02-02-T1 | 02-02 | 1 | SYNC-07 | `cd backend && node --test api/_lib/auth.test.js` |
       | 02-02-T2 | 02-02 | 1 | SYNC-02 | `cd backend && node --test api/_lib/outbox.test.js` |
       | 02-02-T3 | 02-02 | 1 | SYNC-02, SYNC-06 | `cd backend && DATABASE_URL=<neon-dev> node --test api/sync/push.test.js` |
       | 02-03-T1 | 02-03 | 2 | SYNC-01, SYNC-03, SYNC-07 | `flutter test test/data/sync/sync_api_http_test.dart` |
       | 02-03-T2 | 02-03 | 2 | SYNC-01, SYNC-03 | `flutter test test/domain/usecases/run_sync_usecase_test.dart` |
       | 02-04-T1 | 02-04 | 3 | SYNC-01, SYNC-03 | `flutter test test/data/sync/sync_trigger_test.dart` |
       | 02-04-T4 | 02-04 | 3 | SYNC-08 | manual (device) |
       | 02-05-T3 | 02-05 | 4 | los 6 | manual (end-to-end contra Neon real) |

       Corregir cualquier fila cuyo comando o archivo real haya terminado siendo distinto de lo
       planeado — la tabla tiene que describir lo que EXISTE, no lo que se pensó.

    3. Marcar los items de "Wave 0 Requirements" que se completaron, corrigiendo las extensiones
       (`.ts` -> `.js` en los dos archivos de backend, porque el backend terminó en JavaScript ESM,
       y agregando `sync_backoff_test.dart` y `sync_trigger_test.dart` que no estaban previstos).

    4. Marcar los checkboxes de "Validation Sign-Off" y poner `**Approval:** approved <fecha>`.

    NO reescribir el documento entero ni cambiar su estructura: es una actualización de estado.
  </action>

  <verify>
    <automated>grep -c "TBD\|02-XX" .planning/phases/02-backend-real-push-sync/02-VALIDATION.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "TBD" .planning/phases/02-backend-real-push-sync/02-VALIDATION.md` devuelve 0
    - `grep -c "02-XX" .planning/phases/02-backend-real-push-sync/02-VALIDATION.md` devuelve 0
    - `grep -n "nyquist_compliant: true" .planning/phases/02-backend-real-push-sync/02-VALIDATION.md` encuentra una coincidencia
    - `grep -n "wave_0_complete: true" .planning/phases/02-backend-real-push-sync/02-VALIDATION.md` encuentra una coincidencia
    - `grep -c "push.test.ts\|auth.test.ts" .planning/phases/02-backend-real-push-sync/02-VALIDATION.md` devuelve 0 (las extensiones reales son `.js`)
    - `grep -n "Approval:\*\* approved" .planning/phases/02-backend-real-push-sync/02-VALIDATION.md` encuentra una coincidencia
    - Cada requisito de la fase (SYNC-01, 02, 03, 06, 07, 08) aparece al menos una vez en la columna Requirement
  </acceptance_criteria>

  <done>La tabla de validación describe los tests que existen de verdad, con sus comandos reales y su estado.</done>
</task>

</tasks>

<verification>
1. `flutter analyze` — 0 errores
2. `flutter test` — verde
3. `cd backend && node --test` — verde; y con `DATABASE_URL` de la branch dev, los tests de integración corren (no se saltean) y pasan
4. Los 7 casos de la verificación end-to-end (Task 3) aprobados por el usuario
5. `grep -c "TBD\|02-XX" .planning/phases/02-backend-real-push-sync/02-VALIDATION.md` devuelve 0
</verification>

<success_criteria>
Los 6 criterios de éxito de la Fase 2 del ROADMAP confirmados sobre infraestructura real:
1. Crear/editar una entidad online la manda al backend sin sync manual (SYNC-01)
2. Matar la app en medio de un push no genera filas duplicadas (SYNC-02)
3. Escribir offline encola y reintenta con backoff al reconectar (SYNC-03)
4. Requests sin API key válida son rechazados (SYNC-07)
5. La UI muestra pendientes reales y último sync, sin falsos "todo sincronizado" (SYNC-08)
6. El backend conserva sólo estado actual + 1 versión anterior (SYNC-06)

Más: auditoría ponytail documentada, y datos preexistentes del celular intactos.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/02-backend-real-push-sync/02-05-SUMMARY.md`
</output>
