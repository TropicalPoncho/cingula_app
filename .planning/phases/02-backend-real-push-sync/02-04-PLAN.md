---
phase: 02-backend-real-push-sync
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - pubspec.yaml
  - lib/data/sync/sync_trigger.dart
  - lib/data/datasources/local/sync_local_data_source.dart
  - lib/core/di/service_locator.dart
  - lib/presentation/pages/home/widgets/diagnostics_panel.dart
  - test/data/sync/sync_trigger_test.dart
autonomous: false
requirements: [SYNC-01, SYNC-03, SYNC-08]

user_setup:
  - service: android
    why: "connectivity_plus necesita el permiso de estado de red para observar reconexiones (D-05)"
    dashboard_config:
      - task: "Verificar que android/app/src/main/AndroidManifest.xml declare ACCESS_NETWORK_STATE (lo agrega la tarea 2 si falta)"
        location: "android/app/src/main/AndroidManifest.xml"

must_haves:
  truths:
    - "Grabar/editar una entidad estando online la manda al backend sin apretar ningún botón"
    - "Un borrado en cascada que encola 5 filas de outbox dispara UN push, no cinco"
    - "Nunca hay dos pushes corriendo a la vez sobre el mismo outbox"
    - "Recuperar conectividad después de escribir offline dispara un push de lo pendiente"
    - "El panel de debug muestra la cantidad real de pendientes y el último sync exitoso, y muestra explícitamente cuando el último intento falló por auth"
    - "El botón manual de push sigue existiendo (D-06)"
  artifacts:
    - path: "lib/data/sync/sync_trigger.dart"
      provides: "SyncTrigger: debounce + guard de un solo push en vuelo + último estado observable"
      exports: ["SyncTrigger", "SyncTriggerStatus"]
    - path: "test/data/sync/sync_trigger_test.dart"
      provides: "cobertura de coalescing, no-reentrancia y clasificación de estado"
    - path: "lib/data/datasources/local/sync_local_data_source.dart"
      provides: "callback onOutboxEnqueued disparado una vez por fila encolada"
      contains: "onOutboxEnqueued"
  key_links:
    - from: "lib/data/datasources/local/sync_local_data_source.dart"
      to: "lib/data/sync/sync_trigger.dart"
      via: "onOutboxEnqueued -> SyncTrigger.schedule(), cableado en el service locator"
      pattern: "onOutboxEnqueued"
    - from: "lib/data/sync/sync_trigger.dart"
      to: "connectivity_plus"
      via: "onConnectivityChanged -> schedule() cuando aparece una interfaz de red"
      pattern: "onConnectivityChanged"
    - from: "lib/presentation/pages/home/widgets/diagnostics_panel.dart"
      to: "lib/data/sync/sync_trigger.dart"
      via: "suscripción a statusStream para refrescar el estado mostrado"
      pattern: "statusStream"
---

<objective>
Hacer que el push se dispare solo: inmediatamente después de cada escritura local (D-04) y al
recuperar conectividad (D-05), sin loops de polling. Y dejar el panel de debug diciendo la verdad
sobre el estado de sync, incluido el caso de API key inválida.

Purpose: cierra SYNC-01 (el push llega de verdad, sin intervención) y SYNC-08 (estado honesto).
El Core Value del proyecto es minimizar la ventana entre "grabé en el campo" y "está respaldado":
eso es exactamente lo que hace este plan.

Output: `SyncTrigger` con debounce y guard de reentrancia, cableado en el único punto por donde
pasan todas las escrituras, más el panel de debug actualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-real-push-sync/02-CONTEXT.md
@.planning/phases/02-backend-real-push-sync/02-RESEARCH.md

<interfaces>
Punto de enganche (hallazgo clave del research, verificado por lectura directa de código):
los CUATRO data sources locales (`AudioLocalDataSource`, `GeoTriggerLocalDataSource`,
`GeoPathLocalDataSource`, `RegionLocalDataSource`) encolan outbox a través de UN solo método,
`SyncLocalDataSource.enqueueOutbox()` — 16+ call sites, un solo embudo. El disparo automático se
cablea ahí, NO en los 16 call sites.

`lib/data/datasources/local/sync_local_data_source.dart` (método a modificar):
```dart
Future<void> enqueueOutbox({
  required String tableName,
  required String recordUuid,
  required String op,
  Map<String, Object?>? payload,
  String? deviceId,
}) async {
  await _database.insert('sync_outbox', { /* ... */ });
}
```

`lib/domain/usecases/run_sync_usecase.dart` (post-02-03):
```dart
class RunSyncUseCase {
  RunSyncUseCase({required SyncClient client, required SyncApi api});
  Future<SyncRunResult> pushOutboxOnce({int limit = 50});
  // lanza SyncAuthException (terminal) o SyncTransientException (reintentable)
}
class SyncRunResult {
  final int totalOutbox; final int acked; final String? serverCursor; final DateTime syncedAt;
  bool get hadWork; int get unacked;
}
```

`lib/data/sync/sync_errors.dart` (creado en 02-03):
```dart
class SyncAuthException implements Exception { SyncAuthException(this.message); final String message; }
class SyncTransientException implements Exception { SyncTransientException(this.message); final String message; }
```

`DiagnosticsPanel` (`lib/presentation/pages/home/widgets/diagnostics_panel.dart`) — estado actual
relevante: campos `DateTime? _lastSync`, `int _outboxCount`, `String? _serverCursor`; método
`Future<void> _refreshSyncStatus()` (~línea 102) que lee `SyncClient.getState()` y
`SyncClient.pendingOutbox(limit: 200)`; sección `ExpansionTile(title: Text('Panel: Sincronización'))`
(~línea 477) con los `Text` de cursor/última sync/outbox pendiente y dos botones
('Refrescar estado' y 'Push outbox (stub)').

`connectivity_plus` ^7.3.1 — OJO, API v7 (breaking respecto de versiones viejas):
```dart
Future<List<ConnectivityResult>> checkConnectivity();
Stream<List<ConnectivityResult>> get onConnectivityChanged;  // devuelve LISTA, no un valor único
```
"Hay conexión" = la lista contiene algo distinto de `ConnectivityResult.none`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SyncTrigger — debounce, un solo push en vuelo, estado observable</name>
  <files>lib/data/sync/sync_trigger.dart, test/data/sync/sync_trigger_test.dart</files>

  <read_first>
    - lib/domain/usecases/run_sync_usecase.dart (post-02-03: firma de `pushOutboxOnce` y las excepciones que lanza)
    - lib/data/sync/sync_errors.dart (los dos tipos de error a clasificar)
    - test/data/sync/sync_backoff_test.dart (estilo de test del repo)
  </read_first>

  <behavior>
    Tests en `test/data/sync/sync_trigger_test.dart` con un `class _FakeRunSync implements RunSyncUseCase`
    (implements sobre clase concreta: legal en Dart, no invoca el constructor, no necesita DB):
    - `schedule()` llamado 5 veces seguidas dentro de la ventana de debounce produce UNA sola
      llamada a `pushOutboxOnce` (coalescing: el borrado en cascada de 5 filas = 1 push)
    - `schedule()` mientras hay un push en vuelo NO arranca un segundo push concurrente;
      encola una re-corrida que arranca cuando el primero termina
    - Después de un push exitoso, `lastStatus` es `SyncTriggerStatus.ok`
    - Si `pushOutboxOnce` lanza `SyncTransientException`, `lastStatus` es `SyncTriggerStatus.transientError`
      y NO se propaga la excepción (el disparo es fire-and-forget: no puede romper la escritura que lo originó)
    - Si lanza `SyncAuthException`, `lastStatus` es `SyncTriggerStatus.authError`
    - Cada cambio de estado se emite por `statusStream`
    - `dispose()` cancela el timer pendiente y cierra el stream sin lanzar
  </behavior>

  <action>
    Crear `lib/data/sync/sync_trigger.dart`:

    ```dart
    import 'dart:async';

    import '../../domain/usecases/run_sync_usecase.dart';
    import 'sync_errors.dart';

    enum SyncTriggerStatus { idle, running, ok, transientError, authError }

    /// Dispara pushes del outbox de forma automática, coalescida y no reentrante.
    ///
    /// NO hay timer periódico ni loop de polling: los disparos son puramente por evento
    /// (escritura local, reconexión, botón manual). Esto es deliberado — todo este milestone
    /// existe en parte para eliminar el polling continuo que drena batería.
    class SyncTrigger {
      SyncTrigger({
        required RunSyncUseCase runSync,
        Duration debounce = const Duration(seconds: 2),
      })  : _runSync = runSync,
            _debounce = debounce;

      final RunSyncUseCase _runSync;
      final Duration _debounce;
      final _statusController = StreamController<SyncTriggerStatus>.broadcast();

      Timer? _timer;
      Future<void>? _inFlight;
      bool _rerunRequested = false;
      SyncTriggerStatus _lastStatus = SyncTriggerStatus.idle;

      SyncTriggerStatus get lastStatus => _lastStatus;
      Stream<SyncTriggerStatus> get statusStream => _statusController.stream;
      bool get isRunning => _inFlight != null;

      /// Agenda un push. Llamadas repetidas dentro de la ventana de debounce colapsan en una.
      /// Fire-and-forget: nunca lanza, para no romper la escritura local que lo disparó.
      void schedule() {
        _timer?.cancel();
        _timer = Timer(_debounce, () => unawaited(_run()));
      }

      /// Corre un push ya mismo y espera a que termine. Para tests y para el botón manual.
      Future<void> flush() {
        _timer?.cancel();
        return _run();
      }

      Future<void> _run() {
        final current = _inFlight;
        if (current != null) {
          // Ya hay uno corriendo: pedir una re-corrida en vez de solapar pushes sobre el mismo outbox.
          _rerunRequested = true;
          return current;
        }
        final future = _execute();
        _inFlight = future;
        return future;
      }

      Future<void> _execute() async {
        _emit(SyncTriggerStatus.running);
        try {
          await _runSync.pushOutboxOnce();
          _emit(SyncTriggerStatus.ok);
        } on SyncAuthException {
          _emit(SyncTriggerStatus.authError);
        } on SyncTransientException {
          _emit(SyncTriggerStatus.transientError);
        } catch (_) {
          _emit(SyncTriggerStatus.transientError);
        } finally {
          _inFlight = null;
        }
        if (_rerunRequested) {
          _rerunRequested = false;
          await _run();
        }
      }

      void _emit(SyncTriggerStatus status) {
        _lastStatus = status;
        if (!_statusController.isClosed) _statusController.add(status);
      }

      void dispose() {
        _timer?.cancel();
        _statusController.close();
      }
    }
    ```

    Crear `test/data/sync/sync_trigger_test.dart` cubriendo cada caso de `<behavior>`. Construir el
    `SyncTrigger` con `debounce: Duration(milliseconds: 10)` para que los tests sean rápidos, y usar
    `flush()` (no `schedule()` + sleep) donde el test quiera determinismo. Para el caso de coalescing
    sí hace falta `schedule()` x5 + un `await Future.delayed(Duration(milliseconds: 50))`.

    NO agregar: cola de trabajos persistente, política de reintentos propia (eso ya vive en el
    backoff del outbox), ni configuración de debounce por tipo de entidad.
  </action>

  <verify>
    <automated>flutter test test/data/sync/sync_trigger_test.dart</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "class SyncTrigger" lib/data/sync/sync_trigger.dart` encuentra una coincidencia
    - `grep -n "enum SyncTriggerStatus" lib/data/sync/sync_trigger.dart` encuentra una coincidencia y la enum incluye `authError` y `transientError`
    - `grep -n "_rerunRequested" lib/data/sync/sync_trigger.dart` encuentra 3 o más coincidencias (guard de reentrancia)
    - `grep -c "Timer.periodic" lib/data/sync/sync_trigger.dart` devuelve 0 (no hay polling)
    - `grep -n "on SyncAuthException" lib/data/sync/sync_trigger.dart` encuentra una coincidencia
    - `grep -n "Stream<SyncTriggerStatus> get statusStream" lib/data/sync/sync_trigger.dart` encuentra una coincidencia
    - `flutter test test/data/sync/sync_trigger_test.dart` pasa con al menos 7 tests
    - `grep -c "sqflite" test/data/sync/sync_trigger_test.dart` devuelve 0
  </acceptance_criteria>

  <done>Existe un disparador de push que colapsa ráfagas de escrituras en un solo push, nunca corre dos a la vez, y expone su último resultado sin lanzar excepciones hacia el llamador.</done>
</task>

<task type="auto">
  <name>Task 2: Cablear el disparo automático — enqueueOutbox + reconexión + DI</name>
  <files>pubspec.yaml, lib/data/datasources/local/sync_local_data_source.dart, lib/core/di/service_locator.dart, android/app/src/main/AndroidManifest.xml</files>

  <read_first>
    - lib/data/datasources/local/sync_local_data_source.dart (archivo a modificar; ver `enqueueOutbox` post-02-01)
    - lib/core/di/service_locator.dart (registro de `SyncLocalDataSource`, `SyncClient`, `SyncApi`, `RunSyncUseCase`, ~líneas 50-70)
    - lib/data/sync/sync_trigger.dart (creado en Task 1)
    - pubspec.yaml (`connectivity_plus` NO está todavía)
    - android/app/src/main/AndroidManifest.xml (verificar si ya declara ACCESS_NETWORK_STATE)
  </read_first>

  <action>
    PASO 1 — `pubspec.yaml`: agregar `  connectivity_plus: ^7.3.1` en `dependencies:`. Correr
    `flutter pub get`.

    PASO 2 — `lib/data/datasources/local/sync_local_data_source.dart`: agregar un callback público
    y dispararlo al final de `enqueueOutbox`. Este es el ÚNICO cambio necesario para D-04: los
    cuatro data sources ya pasan por acá, así que no hay que tocar ninguno de sus 16+ call sites.

    ```dart
    /// Se invoca después de encolar una fila de outbox. El service locator lo cablea a
    /// SyncTrigger.schedule() para que cada escritura local dispare un push (D-04).
    /// Nullable a propósito: en tests y en el arranque temprano puede no haber trigger todavía.
    void Function()? onOutboxEnqueued;
    ```
    y al final del cuerpo de `enqueueOutbox`, después del `await _database.insert(...)`:
    ```dart
    onOutboxEnqueued?.call();
    ```

    PASO 3 — `lib/core/di/service_locator.dart`: registrar `SyncTrigger` y cablear las dos fuentes
    de disparo. Insertar DESPUÉS del registro de `RunSyncUseCase` y ANTES del registro de los cuatro
    data sources:

    ```dart
    // Disparo automático de push: tras cada escritura local (D-04) y al recuperar red (D-05).
    getIt.registerLazySingleton<SyncTrigger>(
      () => SyncTrigger(runSync: getIt<RunSyncUseCase>()),
    );

    // D-04: toda escritura local pasa por enqueueOutbox, así que un solo cable alcanza.
    getIt<SyncLocalDataSource>().onOutboxEnqueued = () => getIt<SyncTrigger>().schedule();

    // D-05: al aparecer una interfaz de red, reintentar lo pendiente.
    // connectivity_plus reporta TIPO de interfaz, no alcanzabilidad real (puede decir
    // "conectado" en un portal cautivo sin internet). Por eso se usa sólo como pre-check
    // barato para decidir CUÁNDO intentar; la verdad sobre si el push funcionó sigue siendo
    // la respuesta HTTP.
    _connectivitySubscription?.cancel();
    _connectivitySubscription = Connectivity().onConnectivityChanged.listen((results) {
      final online = results.any((r) => r != ConnectivityResult.none);
      if (online) getIt<SyncTrigger>().schedule();
    });
    ```
    Declarar arriba del archivo, junto a `final getIt = GetIt.instance;`:
    ```dart
    StreamSubscription<List<ConnectivityResult>>? _connectivitySubscription;
    ```
    (el `cancel()` antes de resuscribir importa porque `setupServiceLocator(reinitialize: true)` se
    llama en ejecuciones de background y si no se cancelaría acumularía suscripciones).

    Imports a agregar en `service_locator.dart`:
    ```dart
    import 'dart:async';
    import 'package:connectivity_plus/connectivity_plus.dart';
    import '../../data/sync/sync_trigger.dart';
    ```

    PASO 4 — `android/app/src/main/AndroidManifest.xml`: si NO existe ya, agregar dentro de
    `<manifest>` y antes de `<application>`:
    ```xml
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    ```
    Si ya está, no duplicarlo.

    NO agregar: `WorkManager`, tarea periódica, ni servicio en foreground para sync. Los dos
    disparos por evento (escritura, reconexión) más el botón manual cubren SYNC-01/SYNC-03; un
    scheduler periódico sería exactamente el patrón de drenaje de batería que este milestone borra.
  </action>

  <verify>
    <automated>flutter analyze && flutter test</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "connectivity_plus: \^7" pubspec.yaml` encuentra una coincidencia
    - `grep -n "void Function()? onOutboxEnqueued" lib/data/datasources/local/sync_local_data_source.dart` encuentra una coincidencia
    - `grep -n "onOutboxEnqueued?.call()" lib/data/datasources/local/sync_local_data_source.dart` encuentra exactamente una coincidencia (un solo punto de disparo)
    - `grep -c "onOutboxEnqueued" lib/data/datasources/local/audio_local_data_source.dart lib/data/datasources/local/geo_trigger_local_data_source.dart lib/data/datasources/local/geo_path_local_data_source.dart lib/data/datasources/local/region_local_data_source.dart` devuelve 0 en los cuatro (el cable va en el embudo, no en los call sites)
    - `grep -n "registerLazySingleton<SyncTrigger>" lib/core/di/service_locator.dart` encuentra una coincidencia
    - `grep -n "onConnectivityChanged" lib/core/di/service_locator.dart` encuentra una coincidencia
    - `grep -n "_connectivitySubscription?.cancel()" lib/core/di/service_locator.dart` encuentra una coincidencia
    - `grep -c "Timer.periodic\|Workmanager().registerPeriodic" lib/core/di/service_locator.dart lib/data/sync/sync_trigger.dart` devuelve 0
    - `grep -n "ACCESS_NETWORK_STATE" android/app/src/main/AndroidManifest.xml` encuentra exactamente una coincidencia
    - `flutter analyze` sale con 0 errores
    - `flutter test` verde
  </acceptance_criteria>

  <done>Cada escritura local y cada reconexión de red agendan un push automáticamente, con un solo punto de cableado y sin ningún timer periódico.</done>
</task>

<task type="auto">
  <name>Task 3: Panel de debug con estado de sync honesto</name>
  <files>lib/presentation/pages/home/widgets/diagnostics_panel.dart</files>

  <read_first>
    - lib/presentation/pages/home/widgets/diagnostics_panel.dart (archivo a modificar: `_refreshSyncStatus` ~línea 102, campos de estado ~líneas 53-54, sección 'Panel: Sincronización' ~línea 477-518)
    - lib/data/sync/sync_trigger.dart (creado en Task 1: `statusStream`, `lastStatus`, `flush()`)
  </read_first>

  <action>
    PASO 1 — agregar campos de estado junto a `_lastSync` / `_outboxCount`:
    ```dart
    SyncTriggerStatus _syncStatus = SyncTriggerStatus.idle;
    StreamSubscription<SyncTriggerStatus>? _syncStatusSub;
    ```

    PASO 2 — en `initState`, después de la llamada existente a `_refreshSyncStatus()`, suscribirse:
    ```dart
    _syncStatus = getIt<SyncTrigger>().lastStatus;
    _syncStatusSub = getIt<SyncTrigger>().statusStream.listen((status) {
      if (!mounted) return;
      setState(() => _syncStatus = status);
      // El contador de pendientes cambia con cada push: refrescarlo para que nunca quede viejo.
      _refreshSyncStatus();
    });
    ```
    y en `dispose()` agregar `_syncStatusSub?.cancel();`.
    Imports a agregar: `import '../../../../data/sync/sync_trigger.dart';` (y `dart:async` si no está).

    PASO 3 — agregar un helper privado en el State que traduce el estado a texto en español:
    ```dart
    String get _syncStatusLabel {
      switch (_syncStatus) {
        case SyncTriggerStatus.idle:
          return 'sin intentos todavía';
        case SyncTriggerStatus.running:
          return 'enviando...';
        case SyncTriggerStatus.ok:
          return _outboxCount > 0
              ? 'último push OK, quedan $_outboxCount pendientes'
              : 'último push OK, outbox vacío';
        case SyncTriggerStatus.transientError:
          return 'ERROR de red/servidor — reintenta con backoff';
        case SyncTriggerStatus.authError:
          return 'ERROR DE AUTH (401) — revisar --dart-define=CINGULA_SYNC_API_KEY. No se reintenta.';
      }
    }
    ```
    El caso `ok` NUNCA dice "todo sincronizado" a secas: si quedan pendientes lo dice explícitamente.
    Eso es literalmente lo que pide SYNC-08.

    PASO 4 — en la sección `ExpansionTile(title: Text('Panel: Sincronización'))`, agregar debajo del
    `Text('Outbox pendiente: $_outboxCount')`:
    ```dart
    Text('Estado: $_syncStatusLabel'),
    Text('Backend: ${ApiConfig.baseUrl}'),
    Text('API key: ${ApiConfig.apiKey.isEmpty ? 'NO CONFIGURADA' : 'configurada (${ApiConfig.apiKey.length} chars)'}'),
    ```
    (nunca imprimir la key entera, sólo su largo). Agregar el import de `ApiConfig` si falta:
    `import '../../../../core/config/api_config.dart';`

    PASO 5 — el botón manual se QUEDA (D-06: no es una acción destructiva, es el respaldo de debug
    para forzar un push). Sólo cambiar su label de `'Push outbox (stub)'` a `'Forzar push ahora'`,
    porque ya no hay stub. Su `onPressed` sigue llamando `getIt<RunSyncUseCase>().pushOutboxOnce(limit: 100)`
    y el `catch (e)` que muestra el SnackBar `'Error en sync: $e'` se mantiene tal cual — con
    `SyncApiHttp` ahora ese mensaje va a decir `SyncAuthException: ...` o `SyncTransientException: ...`,
    que es información útil sin trabajo extra.

    NO agregar: indicador de sync en el modo usuario final (sigue siendo debug-only, ver Deferred
    Ideas del CONTEXT.md), ni gráficos, ni historial de intentos.
  </action>

  <verify>
    <automated>flutter analyze lib/presentation/pages/home/widgets/diagnostics_panel.dart && flutter test</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "_syncStatusLabel" lib/presentation/pages/home/widgets/diagnostics_panel.dart` encuentra 2 o más coincidencias (definición + uso)
    - `grep -n "statusStream.listen" lib/presentation/pages/home/widgets/diagnostics_panel.dart` encuentra una coincidencia
    - `grep -n "_syncStatusSub?.cancel()" lib/presentation/pages/home/widgets/diagnostics_panel.dart` encuentra una coincidencia
    - `grep -n "CINGULA_SYNC_API_KEY" lib/presentation/pages/home/widgets/diagnostics_panel.dart` encuentra una coincidencia (el mensaje de error de auth)
    - `grep -n "Forzar push ahora" lib/presentation/pages/home/widgets/diagnostics_panel.dart` encuentra una coincidencia
    - `grep -c "Push outbox (stub)" lib/presentation/pages/home/widgets/diagnostics_panel.dart` devuelve 0
    - `grep -c "RunSyncUseCase" lib/presentation/pages/home/widgets/diagnostics_panel.dart` devuelve 2 o más (el botón manual sigue cableado — D-06)
    - `grep -ci "todo sincronizado\|todo al día\|all synced" lib/presentation/pages/home/widgets/diagnostics_panel.dart` devuelve 0
    - `grep -n "ApiConfig.apiKey.length" lib/presentation/pages/home/widgets/diagnostics_panel.dart` encuentra una coincidencia (se muestra el largo, no la key)
    - `flutter analyze` sale con 0 errores
  </acceptance_criteria>

  <done>El panel muestra pendientes reales, último sync, y distingue visiblemente un error de auth de un error de red; el botón manual sigue disponible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verificación en dispositivo del disparo automático y del estado honesto</name>
  <files>ninguno — verificación en dispositivo, no modifica archivos</files>

  <action>
    Tarea de verificación humana — Claude no ejecuta código acá. Antes de pausar, Claude DEBE dejar
    todo listo para que el usuario sólo tenga que correr y mirar:
    1. Correr `flutter analyze && flutter test` y confirmar que están verdes.
    2. Imprimir el comando exacto de `flutter run` con los dos `--dart-define` ya armados
       (pidiendo al usuario la URL del backend y el token si todavía no los tiene a mano).
    3. Presentar los 5 casos de `<how-to-verify>` numerados, uno por uno, y esperar el resultado.
    NO marcar esta tarea como completa sin la señal explícita del usuario.
  </action>

  <verify>
    <automated>flutter analyze && flutter test</automated>
    Los 5 casos de comportamiento en dispositivo los confirma el usuario (no automatizables:
    requieren modo avión real, matar la app desde recientes, y observar la UI).
  </verify>

  <done>El usuario confirmó los 5 casos, o reportó cuál falló con lo observado.</done>

  <what-built>
    Push automático tras cada escritura local (con coalescing de 2s y sin pushes solapados), reintento
    al recuperar conectividad, y panel de debug que muestra pendientes reales + último sync +
    causa del último fallo (red vs auth).
  </what-built>

  <how-to-verify>
    Requisito previo: tener el backend corriendo. Si todavía no se hizo el setup de Neon/Vercel
    (plan 02-05, tarea 2), alcanza con `cd backend && vercel dev` apuntando a una branch de Neon,
    o incluso sin backend para los casos 3 y 4 de abajo.

    Compilar con:
    `flutter run --dart-define=CINGULA_API_BASE_URL=http://<ip-de-tu-máquina>:3000 --dart-define=CINGULA_SYNC_API_KEY=<token>`

    1. Push automático (SYNC-01): entrar al panel de debug, anotar "Outbox pendiente: N".
       Crear o editar una entidad (grabar un geo_path, o crear un trigger). Esperar ~3 segundos SIN
       tocar ningún botón. Volver al panel y apretar "Refrescar estado".
       Esperado: "Estado: último push OK, outbox vacío" y "Outbox pendiente: 0".

    2. Coalescing: borrar un geo_path que tenga varios triggers asociados (dispara 1 delete de path
       + N deletes de triggers en cascada). Esperado: se sincroniza todo, y en los logs
       (`flutter logs` o la consola de `vercel dev`) se ve UNA sola request a /sync/push, no N+1.

    3. Escritura offline + reconexión (SYNC-03, D-05): activar modo avión. Crear una entidad.
       Panel -> "Outbox pendiente" debe ser > 0 y "Estado" debe decir ERROR de red/servidor.
       IMPORTANTE: verificar que en ningún momento aparece un texto que sugiera que está todo
       sincronizado mientras hay pendientes (SYNC-08).
       Desactivar modo avión y esperar ~5 segundos sin tocar nada. Refrescar el panel.
       Esperado: "Outbox pendiente: 0" y "Estado: último push OK".

    4. Error de auth terminal (SYNC-07, D-03): recompilar con una key mal a propósito
       (`--dart-define=CINGULA_SYNC_API_KEY=clave-mala`). Crear una entidad.
       Esperado: el panel muestra "ERROR DE AUTH (401) — revisar --dart-define=CINGULA_SYNC_API_KEY",
       la fila queda pendiente, y NO se ve una tormenta de reintentos en los logs del backend
       (a lo sumo un intento por escritura/reconexión, no un loop).

    5. Botón manual (D-06): confirmar que "Forzar push ahora" sigue existiendo en el panel y que
       al apretarlo muestra un SnackBar con el resultado.
  </how-to-verify>

  <acceptance_criteria>
    - Caso 1: la entidad llega al backend sin haber apretado ningún botón
    - Caso 2: un borrado en cascada produce una sola request POST /sync/push
    - Caso 3: con modo avión activo el panel muestra pendientes > 0 y un estado de error; al reconectar drena solo
    - Caso 4: una key inválida produce el mensaje de auth específico y no un bucle de reintentos
    - Caso 5: el botón "Forzar push ahora" está presente y funciona
    - En ningún caso el panel afirma estar sincronizado mientras "Outbox pendiente" es > 0
  </acceptance_criteria>

  <resume-signal>Escribir "approved" si los 5 casos pasan, o describir cuál falló y qué se vio en su lugar.</resume-signal>
</task>

</tasks>

<verification>
1. `flutter analyze` — 0 errores
2. `flutter test` — suite completa verde
3. `grep -rn "Timer.periodic" lib/data/sync/ lib/core/di/` — sin resultados (no se introdujo polling)
4. `grep -rn "onOutboxEnqueued" lib/data/datasources/local/` — sólo en `sync_local_data_source.dart`
5. Verificación humana (Task 4) aprobada
</verification>

<success_criteria>
- Una escritura local estando online llega al backend sin intervención del usuario (SYNC-01, D-04)
- Recuperar conectividad drena el outbox pendiente (SYNC-03, D-05)
- Un error de auth se distingue visiblemente de uno de red y no consume reintentos (D-03)
- El panel nunca declara sincronización completa con pendientes > 0 (SYNC-08)
- El botón manual sigue disponible (D-06)
- Cero timers periódicos nuevos
</success_criteria>

<output>
Al terminar, crear `.planning/phases/02-backend-real-push-sync/02-04-SUMMARY.md`
</output>
