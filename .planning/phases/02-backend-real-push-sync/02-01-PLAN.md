---
phase: 02-backend-real-push-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/data/datasources/local/app_database.dart
  - lib/data/datasources/local/sync_local_data_source.dart
  - lib/data/datasources/local/geo_trigger_local_data_source.dart
  - lib/data/datasources/local/geo_path_local_data_source.dart
  - lib/data/sync/sync_backoff.dart
  - lib/data/sync/sync_client.dart
  - test/data/sync/sync_backoff_test.dart
autonomous: true
requirements: [SYNC-02, SYNC-03]

must_haves:
  truths:
    - "Una fila de outbox que falló un intento no se reenvía inmediatamente: espera un tiempo que crece exponencialmente con el número de intentos"
    - "El tiempo de espera sobrevive a un kill/restart de la app (está en SQLite, no en memoria)"
    - "Una operación de borrado encolada en el outbox lleva un logical_version en su payload, igual que insert/update"
  artifacts:
    - path: "lib/data/sync/sync_backoff.dart"
      provides: "fullJitterBackoff() + isOutboxRowEligible(), puras, sin dependencia de DB"
      exports: ["fullJitterBackoff", "isOutboxRowEligible", "kBackoffBase", "kBackoffCap"]
    - path: "test/data/sync/sync_backoff_test.dart"
      provides: "cobertura automatizada de la curva de backoff y de la elegibilidad"
    - path: "lib/data/datasources/local/app_database.dart"
      provides: "migración a schema v6 con columna next_attempt_at en sync_outbox"
      contains: "next_attempt_at"
  key_links:
    - from: "lib/data/sync/sync_client.dart"
      to: "lib/data/sync/sync_backoff.dart"
      via: "pendingOutbox filtra con isOutboxRowEligible"
      pattern: "isOutboxRowEligible"
    - from: "lib/data/sync/sync_client.dart"
      to: "SyncLocalDataSource.incrementOutboxAttempt"
      via: "markAttempt calcula nextAttemptAt con fullJitterBackoff y lo persiste"
      pattern: "nextAttemptAt"
---

<objective>
Cerrar los dos huecos de schema que el research encontró leyendo el código, y dejar lista la
aritmética de backoff — todo del lado local, sin HTTP todavía.

Purpose: sin `next_attempt_at` persistido no hay backoff real (se resetea en cada kill de app,
que es exactamente el escenario que SYNC-03 quiere cubrir); sin `logical_version` en los payloads
de delete, el upsert versionado del servidor (SYNC-02/SYNC-06) no puede ordenar los borrados.

Output: schema local v6, `markAttempt` que persiste cuándo reintentar, helper puro de backoff con
tests, y `pendingOutbox` que sólo devuelve filas elegibles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-real-push-sync/02-CONTEXT.md
@.planning/phases/02-backend-real-push-sync/02-RESEARCH.md

<interfaces>
Contratos existentes que este plan toca. No hace falta explorar el codebase para conocerlos.

`lib/data/datasources/local/sync_local_data_source.dart` (actual):
```dart
class SyncLocalDataSource {
  SyncLocalDataSource(this._database, {Uuid? uuidGenerator});
  String newUuid();
  Map<String, Object?> withInsertMetadata(Map<String, Object?> values);
  Map<String, Object?> withUpdateMetadata(Map<String, Object?> values); // logical_version + 1
  Future<void> enqueueOutbox({required String tableName, required String recordUuid,
      required String op, Map<String, Object?>? payload, String? deviceId});
  Future<List<Map<String, Object?>>> readOutbox({int limit = 50}); // ORDER BY created_at ASC, payload ya decodificado
  Future<void> deleteOutboxById(int id);
  Future<void> incrementOutboxAttempt(int id);
  Future<Map<String, Object?>> getSyncState();
  Future<void> upsertSyncState({String? serverCursor, int? lastSyncAt, String? deviceId});
}
```

`lib/data/sync/sync_client.dart` (actual):
```dart
class SyncClient {
  SyncClient({required SyncLocalDataSource sync});
  Future<List<Map<String, Object?>>> pendingOutbox({int limit = 50});
  Future<void> ackOutbox(int id);
  Future<void> markAttempt(int id);
  Future<Map<String, Object?>> getState();
  Future<void> saveState({String? serverCursor, DateTime? lastSync, String? deviceId});
}
```

Schema actual de `sync_outbox` (app_database.dart, `_onCreate` y bloque `oldVersion < 5`):
```sql
CREATE TABLE sync_outbox (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  table_name TEXT NOT NULL,
  record_uuid TEXT NOT NULL,
  op TEXT NOT NULL,
  payload TEXT,
  device_id TEXT,
  created_at INTEGER NOT NULL,
  attempt_count INTEGER NOT NULL DEFAULT 0
);
```
`static const _dbVersion = 5;` está en la línea 13 de `app_database.dart`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración schema v6 — columna next_attempt_at en sync_outbox</name>
  <files>lib/data/datasources/local/app_database.dart, lib/data/datasources/local/sync_local_data_source.dart</files>

  <read_first>
    - lib/data/datasources/local/app_database.dart (leer `_dbVersion`, el DDL de `sync_outbox` en `_onCreate`, y el bloque `if (oldVersion < 5)` para copiar exactamente su patrón try/catch)
    - lib/data/datasources/local/sync_local_data_source.dart (leer `enqueueOutbox` e `incrementOutboxAttempt`)
  </read_first>

  <action>
    Nota de diseño, explícita porque se desvía del research: el research propuso una columna
    `last_attempt_at` y calcular la elegibilidad como `now >= last_attempt_at + backoff(attempt_count)`.
    Se usa en cambio `next_attempt_at` (el instante ya resuelto en el que la fila vuelve a ser elegible).
    Razón: el jitter tiene que decidirse UNA vez, en el momento del fallo. Con `last_attempt_at` habría
    que re-sortear el jitter en cada lectura del outbox, lo que hace la elegibilidad inestable
    (una fila puede parecer elegible y un segundo después no). Con `next_attempt_at` la lectura es una
    sola comparación entera, sin `Random` en el camino de lectura.

    Cambios exactos en `lib/data/datasources/local/app_database.dart`:

    1. Línea 13: cambiar `static const _dbVersion = 5;` por `static const _dbVersion = 6;`

    2. En `_onCreate`, dentro del `CREATE TABLE sync_outbox (...)`, agregar una última columna después
       de `attempt_count INTEGER NOT NULL DEFAULT 0`:
       ```sql
       attempt_count INTEGER NOT NULL DEFAULT 0,
       next_attempt_at INTEGER
       ```
       (sin `NOT NULL`: `NULL` significa "elegible ya mismo", que es el estado de una fila recién encolada)

    3. En `_onUpgrade`, dentro del bloque existente `if (oldVersion < 5)`, agregar la MISMA columna al
       `CREATE TABLE IF NOT EXISTS sync_outbox` que ahí se define, para que un dispositivo que salta
       directo de v4 a v6 cree la tabla ya completa.

    4. En `_onUpgrade`, DESPUÉS del bloque `if (oldVersion < 5) { ... }` y antes del cierre del método,
       agregar un bloque nuevo copiando el patrón try/catch usado en `oldVersion < 4`:
       ```dart
       if (oldVersion < 6) {
         try {
           await db.execute('ALTER TABLE sync_outbox ADD COLUMN next_attempt_at INTEGER;');
         } catch (_) {}
       }
       ```
       El try/catch vacío es el patrón ya establecido en este archivo (v3 y v4 lo usan) para tolerar
       que la columna ya exista; no cambiarlo por otra cosa.

    Cambios exactos en `lib/data/datasources/local/sync_local_data_source.dart`:

    5. En `enqueueOutbox`, agregar `'next_attempt_at': null,` al mapa que se pasa a
       `_database.insert('sync_outbox', {...})`, después de `'attempt_count': 0,`.

    6. Reemplazar `incrementOutboxAttempt` por esta versión (nuevo parámetro nombrado, requerido):
       ```dart
       /// Incrementa el contador de intentos y agenda cuándo la fila vuelve a ser elegible.
       /// [nextAttemptAt] son epoch seconds; null = elegible inmediatamente.
       Future<void> incrementOutboxAttempt(int id, {int? nextAttemptAt}) async {
         await _database.rawUpdate(
           'UPDATE sync_outbox SET attempt_count = attempt_count + 1, next_attempt_at = ? WHERE id = ?',
           [nextAttemptAt, id],
         );
       }
       ```
       El único llamador actual es `SyncClient.markAttempt` (Task 3 lo actualiza); no romper la firma
       de otra forma.

    NO tocar: `readOutbox` (el filtro de elegibilidad va en Dart, en `SyncClient`, ver Task 3),
    ni la tabla `sync_state`, ni ninguna otra migración existente.
  </action>

  <verify>
    <automated>flutter analyze lib/data/datasources/local/app_database.dart lib/data/datasources/local/sync_local_data_source.dart</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "next_attempt_at" lib/data/datasources/local/app_database.dart` devuelve 3 o más (DDL de `_onCreate`, DDL del bloque `oldVersion < 5`, y el `ALTER TABLE` del bloque `oldVersion < 6`)
    - `grep -n "_dbVersion = 6" lib/data/datasources/local/app_database.dart` encuentra exactamente una coincidencia
    - `grep -n "oldVersion < 6" lib/data/datasources/local/app_database.dart` encuentra exactamente una coincidencia
    - `grep -n "ALTER TABLE sync_outbox ADD COLUMN next_attempt_at INTEGER" lib/data/datasources/local/app_database.dart` encuentra una coincidencia
    - `grep -n "next_attempt_at = ? WHERE id = ?" lib/data/datasources/local/sync_local_data_source.dart` encuentra una coincidencia
    - `grep -n "int? nextAttemptAt" lib/data/datasources/local/sync_local_data_source.dart` encuentra una coincidencia
    - `flutter analyze` sale con 0 errores en los dos archivos tocados
  </acceptance_criteria>

  <done>El schema local es v6, `sync_outbox` tiene `next_attempt_at`, un dispositivo que venía de v5 se actualiza sin perder filas, y `incrementOutboxAttempt` puede persistir cuándo reintentar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Helper puro de backoff (Full Jitter) + elegibilidad</name>
  <files>lib/data/sync/sync_backoff.dart, test/data/sync/sync_backoff_test.dart</files>

  <read_first>
    - lib/data/sync/sync_client.dart (para saber dónde se va a consumir en Task 3)
    - test/core/config/app_mode_config_test.dart (estilo de test ya establecido en este repo: `flutter_test`, `group`/`test`/`expect`, sin mocks externos)
  </read_first>

  <behavior>
    - `fullJitterBackoff(0)` devuelve una Duration en `[0ms, 2000ms]` (base = 2s, 2^0 = 1)
    - `fullJitterBackoff(3)` devuelve una Duration en `[0ms, 16000ms]` (2s * 2^3)
    - `fullJitterBackoff(30)` nunca supera el cap de 5 minutos (no desborda ni crece sin límite)
    - `fullJitterBackoff(-1)` se trata como intento 0, no lanza excepción
    - Con un `Random` sembrado (`Random(42)`) el resultado es determinista y reproducible
    - `isOutboxRowEligible({'next_attempt_at': null}, nowSeconds: 100)` es `true`
    - `isOutboxRowEligible({'next_attempt_at': 100}, nowSeconds: 100)` es `true` (borde inclusivo)
    - `isOutboxRowEligible({'next_attempt_at': 101}, nowSeconds: 100)` es `false`
    - Una fila sin la clave `next_attempt_at` (dispositivo recién migrado) es elegible
  </behavior>

  <action>
    Crear `lib/data/sync/sync_backoff.dart` con exactamente estas tres cosas públicas y nada más:

    ```dart
    import 'dart:math';

    /// Base de la curva de backoff: primer reintento cae en [0, 2s].
    const Duration kBackoffBase = Duration(seconds: 2);

    /// Techo de la curva: nunca se espera más de 5 minutos entre intentos.
    const Duration kBackoffCap = Duration(minutes: 5);

    /// Full Jitter (AWS Architecture Blog): delay = random(0, min(cap, base * 2^attempt)).
    /// Se elige Full Jitter y no "exponencial puro" porque un solo dispositivo reintentando
    /// no necesita evitar thundering herd, pero sí evitar sincronizarse con ciclos de red
    /// periódicos (p. ej. reconexiones de red móvil) que producirían fallos correlacionados.
    Duration fullJitterBackoff(int attemptCount, {Random? random}) {
      final exponent = attemptCount.clamp(0, 20);
      final ceilingMs = min(kBackoffCap.inMilliseconds, kBackoffBase.inMilliseconds * (1 << exponent));
      return Duration(milliseconds: (random ?? Random()).nextInt(ceilingMs + 1));
    }

    /// Una fila de outbox es elegible si nunca falló (`next_attempt_at` null/ausente)
    /// o si ya pasó el instante agendado. [nowSeconds] son epoch seconds, misma unidad
    /// que `created_at` / `sync_state.last_sync_at`.
    bool isOutboxRowEligible(Map<String, Object?> row, {required int nowSeconds}) {
      final next = row['next_attempt_at'] as int?;
      return next == null || next <= nowSeconds;
    }
    ```

    El `clamp(0, 20)` es deliberado: acota el exponente para que `1 << exponent` no desborde y para
    que un `attempt_count` absurdamente alto no produzca aritmética rara. Con base 2s el cap de 5min
    ya se alcanza en attempt 8, así que 20 es holgura pura, no un límite funcional.

    NO agregar: clase, singleton, política configurable por entidad, ni "estrategia" pluggable.
    Son dos funciones puras y dos constantes.

    Crear `test/data/sync/sync_backoff_test.dart` cubriendo cada caso listado en `<behavior>`.
    Usar `Random(42)` para los asserts deterministas y `Random()` sólo para los asserts de rango
    (ejecutar el rango en un loop de 100 iteraciones para que el jitter no produzca un test flaky
    que pase por casualidad).
  </action>

  <verify>
    <automated>flutter test test/data/sync/sync_backoff_test.dart</automated>
  </verify>

  <acceptance_criteria>
    - El archivo `lib/data/sync/sync_backoff.dart` existe
    - `grep -n "Duration fullJitterBackoff" lib/data/sync/sync_backoff.dart` encuentra una coincidencia
    - `grep -n "bool isOutboxRowEligible" lib/data/sync/sync_backoff.dart` encuentra una coincidencia
    - `grep -n "kBackoffCap = Duration(minutes: 5)" lib/data/sync/sync_backoff.dart` encuentra una coincidencia
    - `grep -c "class " lib/data/sync/sync_backoff.dart` devuelve 0 (es un archivo de funciones puras, sin clases)
    - `flutter test test/data/sync/sync_backoff_test.dart` pasa con al menos 8 tests
    - El test file no importa `sqflite` ni `package:flutter/material.dart` (es puro Dart): `grep -c "sqflite" test/data/sync/sync_backoff_test.dart` devuelve 0
  </acceptance_criteria>

  <done>La curva de backoff y la regla de elegibilidad existen como funciones puras testeadas, sin necesidad de una base de datos para verificarlas.</done>
</task>

<task type="auto">
  <name>Task 3: SyncClient filtra por elegibilidad y persiste el próximo intento; payloads de delete llevan logical_version</name>
  <files>lib/data/sync/sync_client.dart, lib/data/datasources/local/geo_trigger_local_data_source.dart, lib/data/datasources/local/geo_path_local_data_source.dart, lib/domain/usecases/run_sync_usecase.dart</files>

  <read_first>
    - lib/data/sync/sync_client.dart (archivo a modificar)
    - lib/data/sync/sync_backoff.dart (creado en Task 2 — usar sus funciones, no reimplementarlas)
    - lib/data/datasources/local/geo_trigger_local_data_source.dart (archivo a modificar: `deleteOrphaned` ~línea 24, `deleteByAudioAssetId` ~línea 54)
    - lib/data/datasources/local/geo_path_local_data_source.dart (archivo a modificar: `deleteByAudioAssetId` ~línea 106, `deleteById` ~línea 131)
    - lib/data/datasources/local/sync_local_data_source.dart (ver `withUpdateMetadata`: la regla de bump es `logical_version actual + 1`, o `1` si era null — replicar exactamente esa regla)
  </read_first>

  <action>
    PARTE A — `lib/data/sync/sync_client.dart`

    1. Agregar el import `import 'sync_backoff.dart';`

    2. Reemplazar `pendingOutbox` por:
       ```dart
       /// Devuelve el lote de outbox listo para enviar, salteando filas que están en backoff.
       Future<List<Map<String, Object?>>> pendingOutbox({int limit = 50, DateTime? now}) async {
         final rows = await _sync.readOutbox(limit: limit);
         final nowSeconds = (now ?? DateTime.now()).millisecondsSinceEpoch ~/ 1000;
         // ponytail: corte por cabeza (takeWhile), no filtro global. Si la fila más vieja está en
         // backoff frenamos ahí a propósito: mantiene el orden FIFO global, que es lo que impide
         // que una versión nueva de una entidad llegue al servidor antes que una vieja.
         return rows
             .takeWhile((row) => isOutboxRowEligible(row, nowSeconds: nowSeconds))
             .toList(growable: false);
       }
       ```

    3. Reemplazar `markAttempt` por:
       ```dart
       /// Marca un intento fallido: incrementa el contador y agenda el próximo intento
       /// con Full Jitter sobre el número de intentos ya acumulado.
       Future<void> markAttempt(int id, {required int attemptCount, DateTime? now}) async {
         final base = now ?? DateTime.now();
         final nextAt = base.add(fullJitterBackoff(attemptCount));
         await _sync.incrementOutboxAttempt(
           id,
           nextAttemptAt: nextAt.millisecondsSinceEpoch ~/ 1000,
         );
       }
       ```
       `attemptCount` viene de la columna `attempt_count` de la fila que el llamador ya tiene en mano
       (`readOutbox` la devuelve), así no hace falta una consulta extra.

    PARTE B — payloads de delete con `logical_version`

    Hay exactamente 5 sitios que encolan `op: 'delete'`. Los 5 hacen un `SELECT` del `uuid` ANTES del
    borrado físico. En los 5, agregar `logical_version` a las columnas seleccionadas e incluirlo en el
    payload, bumpeado en 1 (misma regla que `withUpdateMetadata`: `versión actual + 1`, o `1` si era null).

    Para no repetir la aritmética 5 veces, agregar este helper privado top-level en CADA uno de los
    dos archivos (son librerías distintas; duplicar 3 líneas es más barato que crear un archivo
    compartido para esto):
    ```dart
    /// Próxima versión lógica para un borrado, misma regla que SyncLocalDataSource.withUpdateMetadata.
    int _nextDeleteVersion(Object? current) => ((current as int?) ?? 0) + 1;
    ```

    En `lib/data/datasources/local/geo_trigger_local_data_source.dart`:

    - `deleteOrphaned()`: cambiar el rawQuery
      `'SELECT uuid FROM geo_triggers WHERE geo_path_id IS NOT NULL AND geo_path_id NOT IN (SELECT id FROM geo_paths)'`
      por
      `'SELECT uuid, logical_version FROM geo_triggers WHERE geo_path_id IS NOT NULL AND geo_path_id NOT IN (SELECT id FROM geo_paths)'`
      y cambiar el payload `{'reason': 'orphan_geo_path'}` por
      `{'reason': 'orphan_geo_path', 'uuid': uuid, 'logical_version': _nextDeleteVersion(row['logical_version'])}`

    - `deleteByAudioAssetId(int audioAssetId)`: cambiar `columns: ['uuid']` por
      `columns: ['uuid', 'logical_version']`, y el payload `{'audio_asset_id': audioAssetId}` por
      `{'audio_asset_id': audioAssetId, 'uuid': uuid, 'logical_version': _nextDeleteVersion(row['logical_version'])}`

    En `lib/data/datasources/local/geo_path_local_data_source.dart`:

    - `deleteByAudioAssetId(int audioAssetId)`: `columns: ['uuid']` → `columns: ['uuid', 'logical_version']`;
      payload `{'audio_asset_id': audioAssetId}` →
      `{'audio_asset_id': audioAssetId, 'uuid': uuid, 'logical_version': _nextDeleteVersion(row['logical_version'])}`

    - `deleteById(int pathId)`, consulta `triggerRows` sobre `geo_triggers`:
      `columns: ['uuid']` → `columns: ['uuid', 'logical_version']`;
      payload `{'geo_path_id': pathId}` →
      `{'geo_path_id': pathId, 'uuid': uuid, 'logical_version': _nextDeleteVersion(row['logical_version'])}`

    - `deleteById(int pathId)`, consulta `pathRows` sobre `geo_paths`:
      `columns: ['uuid']` → `columns: ['uuid', 'logical_version']`;
      payload `{'id': pathId}` →
      `{'id': pathId, 'uuid': uuid, 'logical_version': _nextDeleteVersion(pathRows.first['logical_version'])}`

    NO tocar el orden de las operaciones (SELECT → DELETE → enqueue) en ninguno de los 5 sitios:
    el SELECT tiene que seguir ocurriendo antes del DELETE físico porque después la fila ya no existe.

    PARTE C — actualizar el único llamador de `markAttempt`

    `lib/domain/usecases/run_sync_usecase.dart` llama `_client.markAttempt(id)` en el loop de no-acked.
    Cambiarlo a pasar el `attempt_count` de la fila correspondiente:
    ```dart
    // Incrementar contador de intentos y agendar backoff para los que no se confirmaron.
    final byId = {for (final row in outbox) row['id'] as int: row};
    for (final id in sentIds.difference(ackedSet)) {
      await _client.markAttempt(id, attemptCount: (byId[id]?['attempt_count'] as int?) ?? 0);
    }
    ```
    Este es el único cambio a `run_sync_usecase.dart` en este plan; el manejo de errores
    transitorios vs terminales llega en el plan 02-03.
  </action>

  <verify>
    <automated>flutter analyze lib/data/sync/ lib/data/datasources/local/ lib/domain/usecases/run_sync_usecase.dart && flutter test test/data/sync/</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "isOutboxRowEligible" lib/data/sync/sync_client.dart` encuentra una coincidencia
    - `grep -n "takeWhile" lib/data/sync/sync_client.dart` encuentra una coincidencia
    - `grep -n "required int attemptCount" lib/data/sync/sync_client.dart` encuentra una coincidencia
    - `grep -n "fullJitterBackoff" lib/data/sync/sync_client.dart` encuentra una coincidencia
    - `grep -c "logical_version" lib/data/datasources/local/geo_trigger_local_data_source.dart` devuelve 5 o más (2 SELECT + 2 payload + 1 helper)
    - `grep -c "logical_version" lib/data/datasources/local/geo_path_local_data_source.dart` devuelve 7 o más (3 SELECT + 3 payload + 1 helper)
    - `grep -c "_nextDeleteVersion" lib/data/datasources/local/geo_trigger_local_data_source.dart` devuelve 3 (definición + 2 usos)
    - `grep -c "_nextDeleteVersion" lib/data/datasources/local/geo_path_local_data_source.dart` devuelve 4 (definición + 3 usos)
    - `grep -n "attemptCount:" lib/domain/usecases/run_sync_usecase.dart` encuentra una coincidencia
    - `grep -c "columns: \['uuid'\]" lib/data/datasources/local/geo_trigger_local_data_source.dart lib/data/datasources/local/geo_path_local_data_source.dart` devuelve 0 en ambos archivos (ya no queda ningún SELECT que pida sólo uuid antes de un delete)
    - `flutter analyze` sale con 0 errores
    - `flutter test` (suite completa) sigue verde — ningún test existente se rompe
  </acceptance_criteria>

  <done>`pendingOutbox` respeta el backoff persistido, `markAttempt` agenda el próximo intento, y las 5 operaciones de delete encolan un `logical_version` que el servidor va a poder usar para ordenar versiones.</done>
</task>

</tasks>

<verification>
1. `flutter analyze` — 0 errores
2. `flutter test` — suite completa verde (incluye los tests nuevos de backoff y los preexistentes)
3. `grep -rn "next_attempt_at" lib/` muestra la columna usada consistentemente en app_database, sync_local_data_source y sync_backoff
4. Ningún archivo nuevo fuera de `lib/data/sync/sync_backoff.dart` y `test/data/sync/sync_backoff_test.dart`
</verification>

<success_criteria>
- El schema local está en v6 con `next_attempt_at` y la migración desde v5 no toca datos existentes (sólo `ALTER TABLE ADD COLUMN`)
- Una fila que falla queda agendada para reintento con Full Jitter, persistido en SQLite (sobrevive kill de app)
- `pendingOutbox` no devuelve filas que aún están en backoff
- Las 5 operaciones de delete encolan payload con `uuid` + `logical_version`
- `flutter test` verde
</success_criteria>

<output>
Al terminar, crear `.planning/phases/02-backend-real-push-sync/02-01-SUMMARY.md`
</output>
