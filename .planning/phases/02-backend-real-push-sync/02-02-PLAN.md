---
phase: 02-backend-real-push-sync
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - backend/vercel.json
  - backend/.gitignore
  - backend/.env.example
  - backend/README.md
  - backend/schema.sql
  - backend/api/_lib/auth.js
  - backend/api/_lib/auth.test.js
  - backend/api/_lib/db.js
  - backend/api/_lib/outbox.js
  - backend/api/_lib/outbox.test.js
  - backend/api/sync/push.js
  - backend/api/sync/push.test.js
  - backend/api/sync/state.js
autonomous: true
requirements: [SYNC-01, SYNC-02, SYNC-06, SYNC-07]

user_setup:
  - service: neon
    why: "Postgres serverless que guarda el estado sincronizado (D-01)"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Console -> Project -> Connection string (pooled). Se necesita también una dev branch para correr backend/api/sync/push.test.js contra Postgres real."
    dashboard_config:
      - task: "Crear proyecto Neon + una branch 'dev' para tests de integración"
        location: "Neon Console -> Branches"
  - service: vercel
    why: "Hosting de las funciones serverless que implementan /sync/push y /sync/state (D-01)"
    env_vars:
      - name: SYNC_API_KEY
        source: "Valor generado por el usuario (p. ej. `openssl rand -hex 32`); se carga con `vercel env add SYNC_API_KEY` y se pasa a la app Flutter como --dart-define=CINGULA_SYNC_API_KEY=<mismo valor>"
    dashboard_config:
      - task: "vercel link del directorio backend/ al proyecto"
        location: "CLI: `cd backend && vercel link`"

must_haves:
  truths:
    - "Un POST a /sync/push sin header Authorization válido es rechazado con 401"
    - "Enviar el mismo record_uuid + logical_version dos veces deja el estado del servidor idéntico (no-op)"
    - "Después de 3 pushes con versiones 1, 2, 3 sobre el mismo uuid, el servidor guarda la versión 3 como actual y la 2 como previa — la 1 ya no existe"
    - "Un push con versión menor a la actual no retrocede el estado del servidor"
  artifacts:
    - path: "backend/schema.sql"
      provides: "DDL de la tabla synced_entities con current_* y previous_*"
      contains: "CREATE TABLE IF NOT EXISTS synced_entities"
    - path: "backend/api/_lib/auth.js"
      provides: "requireApiKey() con comparación en tiempo constante"
      exports: ["requireApiKey"]
    - path: "backend/api/_lib/outbox.js"
      provides: "validateOutboxItem() + extractVersion() + upsertStatement()"
      exports: ["validateOutboxItem", "extractVersion", "upsertStatement"]
    - path: "backend/api/sync/push.js"
      provides: "handler POST /sync/push: auth -> valida -> upsert batch en transacción -> ackedIds"
    - path: "backend/api/_lib/auth.test.js"
      provides: "cobertura automatizada de SYNC-07"
    - path: "backend/api/sync/push.test.js"
      provides: "cobertura de integración de SYNC-02/SYNC-06 contra Postgres real"
  key_links:
    - from: "backend/api/sync/push.js"
      to: "backend/api/_lib/auth.js"
      via: "import requireApiKey y return 401 si falla"
      pattern: "requireApiKey"
    - from: "backend/api/sync/push.js"
      to: "backend/api/_lib/outbox.js"
      via: "upsertStatement por item + sql.transaction(statements)"
      pattern: "sql\\.transaction"
    - from: "backend/vercel.json"
      to: "backend/api/sync/push.js"
      via: "rewrite /sync/:path* -> /api/sync/:path*"
      pattern: "/api/sync/"
---

<objective>
Crear el backend real: un sub-proyecto Node nuevo en `backend/` con dos Vercel Functions
(`/sync/push`, `/sync/state`) sobre Neon Postgres, con auth por bearer token e idempotencia
+ versionado (actual + 1 anterior) resueltos en un solo statement SQL.

Purpose: SYNC-01 pide reemplazar `SyncApiStub` por algo real. Este plan construye el lado servidor
de forma completamente independiente del lado Flutter — no comparte ni un archivo con los otros
planes de esta fase, así que corre en paralelo.

Output: `backend/` desplegable con `vercel`, con tests de `node --test` verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-backend-real-push-sync/02-CONTEXT.md
@.planning/phases/02-backend-real-push-sync/02-RESEARCH.md

<wire_contract>
CONTRATO HTTP — copiado verbatim también en el plan 02-03 (lado Flutter). Los dos lados deben
implementar exactamente esto. Cualquier cambio acá tiene que replicarse allá.

--- POST {base}/sync/push ---

Headers de request:
  Authorization: Bearer <SYNC_API_KEY>
  Content-Type: application/json

Body de request (los items vienen tal cual salen de `SyncLocalDataSource.readOutbox`,
o sea con nombres de columna snake_case y `payload` ya decodificado a objeto):
```json
{
  "deviceId": "abc-123",
  "cursor": "2026-08-29T10:00:00.000Z",
  "outbox": [
    {
      "id": 12,
      "table_name": "geo_triggers",
      "record_uuid": "8f14e45f-ceea-467a-9f4b-1c2d3e4f5a6b",
      "op": "update",
      "payload": { "id": 3, "uuid": "8f14e45f-...", "logical_version": 2, "name": "..." },
      "device_id": "abc-123",
      "created_at": 1756400000,
      "attempt_count": 0,
      "next_attempt_at": null
    }
  ]
}
```

Reglas de lectura server-side:
  - `item.id`            -> entero, es el id LOCAL de la fila de outbox; se devuelve tal cual en `ackedIds`
  - `item.table_name`    -> string, uno de: audio_assets | geo_triggers | geo_paths | regions
  - `item.record_uuid`   -> string no vacío
  - `item.op`            -> uno de: insert | update | delete
  - `item.payload.logical_version` -> entero >= 1. ES LA VERSIÓN. No usar ninguna otra fuente.
  - `deviceId` y `cursor` pueden ser null

Response 200 (éxito):
```json
{
  "ackedIds": [12, 13],
  "serverCursor": "2026-08-29T12:00:00.000Z",
  "receivedAt": "2026-08-29T12:00:00.000Z"
}
```
`serverCursor` = timestamp ISO del servidor. Decisión explícita para la Fase 2 (research Open
Question 2): nada consume su estructura interna todavía; la Fase 3 (pull) puede redefinirlo.

Response 200 con outbox vacío: `{"ackedIds": [], "serverCursor": null, "receivedAt": "<iso>"}`

Response 401 (auth inválida o ausente): `{"error": "invalid or missing API key"}`
Response 400 (payload malformado): `{"error": "<descripción>", "invalidIds": [12, 15]}`
Response 405 (método distinto de POST): `{"error": "method not allowed"}`

--- GET {base}/sync/state ---

Headers: Authorization: Bearer <SYNC_API_KEY>
Response 200: `{"serverCursor": "<iso>|null", "lastSyncAt": "<iso>|null"}`
Response 401: `{"error": "invalid or missing API key"}`

--- /sync/changes ---
NO se implementa en esta fase. Es Fase 3 (SYNC-04/SYNC-05). No crear el archivo.
</wire_contract>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold de backend/ + auth por bearer token</name>
  <files>backend/package.json, backend/.gitignore, backend/.env.example, backend/vercel.json, backend/README.md, backend/api/_lib/auth.js, backend/api/_lib/db.js, backend/api/_lib/auth.test.js</files>

  <read_first>
    - CLAUDE.md (sección Technology Stack: confirma Neon + Vercel + @neondatabase/serverless como decisión cerrada — no re-evaluar)
    - .planning/phases/02-backend-real-push-sync/02-RESEARCH.md (sección Architecture Patterns, Pattern 3)
    - .gitignore (raíz del repo — chequear si ya ignora node_modules; si no, `backend/.gitignore` lo cubre)
  </read_first>

  <behavior>
    - `requireApiKey({headers: {}})` devuelve el string `'invalid or missing API key'`
    - `requireApiKey({headers: {authorization: 'Bearer wrong'}})` con `SYNC_API_KEY=right` devuelve el string de error
    - `requireApiKey({headers: {authorization: 'Bearer right'}})` con `SYNC_API_KEY=right` devuelve `null`
    - `requireApiKey({headers: {authorization: 'bearer right'}})` (minúscula) devuelve `null` — el prefijo es case-insensitive
    - `requireApiKey({headers: {authorization: 'right'}})` (sin prefijo Bearer) devuelve `null`
    - Con `SYNC_API_KEY` vacío/no seteado, CUALQUIER request devuelve error (no se abre el backend por config faltante)
    - Una key de largo distinto no lanza excepción de `timingSafeEqual` (que exige buffers del mismo largo)
  </behavior>

  <action>
    DECISIÓN DE LENGUAJE, explícita porque se desvía del research y de VALIDATION.md: el backend se
    escribe en JavaScript ESM (`.js`), NO en TypeScript. Razón: el entorno tiene Node v22.14, donde
    ejecutar `.ts` con `node --test` requiere `--experimental-strip-types` o un loader (`tsx`), más
    `tsconfig.json` y `typescript` como devDependency. Para 2 endpoints y 3 módulos de ~40 líneas eso
    es andamiaje sin retorno. Vercel despliega funciones `.js` ESM sin configuración extra. Si el
    backend crece a algo con dominio real, migrar a TS es un cambio local y barato.

    Crear `backend/package.json`:
    ```json
    {
      "name": "cingula-sync-backend",
      "private": true,
      "type": "module",
      "engines": { "node": ">=22" },
      "scripts": {
        "test": "node --test"
      },
      "dependencies": {
        "@neondatabase/serverless": "^1.1.0"
      }
    }
    ```

    Crear `backend/.gitignore`:
    ```
    node_modules/
    .vercel/
    .env
    .env.local
    ```

    Crear `backend/.env.example`:
    ```
    # Connection string de Neon (Console -> Project -> Connection string, pooled)
    DATABASE_URL=postgresql://user:pass@ep-xxx.neon.tech/neondb?sslmode=require
    # Token compartido con la app Flutter. Generar con: openssl rand -hex 32
    # La app se compila con --dart-define=CINGULA_SYNC_API_KEY=<mismo valor>
    SYNC_API_KEY=
    ```

    Crear `backend/vercel.json` (necesario porque `ApiConfig` del cliente pega a `/sync/push`,
    mientras que el file-routing de Vercel expone `api/sync/push.js` como `/api/sync/push`):
    ```json
    {
      "rewrites": [
        { "source": "/sync/:path*", "destination": "/api/sync/:path*" }
      ]
    }
    ```

    Crear `backend/api/_lib/auth.js`:
    ```js
    import { timingSafeEqual } from 'node:crypto';

    export const AUTH_ERROR = 'invalid or missing API key';

    /**
     * Valida el bearer token del request. Devuelve null si es válido, o el mensaje de error.
     * Se elige Authorization: Bearer sobre X-API-Key por ser la convención REST más común;
     * el esfuerzo de implementación es idéntico (D-02 / discreción de Claude en CONTEXT.md).
     */
    export function requireApiKey(request) {
      const expected = process.env.SYNC_API_KEY ?? '';
      if (expected.length === 0) return AUTH_ERROR; // config faltante = cerrado, nunca abierto

      const header = request?.headers?.authorization;
      const provided = typeof header === 'string' ? header.replace(/^Bearer\s+/i, '') : '';

      const a = Buffer.from(provided);
      const b = Buffer.from(expected);
      // timingSafeEqual exige mismo largo; el chequeo de largo filtra antes (no es secreto útil).
      if (a.length !== b.length || !timingSafeEqual(a, b)) return AUTH_ERROR;
      return null;
    }
    ```

    Crear `backend/api/_lib/db.js` (lazy a propósito: importar este módulo NO debe reventar cuando
    `DATABASE_URL` no está seteado, porque los tests puros lo importan transitivamente):
    ```js
    import { neon } from '@neondatabase/serverless';

    let _sql;

    export function getSql() {
      const url = process.env.DATABASE_URL;
      if (!url) throw new Error('DATABASE_URL is not set');
      _sql ??= neon(url);
      return _sql;
    }
    ```

    Crear `backend/api/_lib/auth.test.js` con `node:test` + `node:assert/strict`, cubriendo cada
    caso de `<behavior>`. Setear y restaurar `process.env.SYNC_API_KEY` dentro de cada test
    (usar `t.after(...)` o guardar/restaurar el valor original) para no filtrar estado entre tests.

    Crear `backend/README.md` (10-15 líneas máximo) con: qué es, cómo correr `npm install`,
    `npm test`, `vercel dev`, y qué env vars hace falta setear. Sin diagramas ni prosa de más.

    Después de crear los archivos, correr `cd backend && npm install` para generar
    `package-lock.json` y `node_modules/` (que ya queda gitignoreado).
  </action>

  <verify>
    <automated>cd backend && npm install --silent && node --test api/_lib/auth.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `backend/package.json` existe y `grep -n '"type": "module"' backend/package.json` encuentra una coincidencia
    - `grep -n '"test": "node --test"' backend/package.json` encuentra una coincidencia
    - `grep -n '@neondatabase/serverless' backend/package.json` encuentra una coincidencia
    - `grep -n 'timingSafeEqual' backend/api/_lib/auth.js` encuentra 2 coincidencias (import + uso)
    - `grep -n 'expected.length === 0' backend/api/_lib/auth.js` encuentra una coincidencia (fail-closed sin config)
    - `grep -n '/api/sync/:path\*' backend/vercel.json` encuentra una coincidencia
    - `backend/api/_lib/db.js` NO llama `neon()` en el top level: `grep -n "^const sql = neon" backend/api/_lib/db.js` devuelve 0 coincidencias
    - `cd backend && node --test api/_lib/auth.test.js` pasa con al menos 7 tests
    - `backend/.gitignore` contiene `node_modules/` y `.env`
    - `git status --porcelain backend/node_modules` no lista nada (está ignorado)
    - NO existe `backend/tsconfig.json` ni ningún archivo `.ts` en `backend/`
  </acceptance_criteria>

  <done>El sub-proyecto `backend/` existe, `npm test` corre, y la validación de API key está implementada y testeada (SYNC-07).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schema Postgres + upsert versionado idempotente</name>
  <files>backend/schema.sql, backend/api/_lib/outbox.js, backend/api/_lib/outbox.test.js</files>

  <read_first>
    - .planning/phases/02-backend-real-push-sync/02-RESEARCH.md (Architecture Patterns, Pattern 1 — el SQL de referencia; tratarlo como punto de partida fuerte, no como texto sagrado)
    - backend/api/_lib/db.js (creado en Task 1 — usar `getSql()`, no crear otra conexión)
    - El bloque `<wire_contract>` de este plan (nombres exactos de campos que llegan por HTTP)
  </read_first>

  <behavior>
    Tests puros en `backend/api/_lib/outbox.test.js` (sin DB):
    - `extractVersion({payload: {logical_version: 3}})` devuelve 3
    - `extractVersion({payload: null})` devuelve null
    - `extractVersion({payload: {logical_version: '3'}})` devuelve null (no coerce de strings)
    - `validateOutboxItem` devuelve null para un item bien formado
    - `validateOutboxItem` devuelve un string de error si falta `record_uuid`
    - `validateOutboxItem` devuelve error si `op` no es insert/update/delete
    - `validateOutboxItem` devuelve error si `table_name` no está en la whitelist
    - `validateOutboxItem` devuelve error si `payload.logical_version` falta o no es entero >= 1
    - `validateOutboxItem` devuelve error si `id` no es entero
  </behavior>

  <action>
    Crear `backend/schema.sql`:
    ```sql
    -- Una sola tabla genérica en vez de 4 espejando el schema SQLite: en esta fase el payload
    -- es opaco para el servidor (no hay queries por columna hasta el pull de la Fase 3), así que
    -- 4 tablas serían 4x DDL sin ningún beneficio. Ver 02-RESEARCH.md > Alternatives Considered.
    CREATE TABLE IF NOT EXISTS synced_entities (
      table_name           TEXT        NOT NULL,
      record_uuid          TEXT        NOT NULL,
      op                   TEXT        NOT NULL,
      current_version      INTEGER     NOT NULL,
      current_payload      JSONB       NOT NULL,
      current_updated_at   TIMESTAMPTZ NOT NULL,
      previous_version     INTEGER,
      previous_payload     JSONB,
      previous_updated_at  TIMESTAMPTZ,
      deleted_at           TIMESTAMPTZ,
      PRIMARY KEY (table_name, record_uuid)
    );
    ```
    Sin índices extra: la PK cubre el único acceso que existe en esta fase (SYNC-06 no pide más).

    Crear `backend/api/_lib/outbox.js` con exactamente estas tres exports:

    ```js
    export const SYNCABLE_TABLES = ['audio_assets', 'geo_triggers', 'geo_paths', 'regions'];
    const OPS = ['insert', 'update', 'delete'];

    /** La versión SIEMPRE sale de payload.logical_version. Null si no hay una válida. */
    export function extractVersion(item) {
      const v = item?.payload?.logical_version;
      return Number.isInteger(v) ? v : null;
    }

    /** Devuelve null si el item es válido, o un string describiendo el primer problema. */
    export function validateOutboxItem(item) {
      if (!Number.isInteger(item?.id)) return 'id must be an integer';
      if (!SYNCABLE_TABLES.includes(item?.table_name)) return `unknown table_name: ${item?.table_name}`;
      if (typeof item?.record_uuid !== 'string' || item.record_uuid.length === 0) return 'record_uuid must be a non-empty string';
      if (!OPS.includes(item?.op)) return `unknown op: ${item?.op}`;
      const version = extractVersion(item);
      if (version === null || version < 1) return 'payload.logical_version must be an integer >= 1';
      return null;
    }

    /** Un statement atómico por item; el llamador los agrupa con sql.transaction([...]). */
    export function upsertStatement(sql, item) { /* ver SQL abajo */ }
    ```

    El SQL de `upsertStatement` (usar interpolación por tagged template del driver de Neon —
    NUNCA concatenar strings, eso es lo que da inyección SQL):
    ```js
    export function upsertStatement(sql, item) {
      const version = extractVersion(item);
      return sql`
        INSERT INTO synced_entities
          (table_name, record_uuid, op, current_version, current_payload, current_updated_at, deleted_at)
        VALUES
          (${item.table_name}, ${item.record_uuid}, ${item.op}, ${version},
           ${JSON.stringify(item.payload)}::jsonb, now(),
           ${item.op === 'delete' ? 'now()' : null}::timestamptz)
        ON CONFLICT (table_name, record_uuid) DO UPDATE SET
          previous_version    = CASE WHEN synced_entities.current_version < EXCLUDED.current_version
                                     THEN synced_entities.current_version    ELSE synced_entities.previous_version    END,
          previous_payload    = CASE WHEN synced_entities.current_version < EXCLUDED.current_version
                                     THEN synced_entities.current_payload    ELSE synced_entities.previous_payload    END,
          previous_updated_at = CASE WHEN synced_entities.current_version < EXCLUDED.current_version
                                     THEN synced_entities.current_updated_at ELSE synced_entities.previous_updated_at END,
          current_version     = GREATEST(synced_entities.current_version, EXCLUDED.current_version),
          current_payload     = CASE WHEN synced_entities.current_version < EXCLUDED.current_version
                                     THEN EXCLUDED.current_payload    ELSE synced_entities.current_payload    END,
          current_updated_at  = CASE WHEN synced_entities.current_version < EXCLUDED.current_version
                                     THEN EXCLUDED.current_updated_at ELSE synced_entities.current_updated_at END,
          op                  = CASE WHEN synced_entities.current_version < EXCLUDED.current_version
                                     THEN EXCLUDED.op                 ELSE synced_entities.op                 END,
          deleted_at          = CASE WHEN synced_entities.current_version < EXCLUDED.current_version
                                     THEN EXCLUDED.deleted_at         ELSE synced_entities.deleted_at         END
      `;
    }
    ```
    Por qué esto cumple SYNC-02 y SYNC-06 con un solo mecanismo:
    - Reenviar el mismo `uuid` + misma versión (el caso "mataron la app en medio del push"):
      todas las condiciones `current_version < EXCLUDED.current_version` son falsas, así que ninguna
      columna cambia. Es un no-op de verdad, no un "update que escribe lo mismo".
    - Una versión más vieja llegando tarde (out-of-order): mismas condiciones falsas, el estado no
      retrocede.
    - Sólo se guardan 2 versiones porque `previous_*` se sobreescribe con la current anterior en cada
      avance — no hay tabla de historial que pueda crecer.

    Crear `backend/api/_lib/outbox.test.js` con `node:test`, cubriendo cada caso de `<behavior>`.
    NO testear `upsertStatement` acá (necesita Postgres real — eso es Task 3).
  </action>

  <verify>
    <automated>cd backend && node --test api/_lib/outbox.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "CREATE TABLE IF NOT EXISTS synced_entities" backend/schema.sql` encuentra una coincidencia
    - `grep -c "previous_version\|previous_payload\|previous_updated_at" backend/schema.sql` devuelve 3
    - `grep -n "PRIMARY KEY (table_name, record_uuid)" backend/schema.sql` encuentra una coincidencia
    - `grep -n "export function extractVersion" backend/api/_lib/outbox.js` encuentra una coincidencia
    - `grep -n "export function validateOutboxItem" backend/api/_lib/outbox.js` encuentra una coincidencia
    - `grep -n "export function upsertStatement" backend/api/_lib/outbox.js` encuentra una coincidencia
    - `grep -n "ON CONFLICT (table_name, record_uuid) DO UPDATE" backend/api/_lib/outbox.js` encuentra una coincidencia
    - `grep -c "GREATEST(synced_entities.current_version, EXCLUDED.current_version)" backend/api/_lib/outbox.js` devuelve 1
    - `grep -c "Number.isInteger" backend/api/_lib/outbox.js` devuelve 3 o más
    - `backend/api/_lib/outbox.js` no concatena SQL: `grep -n "'INSERT INTO' +\|\`INSERT INTO \${" backend/api/_lib/outbox.js` devuelve 0 coincidencias
    - `cd backend && node --test api/_lib/outbox.test.js` pasa con al menos 9 tests
  </acceptance_criteria>

  <done>El schema Postgres y el statement de upsert versionado existen; la validación de items está testeada sin necesidad de DB.</done>
</task>

<task type="auto">
  <name>Task 3: Handlers /sync/push y /sync/state + test de integración contra Postgres real</name>
  <files>backend/api/sync/push.js, backend/api/sync/state.js, backend/api/sync/push.test.js</files>

  <read_first>
    - backend/api/_lib/auth.js, backend/api/_lib/db.js, backend/api/_lib/outbox.js (creados en Tasks 1-2)
    - El bloque `<wire_contract>` de este plan (forma exacta de request y response)
    - .planning/phases/02-backend-real-push-sync/02-RESEARCH.md (Pattern 2 y Pitfall 4 — semántica todo-o-nada de sql.transaction)
  </read_first>

  <action>
    Crear `backend/api/sync/push.js` con el handler estilo legacy de Vercel Node
    (`export default async function handler(request, response)`), elegido porque parsea el body JSON
    automáticamente en `request.body` — cero código extra:

    ```js
    import { requireApiKey, AUTH_ERROR } from '../_lib/auth.js';
    import { getSql } from '../_lib/db.js';
    import { validateOutboxItem, upsertStatement } from '../_lib/outbox.js';

    export default async function handler(request, response) {
      if (request.method !== 'POST') {
        return response.status(405).json({ error: 'method not allowed' });
      }
      if (requireApiKey(request)) {
        return response.status(401).json({ error: AUTH_ERROR });
      }

      const receivedAt = new Date().toISOString();
      const outbox = request.body?.outbox;

      if (!Array.isArray(outbox) || outbox.length === 0) {
        return response.status(200).json({ ackedIds: [], serverCursor: null, receivedAt });
      }

      // Validar TODO antes de tocar la DB: sql.transaction() es todo-o-nada, así que una fila
      // malformada abortaría el batch entero de forma silenciosa. Preferimos un 400 explícito
      // que nombra qué ids fallaron (ver 02-RESEARCH.md > Pitfall 4).
      const invalid = outbox
        .map((item, index) => ({ item, index, error: validateOutboxItem(item) }))
        .filter((entry) => entry.error !== null);
      if (invalid.length > 0) {
        return response.status(400).json({
          error: invalid.map((e) => `item[${e.index}]: ${e.error}`).join('; '),
          invalidIds: invalid.map((e) => e.item?.id).filter(Number.isInteger),
        });
      }

      const sql = getSql();
      await sql.transaction(outbox.map((item) => upsertStatement(sql, item)));

      return response.status(200).json({
        ackedIds: outbox.map((item) => item.id),
        serverCursor: receivedAt,
        receivedAt,
      });
    }
    ```
    Nota deliberada: como `sql.transaction()` commitea todo o nada, si la transacción resuelve
    entonces TODOS los items se aplicaron, por eso `ackedIds` es la lista completa. Si lanza, no se
    responde 200 y el cliente reintenta el batch entero — lo cual es seguro justamente porque el
    upsert es idempotente (SYNC-02).

    Crear `backend/api/sync/state.js`:
    ```js
    import { requireApiKey, AUTH_ERROR } from '../_lib/auth.js';
    import { getSql } from '../_lib/db.js';

    export default async function handler(request, response) {
      if (requireApiKey(request)) {
        return response.status(401).json({ error: AUTH_ERROR });
      }
      const sql = getSql();
      const rows = await sql`SELECT MAX(current_updated_at) AS last FROM synced_entities`;
      const last = rows[0]?.last ? new Date(rows[0].last).toISOString() : null;
      return response.status(200).json({ serverCursor: last, lastSyncAt: last });
    }
    ```

    NO crear `backend/api/sync/changes.js`. El pull es Fase 3 (SYNC-04/SYNC-05) y nada lo llama.

    Crear `backend/api/sync/push.test.js` — test de INTEGRACIÓN contra Postgres real. Un stand-in
    en memoria/sqlite no validaría `ON CONFLICT` ni `JSONB` de Postgres, que es exactamente lo que
    hay que probar acá. Estructura:

    ```js
    import test from 'node:test';
    import assert from 'node:assert/strict';
    import { getSql } from '../_lib/db.js';
    import { upsertStatement } from '../_lib/outbox.js';

    // Se saltea (no falla) sin DATABASE_URL, para que `npm test` sea verde en cualquier máquina.
    // El plan 02-05 lo corre de verdad contra una branch de Neon antes de cerrar la fase.
    const skip = process.env.DATABASE_URL ? false : 'requires DATABASE_URL (Neon dev branch)';

    test('idempotencia y versionado', { skip }, async (t) => { /* ... */ });
    ```

    Los casos que este test debe cubrir (crear un `record_uuid` único por corrida con
    `crypto.randomUUID()` para no pisarse entre ejecuciones, y limpiar con un
    `DELETE FROM synced_entities WHERE record_uuid = ...` en `t.after`):

    1. SYNC-02 — idempotencia: aplicar el mismo item (uuid X, version 1) DOS veces; leer la fila y
       verificar `current_version === 1`, `previous_version === null`, y que `current_updated_at`
       sea IDÉNTICO entre la primera y la segunda lectura (probar que el segundo push no reescribió).
    2. SYNC-06 — ventana de 2 versiones: aplicar versiones 1, 2 y 3 sobre el mismo uuid; verificar
       `current_version === 3` y `previous_version === 2`. La versión 1 no debe estar en ningún lado
       (verificar que `previous_payload->>'logical_version'` es `'2'`).
    3. Out-of-order: después de aplicar la versión 3, aplicar la versión 2; verificar que
       `current_version` sigue en 3 y `previous_version` sigue en 2 (no retrocedió).
    4. Delete: aplicar un item con `op: 'delete'` y versión mayor; verificar `deleted_at IS NOT NULL`
       y `op === 'delete'`.

    Antes de los asserts, el test debe asegurar que la tabla existe: leer `backend/schema.sql`
    con `node:fs` y ejecutarlo (`CREATE TABLE IF NOT EXISTS` es idempotente).
  </action>

  <verify>
    <automated>cd backend && node --test</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "export default async function handler" backend/api/sync/push.js` encuentra una coincidencia
    - `grep -n "response.status(405)" backend/api/sync/push.js` encuentra una coincidencia
    - `grep -n "response.status(401)" backend/api/sync/push.js` encuentra una coincidencia
    - `grep -n "response.status(400)" backend/api/sync/push.js` encuentra una coincidencia
    - `grep -n "invalidIds" backend/api/sync/push.js` encuentra una coincidencia
    - `grep -n "sql.transaction" backend/api/sync/push.js` encuentra una coincidencia
    - `grep -n "serverCursor: receivedAt" backend/api/sync/push.js` encuentra una coincidencia
    - `backend/api/sync/state.js` existe y `grep -n "MAX(current_updated_at)" backend/api/sync/state.js` encuentra una coincidencia
    - `ls backend/api/sync/changes.js` falla (el archivo NO existe — el pull es Fase 3)
    - `grep -n "skip" backend/api/sync/push.test.js` encuentra el guard de DATABASE_URL
    - `grep -c "current_version" backend/api/sync/push.test.js` devuelve 4 o más (los 4 escenarios asertan sobre la versión)
    - `grep -n "previous_version" backend/api/sync/push.test.js` encuentra al menos 2 coincidencias
    - `grep -n "deleted_at" backend/api/sync/push.test.js` encuentra una coincidencia
    - `cd backend && node --test` termina en verde (los tests de integración salen como `skipped` si no hay `DATABASE_URL`, lo cual es esperado en esta wave)
  </acceptance_criteria>

  <done>Los dos endpoints existen con auth, validación y upsert transaccional; el test de integración está escrito y listo para correrse contra una branch real de Neon en el plan 02-05.</done>
</task>

</tasks>

<verification>
1. `cd backend && npm install && node --test` — verde (los tests que requieren `DATABASE_URL` salen como skipped)
2. `ls backend/api/sync/` muestra exactamente `push.js`, `push.test.js`, `state.js` — nada más
3. `find backend -name "*.ts" -not -path "*/node_modules/*"` no devuelve nada
4. `git status --porcelain` no muestra `backend/node_modules` (está ignorado)
5. El contrato HTTP implementado coincide campo por campo con el bloque `<wire_contract>` de este plan
</verification>

<success_criteria>
- Existe `backend/` con dos funciones serverless desplegables en Vercel
- Un request sin bearer token válido recibe 401 (SYNC-07, testeado)
- El upsert versionado implementa idempotencia por `record_uuid` + `logical_version` (SYNC-02) y ventana de 2 versiones (SYNC-06) en un solo statement
- `npm test` corre sin depender de una DB; el test de integración se saltea limpio y está listo para correr contra Neon
- Cero TypeScript, cero build step, cero dependencias más allá de `@neondatabase/serverless`
</success_criteria>

<output>
Al terminar, crear `.planning/phases/02-backend-real-push-sync/02-02-SUMMARY.md`
</output>
