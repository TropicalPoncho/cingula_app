---
phase: 02-backend-real-push-sync
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - pubspec.yaml
  - lib/core/config/api_config.dart
  - lib/data/sync/sync_errors.dart
  - lib/data/sync/sync_api_http.dart
  - lib/domain/usecases/run_sync_usecase.dart
  - lib/core/di/service_locator.dart
  - test/data/sync/sync_api_http_test.dart
  - test/domain/usecases/run_sync_usecase_test.dart
autonomous: true
requirements: [SYNC-01, SYNC-03, SYNC-07]

must_haves:
  truths:
    - "La app habla HTTP real contra el backend en vez de SyncApiStub"
    - "Un 500 o un corte de red deja la fila pendiente en el outbox y agenda un reintento con backoff"
    - "Un 401 deja la fila pendiente pero NO consume backoff ni incrementa el contador de intentos: es un error de configuración, no de red (D-03)"
    - "Un push que se corta a la mitad y se repite no duplica nada, porque se reenvía el mismo lote sin cambios"
  artifacts:
    - path: "lib/data/sync/sync_api_http.dart"
      provides: "SyncApiHttp: implementación real de SyncApi contra /sync/push y /sync/state"
      exports: ["SyncApiHttp"]
    - path: "lib/data/sync/sync_errors.dart"
      provides: "SyncAuthException (terminal) y SyncTransientException (reintentable)"
      exports: ["SyncAuthException", "SyncTransientException"]
    - path: "test/data/sync/sync_api_http_test.dart"
      provides: "cobertura con MockClient de forma del request, 200, 401, 500 y error de red"
    - path: "test/domain/usecases/run_sync_usecase_test.dart"
      provides: "cobertura de ack/markAttempt, doble push idempotente, y 401 terminal"
  key_links:
    - from: "lib/core/di/service_locator.dart"
      to: "lib/data/sync/sync_api_http.dart"
      via: "registerLazySingleton<SyncApi>(() => SyncApiHttp(...))"
      pattern: "SyncApiHttp"
    - from: "lib/data/sync/sync_api_http.dart"
      to: "lib/core/config/api_config.dart"
      via: "ApiConfig.syncPushUri() + ApiConfig.apiKey"
      pattern: "ApiConfig\\.(syncPushUri|apiKey)"
    - from: "lib/domain/usecases/run_sync_usecase.dart"
      to: "lib/data/sync/sync_errors.dart"
      via: "catch SyncAuthException -> terminal, catch SyncTransientException -> markAttempt"
      pattern: "SyncAuthException"
---

<objective>
Reemplazar `SyncApiStub` por `SyncApiHttp`, una implementación real de `SyncApi` sobre
`package:http`, y enseñarle a `RunSyncUseCase` la diferencia entre un error transitorio
(reintentar con backoff) y uno terminal (401: no reintentar, D-03).

Purpose: es el corazón de SYNC-01 y SYNC-03. Después de este plan la app ya sincroniza de verdad
contra el backend, aunque todavía sólo cuando se aprieta el botón manual — el disparo automático
llega en 02-04.

Output: `SyncApiHttp` + tipos de error + DI apuntando a la implementación real, todo con tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-real-push-sync/02-CONTEXT.md
@.planning/phases/02-backend-real-push-sync/02-RESEARCH.md

<wire_contract>
CONTRATO HTTP — copia verbatim del bloque en el plan 02-02 (lado backend). El cliente debe hablar
exactamente esto.

--- POST {base}/sync/push ---

Headers de request:
  Authorization: Bearer <CINGULA_SYNC_API_KEY>
  Content-Type: application/json

Body de request (los items son exactamente lo que devuelve `SyncClient.pendingOutbox`, sin
transformar: nombres de columna snake_case, `payload` ya decodificado a Map):
```json
{
  "deviceId": "abc-123",
  "cursor": "2026-08-29T10:00:00.000Z",
  "outbox": [
    {
      "id": 12,
      "table_name": "geo_triggers",
      "record_uuid": "8f14e45f-ceea-467a-9f4b-1c2d3e4f5a6b",
      "op": "update",
      "payload": { "id": 3, "uuid": "8f14e45f-...", "logical_version": 2, "name": "..." },
      "device_id": "abc-123",
      "created_at": 1756400000,
      "attempt_count": 0,
      "next_attempt_at": null
    }
  ]
}
```

Response 200: `{"ackedIds": [12, 13], "serverCursor": "<iso>|null", "receivedAt": "<iso>"}`
Response 401: `{"error": "invalid or missing API key"}`
Response 400: `{"error": "<descripción>", "invalidIds": [12]}`
Response 405: `{"error": "method not allowed"}`

--- GET {base}/sync/state ---
Headers: Authorization: Bearer <CINGULA_SYNC_API_KEY>
Response 200: `{"serverCursor": "<iso>|null", "lastSyncAt": "<iso>|null"}`

--- /sync/changes ---
NO existe en esta fase. `pullChanges` debe lanzar `UnimplementedError`.
</wire_contract>

<interfaces>
`lib/data/sync/sync_api.dart` (contrato a implementar, sin cambios):
```dart
abstract class SyncApi {
  Future<SyncPushResult> pushOutbox({required List<Map<String, Object?>> outbox, String? cursor, String? deviceId});
  Future<SyncStateResult> fetchState();
  Future<SyncPullResult> pullChanges({String? cursor});
}
class SyncPushResult { SyncPushResult({required this.ackedIds, required this.serverCursor, required this.receivedAt});
  final List<int> ackedIds; final String serverCursor; final DateTime receivedAt; }
class SyncStateResult { SyncStateResult({required this.serverCursor, required this.lastSyncAt});
  final String? serverCursor; final DateTime? lastSyncAt; }
class SyncPullResult { SyncPullResult({required this.changes, required this.serverCursor});
  final List<Map<String, Object?>> changes; final String serverCursor; }
```

`lib/core/config/api_config.dart` (actual):
```dart
class ApiConfig {
  static const String _defaultBaseUrl = String.fromEnvironment('CINGULA_API_BASE_URL', defaultValue: 'http://localhost:3000');
  static String baseUrl = _defaultBaseUrl;
  static Uri syncPushUri({String? overrideBase});
  static Uri syncStateUri({String? overrideBase});
  static Uri syncChangesUri({String? overrideBase, String? cursor});
}
```

`lib/data/sync/sync_client.dart` DESPUÉS del plan 02-01 (firmas ya actualizadas):
```dart
class SyncClient {
  SyncClient({required SyncLocalDataSource sync});
  Future<List<Map<String, Object?>>> pendingOutbox({int limit = 50, DateTime? now});
  Future<void> ackOutbox(int id);
  Future<void> markAttempt(int id, {required int attemptCount, DateTime? now});
  Future<Map<String, Object?>> getState();
  Future<void> saveState({String? serverCursor, DateTime? lastSync, String? deviceId});
}
```

Registro actual en `lib/core/di/service_locator.dart` (el punto exacto a cambiar):
```dart
  // Capa de red ficticia para probar flujo de sync sin servidor real.
  getIt.registerLazySingleton<SyncApi>(
    () => SyncApiStub(),
  );
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dependencia http, API key en ApiConfig, tipos de error, y SyncApiHttp</name>
  <files>pubspec.yaml, lib/core/config/api_config.dart, lib/data/sync/sync_errors.dart, lib/data/sync/sync_api_http.dart, test/data/sync/sync_api_http_test.dart</files>

  <read_first>
    - pubspec.yaml (ver bloque `dependencies:` — `http` NO está todavía)
    - lib/core/config/api_config.dart (archivo a modificar; replicar el patrón `String.fromEnvironment` + campo estático mutable que ya usa para `baseUrl`)
    - lib/data/sync/sync_api.dart (contrato a implementar)
    - lib/data/sync/sync_api_stub.dart (referencia de cómo se arma un `SyncPushResult`; se borra en la Task 2 de este plan)
    - El bloque `<wire_contract>` de este plan
  </read_first>

  <behavior>
    Tests en `test/data/sync/sync_api_http_test.dart`, todos con `MockClient` de `package:http/testing.dart`:
    - Forma del request: el POST va a `{baseUrl}/sync/push`, con header `Authorization: Bearer test-key`
      y `Content-Type: application/json`, y el body decodificado tiene las claves `outbox`, `cursor`, `deviceId`
    - El array `outbox` del body es idéntico a la lista que se le pasó (no se renombran ni filtran campos)
    - Respuesta 200 con `{"ackedIds":[1,2],"serverCursor":"c1","receivedAt":"2026-08-29T12:00:00.000Z"}`
      produce un `SyncPushResult` con esos tres valores parseados
    - Respuesta 200 con `serverCursor: null` cae de vuelta al cursor que se envió (y a `''` si no había)
    - Respuesta 401 lanza `SyncAuthException`
    - Respuesta 500 lanza `SyncTransientException`
    - Respuesta 503 lanza `SyncTransientException`
    - Respuesta 408 lanza `SyncTransientException`
    - Respuesta 400 lanza `SyncTransientException` con el mensaje del cuerpo incluido
      (decisión explícita, ver Open Question 1 del research)
    - Un `SocketException` / `ClientException` desde el client lanza `SyncTransientException`
    - `fetchState()` hace GET a `{baseUrl}/sync/state` con el header Authorization y parsea `{serverCursor, lastSyncAt}`
    - `pullChanges()` lanza `UnimplementedError`
  </behavior>

  <action>
    PASO 1 — `pubspec.yaml`: agregar `  http: ^1.6.0` dentro del bloque `dependencies:`, respetando
    el orden/indentación de las entradas existentes. NO agregar `dio`, ni `mockito`, ni `mocktail`:
    `package:http/testing.dart` (que viene dentro de `http`) da el `MockClient` que los tests necesitan.
    Correr `flutter pub get` después.

    PASO 2 — `lib/core/config/api_config.dart`: agregar, justo debajo de `static String baseUrl`:
    ```dart
    /// Token compartido con el backend. Se fija en tiempo de compilación con
    /// `--dart-define=CINGULA_SYNC_API_KEY=<token>` (D-02: sin login, sin rotación).
    /// Vacío = la app no puede autenticar y el backend va a responder 401.
    static const String _defaultApiKey = String.fromEnvironment(
      'CINGULA_SYNC_API_KEY',
      defaultValue: '',
    );

    /// Mutable para overrides en runtime desde el panel de debug (mismo patrón que [baseUrl]).
    static String apiKey = _defaultApiKey;
    ```
    No tocar `syncPushUri` / `syncStateUri` / `syncChangesUri`.

    PASO 3 — crear `lib/data/sync/sync_errors.dart`:
    ```dart
    /// Error terminal: reintentarlo no lo va a arreglar. Hoy sólo lo produce un 401
    /// (la API key compilada en el build está mal). Ver D-03 del 02-CONTEXT.md.
    class SyncAuthException implements Exception {
      SyncAuthException(this.message);
      final String message;
      @override
      String toString() => 'SyncAuthException: $message';
    }

    /// Error transitorio: la fila sigue pendiente y se reintenta con backoff.
    class SyncTransientException implements Exception {
      SyncTransientException(this.message);
      final String message;
      @override
      String toString() => 'SyncTransientException: $message';
    }
    ```
    Dos clases, nada más. Sin jerarquía de errores, sin códigos, sin `sealed class`.

    PASO 4 — crear `lib/data/sync/sync_api_http.dart`:
    ```dart
    import 'dart:convert';

    import 'package:http/http.dart' as http;

    import '../../core/config/api_config.dart';
    import 'sync_api.dart';
    import 'sync_errors.dart';

    /// Implementación real de [SyncApi] contra el backend de Vercel + Neon.
    class SyncApiHttp implements SyncApi {
      SyncApiHttp({http.Client? client}) : _client = client ?? http.Client();

      final http.Client _client;

      Map<String, String> get _headers => {
            'Authorization': 'Bearer ${ApiConfig.apiKey}',
            'Content-Type': 'application/json',
          };

      @override
      Future<SyncPushResult> pushOutbox({
        required List<Map<String, Object?>> outbox,
        String? cursor,
        String? deviceId,
      }) async {
        final http.Response response;
        try {
          response = await _client.post(
            ApiConfig.syncPushUri(),
            headers: _headers,
            body: jsonEncode({'outbox': outbox, 'cursor': cursor, 'deviceId': deviceId}),
          );
        } catch (e) {
          // Sin respuesta = no llegamos al servidor: transitorio por definición.
          throw SyncTransientException('network failure: $e');
        }

        if (response.statusCode == 401) {
          throw SyncAuthException('API key rechazada (401)');
        }
        if (response.statusCode != 200) {
          // ponytail: todo lo no-200 y no-401 es transitorio. Un 400 tampoco va a andar
          // reintentando, pero inventar una tercera categoría de error para un caso que
          // sólo aparece con un bug de payload no paga (research Open Question 1).
          throw SyncTransientException('HTTP ${response.statusCode}: ${response.body}');
        }

        final body = jsonDecode(response.body) as Map<String, Object?>;
        return SyncPushResult(
          ackedIds: (body['ackedIds'] as List? ?? const []).cast<int>(),
          serverCursor: (body['serverCursor'] as String?) ?? cursor ?? '',
          receivedAt: DateTime.parse(body['receivedAt'] as String),
        );
      }

      @override
      Future<SyncStateResult> fetchState() async {
        final http.Response response;
        try {
          response = await _client.get(ApiConfig.syncStateUri(), headers: _headers);
        } catch (e) {
          throw SyncTransientException('network failure: $e');
        }
        if (response.statusCode == 401) {
          throw SyncAuthException('API key rechazada (401)');
        }
        if (response.statusCode != 200) {
          throw SyncTransientException('HTTP ${response.statusCode}: ${response.body}');
        }
        final body = jsonDecode(response.body) as Map<String, Object?>;
        final lastSyncAt = body['lastSyncAt'] as String?;
        return SyncStateResult(
          serverCursor: body['serverCursor'] as String?,
          lastSyncAt: lastSyncAt == null ? null : DateTime.parse(lastSyncAt),
        );
      }

      @override
      Future<SyncPullResult> pullChanges({String? cursor}) {
        // El pull es Fase 3 (SYNC-04/SYNC-05). Falla ruidosamente en vez de devolver
        // una lista vacía que parecería "no hay cambios".
        throw UnimplementedError('pullChanges llega en la Fase 3 (sync bidireccional)');
      }
    }
    ```

    PASO 5 — crear `test/data/sync/sync_api_http_test.dart` cubriendo cada caso de `<behavior>`.
    Patrón: en `setUp`, fijar `ApiConfig.baseUrl = 'http://test.local'` y `ApiConfig.apiKey = 'test-key'`;
    construir `SyncApiHttp(client: MockClient((request) async => http.Response(body, status)))`.
    Para el caso de error de red, el `MockClient` debe lanzar
    `http.ClientException('boom')` en vez de devolver una `Response`.
    Restaurar `ApiConfig.baseUrl`/`apiKey` en `tearDown` para no contaminar otros tests.
  </action>

  <verify>
    <automated>flutter test test/data/sync/sync_api_http_test.dart</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "  http: \^1.6" pubspec.yaml` encuentra una coincidencia
    - `grep -c "dio" pubspec.yaml` devuelve 0
    - `grep -c "mockito\|mocktail" pubspec.yaml` devuelve 0
    - `grep -n "CINGULA_SYNC_API_KEY" lib/core/config/api_config.dart` encuentra una coincidencia
    - `grep -n "static String apiKey" lib/core/config/api_config.dart` encuentra una coincidencia
    - `lib/data/sync/sync_errors.dart` existe con `class SyncAuthException` y `class SyncTransientException`
    - `grep -n "class SyncApiHttp implements SyncApi" lib/data/sync/sync_api_http.dart` encuentra una coincidencia
    - `grep -n "'Authorization': 'Bearer \${ApiConfig.apiKey}'" lib/data/sync/sync_api_http.dart` encuentra una coincidencia
    - `grep -n "statusCode == 401" lib/data/sync/sync_api_http.dart` encuentra 2 coincidencias (push y state)
    - `grep -n "UnimplementedError" lib/data/sync/sync_api_http.dart` encuentra una coincidencia
    - `grep -n "package:http/testing.dart" test/data/sync/sync_api_http_test.dart` encuentra una coincidencia
    - `flutter test test/data/sync/sync_api_http_test.dart` pasa con al menos 12 tests
  </acceptance_criteria>

  <done>`SyncApiHttp` habla el contrato del backend y clasifica cada respuesta como éxito, terminal (401) o transitoria, con cobertura automatizada de los cinco caminos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RunSyncUseCase distingue terminal de transitorio + DI apunta al backend real</name>
  <files>lib/domain/usecases/run_sync_usecase.dart, lib/core/di/service_locator.dart, lib/data/sync/sync_api_stub.dart (se borra), test/domain/usecases/run_sync_usecase_test.dart</files>

  <read_first>
    - lib/domain/usecases/run_sync_usecase.dart (archivo a modificar; ya fue tocado por el plan 02-01 para pasar `attemptCount` a `markAttempt`)
    - lib/data/sync/sync_errors.dart (creado en Task 1)
    - lib/data/sync/sync_client.dart (firmas post-02-01: `pendingOutbox({limit, now})`, `markAttempt(id, {required attemptCount, now})`)
    - lib/core/di/service_locator.dart (leer el bloque de registro de `SyncApi` / `RunSyncUseCase`, ~líneas 51-70)
  </read_first>

  <behavior>
    Tests en `test/domain/usecases/run_sync_usecase_test.dart`, usando fakes escritos a mano
    (`class _FakeSyncApi implements SyncApi` y `class _FakeSyncClient implements SyncClient`;
    `implements` sobre una clase concreta de otra librería es legal en Dart y no invoca su
    constructor, así que no hace falta ninguna DB ni ninguna librería de mocking):
    - Outbox vacío: no se llama a la API, el resultado tiene `totalOutbox == 0` y `hadWork == false`
    - Todos acked: se llama `ackOutbox` una vez por id, nunca se llama `markAttempt`, y se guarda estado
    - Acked parcial (server ackea 1 de 2): `ackOutbox` para el ackeado, `markAttempt` para el otro
      con el `attemptCount` que traía esa fila
    - Doble push idempotente (simula "mataron la app a la mitad"): correr `pushOutboxOnce` dos veces
      con el mismo lote produce los mismos acks y ninguna llamada extra a `markAttempt`
    - `SyncTransientException` desde la API: se llama `markAttempt` para TODAS las filas del lote,
      NO se llama `ackOutbox`, NO se guarda `lastSync`, y la excepción se propaga al llamador
    - `SyncAuthException` desde la API: NO se llama `markAttempt` para ninguna fila (D-03: un 401 no
      consume backoff), NO se llama `ackOutbox`, y la excepción se propaga
  </behavior>

  <action>
    PASO 1 — `lib/domain/usecases/run_sync_usecase.dart`: envolver la llamada a `_api.pushOutbox`
    en un try/catch que distinga los dos tipos de error. El método queda así (a partir de la versión
    que dejó el plan 02-01):

    ```dart
    final SyncPushResult response;
    try {
      response = await _api.pushOutbox(outbox: outbox, cursor: cursor, deviceId: deviceId);
    } on SyncAuthException {
      // D-03: un 401 es un problema de configuración (API key mal compilada), no de red.
      // No se toca attempt_count ni next_attempt_at: reintentar con backoff sólo gastaría
      // batería sin arreglar nada. Las filas quedan pendientes hasta que se corrija la clave.
      rethrow;
    } on SyncTransientException {
      // Fallo de red/servidor: cada fila del lote suma un intento y queda agendada con backoff.
      for (final row in outbox) {
        final id = row['id'];
        if (id is int) {
          await _client.markAttempt(id, attemptCount: (row['attempt_count'] as int?) ?? 0);
        }
      }
      rethrow;
    }
    ```
    Agregar el import `import '../../data/sync/sync_errors.dart';`.

    El resto del método (ack de los acked, `markAttempt` de los no-acked, `saveState`) queda como
    está — sólo corre en el camino feliz, que es lo correcto: si la petición falló entero no hay
    `serverCursor` ni `receivedAt` que guardar, y marcar `lastSync` ahí sería exactamente el
    "todo sincronizado" mentiroso que SYNC-08 prohíbe.

    PASO 2 — `lib/core/di/service_locator.dart`: reemplazar el bloque
    ```dart
      // Capa de red ficticia para probar flujo de sync sin servidor real.
      getIt.registerLazySingleton<SyncApi>(
        () => SyncApiStub(),
      );
    ```
    por
    ```dart
      // Backend real (Vercel Functions + Neon). Base URL y API key vienen por --dart-define:
      //   --dart-define=CINGULA_API_BASE_URL=https://<proyecto>.vercel.app
      //   --dart-define=CINGULA_SYNC_API_KEY=<token>
      getIt.registerLazySingleton<SyncApi>(
        () => SyncApiHttp(),
      );
    ```
    y cambiar el import de `sync_api_stub.dart` por `sync_api_http.dart`.

    PASO 3 — borrar `lib/data/sync/sync_api_stub.dart`. Ya no lo usa nadie y dejarlo invita a que
    alguien vuelva a registrarlo "para probar". Verificar antes con
    `grep -rn "SyncApiStub" lib/ test/` que no queden referencias; si aparece alguna, quitarla.

    PASO 4 — crear `test/domain/usecases/run_sync_usecase_test.dart` cubriendo cada caso de
    `<behavior>`. Los fakes van en el mismo archivo de test, privados (prefijo `_`). El
    `_FakeSyncClient` mantiene la lista de outbox en memoria y registra las llamadas
    (`ackedIds`, `markedAttempts`, `savedStates`) para poder asertar sobre ellas.
  </action>

  <verify>
    <automated>flutter test test/domain/usecases/run_sync_usecase_test.dart && flutter analyze</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "on SyncAuthException" lib/domain/usecases/run_sync_usecase.dart` encuentra una coincidencia
    - `grep -n "on SyncTransientException" lib/domain/usecases/run_sync_usecase.dart` encuentra una coincidencia
    - `grep -n "SyncApiHttp()" lib/core/di/service_locator.dart` encuentra una coincidencia
    - `grep -rn "SyncApiStub" lib/ test/` devuelve 0 coincidencias
    - `ls lib/data/sync/sync_api_stub.dart` falla (el archivo fue borrado)
    - `grep -c "class _Fake" test/domain/usecases/run_sync_usecase_test.dart` devuelve 2
    - `grep -c "sqflite" test/domain/usecases/run_sync_usecase_test.dart` devuelve 0 (los fakes no necesitan DB)
    - `flutter test test/domain/usecases/run_sync_usecase_test.dart` pasa con al menos 6 tests
    - `flutter analyze` sale con 0 errores
    - `flutter test` (suite completa) verde
  </acceptance_criteria>

  <done>La app está cableada al backend real, un 401 no consume backoff, un error de red sí lo consume, y todo eso está cubierto por tests que no necesitan ni servidor ni base de datos.</done>
</task>

</tasks>

<verification>
1. `flutter analyze` — 0 errores
2. `flutter test` — suite completa verde
3. `grep -rn "SyncApiStub" lib/ test/` — sin resultados (el stub se fue)
4. `grep -rn "pullChanges" lib/` — sólo aparece en la definición del contrato y en el `UnimplementedError` (no se implementó pull, que es Fase 3)
5. Correr la app con `--dart-define=CINGULA_API_BASE_URL=http://localhost:3000 --dart-define=CINGULA_SYNC_API_KEY=x` compila
</verification>

<success_criteria>
- `SyncApi` en el service locator resuelve a `SyncApiHttp`, no al stub (SYNC-01)
- El header `Authorization: Bearer <key>` se manda en push y en state (SYNC-07)
- Errores transitorios agendan reintento con backoff; un 401 no (SYNC-03, D-03)
- Cero dependencias nuevas más allá de `http`
- `flutter test` verde
</success_criteria>

<output>
Al terminar, crear `.planning/phases/02-backend-real-push-sync/02-03-SUMMARY.md`
</output>
