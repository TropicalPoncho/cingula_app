---
phase: 02.1
plan: 12
type: execute
wave: 8
depends_on: ["02.1-11"]
files_modified:
  - .planning/phases/02.1-modelo-de-datos-objetivo/02.1-CUTOVER.md
  - .planning/phases/02.1-modelo-de-datos-objetivo/02.1-NEON-REHEARSAL.md
autonomous: false
requirements: [MODEL-05, MODEL-06, MODEL-08]
must_haves:
  truths:
    - "Está confirmado a qué rama de Neon apuntan backend/.env y el deploy de Vercel"
    - "El script de Neon se ensayó en una rama descartable antes de tocar la rama real"
    - "La migración se vio correr sobre una copia de la base real, fuera del celular, antes del corte"
    - "Antes de instalar la app nueva existe una copia independiente de la base Y de los .wav en la PC"
    - "El corte en el celular real fue la primera y única corrida sobre el dispositivo"
  artifacts:
    - path: ".planning/phases/02.1-modelo-de-datos-objetivo/02.1-CUTOVER.md"
      provides: "bitácora del corte: rama confirmada, ensayo, conteos antes/después y resultado"
      contains: "conteos"
  key_links:
    - from: ".planning/phases/02.1-modelo-de-datos-objetivo/02.1-CUTOVER.md"
      to: "backend/scripts/migrate_v2.mjs"
      via: "salida real del dry-run y del apply"
      pattern: "migrate_v2"
---

<objective>
La compuerta humana de la fase: confirmar el entorno, ensayar en una rama descartable, ver la migración correr sobre una copia real fuera del celular, respaldar de forma independiente, y recién entonces hacer el corte.

Purpose: todo lo automatizable ya está verde. Lo que queda son las cinco cosas que solo puede hacer una persona con acceso al celular, a Neon y a Vercel — y cada una es irreversible a su manera.
Output: `02.1-CUTOVER.md` con la bitácora, y la app nueva corriendo sobre los datos migrados.

Precondición dura: este plan NO arranca si `flutter test`, `cd backend && npm test` o la auditoría del plan 02.1-11 no están en verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-modelo-de-datos-objetivo/02.1-CONTEXT.md
@.planning/phases/02.1-modelo-de-datos-objetivo/02.1-RESEARCH.md
@.planning/phases/02.1-modelo-de-datos-objetivo/02.1-PREFLIGHT.md
@.planning/phases/02.1-modelo-de-datos-objetivo/02.1-NEON-REHEARSAL.md
@.planning/phases/02-backend-real-push-sync/02-05-SUMMARY.md
@backend/scripts/migrate_v2.mjs
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: confirmar a qué rama de Neon apuntan backend/.env y el deploy de Vercel</name>
  <read_first>
    - backend/api/_lib/db.js (de dónde sale `DATABASE_URL`)
    - .planning/phases/02-backend-real-push-sync/02-05-SUMMARY.md (cómo quedó configurado el deploy en la Fase 2)
  </read_first>
  <files>.planning/phases/02.1-modelo-de-datos-objetivo/02.1-CUTOVER.md</files>
  <action>
    Crear `02.1-CUTOVER.md` si no existe y registrar en él la respuesta del usuario: rama de `backend/.env`, rama del deploy de Vercel y opción elegida. El ejecutor NO abre credenciales ni ejecuta nada contra Neon en esta tarea.
  </action>
  <decision>Contra qué rama de Neon se ensaya y contra cuál se hace el corte.</decision>
  <context>
    El research dejó este dato explícitamente SIN CONFIRMAR: no se sabe si el `DATABASE_URL` de `backend/.env` y el que tiene configurado el deploy de Vercel apuntan a `main`, a `dev` o a otra rama. Correr el script de migración contra la rama equivocada significaría migrar la base de desarrollo y dejar la de producción con el esquema viejo (o, peor, al revés y sin ensayo previo).
    El script imprime el host al que se conectó, pero eso se ve DESPUÉS de conectarse: la decisión va antes.
  </context>
  <options>
    <option id="rehearse-dev-cutover-main">
      <name>Ensayar en una rama descartable creada desde main, cortar en main</name>
      <pros>La rama descartable es copy-on-write y trae los datos reales: el ensayo prueba la traducción contra los payloads de verdad. Es lo que recomienda el research.</pros>
      <cons>Hay que crear y borrar una rama en la consola de Neon.</cons>
    </option>
    <option id="rehearse-dev-existing">
      <name>Ensayar en la rama dev que ya existe, cortar en main</name>
      <pros>Sin pasos de consola.</pros>
      <cons>`dev` puede no tener los mismos datos que `main`; el ensayo prueba menos.</cons>
    </option>
  </options>
  <how-to-verify>
    1. Abrir `backend/.env` y mirar el host de `DATABASE_URL` (solo el host, no pegar credenciales en el chat).
    2. En el panel de Vercel, proyecto de Cíngula → Settings → Environment Variables → ver el host de `DATABASE_URL` de Production.
    3. En la consola de Neon, ver qué ramas existen y cuál corresponde a cada host.
    4. Responder con: qué rama usa `backend/.env`, qué rama usa el deploy de Vercel, y cuál de las dos opciones de arriba se elige.
  </how-to-verify>
  <acceptance_criteria>
    - `02.1-CUTOVER.md` registra: host/rama de `backend/.env`, host/rama del deploy de Vercel, y la opción elegida
    - No se pegó ninguna credencial completa en la bitácora
  </acceptance_criteria>
  <resume-signal>Responder: "env=<rama>, vercel=<rama>, opción=<rehearse-dev-cutover-main|rehearse-dev-existing>".</resume-signal>
  <done>La bitácora registra a qué rama apunta cada entorno y cuál se usa para ensayar y para cortar.</done>
  <verify>
    <automated>grep -qiE "rama|branch" .planning/phases/02.1-modelo-de-datos-objetivo/02.1-CUTOVER.md &amp;&amp; echo "rama registrada"</automated>
  </verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: ensayo del script de Neon en una rama y revisión del dry-run</name>
  <read_first>
    - backend/scripts/migrate_v2.mjs (los modos: dry-run por defecto, `--apply`, `--finalize`)
    - .planning/phases/02.1-modelo-de-datos-objetivo/02.1-PREFLIGHT.md (los conteos reales esperados)
  </read_first>
  <files>.planning/phases/02.1-modelo-de-datos-objetivo/02.1-CUTOVER.md</files>
  <action>
    Guiar al usuario por los pasos de `<how-to-verify>` y, cuando él provea la URL de la rama de ensayo, ejecutar por él `node scripts/migrate_v2.mjs` (dry-run), luego `--apply`, luego `--apply` otra vez, y `npm test` con esa `DATABASE_URL`. Pegar las salidas en `02.1-CUTOVER.md`. NUNCA usar una URL que no sea la de la rama de ensayo en esta tarea.

    Leer primero `02.1-NEON-REHEARSAL.md` (plan 02.1-07 Task 3): si ahí ya hay una corrida contra una rama dev, esta tarea la repite igual —la rama de ensayo de acá sale de `main`, con los datos que de verdad se van a migrar— pero se sabe de antemano que el script funciona y qué conteos esperar. Si ahí dice "postergado", esta es la PRIMERA corrida real del script y de los tests de Postgres: revisar la salida con más cuidado y no seguir al corte si algo sorprende.
  </action>
  <what-built>
    El script `backend/scripts/migrate_v2.mjs`: traduce `synced_entities` a las tablas tipadas, corre en dry-run por defecto, aborta si el mapa de ids es ambiguo, aplica en una sola transacción y solo renombra la tabla vieja con `--finalize`.
  </what-built>
  <how-to-verify>
    1. Según lo decidido en la Task 1, crear la rama de ensayo en la consola de Neon (Branches → Create branch desde `main`) y copiar su connection string.
    2. Dry-run: `cd backend && DATABASE_URL="<url de la rama de ensayo>" node scripts/migrate_v2.mjs`
       Revisar en la salida:
       - la línea `Conectado a: <host>/<db>` — QUE SEA LA RAMA DE ENSAYO, no main;
       - los conteos de origen por tabla;
       - los conteos de destino (deberían dar `audios` = audios del celular, `paths` = paths + triggers sueltos, `obras` = paths);
       - la lista de `skipped` (tombstones que no se pueden reconstruir: es esperable y no es pérdida, el celular ya los borró);
       - que NO haya ambigüedades (si las hay, el script aborta: hay que decidir caso por caso antes de seguir).
    3. Aplicar en la rama de ensayo: agregar `--apply`. Debe terminar con la verificación de conteos en verde.
    4. Volver a correrlo con `--apply` una segunda vez: tiene que ser idempotente (mismos conteos, sin duplicados).
    5. Correr los tests de integración del backend contra esa rama: `cd backend && DATABASE_URL="<url de ensayo>" npm test` — verde.
    6. Borrar la rama de ensayo en la consola de Neon.
    7. Pegar en el chat los conteos del dry-run y si algo no cuadró.
  </how-to-verify>
  <acceptance_criteria>
    - `02.1-CUTOVER.md` tiene pegada la salida del dry-run (conteos origen/destino, skipped, warnings)
    - La segunda corrida con `--apply` no cambió los conteos (idempotencia comprobada)
    - `npm test` con `DATABASE_URL` de la rama de ensayo terminó en verde
    - La rama de ensayo fue borrada
  </acceptance_criteria>
  <resume-signal>Pegar los conteos y escribir "ensayo ok" o describir qué falló.</resume-signal>
  <done>El ensayo en una rama descartable terminó con conteos correctos e idempotencia comprobada, y quedó pegado en la bitácora.</done>
  <verify>
    <automated>cd backend &amp;&amp; npm test</automated>
  </verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: ver la migración correr sobre una copia de la base real, fuera del celular</name>
  <read_first>
    - test/data/migration/v7_migration_test.dart (el caso con `CINGULA_REAL_DB`, que trabaja sobre una copia temporal)
    - .planning/phases/02.1-modelo-de-datos-objetivo/02.1-PREFLIGHT.md (los conteos de partida)
  </read_first>
  <files>.planning/phases/02.1-modelo-de-datos-objetivo/02.1-CUTOVER.md</files>
  <action>
    Ejecutar la Parte A (`CINGULA_REAL_DB=<ruta> flutter test test/data/migration -r expanded`) y anotar los conteos en `02.1-CUTOVER.md`. Para la Parte B, entregarle al usuario los comandos de emulador y esperar su reporte; el ejecutor no corre nada contra el celular real.
  </action>
  <what-built>
    La migración automática completa: respaldo previo, una sola transacción, verificación por conteos y diffs exactos, y vuelta atrás automática ante cualquier discrepancia.
  </what-built>
  <how-to-verify>
    **Parte A — automatizada, sobre la copia que ya está en la PC (la del plan 02.1-04):**
    `CINGULA_REAL_DB="<ruta de la copia>" flutter test test/data/migration -r expanded`
    Tiene que pasar. Anotar los conteos que imprime: audios, obras, paths (route + portal), triggers.

    **Parte B — la app corriendo sobre los datos migrados (emulador, NUNCA el celular):**

    ⚠️ **Antes de tocar `adb`: desconectar el celular real del USB.** Los comandos de esta parte SOBRESCRIBEN la base de datos de la app. Con dos dispositivos conectados, `adb` sin `-s` elige por su cuenta y puede elegir el celular: sería pisar la única copia de los datos con una copia vieja, que es exactamente lo que toda la fase existe para evitar.

    1. Levantar un emulador Android.
    2. Confirmar qué dispositivos ve adb ANTES de cualquier otro comando:
       `adb devices -l`
       Tiene que listar **un solo** dispositivo y tiene que decir `emulator-XXXX`. Si aparece el celular, desconectarlo y repetir. Anotar el serial del emulador: `$EMU` (por ejemplo `emulator-5554`).
    3. Instalar la app nueva en el emulador y abrirla una vez, para que cree su directorio de datos:
       `adb -s $EMU install -r build/app/outputs/flutter-apk/app-debug.apk`
    4. Cerrar la app y reemplazar su base por la COPIA v6 — **todos los comandos con `-s $EMU`**:
       `adb -s $EMU push "<copia v6>" /sdcard/cingula.db`
       `adb -s $EMU shell "run-as <applicationId> rm -f app_flutter/cingula.db-wal app_flutter/cingula.db-shm"`
       `adb -s $EMU shell "run-as <applicationId> cp /sdcard/cingula.db app_flutter/cingula.db"`
       (`-e` sirve igual que `-s <serial>` cuando hay exactamente un emulador; `-s` es explícito y no depende de eso).
    5. Abrir la app. En el primer arranque tiene que: migrar, mostrar el aviso "Datos migrados; respaldo en <archivo>", y NO mostrar la pantalla de error.
    6. Entrar al panel de debug → "Ver tablas de BD" y comprobar: los conteos de audios/obras/paths/triggers coinciden con los de la Parte A; cada path muestra el nombre de su obra; los triggers de un path están en orden.
    7. Comprobar que el outbox quedó con el reenvío completo (Panel: Sincronización → "Outbox pendiente" con ~700 filas) y que el cursor está vacío.
    8. Reproducir: caminar/simular una posición sobre un trigger conocido y confirmar que suena el audio del path, como antes.
    9. Cerrar y volver a abrir la app: no debe volver a migrar ni a respaldar.
  </how-to-verify>
  <acceptance_criteria>
    - La Parte A pasó y sus conteos están anotados en `02.1-CUTOVER.md`
    - `adb devices -l` quedó pegado en `02.1-CUTOVER.md` mostrando un único dispositivo `emulator-*`, y todos los comandos de la Parte B llevan `-s <serial>`
    - La Parte B se hizo en emulador; en ningún momento se corrió la app nueva ni un `adb push` contra el celular real (MODEL-08)
    - El aviso de respaldo apareció y se pudo descartar; NO hay ningún botón para borrar el respaldo (D-26)
    - Los conteos del data browser coinciden con los de la Parte A
  </acceptance_criteria>
  <resume-signal>Escribir "migración verificada en emulador" + los conteos, o describir la diferencia encontrada.</resume-signal>
  <done>La migración se vio correr sobre una copia real en emulador, con conteos coincidentes y el aviso de respaldo visible.</done>
  <verify>
    <automated>flutter test test/data/migration</automated>
  </verify>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: copia independiente de la base Y de los .wav a la PC</name>
  <read_first>
    - .planning/phases/02.1-modelo-de-datos-objetivo/02.1-CONTEXT.md (D-11: los .wav existen SOLO en el celular)
  </read_first>
  <files>.planning/phases/02.1-modelo-de-datos-objetivo/02.1-CUTOVER.md</files>
  <action>
    Presentar los pasos y esperar. El ejecutor no ejecuta `adb`: registra en `02.1-CUTOVER.md` la ruta, la cantidad de archivos de audio y el tamaño que informe el usuario.
  </action>
  <what-built>
    Nada: este paso es la red de seguridad que vive fuera de la app. El respaldo que toma la migración queda en el MISMO celular; si el celular se pierde o se rompe, no sirve de nada. Y los .wav no los respalda nadie todavía (eso es la Fase 2.2).
  </what-built>
  <how-to-verify>
    1. Cerrar la app Cíngula en el celular.
    2. Copiar la base y sus sidecars a una carpeta nueva de la PC, con fecha en el nombre (no reusar la copia del pre-flight: esta es la de antes del corte).
    3. Copiar TODA la carpeta de grabaciones:
       `adb exec-out run-as <applicationId> tar c app_flutter/recordings > C:\Users\ramac\cingula-backup-<fecha>\recordings.tar`
       (o copiar archivo por archivo si `tar` no está disponible en ese dispositivo).
    4. Verificar en la PC que la cantidad de .wav/.m4a copiados coincide con la que hay en el celular, y que el tamaño total es razonable (no 0).
    5. Idealmente, dejar una segunda copia en otro disco o en la nube.
    6. Informar: ruta de la carpeta, cantidad de archivos de audio y tamaño total.
  </how-to-verify>
  <acceptance_criteria>
    - Existe en la PC una carpeta con la base Y los archivos de audio, fechada, distinta de la copia del pre-flight
    - La cantidad de archivos de audio copiados coincide con la del celular
    - `02.1-CUTOVER.md` registra la ruta, la cantidad de archivos y el tamaño
  </acceptance_criteria>
  <resume-signal>Pegar la ruta, la cantidad de archivos de audio y el tamaño total.</resume-signal>
  <done>Existe en la PC una copia independiente y fechada de la base y de los .wav, anterior al corte.</done>
  <verify>
    <automated>grep -qiE "recordings|\.wav|audio" .planning/phases/02.1-modelo-de-datos-objetivo/02.1-CUTOVER.md &amp;&amp; echo "respaldo de audios registrado"</automated>
  </verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: el corte</name>
  <read_first>
    - backend/scripts/migrate_v2.mjs (el comentario final con el orden de operaciones)
    - .planning/phases/02.1-modelo-de-datos-objetivo/02.1-CUTOVER.md (lo registrado en las tareas anteriores)
  </read_first>
  <files>.planning/phases/02.1-modelo-de-datos-objetivo/02.1-CUTOVER.md</files>
  <action>
    Acompañar el orden de operaciones paso por paso. Los comandos de `migrate_v2.mjs` contra la rama de corte los ejecuta el ejecutor SOLO cuando el usuario confirma explícitamente cada paso; la instalación en el celular y la verificación de reproducción las hace el usuario. Registrar los conteos finales de ambos lados en `02.1-CUTOVER.md`.
  </action>
  <what-built>
    Todo lo de la fase: modelo nuevo en el celular y en el servidor, migración automática con red de seguridad, y el protocolo con compuerta `schema_version: 2`.
  </what-built>
  <how-to-verify>
    Orden exacto (D-20: ventana corta sin sync, un solo cliente, sin protocolo dual):
    1. **Quietar el celular**: cerrar la app y ponerlo en modo avión. Nada debe pushear durante la ventana.
    2. **Migrar el servidor**: `cd backend && DATABASE_URL="<rama de corte>" node scripts/migrate_v2.mjs` (dry-run, revisar), después `--apply`. Verificación de conteos en verde.
    3. **Desplegar el backend nuevo** (Vercel). Comprobar que `/api/sync/state` responde y que un push sin `schema_version` devuelve 400.
    4. **Re-correr el script con `--apply`** una vez más: recoge cualquier push que haya entrado en el blob viejo durante la ventana. Idempotente.
    5. **`--finalize`**: `node scripts/migrate_v2.mjs --finalize` renombra `synced_entities` a `synced_entities_legacy`. No se borra nada.
    6. **Instalar la app nueva en el celular** y abrirla. Primera y única corrida real: migra, respalda y reconstruye el outbox.
    7. Sacar el modo avión y forzar un push desde el panel ("Forzar push ahora"). El outbox (~700 filas) tiene que drenar. Vigilar el estado de sync: no debe quedar en error de auth ni en 400.
    8. Confirmar en Neon que los conteos de `audios`/`obras`/`paths`/`triggers` coinciden con los del celular.
    9. Reproducir un recorrido conocido en el celular y confirmar que suena igual que antes.
    10. Anotar en `02.1-CUTOVER.md` los conteos finales de ambos lados y la ruta del respaldo que quedó en el celular.

    Si algo sale mal en el paso 6: la app muestra la pantalla bloqueante, los datos quedan intactos y el respaldo está en el celular (y la copia independiente, en la PC). No borrar nada; reportar el mensaje de error.
  </how-to-verify>
  <acceptance_criteria>
    - `02.1-CUTOVER.md` tiene los conteos finales del celular y de Neon, y coinciden
    - El outbox del celular drenó (0 pendientes) y el estado de sync no muestra error
    - `synced_entities_legacy` existe en Neon (renombrada, no borrada)
    - El respaldo `*.pre-v7-*` existe en el celular y su ruta quedó anotada
  </acceptance_criteria>
  <resume-signal>Escribir "corte ok" + los conteos finales, o describir en qué paso se frenó y con qué mensaje.</resume-signal>
  <done>El corte se completó: servidor migrado, app nueva instalada, outbox drenado y conteos coincidentes en ambos lados.</done>
  <verify>
    <automated>flutter test &amp;&amp; cd backend &amp;&amp; npm test</automated>
  </verify>
</task>

</tasks>

<verification>
- `02.1-CUTOVER.md` completo: rama confirmada, ensayo, verificación en emulador, respaldo independiente, conteos finales.
- `flutter test` y `cd backend && npm test` siguen verdes después del corte.
- Neon: `synced_entities_legacy` presente; las 6 tablas tipadas con los conteos del celular.
</verification>

<success_criteria>
1. El script se ensayó en una rama descartable antes de tocar la rama real.
2. La migración se vio correr sobre una copia real en emulador, nunca en el celular, antes del corte.
3. Existe en la PC una copia independiente de la base y de los .wav, previa al corte.
4. Tras el corte, los conteos del celular y de Neon coinciden y el outbox drenó.
5. Ningún dato existente se perdió.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/02.1-modelo-de-datos-objetivo/02.1-12-SUMMARY.md`.
</output>
