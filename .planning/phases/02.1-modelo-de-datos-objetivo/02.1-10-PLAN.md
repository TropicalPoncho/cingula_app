---
phase: 02.1
plan: 10
type: execute
wave: 6
depends_on: ["02.1-09"]
files_modified:
  - lib/presentation/pages/home/widgets/obra_section.dart
  - lib/presentation/pages/home/widgets/diagnostics_panel.dart
  - lib/presentation/pages/data_browser_page.dart
  - lib/domain/repositories/obra_repository.dart
  - lib/data/repositories/obra_repository_impl.dart
  - lib/data/datasources/local/obra_local_data_source.dart
  - lib/core/services/recorder_service.dart
  - test/presentation/pages/home/widgets/obra_section_test.dart
  - test/data/datasources/local/obra_cover_test.dart
autonomous: true
requirements: [MODEL-02, MODEL-07]
must_haves:
  truths:
    - "El panel de debug tiene una sección Obra con nombre + crear + elegir, en el lugar donde estaba la de Región"
    - "Lo grabado queda en la obra elegida"
    - "Si no se eligió ninguna obra, al terminar se crea sola una obra draft con el nombre de la grabación"
    - "La cobertura de una obra se recalcula cuando cambian sus triggers y viaja como columnas normales de la obra"
    - "El data browser muestra a qué obra pertenece cada path"
  artifacts:
    - path: "lib/presentation/pages/home/widgets/obra_section.dart"
      provides: "sección Obra aislada y testeable (nombre + crear + desplegable)"
    - path: "test/presentation/pages/home/widgets/obra_section_test.dart"
      provides: "widget test de la sección Obra con un repositorio falso"
  key_links:
    - from: "lib/presentation/pages/home/widgets/diagnostics_panel.dart"
      to: "lib/presentation/pages/home/widgets/obra_section.dart"
      via: "la sección Obra ocupa el lugar del viejo Panel: Región"
      pattern: "ObraSection"
    - from: "lib/data/datasources/local/obra_local_data_source.dart"
      to: "lib/data/migration/cover.dart"
      via: "refreshCover recalcula la caja de la obra desde sus triggers"
      pattern: "computeCover"
---

<objective>
Cerrar la parte visible de la fase: la sección Obra del panel de debug (D-22) y la cobertura derivada que se mantiene al día (D-16/D-29).

Purpose: Región desapareció del panel en el plan 02.1-06 y grabar necesita una obra; esta es la única pantalla nueva que la fase agrega (y ni siquiera es nueva: es el mismo patrón nombre + crear + elegir en el mismo lugar).
Precondición: el árbol está verde (lo dejó así el plan 02.1-09) y sigue verde al cerrar este plan.
Output: sección Obra, cobertura recalculada al cambiar triggers, y el data browser mostrando la obra de cada path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-modelo-de-datos-objetivo/02.1-CONTEXT.md
@.planning/phases/02.1-modelo-de-datos-objetivo/02.1-RESEARCH.md
@lib/presentation/pages/home/widgets/diagnostics_panel.dart
@lib/data/migration/cover.dart
@lib/data/datasources/local/obra_local_data_source.dart
@lib/core/services/recorder_service.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sección Obra en el panel de debug</name>
  <files>lib/presentation/pages/home/widgets/obra_section.dart, lib/presentation/pages/home/widgets/diagnostics_panel.dart, lib/core/services/recorder_service.dart, test/presentation/pages/home/widgets/obra_section_test.dart</files>
  <read_first>
    - lib/presentation/pages/home/widgets/diagnostics_panel.dart (dónde estaba el `ExpansionTile` "Panel: Región" — primer hijo de la columna — y cómo el "Panel: Path + audio" llama a `RecorderService.startRecording` / `startRecordingWithMic`)
    - lib/core/services/recorder_service.dart (firma de `startRecordingWithMic`, que ya acepta `obraUuid` tras el plan 02.1-09)
    - lib/domain/repositories/obra_repository.dart (`fetchAll`, `createDraft`)
    - .planning/phases/02.1-modelo-de-datos-objetivo/02.1-CONTEXT.md (D-22 textual)
  </read_first>
  <behavior>
    - Con el repositorio falso devolviendo 2 obras, el desplegable muestra 3 opciones: "Sin obra (se crea una)" más las 2
    - Escribir un nombre y tocar "Crear obra" llama a `createDraft` con ese nombre y la deja seleccionada
    - Tocar "Crear obra" con el campo vacío no llama a `createDraft` y muestra un aviso
    - Elegir una obra dispara el callback `onObraSelected` con su uuid
    - Elegir "Sin obra" dispara el callback con null
  </behavior>
  <action>
1. Crear `lib/presentation/pages/home/widgets/obra_section.dart` con un `StatefulWidget` `ObraSection`:
```dart
class ObraSection extends StatefulWidget {
  const ObraSection({required this.repository, required this.onObraSelected, this.selectedUuid, super.key});
  final ObraRepository repository;
  final void Function(String? obraUuid) onObraSelected;
  final String? selectedUuid;
}
```
   Recibe el repositorio por parámetro (no lo saca de `getIt` adentro) para que el widget test lo pueda reemplazar sin montar todo el service locator. Contenido, con el mismo patrón que tenía la sección Región:
   - `ExpansionTile(initiallyExpanded: true, title: const Text('Panel: Obra'), children: [...])`
   - Un `TextField` "Nombre de la obra" (valor inicial 'Mi obra').
   - Un `ElevatedButton.icon(icon: Icons.library_music, label: 'Crear obra')`: con el nombre vacío muestra un `SnackBar` "Ingresa un nombre para la obra" y no hace nada; con nombre llama a `repository.createDraft(name)`, recarga la lista, deja la obra nueva seleccionada y avisa por `SnackBar` "Obra «X» creada".
   - Un `DropdownButton<String?>` con la primera opción `null` → texto **"Sin obra (se crea una al grabar)"** y una opción por obra (`Text(obra.name)`); `onChanged` llama a `widget.onObraSelected`.
   - Si no hay obras: texto gris "No hay obras. Creá una arriba o grabá sin elegir: se crea sola.".

2. En `diagnostics_panel.dart`: agregar `String? _selectedObraUuid;` y poner `ObraSection(repository: getIt<ObraRepository>(), selectedUuid: _selectedObraUuid, onObraSelected: (u) => setState(() => _selectedObraUuid = u))` como PRIMER hijo de la columna, exactamente donde estaba "Panel: Región". Pasar `_selectedObraUuid` a las dos llamadas de grabación (`startRecording` y `startRecordingWithMic`) como `obraUuid:`.

3. En `recorder_service.dart`: `startRecording` acepta `String? obraUuid` y se lo pasa a `createPath`. Confirmar el comportamiento de D-22: con `obraUuid == null`, `createPath` crea la obra draft con el nombre del path/grabación (ya implementado en el repositorio en el plan 02.1-08 y cableado en el grabador en el 02.1-09; acá solo se le pasa el valor elegido cuando lo hay).

4. `test/presentation/pages/home/widgets/obra_section_test.dart`: widget test con un `_FakeObraRepository` en memoria, cubriendo el bloque `<behavior>` con `tester.pumpWidget(MaterialApp(home: Scaffold(body: ObraSection(...))))`.
  </action>
  <acceptance_criteria>
    - `grep -c "Panel: Obra" lib/presentation/pages/home/widgets/obra_section.dart` == 1
    - `grep -c "Sin obra" lib/presentation/pages/home/widgets/obra_section.dart` >= 1
    - `grep -c "ObraSection" lib/presentation/pages/home/widgets/diagnostics_panel.dart` >= 1
    - `grep -c "getIt" lib/presentation/pages/home/widgets/obra_section.dart` == 0 (el repositorio entra por parámetro)
    - `grep -c "obraUuid:" lib/presentation/pages/home/widgets/diagnostics_panel.dart` >= 2
    - `flutter test test/presentation/pages/home/widgets/obra_section_test.dart` pasa
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/presentation</automated>
  </verify>
  <done>El panel tiene su sección Obra donde estaba la de Región, lo grabado va a la obra elegida y sin elección se crea una draft.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cobertura derivada de la obra, al día</name>
  <files>lib/data/datasources/local/obra_local_data_source.dart, lib/data/repositories/obra_repository_impl.dart, lib/domain/repositories/obra_repository.dart, lib/data/datasources/local/geo_trigger_local_data_source.dart, test/data/datasources/local/obra_cover_test.dart</files>
  <read_first>
    - lib/data/migration/cover.dart (`computeCover` y el redondeo a 6 decimales; es la MISMA función que usa la migración y que está portada a JS)
    - lib/data/datasources/local/obra_local_data_source.dart (creado en el plan 02.1-08)
    - lib/data/datasources/local/app_database.dart NO se toca en este plan
    - lib/data/datasources/local/geo_trigger_local_data_source.dart (insert/delete de triggers)
    - lib/data/datasources/local/sync_local_data_source.dart (`withUpdateMetadata` para bumpear `logical_version`)
    - .planning/phases/02.1-modelo-de-datos-objetivo/02.1-CONTEXT.md (D-16, D-29)
  </read_first>
  <behavior>
    - Insertar el primer trigger de un path deja la obra con `cover_lat`/`cover_lon`/`cover_min_*`/`cover_max_*` iguales a ese punto
    - Insertar un segundo trigger expande la caja y mueve el centro a la media aritmética
    - Borrar todos los triggers de la obra deja las seis columnas en NULL
    - Un recálculo que da el MISMO resultado no bumpea `logical_version` ni agrega una fila de outbox
    - Un recálculo que cambia algún valor bumpea `logical_version` y encola UN update de `obras`
    - Los valores coinciden con `computeCover` sobre los mismos puntos (el vector compartido del plan 02.1-01 vale acá)
  </behavior>
  <action>
1. Agregar a `ObraLocalDataSource`:
```dart
/// Recalcula la cobertura de la obra a partir de TODOS los triggers de sus paths.
/// El celular es el único escritor hoy (D-29): calcula acá y la cobertura viaja
/// como columnas normales de la obra; el servidor no la recalcula (dos cálculos = divergencia).
Future<void> refreshCover(String obraUuid) async { ... }
```
   Implementación: `SELECT t.latitude, t.longitude FROM triggers t JOIN paths p ON p.uuid = t.path_uuid WHERE p.obra_uuid = ? AND t.deleted_at IS NULL`, pasar por `computeCover`, comparar con los valores actuales de la obra y **escribir solo si alguno cambió**; en ese caso usar `withUpdateMetadata` (bump de `logical_version`) y encolar un `update` de tabla `obras`.
   Exponerla en `ObraRepository` como `Future<void> refreshCover(String obraUuid)`.

2. Llamarla desde `GeoTriggerLocalDataSource` después de `insertTrigger`, de `deleteByPathUuid` y de `deleteOrphaned`, resolviendo el `obra_uuid` con `SELECT obra_uuid FROM paths WHERE uuid = ?`. Para no crear una dependencia circular entre data sources, pasar un callback `Future<void> Function(String obraUuid)? onObraTouched` por constructor, cableado en `service_locator.dart` a `ObraRepository.refreshCover`. Una sola llamada por operación: al borrar un path, recalcular UNA vez al final, no por trigger.

3. `test/data/datasources/local/obra_cover_test.dart`: con `sqflite_common_ffi` sobre una base v7 limpia, cubrir el bloque `<behavior>`, incluido el caso "mismo resultado → sin bump ni outbox".
  </action>
  <acceptance_criteria>
    - `grep -c "computeCover" lib/data/datasources/local/obra_local_data_source.dart` >= 1
    - `grep -c "refreshCover" lib/domain/repositories/obra_repository.dart` == 1
    - `grep -c "onObraTouched" lib/data/datasources/local/geo_trigger_local_data_source.dart lib/core/di/service_locator.dart` >= 2
    - `grep -c "cover_min_lat" lib/data/datasources/local/obra_local_data_source.dart` >= 1
    - `flutter test test/data/datasources/local/obra_cover_test.dart` pasa
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/data</automated>
  </verify>
  <done>La cobertura de cada obra se mantiene al día con una sola función compartida con la migración, sin escrituras ni versiones de más.</done>
</task>

<task type="auto">
  <name>Task 3: el data browser muestra la obra de cada path</name>
  <files>lib/presentation/pages/data_browser_page.dart</files>
  <read_first>
    - lib/presentation/pages/data_browser_page.dart (tabla de paths, adaptada mecánicamente a uuid en el plan 02.1-09)
    - lib/domain/repositories/obra_repository.dart
  </read_first>
  <action>
Cargar las obras en `_loadData` (`await getIt<ObraRepository>().fetchAll()`), guardarlas en `List<Obra> _obras`, y reemplazar en la tabla de paths la celda que hoy muestra el `obra_uuid` recortado por el NOMBRE de la obra (buscándola por uuid; si no aparece, mostrar el uuid recortado a 8 caracteres). Agregar una sección de conteo "Obras: N" junto a las otras. Nada más: reagrupar paths entre obras es solo para la web y más adelante (D-23), así que no se agrega ninguna acción de edición.
  </action>
  <acceptance_criteria>
    - `grep -c "ObraRepository" lib/presentation/pages/data_browser_page.dart` >= 1
    - `grep -ciE "obra" lib/presentation/pages/data_browser_page.dart` >= 3
    - `grep -ciE "reagrupar|cambiar obra|mover path" lib/presentation/pages/data_browser_page.dart` devuelve 0
    - `flutter analyze` 0 errores; `flutter test` verde
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze --no-fatal-warnings &amp;&amp; flutter test</automated>
  </verify>
  <done>Cada path muestra el nombre de su obra en el data browser.</done>
</task>

</tasks>

<verification>
- `flutter analyze` 0 errores; `flutter test` verde.
- `grep -rn "Panel: Obra" lib/presentation` → aparece una vez.
- `grep -rniE "artistas|obra_artistas|share_token" lib/presentation lib/domain/repositories` → vacío (siguen siendo tablas/columnas sin consumidor en 2.1, a propósito).
</verification>

<success_criteria>
1. El panel de debug tiene sección Obra funcional, con test de widget propio.
2. Grabar con obra elegida deja el path en esa obra; sin elegir, crea una draft con el nombre de la grabación.
3. La cobertura de la obra se recalcula al cambiar sus triggers y solo escribe cuando cambia algo.
4. El data browser muestra el nombre de la obra de cada path.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/02.1-modelo-de-datos-objetivo/02.1-10-SUMMARY.md`.
</output>
