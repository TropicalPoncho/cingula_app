---
phase: 02.1-modelo-de-datos-objetivo
plan: 03
subsystem: database
tags: [sqflite, migration, uuid, v7-schema]
requires: [02.1-01]
provides:
  - "createV7Tables / v7CreateStatements: unica definicion del DDL v7"
  - "migrateToV7(db): copia v6 -> v7 con uuid, verificacion EXCEPT, MigrationException"
affects: [02.1-05, 02.1-08]
key-files:
  created:
    - lib/data/migration/v7_schema.dart
    - lib/data/migration/v7_migration.dart
    - test/data/migration/v7_migration_test.dart
key-decisions:
  - "migrateToV7 devuelve un reporte (Map) en vez de void, para exponer portalsFromDangling"
  - "PRAGMA foreign_key_check se corre por tabla nueva, no global: las tablas legacy pueden traer refs colgantes de origen"
requirements-completed: [MODEL-01, MODEL-02, MODEL-04, MODEL-05]
duration: 25min
completed: 2026-09-20
---

# Phase 2.1 Plan 03: Migracion local v6 -> v7 Summary

Nucleo de la migracion v6 -> v7 aislado y probado contra el dataset sintetico 77/70/459/20: crea las tablas nuevas, copia todo traduciendo ids a uuid (obras y portales con uuid v5 deterministico), y verifica con diffs EXCEPT en ambos sentidos; ante cualquier discrepancia lanza y sqflite revierte a v6.

## Tasks
1. DDL v7 en un solo archivo - 0939d64 (comentario reajustado en b3fd14f)
2. migrateToV7 - b3fd14f (+ 74e52cf ajuste de comentario para el grep de aceptacion)
3. Tests (13 pasan, 1 skip por CINGULA_REAL_DB) - b3fd14f

## Deviations from Plan

**1. [Rule 1 - Bug] foreign_key_check global falla con refs colgantes legacy**
- Found during: Task 3 (casos addDanglingPathRef / addDanglingAudioRef)
- Fix: `PRAGMA foreign_key_check(<tabla>)` solo sobre audio_local, paths, triggers, path_progress.

**2. [Interface] migrateToV7 retorna `Future<Map<String,Object?>>` (reporte) en vez de `Future<void>`**; compatible con quien lo hace `await`.

**3. Caso `addInvalidUuidFormat`** inserta en `regions` (que no se migra); el test de uuid invalido corrompe el uuid de un `audio_assets` para ejercer el camino real.

Diffs adelante se restringen a uuid validos (GLOB hex) y los reversos a uuid que ya existian en legacy, porque las filas con uuid NULL/invalido reciben v4 nuevo.

## Ponytail audit
Sin abstracciones extra: un solo hook de test (`onBeforeVerify`, marcado `ponytail:`), sin ROW_NUMBER (contador en Dart). Nada que recortar.

## Known Stubs
None.

## Self-Check: PASSED
Archivos y commits 0939d64, b3fd14f, 74e52cf verificados; `flutter test` completo verde (76 pass, 1 skip).
