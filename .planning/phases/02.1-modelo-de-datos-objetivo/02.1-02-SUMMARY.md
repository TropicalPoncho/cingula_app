---
phase: 02.1
plan: 02
subsystem: backend
tags: [postgres, neon, sync, schema, node]
requires: ["02.1-01"]
provides:
  - typed Postgres schema v2 (6 entity tables, entity_prev, change_seq)
  - spec.js single source of truth for tables/columns
  - schema_version 2 gate on /api/sync/push
  - numeric change_seq cursor from /api/sync/state
affects: [02.1-05, 02.1-07, 02.1-12, phase-03]
tech-stack:
  added: []
  patterns: [spec-driven parametrized SQL, deferred FKs, PK-enforced single previous version]
key-files:
  created:
    - backend/api/_lib/schema_loader.js
    - backend/api/_lib/spec.js
    - backend/api/_lib/spec.test.js
  modified:
    - backend/schema.sql
    - backend/api/_lib/outbox.js
    - backend/api/_lib/outbox.test.js
    - backend/api/sync/push.js
    - backend/api/sync/push.test.js
    - backend/api/sync/state.js
key-decisions:
  - "entity_prev PK (table_name, record_uuid) enforces current+1 previous with no pruning code"
  - "upsertStatement now returns sql.query(text, params) with identifiers only from TABLE_SPEC"
requirements-completed: [MODEL-01, MODEL-02, MODEL-03]
duration: 25min
completed: 2026-09-20
---

# Phase 02.1 Plan 02: Typed backend schema Summary

Typed Postgres tables with change_seq cursor, one-previous-version trigger, spec-driven upserts and a schema_version 2 gate on push.

## Tasks

| Task | Commit | Notes |
|------|--------|-------|
| 1 schema.sql + schema_loader | d909862 | 20 statements, dollar-quote aware split |
| 2 spec.js + outbox.js + tests | f03bbe0 | spec-vs-DDL drift test |
| 3 push gate + state cursor | f3bddde | DB cases skip without DATABASE_URL |

## Verification

`cd backend && npm test`: 24 pass, 1 skipped (Neon integration, needs DATABASE_URL). No real DB touched; the integration test runs in plan 02.1-07 Task 3. No `synced_entities` references remain under `backend/api/`. No changes under `lib/` or `test/` from this plan.

## Deviations from Plan

**1. [Rule 3 - Blocking] `node --test api/_lib/` fails on Node 22** (directory treated as a module path). Used `node --test` (auto-discovery) via `npm test` instead.

**2. Empty-outbox push** still returns `serverCursor: null` (early return before any DB access), after the schema_version gate. Unchanged from Phase 2 behavior.

## Ponytail audit

No new dependencies, one loader (no parser), no config. Deletes are non-inserting no-ops for unknown uuids (marked `ponytail:` in outbox.js).

## Known Stubs

None.

## Self-Check: PASSED
