---
phase: 02.1
plan: 06
subsystem: domain/presentation
tags: [refactor, region-removal, geofence]
requires: []
provides:
  - "Cero codigo de Region en lib/ y test/ (fuera de app_database.dart)"
  - "Monitor evalua todos los triggers via fetchAll() sin prefiltro"
affects: [02.1 identity change to uuid (touches fewer files)]
tech-stack:
  added: []
  patterns: []
key-files:
  created: [test/geo_trigger_model_test.dart]
  modified:
    - lib/domain/usecases/monitor_user_location_usecase.dart
    - lib/core/services/geofence_background_service.dart
    - lib/presentation/notifiers/playback_notifier.dart
    - lib/presentation/pages/home/widgets/diagnostics_panel.dart
    - lib/presentation/pages/data_browser_page.dart
    - lib/presentation/widgets/trigger_map.dart
    - lib/core/di/service_locator.dart
    - lib/data/datasources/local/app_database.dart
    - lib/data/datasources/local/seed_data.dart
    - lib/domain/entities/geo_trigger.dart
    - lib/data/models/geo_trigger_model.dart
  deleted: [region.dart, region_repository.dart, region_model.dart, region_repository_impl.dart, region_local_data_source.dart, test/region_and_trigger_model_test.dart]
decisions:
  - "SQLite schema untouched: regions table and geo_triggers.region_id remain (marked // legacy-upgrade: no tocar); only getDatabaseStats stopped counting regions"
metrics:
  duration: ~20min
  completed: 2026-09-20
---

# Phase 02.1 Plan 06: Eliminar Region Summary

Region removida por completo del codigo (entidad, repo, modelo, datasource, UI, mapa, geocercas); el monitor considera todos los triggers y el esquema SQLite queda intacto con sus 20 filas de regiones.

## Tasks

| Task | Commit | Result |
|------|--------|--------|
| 1. Region fuera del runtime (monitor, geofence, notifier, seed, tests) | 9c514dd | onStateChanged pasa a `(String samplingMode)`, geocercas solo de triggers |
| 2. Region fuera de UI y service locator | 48b34c3 | Panel Region, dropdown, mapa, data browser, registros getIt |
| 3. Borrar archivos y `regionId` | 9710f97 | 5 archivos borrados, comentarios legacy-upgrade, test renombrado a geo_trigger_model_test.dart |

## Verification

- `grep -rniE "region"` sobre lib/test (excluyendo app_database.dart y migracion) devuelve 0 lineas.
- `flutter test`: 69 pasan. `flutter analyze`: 0 errores propios (1 info preexistente WillPopScope).
- DDL de app_database.dart sin cambios; solo se quito el conteo en `getDatabaseStats` y se agregaron comentarios.

## Deviations from Plan

None - plan executed as written. Extras menores: removi imports que quedaron sin uso (`location_repository.dart` en diagnostics_panel.dart) y `regionId` de los fixtures del test del monitor.

## Ponytail audit

- Dead code residual: `PlaybackNotifier.samplingMode`/`_handleStateChanged` y el callback `onStateChanged` del monitor ya no se invocan nunca (solo los disparaba el cambio de region). Se conservaron porque el plan fija la nueva firma explicitamente; candidato a borrar en una fase posterior (Fase 4 geofencing podria reutilizarlo).

## Notes

- Un error de analyze visto transitoriamente en `test/data/migration/preflight_test.dart` pertenece al plan 02.1-01 (agente paralelo), fuera de alcance.

## Self-Check: PASSED

Commits 9c514dd, 48b34c3, 9710f97 existen; los 5 archivos de Region no existen.
