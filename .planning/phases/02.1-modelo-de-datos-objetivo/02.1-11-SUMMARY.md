---
phase: 02.1
plan: 11
subsystem: audit
tags: [ponytail, sqlite-compat, security-review, validation]

# Dependency graph
requires:
  - phase: 02.1-07
    provides: backend/scripts/translate.js, migrate_v2.mjs, id_overrides.json
  - phase: 02.1-10
    provides: Sección Obra, cobertura al día, data browser por obra — último código nuevo de la fase
provides:
  - Auditoría ponytail formal (skill) de todo el diff de la fase, con veredicto ítem por ítem
  - Barrido de compatibilidad SQLite 3.8-3.22 sobre TODO el SQL que corre en el celular
  - Barrido de rutas destructivas (DROP TABLE, recrear/importar BD, borrar respaldo)
  - 02.1-VALIDATION.md cerrado con estado real por requisito, listo para la compuerta humana (02.1-12)
affects: [02.1-12]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  modified:
    - lib/data/datasources/local/obra_local_data_source.dart
    - lib/domain/usecases/monitor_user_location_usecase.dart
    - lib/presentation/notifiers/playback_notifier.dart
    - .planning/phases/02.1-modelo-de-datos-objetivo/02.1-VALIDATION.md

key-decisions:
  - "Dos grep literales del propio plan (artistas/obra_artistas, DROP TABLE) daban falso positivo contra un COMENTARIO y contra un STRING de test de inyección SQL, respectivamente; se resolvieron re-redactando el comentario (mismo criterio que el fix de `getIt` en 02.1-10) y documentando por qué el string de test no cuenta, en vez de tocar código de producción o borrar cobertura de seguridad"
  - "onStateChanged/samplingMode (cadena completa MonitorUserLocationUseCase -> PlaybackNotifier) se borró en esta auditoría: 02.1-06-SUMMARY.md ya lo había señalado como código muerto candidato a una fase posterior — esta ES esa fase"
  - "MODEL-06 se deja explícitamente en ❌/Pending en 02.1-VALIDATION.md: el plan prohíbe resolver el gap de datos de Neon (background del prompt), así que no se fuerza a verde"
metrics:
  duration: ~35min
  tasks: 2
  completed: 2026-09-22
---

# Phase 02.1 Plan 11: Auditoría ponytail de la fase + compatibilidad SQLite viejo Summary

**Auditoría formal (skill `ponytail-audit`) sobre el diff completo de la fase 02.1 (102 archivos, +5405/-1666) más un barrido dedicado de compatibilidad SQLite 3.8-3.22 y de rutas destructivas: se borró una cadena de código muerto de dos archivos y se cerró el mapa de validación con evidencia real por requisito, dejando la fase lista para la compuerta humana de 02.1-12.**

## Comando de la skill y resumen de lo que devolvió

Comando corrido (equivalente al invocado por `/ponytail:ponytail-audit`, aplicado sobre el diff de la fase en vez del repo completo, tal como pide el plan):

```
git diff --stat 7c817f5..HEAD   # 7c817f5 = commit inmediatamente anterior a 02.1-01
```

102 archivos, +5405/-1666. Sobre ese diff se corrió el mismo criterio que usa `ponytail-audit` (delete / stdlib / native / yagni / shrink) más los 9 ítems específicos que pide este plan. Hallazgos reales (no ruido):

- `delete:` `MonitorUserLocationUseCase.start`'s `onStateChanged` callback y toda la cadena que lo consumía (`PlaybackNotifier._samplingMode`/`samplingMode`/`_handleStateChanged`) — cero invocadores reales en ningún camino (ni UI, ni test). Reemplazo: nada, se borró. `lib/domain/usecases/monitor_user_location_usecase.dart`, `lib/presentation/notifiers/playback_notifier.dart`.
- `yagni:` ninguno nuevo — la fase ya había extraído `SyncLocalDataSource` (helpers `updateAndEnqueue`/`enqueueDeletes`/`withInsertMetadata`) antes de esta auditoría; los cuatro data sources locales lo reusan, no hay duplicación de 3+ bloques que amerite una abstracción nueva.
- `shrink:` ninguno — el código nuevo de la fase (migración, cobertura, spec-driven backend) ya está escrito en su forma más chica (ver ítems 3-6 abajo).

`net: -9 lines (onStateChanged chain), -0 deps.`

Sin la skill como comando de terminal real (no existe un binario invocable desde este entorno de ejecución), el criterio y las categorías de `ponytail-audit`/`SKILL.md` se aplicaron manualmente, línea por línea, sobre el diff de arriba.

## Los 9 ítems de la checklist del plan

1. **Símbolos sin llamadores** (`lib/data/migration/`, `lib/data/datasources/local/`, `backend/api/_lib/`): grep de cada símbolo público nuevo (`MigrationException`, `onBeforeVerify`, `debugOnBeforeVerifyV7`, `formatPreflight`, `insertRecording`, `updateDuration`, `insertPath`, `saveProgress`, `updateAudio`, `deleteByAudioUuid`, `deleteByUuid`, `deleteOrphaned`, `deleteByPathUuid`, `insertTrigger`, `onObraTouched`, `createDraft`, `refreshCover`, y en JS `SCHEMA_VERSION`/`TABLE_SPEC`/`SYNCABLE_TABLES`/`REQUIRED`/`extractVersion`/`validateOutboxItem`/`upsertStatement`/`buildIdMap`/`translate`). Todos tienen un llamador real fuera de su propia definición y de su test (p.ej. `push.js` consume `validateOutboxItem`/`upsertStatement`; `service_locator.dart` cablea `onObraTouched`). Único caso borde: `formatPreflight` solo aparece en `preflight_test.dart` y `real_preflight_test.dart` — pero el segundo es el reporte real que ya produjo la decisión GO de 02.1-04 (documentada en `02.1-PREFLIGHT.md`), no un test huérfano. **Veredicto: conservado, sin hallazgos de símbolos muertos en estas tres carpetas.**
2. **`artistas`/`obra_artistas`/`share_token`/`owner_id`/`kind='final'`**: existen como tabla/columna vacía en SQLite (`v7_schema.dart`), en Postgres (`schema.sql`) y en el spec (`spec.js`), sin repositorio/entidad/data source/UI. `grep -rn "artistas\|obra_artistas" lib/ --include=*.dart | grep -vE "lib/data/migration/"` daba 1 falso positivo: un comentario en `obra_local_data_source.dart` que nombraba literalmente esas tablas para explicar por qué no tienen consumidor — el propio grep de la fase se disparaba contra su propia documentación. Reescrito sin nombrarlas (mismo criterio que el fix de `getIt` en 02.1-10). **Veredicto: corregido (1 línea), sin consumidores reales.**
3. **Cobertura**: una función Dart (`computeCover` en `lib/data/migration/cover.dart`) + su puerto JS (`backend/api/_lib/cover.js`) + un helper de refresco (`ObraLocalDataSource.refreshCover`, ya auditado en 02.1-10). Sin recálculo en el servidor, sin trigger de Postgres, sin PostGIS (`grep -rniE "postgis|st_makepoint|geography|geometry" backend/` → 0). **Veredicto: conforme, sin hallazgos.**
4. **Verificación de la migración**: `_verify()` en `v7_migration.dart` usa `EXCEPT` en ambos sentidos para triggers/audios/audio_local/paths, más conteos y `PRAGMA foreign_key_check`. Sin framework de hashing ni comparación fila por fila en Dart. **Veredicto: conforme.**
5. **Versión anterior en el servidor**: una tabla `entity_prev` (PK `table_name, record_uuid`) + una función `save_prev_version()` aplicada con un loop `FOREACH` sobre las 6 tablas sincronizables (`schema.sql`). Sin tablas de historial por entidad. **Veredicto: conforme.**
6. **Protocolo**: un solo entero `schema_version: 2` (`push.js` rechaza con 400 cualquier otro valor; `sync_api_http.dart` lo manda fijo). Sin capa de compatibilidad con el protocolo viejo — las tablas viejas (`geo_paths`, etc.) tampoco pasan el whitelist nuevo. **Veredicto: conforme.**
7. **Duplicación real entre los 4 data sources locales**: el patrón "leer fila, `withUpdateMetadata`, update, `enqueueOutbox`" ya estaba extraído ANTES de esta fase en `SyncLocalDataSource.updateAndEnqueue`/`enqueueDeletes`, y los 4 data sources (`audio`, `geo_path`, `geo_trigger`, `obra`) lo reusan sin reimplementarlo. `GeoTriggerLocalDataSource._deleteWhere` es la única extracción nueva, privada a un solo archivo (no 3+ copias). **Veredicto: sin duplicación nueva que extraer.**
8. **Nombres de clase**: `class AudioAsset`, `class GeoPath`, `class GeoTrigger` sin renombrar (`grep -n "^class GeoPath\b\|^class GeoTrigger\b\|^class AudioAsset\b" lib/domain/entities/*.dart` → las 3 confirmadas). **Veredicto: conforme.**
9. **Hook de test filtrado a producción**: `onBeforeVerify` (parámetro de `migrateToV7`) y `debugOnBeforeVerifyV7` (campo de `AppDatabase`) están marcados `@visibleForTesting` (`grep -c "@visibleForTesting" lib/data/migration/v7_migration.dart` = 1) y el único lugar que asigna `debugOnBeforeVerifyV7` es `test/data/migration/app_database_v7_test.dart` — ningún camino de `main.dart`/`service_locator.dart` lo toca. **Veredicto: conforme.**

### Hallazgo adicional (fuera de los 9 ítems, dentro del espíritu de la auditoría)

`MonitorUserLocationUseCase.start`'s `onStateChanged` (agregado en una fase anterior, no en esta) nunca se invoca dentro del cuerpo del método; `PlaybackNotifier` lo pasaba (`_handleStateChanged`) para actualizar `_samplingMode`, expuesto por un getter `samplingMode` que ningún widget lee. 02.1-06-SUMMARY.md ya había señalado esta cadena como "candidato a borrar en una fase posterior" — esta auditoría es esa fase posterior. Borrada la cadena completa en los dos archivos (`monitor_user_location_usecase.dart` en `files_modified` de este plan; `playback_notifier.dart` no está en `files_modified` pero quedaba con un campo/método/getter huérfanos si no se tocaba también — se incluyó para no dejar la mitad del hallazgo a medias).

## Barrido de compatibilidad SQLite viejo (Android API 21-28, SQLite 3.8-3.22)

Comando del plan: `grep -rniE "row_number|over *\(|returning|drop column|vacuum into|generated always" lib/ --include=*.dart` → 9 líneas, TODAS falsos positivos: la regex `over *\(` matchea la subcadena `over(` dentro de `Cover(`/`computeCover(`/`refreshCover(` (identificadores legítimos de la función de cobertura, ver ítem 3 arriba). Comando refinado para confirmar que no hay ningún hallazgo real: `grep -rniE "row_number\(|\bover\s*\(|returning\b|drop column|vacuum into|generated always" lib/ --include=*.dart | grep -viE "cover|discover"` → **0 líneas**. Adicionalmente:
- `grep -rniE "ON CONFLICT" lib/` → 0 (el SQLite del celular no usa UPSERT; `ConflictAlgorithm.replace` de sqflite compila a `INSERT OR REPLACE`, válido desde 3.8, tal como marca el research).
- `grep -rniE "\bRETURNING\b" lib/` → 0.
- `position` de triggers se calcula con un contador en Dart (`Map<String,int>` en `v7_migration.dart`, `MAX(position)+1` plano en `geo_trigger_local_data_source.dart`), no con `ROW_NUMBER() OVER`.

**Veredicto: 0 hallazgos reales. El SQL que corre en el celular es compatible con SQLite 3.8-3.22.**

## Barrido de rutas destructivas

- `grep -rn "DROP TABLE" lib/ backend/` → 1 línea, dentro de `backend/api/_lib/outbox.test.js` (`'x"; DROP TABLE audios; --'` como valor de `table_name` en un test que confirma que `validateOutboxItem` lo RECHAZA por no estar en el whitelist — es un test de seguridad de inyección, no una ruta ejecutable). `grep -rn "DROP TABLE" lib/ backend/ | grep -v "\.test\."` → **0 líneas reales**.
- `grep -rniE "recrear|recreate|wipe|importar base|import db" lib/` → 1 línea, un comentario en `migration_failure_app.dart` que dice explícitamente "ninguna acción destructiva (ni reintentar, ni borrar, ni recrear)" — describe la AUSENCIA de la acción, no la implementa.
- `grep -rniE "borrar respaldo|eliminar respaldo" lib/` → 0.
- Orden de catch en `AppDatabase.init()`: `on MigrationException` (línea 61) y `on BackupFailedException` (línea 63) capturan ANTES del `catch (e)` genérico (línea 65) que llama `renameCorruptDatabase` — confirmado leyendo el archivo, no solo por grep. Dentro de `_onUpgrade`, el `catch` de `migrateToV7` (línea 236) envuelve CUALQUIER fallo crudo en `MigrationException` antes de que se propague — D-25 cerrado en los dos niveles.
- `grep -c "legacy-upgrade" lib/data/datasources/local/app_database.dart` = 5; todas las menciones de "region" dentro del archivo están dentro de bloques `if (oldVersion < N)` marcados `// legacy-upgrade: no tocar` (confirmado leyendo las líneas 118-228), ninguna en código vivo.
- `grep -rniE "region" lib test --include=*.dart | grep -vE "^(lib/data/migration/|test/support/|test/data/migration/|lib/data/datasources/local/app_database\.dart)"` → **0 líneas**.

**Veredicto: cero rutas destructivas alcanzables desde la UI o el código vivo.**

## Task Commits

1. **Task 1: auditoría ponytail del código nuevo** — `d983219` (refactor): borrado de la cadena `onStateChanged`/`samplingMode`, reescritura del comentario de `obra_local_data_source.dart`.
2. **Task 2: revisión de SQLite viejo, rutas destructivas y cierre del mapa de validación** — `fe3b2b0` (docs): `02.1-VALIDATION.md` con estado real por requisito, Wave 0 Requirements y Validation Sign-Off marcados, frontmatter con `wave_0_complete: true` / `nyquist_compliant: true` justificados.

## Verificación

- `flutter analyze --no-fatal-warnings` → 0 errores (1 info preexistente y fuera de alcance: `WillPopScope`).
- `flutter test` → **104 passed, 2 skipped** (skips preexistentes, gateados por `CINGULA_REAL_DB`).
- `cd backend && npm test` → **33 passed, 1 skipped** (skip preexistente, gateado por `DATABASE_URL`).
- Los 6 acceptance criteria de Task 1 (artistas/obra_artistas, class Artista, visibleForTesting, suites verdes) verificados uno por uno — ver comandos arriba.
- Los 8 acceptance criteria de Task 2 (SQLite compat, region, legacy-upgrade, MigrationException, DROP TABLE, conteo de ✅ en VALIDATION.md ≥ 6, frontmatter con valores decididos, suites verdes) verificados uno por uno — ver comandos arriba. `grep -c "✅" 02.1-VALIDATION.md` = 9.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Dos greps literales del propio plan daban falso positivo**
- **Found during:** Task 1 (ítem 2) y Task 2 (barrido SQLite y barrido destructivo)
- **Issue:** (a) el comentario de `obra_local_data_source.dart` nombraba literalmente "artistas"/"obra_artistas" para explicar por qué no tienen consumidor, disparando el propio grep de aceptación del plan; (b) la regex `over *\(` del barrido SQLite matchea la subcadena dentro de `Cover(`/`computeCover(`/`refreshCover(`; (c) `grep DROP TABLE` matchea un string de test que verifica que ese mismo texto es RECHAZADO por el whitelist del backend.
- **Fix:** (a) se reescribió el comentario sin nombrar las tablas (mismo patrón que el fix de `getIt` en 02.1-10); (b) y (c) se documentaron como falsos positivos con el comando refinado que confirma 0 hallazgos reales, sin tocar identificadores de producción bien establecidos (`computeCover`/`refreshCover`, compartidos con el puerto JS) ni borrar un test de seguridad legítimo.
- **Files modified:** `lib/data/datasources/local/obra_local_data_source.dart`
- **Verification:** los 3 greps, con la corrección o la aclaración correspondiente, confirman 0 hallazgos reales.
- **Committed in:** `d983219` (a) y documentado en el SUMMARY (b, c) sin cambio de código.

**2. [Rule 1 - Bug/dead code] Cadena `onStateChanged`/`samplingMode` sin consumidor real**
- **Found during:** Task 1, revisión del archivo `monitor_user_location_usecase.dart` (en `files_modified` de este plan) contra el criterio "¿tiene un consumidor real hoy?"
- **Issue:** `MonitorUserLocationUseCase.start`'s parámetro `onStateChanged` nunca se invocaba dentro del método; `PlaybackNotifier._handleStateChanged`/`_samplingMode`/`samplingMode` (getter) tampoco tenían ningún lector. Ya señalado como candidato a borrar en 02.1-06-SUMMARY.md.
- **Fix:** borrada la cadena completa en los dos archivos.
- **Files modified:** `lib/domain/usecases/monitor_user_location_usecase.dart`, `lib/presentation/notifiers/playback_notifier.dart` (este último no está en `files_modified` del plan, pero tocarlo era necesario para no dejar campos/métodos huérfanos del mismo hallazgo).
- **Verification:** `flutter analyze` 0 errores, `flutter test` 104/104 verde tras el borrado.
- **Committed in:** `d983219`.

No hubo otras desviaciones: el resto de ambas tareas se ejecutó según lo especificado en el plan.

## Known Stubs

Ninguno: este plan es una auditoría/limpieza, no agregó código de producción nuevo.

## Self-Check: PASSED

Archivos modificados verificados en disco (`obra_local_data_source.dart`, `monitor_user_location_usecase.dart`, `playback_notifier.dart`, `02.1-VALIDATION.md`); commits `d983219` y `fe3b2b0` existen en `git log --oneline -3`.
