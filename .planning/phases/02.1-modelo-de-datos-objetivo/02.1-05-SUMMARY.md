---
phase: 02.1
plan: 05
subsystem: migration
tags: [sqlite, migration, outbox, backup, sync]
requires: [02.1-02, 02.1-03, 02.1-04]
provides:
  - migrateToV7 completa (outbox reenvio, deletes traducidos, cursor NULL, tablas legacy_*)
  - backupBeforeMigration + BackupFailedException
  - push con schema_version 2
affects: [02.1-08]
key-files:
  created: [lib/data/migration/db_backup.dart, test/data/migration/db_backup_test.dart]
  modified: [lib/data/migration/v7_migration.dart, test/data/migration/v7_migration_test.dart, lib/data/sync/sync_api_http.dart, test/data/sync/sync_api_http_test.dart]
decisions:
  - "Payload del reenvio = SELECT * de la tabla nueva: las tablas sincronizables de v7 tienen exactamente las columnas de TABLE_SPEC"
metrics:
  duration: 15min
  tasks: 3
  files: 6
  completed: 2026-09-21
---

# Phase 02.1 Plan 05: Cierre de la migracion local Summary

Reenvio completo idempotente del outbox en orden padre->hijo con deletes pendientes traducidos, cursor reiniciado, tablas viejas archivadas como `legacy_*`, y respaldo verificado previo a abrir en v7; el cliente manda `schema_version: 2`.

## Tasks

| Task | Commit | Notes |
|------|--------|-------|
| 1 outbox/cursor/legacy | 6edb14e | 706 inserts + 2 deletes traducidos en el fixture; regions descartadas; 0 DROP TABLE |
| 2 db_backup | caf31ac | copia + sidecars, verificacion por conteos, sin funcion de borrado |
| 3 schema_version 2 | d02732f | asertado en test |

## Deviations from Plan

None - plan executed as written. Con la base real (outbox vacio, sin deletes pendientes) la traduccion no tiene efecto; los conteos reales (78/72/462/20) los usan las verificaciones dinamicas, no constantes.

## Verification

`flutter test` completo verde (82), `flutter analyze` 0 errores (1 info preexistente: WillPopScope), `grep DROP TABLE lib/` vacio.

## Ponytail audit

Sin abstracciones nuevas: el payload es `SELECT *` (sin mapeo de columnas duplicado del spec); el backup es una sola funcion. Nada que recortar.

## Known Stubs

None.

## Self-Check: PASSED
