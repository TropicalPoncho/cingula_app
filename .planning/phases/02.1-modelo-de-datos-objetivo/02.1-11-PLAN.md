---
phase: 02.1
plan: 11
type: execute
wave: 7
depends_on: ["02.1-07", "02.1-10"]
files_modified:
  - lib/data/migration/v7_migration.dart
  - lib/data/migration/preflight.dart
  - lib/data/datasources/local/audio_local_data_source.dart
  - lib/data/datasources/local/geo_path_local_data_source.dart
  - lib/data/datasources/local/geo_trigger_local_data_source.dart
  - lib/data/datasources/local/obra_local_data_source.dart
  - lib/domain/usecases/monitor_user_location_usecase.dart
  - backend/api/_lib/spec.js
  - backend/api/_lib/outbox.js
  - backend/scripts/translate.js
  - .planning/phases/02.1-modelo-de-datos-objetivo/02.1-VALIDATION.md
autonomous: true
requirements: [MODEL-02, MODEL-03, MODEL-04, MODEL-07]
must_haves:
  truths:
    - "No queda código nuevo sin consumidor ni abstracción sin segunda implementación"
    - "No hay ninguna sentencia SQL que el SQLite de Android viejo (3.8-3.22) no entienda en código que corre en el celular"
    - "No existe ninguna ruta destructiva alcanzable: ni DROP TABLE, ni borrado de respaldos, ni recreación de la base"
    - "Las suites completas de Flutter y de backend están en verde"
  artifacts:
    - path: ".planning/phases/02.1-modelo-de-datos-objetivo/02.1-VALIDATION.md"
      provides: "mapa de verificación con el estado real de cada requisito"
  key_links:
    - from: ".planning/phases/02.1-modelo-de-datos-objetivo/02.1-VALIDATION.md"
      to: "los tests de la fase"
      via: "estado por requisito actualizado tras la auditoría"
      pattern: "MODEL-0"
---

<objective>
Auditoría ponytail de toda la fase (regla del proyecto: cada fase de ejecución la incluye en su checklist) más la revisión de compatibilidad con el SQLite viejo de Android, antes de la compuerta humana.

Purpose: esta fase agregó mucho código nuevo de una sola pasada. La auditoría es el momento de borrar lo que no tiene consumidor, y la revisión de SQLite es la única red contra un fallo que los tests NO pueden detectar (el ffi de escritorio trae SQLite 3.50; el celular del usuario puede traer 3.22).
Output: código más chico, cero rutas destructivas, suites verdes y el mapa de validación actualizado.

CLAUDE.md pide la auditoría "con la skill `ponytail`", no a ojo: la Task 1 la invoca explícitamente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-modelo-de-datos-objetivo/02.1-CONTEXT.md
@.planning/phases/02.1-modelo-de-datos-objetivo/02.1-RESEARCH.md
@.planning/phases/02.1-modelo-de-datos-objetivo/02.1-VALIDATION.md
@.planning/phases/02-backend-real-push-sync/02-05-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: auditoría ponytail del código nuevo</name>
  <files>lib/data/migration/v7_migration.dart, lib/data/migration/preflight.dart, lib/data/datasources/local/audio_local_data_source.dart, lib/data/datasources/local/geo_path_local_data_source.dart, lib/data/datasources/local/geo_trigger_local_data_source.dart, lib/data/datasources/local/obra_local_data_source.dart, lib/domain/usecases/monitor_user_location_usecase.dart, backend/api/_lib/spec.js, backend/api/_lib/outbox.js, backend/scripts/translate.js</files>
  <read_first>
    - .planning/phases/02.1-modelo-de-datos-objetivo/02.1-RESEARCH.md (sección "Ponytail audit flags": los sospechosos que el research ya identificó)
    - .planning/phases/02-backend-real-push-sync/02-05-SUMMARY.md (cómo se hizo la auditoría de la Fase 2: qué se borró y qué se conservó con un comentario `ponytail:` explicando por qué)
    - todos los SUMMARY de esta fase (02.1-01 a 02.1-10)
  </read_first>
  <action>
**Paso 0 — correr la skill.** CLAUDE.md (Constraints) exige que cada fase incluya "una auditoría con la skill `ponytail`". Invocarla sobre el diff de la fase, no sobre el repo entero:
```
git diff --stat <commit anterior a 02.1-01>..HEAD
```
y pasarle ese conjunto de archivos a `/ponytail:ponytail-audit`. Anotar en el SUMMARY el comando exacto que se corrió y un resumen de lo que devolvió. Los 9 ítems de abajo son la checklist específica de ESTA fase y se revisan además de lo que reporte la skill; donde los dos coincidan, gana el criterio más chico (borrar).

Revisar el código NUEVO de la fase con el criterio de "¿esto tiene un consumidor real hoy?". Lista de sospechosos concretos a verificar uno por uno, con la acción esperada:

1. **Símbolos sin llamadores.** Correr, por cada símbolo público agregado en `lib/data/migration/`, `lib/data/datasources/local/` y `backend/api/_lib/`, un `grep -rn "<símbolo>" lib test backend --include=*.dart --include=*.js`. Si el único resultado es su propia definición (o solo su test), BORRARLO — salvo que sea la única forma determinística de probar algo, en cuyo caso conservarlo con un comentario `ponytail:` de una línea que diga por qué (ese es exactamente el criterio que se usó con `SyncTrigger.flush()` en la Fase 2).
2. **`artistas` / `obra_artistas` / `share_token` / `owner_id` / `kind='final'`.** Tienen que existir como tabla/columna vacía en SQLite, en Postgres y en el spec, y NO tener repositorio, entidad, data source ni UI en 2.1. Si aparecieron consumidores, borrarlos. Si NO están en el modelo, agregarlos (MODEL-02 los exige).
3. **Cobertura**: tiene que ser UNA función Dart + su porte a JS + un helper de refresco. Si hay recálculo del lado servidor, trigger de Postgres o cualquier cosa parecida a PostGIS, sacarlo (D-29).
4. **Verificación de la migración**: tiene que ser `EXCEPT` en SQL. Si apareció algún framework de hashing o comparación fila por fila en Dart, reemplazarlo por el `EXCEPT`.
5. **Versión anterior en el servidor**: una tabla `entity_prev` + un trigger. Si hay tablas de historial por entidad o código de poda, eliminarlas.
6. **Protocolo**: un solo entero (`schema_version`). Si apareció cualquier capa de compatibilidad con el protocolo viejo, sacarla (D-20: no hay protocolo dual).
7. **Duplicación real**: buscar bloques repetidos entre los cuatro data sources locales (el patrón "leer uuid + logical_version, `withUpdateMetadata`, update, `enqueueOutbox`" se repetía ya antes de esta fase). Si el mismo bloque aparece 3+ veces idéntico, extraer UN helper en `SyncLocalDataSource`; si aparece 2 veces, dejarlo (dos no son un patrón).
8. **Nombres de clase**: confirmar que no se renombró `GeoPath`/`GeoTrigger`/`AudioAsset` (un renombre masivo habría inflado el diff sin beneficio).
9. **Parámetros de test filtrados a producción**: el hook de fallo forzado de la verificación (`failVerificationForTest` / `onBeforeVerify`) tiene que estar marcado `@visibleForTesting` y no ser alcanzable desde ningún camino de la app.

Documentar en el SUMMARY, con el mismo formato que la Fase 2: qué se borró, qué se conservó a propósito y por qué.
  </action>
  <acceptance_criteria>
    - El SUMMARY del plan nombra el comando de la skill `ponytail` que se corrió y resume su salida
    - El SUMMARY del plan lista, punto por punto, los 9 ítems con su veredicto (borrado / conservado + motivo / sin hallazgos)
    - `grep -rn "artistas\|obra_artistas" lib/ --include=*.dart | grep -vE "lib/data/migration/"` devuelve 0 (sin consumidores fuera del esquema/migración)
    - `grep -rnE "class .*Artista|ArtistaRepository|ArtistaModel" lib/` devuelve 0
    - `grep -rn "@visibleForTesting" lib/data/migration/v7_migration.dart` >= 1 si el hook de test existe
    - `flutter analyze` 0 errores, `flutter test` verde, `cd backend && npm test` verde
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze --no-fatal-warnings &amp;&amp; flutter test &amp;&amp; cd backend &amp;&amp; npm test</automated>
  </verify>
  <done>El código nuevo quedó sin piezas sin consumidor, con las simplificaciones deliberadas comentadas, y las suites siguen verdes.</done>
</task>

<task type="auto">
  <name>Task 2: revisión de SQLite viejo, de rutas destructivas y cierre del mapa de validación</name>
  <files>.planning/phases/02.1-modelo-de-datos-objetivo/02.1-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02.1-modelo-de-datos-objetivo/02.1-RESEARCH.md (Pitfall 7: Android API 21-28 trae SQLite 3.8-3.22)
    - .planning/phases/02.1-modelo-de-datos-objetivo/02.1-VALIDATION.md (mapa por requisito a completar)
    - lib/data/migration/v7_schema.dart, lib/data/migration/v7_migration.dart y los cuatro data sources locales (el SQL que de verdad corre en el celular)
  </read_first>
  <action>
1. **Revisión manual de compatibilidad SQLite** (los tests con ffi NO la detectan: corren con SQLite 3.50). Recorrer TODO el SQL de `lib/` (schema v7, migración, data sources) buscando construcciones posteriores a 3.22 y reemplazarlas si aparecen:
   - funciones de ventana (`ROW_NUMBER`, `OVER (`) — no existen antes de 3.25
   - `INSERT ... ON CONFLICT` (UPSERT) — 3.24, inseguro en API 21-27; usar `INSERT OR REPLACE` o un `SELECT` + `INSERT`/`UPDATE`
   - `RETURNING` (3.35), `ALTER TABLE ... DROP COLUMN` (3.35), `VACUUM INTO` (3.27), columnas generadas (3.31)
   Comando de barrido: `grep -rniE "row_number|over *\(|returning|drop column|vacuum into|generated always" lib/ --include=*.dart`
   NOTA: `INSERT OR REPLACE` sí es válido en 3.8 y es la forma correcta para `path_progress`.
   El barrido NO distingue código de comentario: si aparece un hallazgo dentro de un comentario (el caso previsto es la nota de compatibilidad de `v7_schema.dart`), la corrección es **reescribir el comentario sin nombrar la construcción**, no agregarle una excepción al grep. La regla está escrita así en el plan 02.1-03 Task 1.
   Dejar el resultado del barrido escrito en el SUMMARY (aunque sea "0 hallazgos"): es la única evidencia de que se revisó.

2. **Barrido de rutas destructivas** (memoria de proyecto `feedback_no_destructive_automation` + DATA-01):
   - `grep -rn "DROP TABLE" lib/ backend/` → tiene que ser 0
   - `grep -rniE "recrear|recreate|wipe|importar base|import db" lib/` → 0
   - ninguna acción de borrar respaldo en la UI (`grep -rniE "borrar respaldo|eliminar respaldo" lib/` → 0)
   - la única ruta que renombra la base sigue siendo la de BD corrupta de la Fase 1, y está DESPUÉS del `on MigrationException` en `app_database.dart`
   - **y, sobre todo, que el envoltorio existe**: `grep -c "throw MigrationException(" lib/data/datasources/local/app_database.dart` >= 1 dentro del `catch` de `migrateToV7` (plan 02.1-08 Task 3). Sin él, el orden de los `catch` no protege nada, porque la migración casi nunca falla con esa clase: falla con una `DatabaseException` cruda (D-25)
   - barrido de "region" con su exclusión documentada: `grep -rniE "region" lib test --include=*.dart | grep -vE "^(lib/data/migration/|test/support/|test/data/migration/|lib/data/datasources/local/app_database\.dart)"` → vacío. `app_database.dart` está excluido porque conserva el camino de upgrade histórico (bloques marcados `// legacy-upgrade`); confirmar que esas apariciones están TODAS dentro de bloques `oldVersion < N` o del comentario, y ninguna en código vivo

3. **Actualizar `02.1-VALIDATION.md`**: completar la columna "Existe" de cada requisito con ✅/❌ según el estado real tras la fase, con el comando concreto que lo prueba. Marcar el checklist de "Wave 0 Requirements" y el de "Validation Sign-Off". Poner `wave_0_complete: true` y `nyquist_compliant: true` en el frontmatter SOLO si de verdad cada tarea de la fase tiene una verificación automatizada y no hay tres tareas seguidas sin una; si no, dejarlo en false y anotar por qué.
  </action>
  <acceptance_criteria>
    - `grep -rniE "row_number|over *\(|returning|drop column|vacuum into|generated always" lib/ --include=*.dart` devuelve 0
    - `grep -rniE "region" lib test --include=*.dart | grep -vE "^(lib/data/migration/|test/support/|test/data/migration/|lib/data/datasources/local/app_database\.dart)"` devuelve 0 líneas
    - `grep -c "legacy-upgrade" lib/data/datasources/local/app_database.dart` >= 1 (la exclusión está justificada EN el archivo, no solo en el plan)
    - `grep -c "throw MigrationException(" lib/data/datasources/local/app_database.dart` >= 1
    - `grep -rn "DROP TABLE" lib/ backend/` devuelve 0
    - `grep -c "✅" .planning/phases/02.1-modelo-de-datos-objetivo/02.1-VALIDATION.md` >= 6
    - El frontmatter de `02.1-VALIDATION.md` tiene `wave_0_complete` y `nyquist_compliant` con un valor decidido (no el `false` por defecto sin justificación)
    - `flutter test` y `cd backend && npm test` verdes
  </acceptance_criteria>
  <verify>
    <automated>flutter test &amp;&amp; cd backend &amp;&amp; npm test</automated>
  </verify>
  <done>El SQL que corre en el celular es compatible con SQLite 3.22, no hay ninguna ruta destructiva alcanzable y el mapa de validación refleja el estado real.</done>
</task>

</tasks>

<verification>
- `flutter analyze` 0 errores; `flutter test` verde; `cd backend && npm test` verde.
- `grep -rn "DROP TABLE" lib/ backend/` → vacío.
- `02.1-VALIDATION.md` con estado real por requisito.
</verification>

<success_criteria>
1. La skill `ponytail` se corrió sobre el diff de la fase y los 9 ítems de la checklist tienen veredicto escrito en el SUMMARY.
2. El barrido de SQLite viejo está hecho y documentado.
3. Cero rutas destructivas alcanzables.
4. Ambas suites en verde, listas para la compuerta humana.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/02.1-modelo-de-datos-objetivo/02.1-11-SUMMARY.md`.
</output>
