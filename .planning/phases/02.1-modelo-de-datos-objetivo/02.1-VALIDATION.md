---
phase: 02.1
slug: modelo-de-datos-objetivo
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-09-19
---

# Phase 2.1 — Validation Strategy

> Contrato de validación de la fase: qué se muestrea durante la ejecución y qué requiere un humano. Fuente: `02.1-RESEARCH.md` → Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `flutter_test` (64/64 hoy) + `sqflite_common_ffi` (nuevo, dev) para migraciones; backend `node --test` |
| **Config file** | ninguno — Wave 0 agrega `test/support/v6_fixture.dart` |
| **Quick run command** | `flutter test test/data/migration` ; `cd backend && npm test` |
| **Full suite command** | `flutter test` ; `cd backend && DATABASE_URL=<rama Neon dev> npm test` |
| **Estimated runtime** | ~60–120 segundos (Flutter) + ~10 segundos (backend sin DB) |

---

## Sampling Rate

- **Después de cada commit de tarea:** el quick run del track tocado (Flutter o backend).
- **Después de cada wave:** `flutter test` + `cd backend && npm test`.
- **Antes de `/gsd:verify-work`:** suites completas en verde + reporte de pre-flight revisado + ensayo contra una rama de Neon en verde, todo ANTES de cualquier paso humano.
- **Max feedback latency:** 120 segundos.

---

## Per-Requirement Verification Map

Los Task IDs concretos los asigna el planificador al armar los PLAN.md; este mapa fija qué prueba cubre cada requisito.

| Requirement | Comportamiento verificado | Tipo | Comando automatizado | Existe |
|-------------|---------------------------|------|----------------------|--------|
| MODEL-01 | Sin columnas FK enteras; toda FK es un uuid que resuelve; `PRAGMA foreign_key_check` vacío; constraints FK en el servidor | unit (ffi) + integración node | `flutter test test/data/migration/v7_migration_test.dart` ; `cd backend && npm test` | ❌ W0 |
| MODEL-02 | Existen tablas, columnas y CHECKs (visibilidad, `kind` de path y de audio, orden de triggers); un portal tiene exactamente 1 trigger | unit + node | ídem | ❌ W0 |
| MODEL-03 | Push idempotente (misma versión = no-op); ventana de 2 versiones vía `entity_prev`; `change_seq` monótono y sin avance en no-op; tombstone; lote con FK diferidas en cualquier orden; compuerta `schema_version` | integración node (`DATABASE_URL`) + unit node de validación | `cd backend && DATABASE_URL=... node --test` | ❌ Wave 1a |
| MODEL-04 | `saveProgress` escribe en `path_progress` sin cambiar el conteo del outbox; `audio_local` guarda ruta/estado | unit (ffi) | `flutter test test/data` | ❌ Wave 3 |
| MODEL-05 | Migración sobre dataset sintético y sobre copia real: conteos, diffs con `EXCEPT`, rollback deja v6 intacto ante fallo forzado, backup creado y verificado, `init()` NO renombra la BD ante `MigrationException`, reconstrucción del outbox y traducción de deletes pendientes, reinicio del cursor, filas con uuid NULL, ids v5 idempotentes | unit (ffi) | `flutter test test/data/migration` | ❌ Wave 1b |
| MODEL-06 | Dry-run del script; aplicación en rama Neon; conteos por tabla iguales; aborto ante ambigüedad; vectores v5 y de cobertura iguales a Dart | unit node (traducción pura sobre fixture JSON) + corrida manual contra rama | `node --test backend/scripts` ; `node scripts/migrate_v2.mjs` (dry-run) | ❌ Wave 4 |
| MODEL-07 | `grep -rniE "region" lib test` sin resultados fuera del código de migración legacy; el monitor considera todos los triggers; widget test del panel Obra; grabar sin obra crea una draft | unit/widget + grep | `flutter test` ; `grep -rniE "region" lib test` | ❌ Wave 2/3 |
| MODEL-08 | Los tests solo tocan copias temporales; ningún camino de código referencia el celular; el test con copia real se saltea sin la variable de entorno | política + unit | revisión de código + `flutter test` | ❌ Wave 0 |

*Estado: ⬜ pendiente · ✅ verde · ❌ rojo/inexistente · ⚠️ inestable*

---

## Wave 0 Requirements

- [ ] `flutter pub add --dev sqflite_common_ffi` (probado en un proyecto de prueba en esta máquina)
- [ ] `test/support/v6_fixture.dart` — DDL v6 congelado + dataset sintético (77/70/459/20) + constructores de casos borde (audios huérfanos, triggers sueltos, uuid NULL, referencias colgantes)
- [ ] `test/data/migration/v7_migration_test.dart` — incluye el test con copia real, salteado sin `CINGULA_REAL_DB`
- [ ] `backend/api/_lib/spec.test.js` y `push.test.js` reescrito para tablas tipadas
- [ ] Vector cruzado uuid v5 + cobertura, idéntico en Dart y en JS

---

## Manual-Only Verifications

Solo DESPUÉS de que todas las compuertas automáticas estén verdes. Ninguno se hace contra el celular real hasta el paso 5.

| Comportamiento | Requisito | Por qué manual | Instrucciones |
|----------------|-----------|----------------|---------------|
| Pre-flight sobre copia real de la BD | MODEL-05, MODEL-08 | Los casos borde reales (uuid NULL, ids reutilizados, referencias colgantes) solo se ven con los datos reales | `adb pull` de una COPIA de `cingula.db` (solo lectura, hash antes y después); correr el reporte; decidir go/no-go |
| App sobre una copia de la BD real | MODEL-05, MODEL-07 | Ver los datos migrados y el data browser | Correr en Windows desktop o emulador contra la copia |
| Dry-run y ensayo del script de Neon | MODEL-06 | Confirmar conteos y la lista de tombstones no reconstruibles | Revisar el reporte y ensayar en una rama de Neon |
| Copia independiente antes del corte | MODEL-05 | El backup dentro de la app vive en el mismo celular, y los .wav son las únicas copias | Copiar la BD **y los .wav** a la PC antes de instalar |
| El corte en el celular real | MODEL-05 | Es la primera y única corrida en el celular; es un release con compuerta, no una prueba | Instalar la app nueva y verificar conteos |

---

## Validation Sign-Off

- [ ] Todas las tareas tienen verificación `<automated>` o dependen de Wave 0
- [ ] Continuidad de muestreo: no hay 3 tareas seguidas sin verificación automatizada
- [ ] Wave 0 cubre todas las referencias faltantes
- [ ] Sin flags de watch-mode
- [ ] Latencia de feedback < 120 s
- [ ] `nyquist_compliant: true` en el frontmatter

**Approval:** pendiente
