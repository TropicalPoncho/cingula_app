---
phase: 02.1
plan: 04
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - test/data/migration/real_preflight_test.dart
  - .planning/phases/02.1-modelo-de-datos-objetivo/02.1-PREFLIGHT.md
autonomous: false
requirements: [MODEL-05, MODEL-08]
must_haves:
  truths:
    - "Existe un reporte de pre-flight generado sobre una COPIA de la base real del celular, no sobre el celular"
    - "El usuario dio un go/no-go explícito antes de que la migración se dé por cerrada"
    - "La copia de la base real no quedó commiteada en el repositorio"
  artifacts:
    - path: ".planning/phases/02.1-modelo-de-datos-objetivo/02.1-PREFLIGHT.md"
      provides: "reporte de pre-flight sobre los datos reales + decisión go/no-go"
      contains: "go/no-go"
    - path: "test/data/migration/real_preflight_test.dart"
      provides: "corrida del reporte sobre una copia, salteada sin CINGULA_REAL_DB"
  key_links:
    - from: "test/data/migration/real_preflight_test.dart"
      to: "lib/data/migration/preflight.dart"
      via: "preflight() + formatPreflight() sobre una copia temporal"
      pattern: "CINGULA_REAL_DB"
---

<objective>
Mirar los datos REALES antes de terminar de escribir la migración: correr el reporte de pre-flight sobre una copia de la base del celular y decidir go/no-go.

Purpose: el research asume formas (uuid NULL en filas sembradas, ids reutilizados, referencias colgantes, deletes con payload fino) que solo se confirman con los datos reales. Si algo no coincide, es mucho más barato enterarse ahora que durante el corte.
Output: `02.1-PREFLIGHT.md` con el reporte y la decisión del usuario.

REGLA DURA (MODEL-08 / D-11): nada en este plan toca el celular. El usuario extrae la copia; el ejecutor solo lee el archivo que ya está en la PC. Los .wav son la única copia que existe y no se tocan en ningún paso.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-modelo-de-datos-objetivo/02.1-CONTEXT.md
@.planning/phases/02.1-modelo-de-datos-objetivo/02.1-RESEARCH.md
@.planning/phases/02-backend-real-push-sync/02-05-SUMMARY.md
@lib/data/migration/preflight.dart
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: el usuario extrae una COPIA de solo lectura de la base del celular</name>
  <read_first>
    - .planning/phases/02-backend-real-push-sync/02-05-SUMMARY.md (los pasos de adb + hash SHA-256 que ya se usaron con seguridad en la Fase 2)
  </read_first>
  <files>ninguno (paso humano)</files>
  <action>
    Presentarle al usuario los pasos de `<how-to-verify>` y esperar. El ejecutor NO corre `adb` ni ningún comando contra el dispositivo (MODEL-08 / D-11): solo recibe la ruta del archivo ya copiado a la PC y la anota para la Task 2.
  </action>
  <what-built>
    Nada todavía: este paso existe porque solo una persona con el celular en la mano puede sacar la copia, y porque el ejecutor tiene prohibido ejecutar nada contra el dispositivo (MODEL-08).
  </what-built>
  <how-to-verify>
    1. Cerrar la app Cíngula en el celular (que no quede escribiendo).
    2. Con el celular conectado por USB y depuración activada, desde PowerShell (adb vive en el Android SDK, `platform-tools`; NO está en el PATH de Git Bash):
       `adb shell "run-as com.example.cingula_app ls -l app_flutter/"` para confirmar la ruta y el tamaño de `cingula.db` (ajustar el applicationId si difiere).
    3. Copiar (NO mover) la base y sus sidecars si existen:
       `adb exec-out run-as <applicationId> cat app_flutter/cingula.db > C:\Users\ramac\cingula-db-copia\cingula.db`
       (repetir con `cingula.db-wal` y `cingula.db-shm` si aparecen en el listado).
    4. Verificar el hash de la copia y compararlo contra el del archivo en el celular:
       `Get-FileHash C:\Users\ramac\cingula-db-copia\cingula.db -Algorithm SHA256`
       `adb exec-out run-as <applicationId> sha256sum app_flutter/cingula.db`
       Los dos deben coincidir. Si no coinciden, la app estaba escribiendo: volver a cerrarla y repetir.
    5. La carpeta `C:\Users\ramac\cingula-db-copia\` debe estar FUERA del repositorio (o cubierta por el `.gitignore` del plan 02.1-01). No copiar ningún .wav: no hacen falta acá.
    6. Pegar en el chat: la ruta completa de la copia, su tamaño y el hash.
  </how-to-verify>
  <acceptance_criteria>
    - El usuario informó una ruta absoluta existente a un archivo `cingula.db` en la PC
    - Los dos hashes SHA-256 (celular y copia) coinciden
    - `git status --porcelain` no muestra ningún archivo `.db` sin ignorar
  </acceptance_criteria>
  <resume-signal>Pegar la ruta de la copia y los dos hashes, o describir qué falló.</resume-signal>
  <done>Existe en la PC una copia de `cingula.db` con hash verificado contra el del celular, fuera del control de versiones.</done>
  <verify>
    <automated>git status --porcelain | grep -E "\.db$" &amp;&amp; exit 1 || echo "sin .db sin ignorar: ok"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: correr el reporte de pre-flight sobre la copia y escribirlo</name>
  <files>test/data/migration/real_preflight_test.dart, .planning/phases/02.1-modelo-de-datos-objetivo/02.1-PREFLIGHT.md</files>
  <read_first>
    - lib/data/migration/preflight.dart (claves exactas del reporte y `formatPreflight`)
    - test/data/migration/preflight_test.dart (patrón de apertura con ffi + temp dir)
  </read_first>
  <action>
1. Crear `test/data/migration/real_preflight_test.dart`:
```dart
final real = Platform.environment['CINGULA_REAL_DB'];
final out  = Platform.environment['CINGULA_PREFLIGHT_OUT'];
test('pre-flight sobre una copia de la base real', () async {
  // MODEL-08: esto NUNCA corre contra el celular. Recibe la ruta de una copia
  // ya extraída a la PC y, aun así, trabaja sobre una segunda copia temporal.
  final tmp = Directory.systemTemp.createTempSync('cgpf');
  final work = File(p.join(tmp.path, 'copy.db'));
  File(real!).copySync(work.path);
  sqfliteFfiInit();
  final db = await databaseFactoryFfi.openDatabase(work.path);  // sin version/onUpgrade: no migra nada
  final report = await preflight(db);
  await db.close();
  final text = formatPreflight(report);
  if (out != null) File(out).writeAsStringSync(text);
  print(text);
}, skip: real == null ? 'requiere CINGULA_REAL_DB (ruta a una COPIA de la base)' : false);
```
   Importante: abrir SIN `version:` ni `onUpgrade:` para que sqflite no dispare ninguna migración sobre la copia.

2. Ejecutarlo con la ruta que dio el usuario en la Task 1:
   `CINGULA_REAL_DB="<ruta>" CINGULA_PREFLIGHT_OUT="<scratchpad>/preflight.txt" flutter test test/data/migration/real_preflight_test.dart -r expanded`

3. Crear `.planning/phases/02.1-modelo-de-datos-objetivo/02.1-PREFLIGHT.md` con:
   - Fecha, ruta de la copia (sin datos sensibles), hash informado por el usuario.
   - El reporte completo, tal cual lo devolvió `formatPreflight`, en un bloque de código.
   - Una sección **Lecturas** que compare contra lo que asume el diseño y marque cada punto como ✅ esperado o ⚠️ sorpresa:
     * conteos 77 / 70 / 459 / 20 (si no coinciden, ⚠️ y anotar los reales: son los conteos que van a las verificaciones de la migración);
     * `nullUuid` por tabla (el research espera filas sembradas sin uuid; si es 0, anotar que Neon tenga 626 entidades es consistente);
     * `duplicateUuids` — si no está vacío, es un **no-go** hasta decidir qué hacer con cada duplicado;
     * `invalidUuidFormat` — si no está vacío, **no-go** (el servidor castea a `uuid`);
     * `danglingPathRefs` / `danglingAudioRefsTriggers` / `danglingAudioRefsPaths` — cuántos, y confirmar que la regla "el trigger colgante se vuelve portal, nunca se descarta" los cubre;
     * `standaloneTriggers` — es la cantidad de portales y de obras extra que va a crear la migración (afecta los conteos esperados);
     * `outboxByTableOp` y `outboxThinDeletes` — cuántos deletes pendientes hay que traducir a tombstones (plan 02.1-05) y cuántos no traen `id`;
     * `syncStateCursor` — confirmar que hoy es un timestamp ISO (se reinicia en la migración);
     * `pathsWithPoints` — cuántos paths tienen `points` distinto de `'[]'`. La columna `points` NO existe en el modelo v7 (la geometría de un recorrido son sus triggers) y solo sobrevive en `legacy_geo_paths`. Si el número es > 0 — y el seed de instalación limpia sugiere que al menos el path sembrado lo es — marcarlo como ⚠️ y describir en una línea qué se pierde de USO (el dibujo del camino grabado, que hoy no se muestra en ninguna pantalla) y qué no se pierde de DATO (la columna entera queda en `legacy_geo_paths`). Es una lectura del go/no-go, no un bloqueante automático.
   - Una sección **go/no-go** vacía, para que la complete el usuario en la Task 3.
  </action>
  <acceptance_criteria>
    - `.planning/phases/02.1-modelo-de-datos-objetivo/02.1-PREFLIGHT.md` existe y contiene un bloque de código con el reporte
    - `grep -c "go/no-go" .planning/phases/02.1-modelo-de-datos-objetivo/02.1-PREFLIGHT.md` >= 1
    - `grep -cE "duplicateUuids|standaloneTriggers|outboxThinDeletes" .planning/phases/02.1-modelo-de-datos-objetivo/02.1-PREFLIGHT.md` >= 3
    - `grep -c "pathsWithPoints" .planning/phases/02.1-modelo-de-datos-objetivo/02.1-PREFLIGHT.md` >= 1
    - `grep -c "skip:" test/data/migration/real_preflight_test.dart` >= 1
    - `flutter test test/data/migration/real_preflight_test.dart` (sin la variable) se saltea y no falla
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/data/migration/real_preflight_test.dart</automated>
  </verify>
  <done>El reporte sobre los datos reales está escrito y contrastado contra lo que asume el diseño.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: go/no-go sobre el reporte</name>
  <read_first>
    - .planning/phases/02.1-modelo-de-datos-objetivo/02.1-PREFLIGHT.md
  </read_first>
  <files>.planning/phases/02.1-modelo-de-datos-objetivo/02.1-PREFLIGHT.md</files>
  <action>
    Mostrarle al usuario la sección **Lecturas** del reporte, destacando los ⚠️, y escribir su decisión en la sección **go/no-go** de `02.1-PREFLIGHT.md` con fecha. Si la decisión es "no-go", detener la fase y reportarlo al orquestador sin tocar nada más.
  </action>
  <what-built>
    El reporte de pre-flight sobre una copia de tu base real, con cada punto marcado como esperado o sorpresa.
  </what-built>
  <how-to-verify>
    1. Leer `02.1-PREFLIGHT.md`, en especial la sección **Lecturas**.
    2. Confirmar que los conteos coinciden con lo que esperás tener (audios, paths, triggers, regiones).
    3. Mirar si hay ⚠️. Bloquean el avance: `duplicateUuids` no vacío o `invalidUuidFormat` no vacío.
    4. Responder con una de estas tres cosas:
       - **go**: seguimos; los conteos reales quedan como los esperados de la verificación de la migración.
       - **no-go**: hay algo que resolver primero (decir qué).
       - **go con nota**: seguimos, pero anotar el caso a tratar de forma especial.
  </how-to-verify>
  <acceptance_criteria>
    - La sección **go/no-go** de `02.1-PREFLIGHT.md` quedó completada con la decisión del usuario, con fecha
    - Si hubo ⚠️ bloqueantes, están listados con la decisión tomada para cada uno
  </acceptance_criteria>
  <resume-signal>Escribir "go", "no-go" + motivo, o "go con nota: ...".</resume-signal>
  <done>La sección go/no-go de `02.1-PREFLIGHT.md` está completada y fechada con la decisión del usuario.</done>
  <verify>
    <automated>grep -A4 "go/no-go" .planning/phases/02.1-modelo-de-datos-objetivo/02.1-PREFLIGHT.md | grep -qiE "go|no-go" &amp;&amp; echo "decision registrada"</automated>
  </verify>
</task>

</tasks>

<verification>
- `.planning/phases/02.1-modelo-de-datos-objetivo/02.1-PREFLIGHT.md` tiene reporte + decisión fechada.
- `git status --porcelain | grep -E "\.db$"` no devuelve nada.
- `flutter test` sigue verde.
</verification>

<success_criteria>
1. El reporte salió de una COPIA en la PC, verificada por hash; el celular nunca fue tocado por el ejecutor.
2. Los conteos reales quedan registrados y son los que usan las verificaciones de la migración.
3. La decisión go/no-go está escrita y fechada.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/02.1-modelo-de-datos-objetivo/02.1-04-SUMMARY.md`.
</output>
