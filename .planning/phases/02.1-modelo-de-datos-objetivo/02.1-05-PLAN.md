---
phase: 02.1
plan: 05
type: execute
wave: 3
depends_on: ["02.1-02", "02.1-03", "02.1-04"]
files_modified:
  - lib/data/migration/v7_migration.dart
  - lib/data/migration/db_backup.dart
  - test/data/migration/v7_migration_test.dart
  - test/data/migration/db_backup_test.dart
  - lib/data/sync/sync_api_http.dart
  - test/data/sync/sync_api_http_test.dart
autonomous: true
requirements: [MODEL-03, MODEL-05]
must_haves:
  truths:
    - "Después de migrar, el outbox contiene un reenvío completo de todas las entidades en orden padre→hijo"
    - "Los deletes que quedaron pendientes en el formato viejo se traducen a tombstones de las tablas nuevas y no se pierden"
    - "El cursor de sync se reinicia: ya no queda un timestamp ISO guardado"
    - "Las tablas viejas quedan renombradas con prefijo legacy_, ninguna se borra"
    - "Antes de abrir la base con la versión nueva existe una copia verificada con timestamp"
    - "El cliente manda schema_version 2 en cada push"
    - "Los asserts de la migración sobre las tablas viejas pasan a nombrarlas legacy_* y siguen verdes"
  artifacts:
    - path: "lib/data/migration/db_backup.dart"
      provides: "backupBeforeMigration(): checkpoint WAL + copia + sidecars + verificación de la copia"
      exports: ["backupBeforeMigration", "BackupFailedException"]
  key_links:
    - from: "lib/data/migration/v7_migration.dart"
      to: "sync_outbox"
      via: "reconstrucción completa por uuid en orden de rank"
      pattern: "legacy_sync_outbox"
    - from: "lib/data/sync/sync_api_http.dart"
      to: "backend/api/sync/push.js"
      via: "campo schema_version del body"
      pattern: "schema_version"
---

<objective>
Cerrar la migración local: reconstruir el outbox, traducir los deletes pendientes, reiniciar el cursor, archivar las tablas viejas renombrándolas, y construir el respaldo previo a abrir la base con la versión nueva.

Purpose: tras el corte (D-20) el servidor habla otro protocolo, así que la cola vieja no se puede drenar: se reemplaza por un reenvío completo idempotente por uuid (D-31). Y el respaldo tiene que tomarse ANTES de abrir la base en v7, porque `onUpgrade` no puede copiar de forma segura la base que él mismo tiene abierta.
Output: `migrateToV7` completa + `db_backup.dart` + el cliente mandando `schema_version: 2`.

Por qué depende de tres planes: de **02.1-03** por el código que extiende; de **02.1-02** porque el payload del reenvío tiene que encajar columna por columna en `backend/api/_lib/spec.js` (si el spec no existe todavía, el reenvío se escribe a ciegas y el primer push del corte se rechaza entero con `unknown column`); y de **02.1-04** porque el go/no-go humano y los conteos reales de deletes pendientes (`outboxByTableOp`, `outboxThinDeletes`) son la entrada de la Task 1 — si el pre-flight dice "no-go", este plan no se escribe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-modelo-de-datos-objetivo/02.1-CONTEXT.md
@.planning/phases/02.1-modelo-de-datos-objetivo/02.1-RESEARCH.md
@.planning/phases/02.1-modelo-de-datos-objetivo/02.1-PREFLIGHT.md
@lib/data/migration/v7_migration.dart
@lib/data/datasources/local/db_recovery.dart
@lib/data/datasources/local/sync_local_data_source.dart
@lib/data/sync/sync_api_http.dart
@backend/api/_lib/spec.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reconstrucción del outbox, traducción de deletes, reinicio de cursor y renombre de tablas legacy</name>
  <files>lib/data/migration/v7_migration.dart, test/data/migration/v7_migration_test.dart</files>
  <read_first>
    - lib/data/migration/v7_migration.dart (pasos 1-5 ya escritos en el plan 02.1-03; esto se agrega DESPUÉS de la verificación)
    - lib/data/datasources/local/sync_local_data_source.dart (forma exacta de una fila de outbox: `table_name`, `record_uuid`, `op`, `payload` JSON, `device_id`, `created_at`, `attempt_count`, `next_attempt_at`)
    - backend/api/_lib/spec.js (nombres de tabla y columnas que el servidor acepta: el payload del reenvío tiene que encajar en el spec)
    - .planning/phases/02.1-modelo-de-datos-objetivo/02.1-RESEARCH.md (Q7)
    - .planning/phases/02.1-modelo-de-datos-objetivo/02.1-PREFLIGHT.md (cuántos deletes pendientes hay de verdad y cuántos sin `id`)
  </read_first>
  <behavior>
    - Tras migrar el dataset sintético, `sync_outbox` tiene exactamente una fila `insert` por cada fila de `audios`, `obras`, `paths` y `triggers` (77 + 85 + 85 + 459 = 706), más las traducciones de delete
    - Las filas del reenvío están ordenadas por `created_at` de forma que audios < artistas < obras < obra_artistas < paths < triggers (orden de rank del spec)
    - El payload de cada fila del reenvío contiene exactamente las columnas que `TABLE_SPEC` acepta para esa tabla, con `uuid` == `record_uuid`
    - El delete pendiente de `geo_triggers` del fixture aparece como `{table_name: 'triggers', op: 'delete', payload: {uuid, logical_version}}`
    - El delete pendiente de `audio_assets` aparece como `table_name: 'audios'`
    - La fila de outbox de `regions` NO aparece en la cola nueva
    - `sync_state.server_cursor` queda en NULL
    - `legacy_audio_assets`, `legacy_geo_paths`, `legacy_geo_triggers`, `legacy_regions` y `legacy_sync_outbox` existen con sus conteos originales; `audio_assets`, `geo_paths`, `geo_triggers` y `regions` ya no existen como tales
    - `attempt_count` = 0 y `next_attempt_at` = NULL en todas las filas nuevas
  </behavior>
  <action>
Agregar a `migrateToV7`, DESPUÉS del paso de verificación (así un fallo revierte también esto) y en este orden:

**6. Reconstrucción del outbox (D-31).**
   a. `ALTER TABLE sync_outbox RENAME TO legacy_sync_outbox;` — se conserva para auditoría, no se borra nunca.
   b. Crear `sync_outbox` de nuevo con el MISMO DDL que hoy tiene (copiarlo de `app_database.dart`: `id INTEGER PRIMARY KEY AUTOINCREMENT, table_name TEXT NOT NULL, record_uuid TEXT NOT NULL, op TEXT NOT NULL, payload TEXT, device_id TEXT, created_at INTEGER NOT NULL, attempt_count INTEGER NOT NULL DEFAULT 0, next_attempt_at INTEGER`).
   c. Encolar un `insert` por cada fila de las tablas nuevas, recorriendo las tablas en este orden: `audios`, `artistas`, `obras`, `obra_artistas`, `paths`, `triggers`. El `payload` es la fila entera de la tabla nueva, con las MISMAS claves que `TABLE_SPEC` (sin columnas locales: nada de `audio_local` ni `path_progress`, que no se sincronizan — MODEL-04). `created_at` = `nowSec` + el índice de rank de la tabla (así el orden FIFO de `SyncClient.pendingOutbox` manda padres antes que hijos), `device_id` = el que ya esté en `sync_state`, `attempt_count` = 0, `next_attempt_at` = NULL. Es idempotente por `uuid + logical_version`: si el servidor ya tiene esa versión, el push es un no-op.
   d. **Traducir los deletes pendientes** (un reenvío completo no puede expresar un tombstone): por cada fila de `legacy_sync_outbox` con `op='delete'` y `table_name` en `audio_assets|geo_paths|geo_triggers`, encolar `{table_name: <audios|paths|triggers>, record_uuid: <el mismo>, op: 'delete', payload: {uuid: <record_uuid>, logical_version: <el del payload viejo, o 1 si falta>}}` con `created_at` posterior al del reenvío. Las filas con `table_name='regions'` se descartan (la entidad ya no existe, D-16). Las filas `insert`/`update` viejas quedan subsumidas por el reenvío y no se traducen.

**7. Reinicio del cursor.** `UPDATE sync_state SET server_cursor = NULL WHERE id = 1;` — hoy guarda un timestamp ISO y el protocolo nuevo usa un `change_seq` numérico; dejarlo sería mandar un cursor sin sentido en el primer pull de la Fase 3.

**8. Archivado de las tablas viejas.** Al final de todo, `ALTER TABLE <t> RENAME TO legacy_<t>` para `audio_assets`, `geo_paths`, `geo_triggers` y `regions`. Se renombran las cuatro juntas y en el mismo paso a propósito: desde SQLite 3.26 un RENAME reescribe las `REFERENCES` de las tablas hermanas, y como todas las hermanas se renombran en el mismo bloque el resultado queda consistente. NINGÚN `DROP TABLE` en toda la fase.

Actualizar la verificación del paso 5 para que también compruebe, antes de los renombres: que la cantidad de filas del reenvío es igual a `audios + artistas + obras + obra_artistas + paths + triggers`, y que todo `record_uuid` de la cola nueva existe en su tabla (salvo los deletes traducidos, cuyo uuid puede ya no existir localmente).

**Actualizar los asserts que escribió el plan 02.1-03.** El paso 8 renombra las cuatro tablas, así que en `test/data/migration/v7_migration_test.dart` todos los asserts POST-migración que hoy dicen `audio_assets` / `geo_paths` / `geo_triggers` / `regions` (los conteos 77 / 70 / 459 / 20 de "las tablas viejas siguen completas", y los `EXCEPT` que comparan contra ellas dentro de `_verify` no: esos corren ANTES del renombre y no cambian) pasan a decir `legacy_audio_assets` / `legacy_geo_paths` / `legacy_geo_triggers` / `legacy_regions`. Agregar además un assert nuevo de que `SELECT name FROM sqlite_master WHERE name IN ('audio_assets','geo_paths','geo_triggers','regions')` no devuelve nada después de migrar. Los asserts del caso de ROLLBACK no cambian: ahí la migración falló y las tablas conservan su nombre original.
  </action>
  <acceptance_criteria>
    - `grep -c "legacy_sync_outbox" lib/data/migration/v7_migration.dart` >= 2
    - `grep -cE "RENAME TO legacy_(audio_assets|geo_paths|geo_triggers|regions)" lib/data/migration/v7_migration.dart` devuelve 4
    - `grep -c "DROP TABLE" lib/data/migration/v7_migration.dart` devuelve 0
    - `grep -c "server_cursor = NULL" lib/data/migration/v7_migration.dart` >= 1
    - `grep -c "'regions'" lib/data/migration/v7_migration.dart` >= 1 (el descarte explícito de las filas de outbox de regiones)
    - `grep -c "legacy_geo_triggers" test/data/migration/v7_migration_test.dart` >= 1 (los asserts post-migración ya nombran las tablas renombradas)
    - `flutter test test/data/migration/v7_migration_test.dart` pasa con los casos nuevos del bloque `<behavior>`
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/data/migration</automated>
  </verify>
  <done>La migración deja la cola lista para un reenvío completo ordenado, con los deletes pendientes traducidos, el cursor en NULL y las cuatro tablas viejas archivadas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: respaldo verificado antes de abrir la base en v7</name>
  <files>lib/data/migration/db_backup.dart, test/data/migration/db_backup_test.dart</files>
  <read_first>
    - lib/data/datasources/local/db_recovery.dart (`formatRecoveryTimestamp` y el criterio renombrar-nunca-borrar de la Fase 1, que se reutiliza)
    - test/data/datasources/local/db_recovery_test.dart (estilo de test puro con `dart:io`, sin plugins)
    - .planning/phases/02.1-modelo-de-datos-objetivo/02.1-RESEARCH.md (Pattern 2)
    - .planning/phases/02.1-modelo-de-datos-objetivo/02.1-CONTEXT.md (D-19, D-25, D-26)
  </read_first>
  <behavior>
    - Con una base v6 en el disco, `backupBeforeMigration(path, now)` deja `<path>.pre-v7-yyyyMMdd-HHmmss` y devuelve su ruta
    - Los sidecars `-wal`, `-shm` y `-journal` que existan se copian con el mismo sufijo
    - La copia se verifica abriéndola y comparando los conteos de `audio_assets`, `geo_paths`, `geo_triggers` y `regions` con los del original; si no coinciden se lanza `BackupFailedException` y NO se migra
    - Si la base ya está en `user_version` 7, la función devuelve null y no copia nada (no se re-respalda en cada arranque)
    - Si no hay archivo de base (instalación limpia), devuelve null sin error
    - El original queda intacto: mismo tamaño y mismos conteos después de la llamada
    - No existe ninguna función que borre un respaldo
  </behavior>
  <action>
Crear `lib/data/migration/db_backup.dart`:

```dart
class BackupFailedException implements Exception { BackupFailedException(this.message); final String message; }

/// Copia la base ANTES de abrirla con la versión nueva. `onUpgrade` no puede
/// copiar de forma segura la base que tiene abierta, así que esto corre primero.
/// Devuelve la ruta del respaldo, o null si no hacía falta (sin archivo, o ya en v7).
Future<String?> backupBeforeMigration(
  String dbPath,
  DateTime now, {
  DatabaseFactory? factory,   // inyectable para poder probarlo con ffi
}) async { ... }
```
Pasos:
1. Si `!File(dbPath).existsSync()` → devolver null.
2. Abrir con `openDatabase(dbPath)` SIN `version` ni callbacks (no dispara ninguna migración), leer `PRAGMA user_version`. Si es >= 7 → cerrar y devolver null.
3. `PRAGMA wal_checkpoint(TRUNCATE)` (inofensivo si no está en modo WAL) y cerrar.
4. `backupPath = '$dbPath.pre-v7-${formatRecoveryTimestamp(now)}'` reutilizando el formateador de `db_recovery.dart`. Copiar el archivo y, si existen, `-wal`, `-shm` y `-journal` con el mismo sufijo. Es COPIA, no rename: la base original tiene que seguir en su lugar para que la app la abra y la migre.
5. Verificar la copia: abrirla sin `version`, contar `audio_assets`, `geo_paths`, `geo_triggers`, `regions` y comparar con los conteos del original (leídos en el paso 2). Si alguno difiere, o si la copia no abre, lanzar `BackupFailedException` — abortar el arranque es preferible a migrar sin red.
6. Devolver `backupPath`.

NO escribir ninguna función de borrado ni de limpieza de respaldos: "confirmar el respaldo" es descartar el aviso en la UI, nada más (D-26 + memoria de proyecto `feedback_no_destructive_automation`). Dejarlo dicho en un comentario del archivo.

El test usa `sqflite_common_ffi` con el fixture v6 en un directorio temporal, cubre el bloque `<behavior>` y verifica que el original no cambió.
  </action>
  <acceptance_criteria>
    - `grep -c "pre-v7-" lib/data/migration/db_backup.dart` >= 1
    - `grep -c "formatRecoveryTimestamp" lib/data/migration/db_backup.dart` >= 1
    - `grep -cE "\.delete\(|deleteSync\(|rename\(" lib/data/migration/db_backup.dart` devuelve 0 (ni borra ni mueve nada)
    - `grep -c "wal_checkpoint" lib/data/migration/db_backup.dart` >= 1
    - `grep -c "class BackupFailedException" lib/data/migration/db_backup.dart` == 1
    - `flutter test test/data/migration/db_backup_test.dart` pasa
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/data/migration/db_backup_test.dart</automated>
  </verify>
  <done>Existe un respaldo con timestamp, con sidecars y verificado por conteos, y ninguna ruta que borre respaldos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: el cliente manda schema_version 2</name>
  <files>lib/data/sync/sync_api_http.dart, test/data/sync/sync_api_http_test.dart</files>
  <read_first>
    - lib/data/sync/sync_api_http.dart (el body actual de `pushOutbox`, línea ~31)
    - test/data/sync/sync_api_http_test.dart (cómo se arma el cliente falso y se inspecciona el body)
    - backend/api/sync/push.js (la compuerta que rechaza con 400 sin `schema_version: 2`)
  </read_first>
  <behavior>
    - El body de `pushOutbox` incluye `schema_version: 2` junto a `outbox`, `cursor` y `deviceId`
    - Un 400 del servidor sigue clasificándose como `SyncTransientException` (comportamiento de Fase 2, sin cambios)
  </behavior>
  <action>
En `SyncApiHttp.pushOutbox`, cambiar el body de
`jsonEncode({'outbox': outbox, 'cursor': cursor, 'deviceId': deviceId})`
a
`jsonEncode({'schema_version': 2, 'outbox': outbox, 'cursor': cursor, 'deviceId': deviceId})`
con un comentario de una línea: "compuerta del corte (D-20): el backend nuevo rechaza con 400 cualquier body sin esto; no hay protocolo dual".

Agregar al test existente un caso que decodifique el body enviado y afirme `body['schema_version'] == 2`. No tocar nada más del archivo.
  </action>
  <acceptance_criteria>
    - `grep -c "'schema_version': 2" lib/data/sync/sync_api_http.dart` == 1
    - `grep -c "schema_version" test/data/sync/sync_api_http_test.dart` >= 1
    - `flutter test test/data/sync/sync_api_http_test.dart` pasa
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/data/sync</automated>
  </verify>
  <done>Cada push lleva `schema_version: 2`, asertado en el test.</done>
</task>

</tasks>

<verification>
- `flutter test` completo verde.
- `flutter analyze` 0 errores.
- `grep -rn "DROP TABLE" lib/` → 0 resultados.
- `grep -rniE "borrar.*respaldo|delete.*backup" lib/` → 0 resultados.
</verification>

<success_criteria>
1. Migrar el dataset sintético deja 706 filas de reenvío + las traducciones de delete, en orden padre→hijo.
2. Las cuatro tablas viejas existen con prefijo `legacy_` y sus conteos originales, y los asserts del plan 02.1-03 quedaron actualizados a esos nombres (suite verde, no roja).
3. `sync_state.server_cursor` queda en NULL.
4. `backupBeforeMigration` deja una copia verificada y falla ruidosamente si la copia no coincide.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/02.1-modelo-de-datos-objetivo/02.1-05-SUMMARY.md`.
</output>
