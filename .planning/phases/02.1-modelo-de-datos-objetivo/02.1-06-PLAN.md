---
phase: 02.1
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/domain/usecases/monitor_user_location_usecase.dart
  - lib/core/services/geofence_background_service.dart
  - lib/presentation/notifiers/playback_notifier.dart
  - lib/presentation/pages/home/widgets/diagnostics_panel.dart
  - lib/presentation/pages/data_browser_page.dart
  - lib/presentation/widgets/trigger_map.dart
  - lib/core/di/service_locator.dart
  - lib/core/config/location_config.dart
  - lib/data/datasources/local/seed_data.dart
  - lib/data/datasources/local/app_database.dart
  - lib/domain/entities/geo_trigger.dart
  - lib/data/models/geo_trigger_model.dart
  - lib/domain/entities/region.dart
  - lib/domain/repositories/region_repository.dart
  - lib/data/models/region_model.dart
  - lib/data/repositories/region_repository_impl.dart
  - lib/data/datasources/local/region_local_data_source.dart
  - test/region_and_trigger_model_test.dart
  - test/monitor_usecase_trigger_path_test.dart
  - test/core/background/background_poller_test.dart
autonomous: true
requirements: [MODEL-07]
must_haves:
  truths:
    - "El monitor considera todos los triggers de la biblioteca, sin filtrar por región activa"
    - "No queda ninguna clase, archivo, repositorio ni test de Region en la app"
    - "El panel de debug no tiene sección Región ni desplegable de región"
    - "El mapa ya no dibuja regiones"
    - "La tabla regions y la columna region_id siguen existiendo en la base, con sus 20 filas"
    - "La reproducción se comporta igual que antes en todo lo demás"
  artifacts:
    - path: "lib/domain/usecases/monitor_user_location_usecase.dart"
      provides: "monitoreo sobre todos los triggers, sin RegionRepository"
  key_links:
    - from: "lib/domain/usecases/monitor_user_location_usecase.dart"
      to: "GeoTriggerRepository.fetchAll()"
      via: "candidatos sin prefiltro de región"
      pattern: "fetchAll\\(\\)"
---

<objective>
Eliminar Región como entidad y todo el código que depende de ella (D-16 / MODEL-07), sin tocar la base de datos.

Purpose: se hace ANTES del cambio de identidad a uuid para que ese cambio atraviese menos archivos. La cobertura derivada por obra, que reemplaza funcionalmente a la región, llega en la Fase 4; mientras tanto el monitor considera todos los triggers, que es exactamente lo que hace hoy cuando no hay región activa.
Output: cero código de Region, comportamiento de reproducción sin cambios, suite verde.

NO se toca el esquema SQLite: `regions` y `geo_triggers.region_id` siguen existiendo tal cual y conservan sus 20 filas; el archivado por renombre lo hace la migración (planes 02.1-03/05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-modelo-de-datos-objetivo/02.1-CONTEXT.md
@.planning/phases/02.1-modelo-de-datos-objetivo/02.1-RESEARCH.md
@lib/domain/usecases/monitor_user_location_usecase.dart
@lib/core/services/geofence_background_service.dart
@lib/presentation/pages/home/widgets/diagnostics_panel.dart
@lib/presentation/pages/data_browser_page.dart
@lib/presentation/widgets/trigger_map.dart
@lib/core/di/service_locator.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: dejar de usar Region en runtime (monitor, geofence, notifier, seed)</name>
  <files>lib/domain/usecases/monitor_user_location_usecase.dart, lib/core/services/geofence_background_service.dart, lib/presentation/notifiers/playback_notifier.dart, lib/core/di/service_locator.dart, lib/data/datasources/local/seed_data.dart, test/monitor_usecase_trigger_path_test.dart, test/core/background/background_poller_test.dart</files>
  <read_first>
    - lib/domain/usecases/monitor_user_location_usecase.dart (el filtro vive en las líneas 173-178; `_activeRegion` en 47, 99, 123; `_regionRepository` en 25/32/40/74)
    - lib/core/services/geofence_background_service.dart (parámetros `regions`/`onRegionChange`, el listener `type == 'region'` y las geocercas `region_<id>`)
    - lib/presentation/notifiers/playback_notifier.dart (`_activeRegion` y `_handleStateChanged`)
    - lib/core/di/service_locator.dart (el argumento `regionRepository:` que le pasa al monitor; los REGISTROS de `RegionLocalDataSource`/`RegionRepository` se borran recién en la Task 2, cuando la UI deje de pedirlos)
    - lib/data/datasources/local/seed_data.dart (la lista `_regions` y la clave `'region_id': 1`)
  </read_first>
  <action>
1. **`monitor_user_location_usecase.dart`**: sacar el parámetro `regionRepository` del constructor y el campo `_regionRepository`; sacar el campo `_activeRegion` y sus reseteos; sacar `final regions = await _regionRepository.fetchAll();` y el argumento `regions:` de `_geofenceBackgroundService.start(...)`; sacar el callback `onRegionChange:` entero. Borrar el bloque de filtrado (hoy líneas 173-178):
```dart
List<GeoTrigger> triggers = await _geoTriggerRepository.fetchAll();
if (_activeRegion != null) { ... }
```
   y dejar solo `final triggers = await _geoTriggerRepository.fetchAll();`. Cambiar la firma del callback `onStateChanged` de `void Function(Object? activeRegion, String samplingMode)?` a `void Function(String samplingMode)?` y actualizar sus llamadas. Sacar el import de `region.dart` y de `region_repository.dart`. Nada más de la lógica de reproducción cambia.

2. **`geofence_background_service.dart`**: sacar los parámetros `regions` y `onRegionChange` de `start()` y de `_configureListeners()`, sacar el import de `region.dart`, y borrar del listener `addGeofenceStatusChangeListener` todo el bloque de `type == 'region'`. Las geocercas registradas quedan solo las de triggers (`...triggers.map(...)`); borrar el `...regions.map(...)`. Si tras eso el listener de estado de geocerca solo llama a `onLog`, dejarlo así (sigue siendo útil para el log de debug).

3. **`playback_notifier.dart`**: borrar `_activeRegion`, el getter `activeRegion` y el parámetro de región de `_handleStateChanged`, ajustándolo a la firma nueva de `onStateChanged`.

4. **`service_locator.dart`**: borrar SOLO el argumento `regionRepository: getIt<RegionRepository>()` del `MonitorUserLocationUseCase`. Los registros de `RegionLocalDataSource` y `RegionRepository` siguen en pie hasta la Task 2: el panel de debug todavía los pide por `getIt` y sacarlos acá dejaría la app tirando `StateError` en runtime aunque `flutter analyze` siga verde.

5. **`seed_data.dart`**: borrar la lista `_regions` y su bucle de inserción, y la clave `'region_id': 1` de la fila de `_geoTriggers`. El resto del seed queda igual (se reescribe a la forma nueva en el plan 02.1-08).

6. **Tests**: en `test/monitor_usecase_trigger_path_test.dart` borrar el fake de `RegionRepository`, el argumento `regionRepository:` de las construcciones del caso de uso y cualquier assert sobre filtrado por región; el resto de los casos (trigger→path, guardado de progreso, camino completado) queda intacto. En `test/core/background/background_poller_test.dart` sacar las referencias a región de los fakes. Los dos archivos deben quedar verdes sin agregar casos nuevos.
  </action>
  <acceptance_criteria>
    - `grep -rniE "region" lib/domain/usecases/monitor_user_location_usecase.dart lib/core/services/geofence_background_service.dart lib/presentation/notifiers/playback_notifier.dart lib/data/datasources/local/seed_data.dart` devuelve 0 líneas
    - `grep -c "regionRepository" lib/core/di/service_locator.dart` devuelve 0
    - `grep -rn "regionRepository" test` devuelve 0
    - `flutter analyze` 0 errores
    - `flutter test` verde
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze --no-fatal-warnings &amp;&amp; flutter test</automated>
  </verify>
  <done>El monitor evalúa todos los triggers sin prefiltro, el servicio de geocercas solo registra triggers y el seed ya no siembra regiones; la suite sigue verde.</done>
</task>

<task type="auto">
  <name>Task 2: sacar Region de la UI (panel, data browser, mapa) y del service locator</name>
  <files>lib/presentation/pages/home/widgets/diagnostics_panel.dart, lib/presentation/pages/data_browser_page.dart, lib/presentation/widgets/trigger_map.dart, lib/core/di/service_locator.dart</files>
  <read_first>
    - lib/presentation/pages/home/widgets/diagnostics_panel.dart (`ExpansionTile` "Panel: Región" líneas 168-241; el desplegable "Región (opcional)" líneas 247-258; `_mapRegions`, `_regionNameController`, `_regionRadiusController`, `_selectedRegionId`; el `regions:` que le pasa al mapa en la línea 448)
    - lib/presentation/pages/data_browser_page.dart (`_regions`, `_regionNameForPath`, la columna `region` de la tabla de paths, `stats['regions']`)
    - lib/presentation/widgets/trigger_map.dart (parámetro `regions`, polígonos línea ~186, marcadores línea ~219)
    - lib/core/di/service_locator.dart (registros de `RegionLocalDataSource` y `RegionRepository`)
  </read_first>
  <action>
1. **`diagnostics_panel.dart`**: borrar el `ExpansionTile` completo "Panel: Región"; borrar del "Panel: Path + audio" el texto "Región (opcional):" y el `DropdownButton` de regiones (el `_selectedRegionId` ya hoy no se le pasa a `RecorderService`, así que no hay comportamiento que preservar); borrar los campos `_mapRegions`, `_regionNameController`, `_regionRadiusController`, `_selectedRegionId`, sus `dispose()`, la línea `final regions = await getIt<RegionRepository>().fetchAll();` de `_refreshMapData` y el argumento `regions: _mapRegions` del `TriggerMapWidget`. Sacar los imports de `region.dart` y `region_repository.dart`. El hueco que deja "Panel: Región" lo ocupa la sección Obra en el plan 02.1-10; por ahora simplemente no está.

2. **`trigger_map.dart`**: borrar el campo `regions`, su parámetro en el constructor, el import de `region.dart`, el bucle de polígonos de regiones y el de marcadores de regiones.

3. **`data_browser_page.dart`**: borrar `_regions`, la carga desde `RegionRepository`, el método `_regionNameForPath`, la `DataColumn(label: Text('region'))` y su `DataCell`, y la línea `Text('🗺️  Regions: ${stats['regions']}')`. Sacar los imports de `region.dart` y `region_repository.dart`.

4. **`service_locator.dart`**: recién ahora, borrar los registros de `RegionLocalDataSource` y `RegionRepository` y sus imports. Este es el orden que importa: primero dejan de pedirse, después se dan de baja.
  </action>
  <acceptance_criteria>
    - `grep -rniE "region" lib/presentation/pages/home/widgets/diagnostics_panel.dart lib/presentation/pages/data_browser_page.dart lib/presentation/widgets/trigger_map.dart lib/core/di/service_locator.dart` devuelve 0 líneas
    - `grep -c "Panel: Región" lib/presentation/pages/home/widgets/diagnostics_panel.dart` devuelve 0
    - `grep -rn "RegionRepository\|RegionLocalDataSource" lib` devuelve solo las definiciones de `lib/domain/repositories/region_repository.dart`, `lib/data/repositories/region_repository_impl.dart` y `lib/data/datasources/local/region_local_data_source.dart` (archivos que borra la Task 3)
    - `flutter analyze` 0 errores
    - `flutter test` verde
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze --no-fatal-warnings &amp;&amp; flutter test</automated>
  </verify>
  <done>La UI no muestra ni carga regiones y el service locator ya no las registra; la suite sigue verde.</done>
</task>

<task type="auto">
  <name>Task 3: borrar los archivos de Region y el campo regionId</name>
  <files>lib/domain/entities/region.dart, lib/domain/repositories/region_repository.dart, lib/data/models/region_model.dart, lib/data/repositories/region_repository_impl.dart, lib/data/datasources/local/region_local_data_source.dart, lib/domain/entities/geo_trigger.dart, lib/data/models/geo_trigger_model.dart, lib/data/datasources/local/app_database.dart, lib/core/config/location_config.dart, test/region_and_trigger_model_test.dart</files>
  <read_first>
    - lib/domain/entities/geo_trigger.dart (campo `regionId`, líneas 15 y 40)
    - lib/data/models/geo_trigger_model.dart (`super.regionId`, `map['region_id']`, la clave `'region_id'` de `toMap`)
    - lib/data/datasources/local/app_database.dart (`getDatabaseStats` cuenta `regions`, líneas ~328-341; el DDL NO se toca)
    - lib/core/config/location_config.dart (comentario de la línea 15 que menciona regiones)
    - test/region_and_trigger_model_test.dart (el primer caso prueba `RegionModel`; el segundo prueba `GeoTriggerModel`)
  </read_first>
  <action>
1. Borrar estos cinco archivos con `git rm`:
   `lib/domain/entities/region.dart`, `lib/domain/repositories/region_repository.dart`, `lib/data/models/region_model.dart`, `lib/data/repositories/region_repository_impl.dart`, `lib/data/datasources/local/region_local_data_source.dart`.

2. `lib/domain/entities/geo_trigger.dart`: borrar el parámetro `this.regionId` y el campo `final int? regionId;`.

3. `lib/data/models/geo_trigger_model.dart`: borrar `super.regionId`, `regionId: (map['region_id'] as int?)` y la clave `'region_id': regionId` de `toMap()`. La columna `region_id` sigue existiendo en la tabla v6 y sus valores quedan archivados en `legacy_geo_triggers` por la migración; simplemente el código deja de leerla (D-16).

4. `lib/data/datasources/local/app_database.dart`: en `getDatabaseStats()` borrar el conteo de `regions` y su clave del mapa devuelto. NO tocar el DDL de `_onCreate` ni `_onUpgrade`: la tabla `regions` se conserva. Agregar un comentario `// legacy-upgrade: no tocar` arriba del `CREATE TABLE regions` de `_onCreate` y de cada bloque `if (oldVersion < N)` que nombre regiones o `region_id`; ese es el motivo por el que este archivo queda EXCLUIDO del barrido `grep -rniE "region"` de MODEL-07, acá y en el plan 02.1-11.

5. `lib/core/config/location_config.dart`: reescribir el comentario de la línea 15 sin mencionar regiones (por ejemplo: "Polling coarse (segundos) cuando no hay ningún trigger cerca.").

6. `test/region_and_trigger_model_test.dart`: borrar el caso de `RegionModel` y sus imports; conservar el caso de `GeoTriggerModel` quitándole `'region_id'` del mapa de entrada. Si al terminar el archivo queda con un solo caso de mapeo de trigger, renombrarlo a `test/geo_trigger_model_test.dart` con `git mv` para que el nombre no mienta.
  </action>
  <acceptance_criteria>
    - `ls lib/domain/entities/region.dart lib/data/models/region_model.dart lib/data/repositories/region_repository_impl.dart lib/data/datasources/local/region_local_data_source.dart lib/domain/repositories/region_repository.dart 2>/dev/null` no lista ningún archivo
    - `grep -rniE "region" lib test --include=*.dart | grep -vE "^(lib/data/migration/|test/support/|test/data/migration/|lib/data/datasources/local/app_database\.dart)"` devuelve 0 líneas. **`app_database.dart` está excluido a propósito y para siempre**: conserva el DDL histórico (`CREATE TABLE regions`, `ADD COLUMN region_id`, el bucle `['audio_assets','geo_triggers','geo_paths','regions']` de `oldVersion < 4`) que una base muy vieja todavía necesita para llegar a v6 antes de entrar a v7. Marcar esos bloques con un comentario `// legacy-upgrade: no tocar` para que el motivo quede en el archivo y no solo en el plan.
    - `grep -c "regions" lib/data/datasources/local/app_database.dart` sigue siendo >= 1 (la tabla se conserva en el DDL) y `grep -c "FROM regions" lib/data/datasources/local/app_database.dart` devuelve 0
    - `flutter analyze` 0 errores
    - `flutter test` verde
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze --no-fatal-warnings &amp;&amp; flutter test</automated>
  </verify>
  <done>No existe código de Region en `lib/` ni en `test/` fuera de la migración legacy, y la tabla `regions` sigue intacta en el esquema.</done>
</task>

</tasks>

<verification>
- `grep -rniE "region" lib test --include=*.dart | grep -vE "^(lib/data/migration/|test/support/|test/data/migration/|lib/data/datasources/local/app_database\.dart)"` → vacío (criterio de MODEL-07; `app_database.dart` queda excluido por su camino de upgrade histórico).
- `flutter analyze` 0 errores; `flutter test` verde.
- `git diff --stat lib/data/datasources/local/app_database.dart` muestra solo el cambio de `getDatabaseStats`, ningún cambio de DDL.
</verification>

<success_criteria>
1. Los cinco archivos de Region están borrados del repositorio.
2. El monitor usa `fetchAll()` sin prefiltro y los tests de trigger→path siguen verdes.
3. El panel de debug ya no tiene sección Región ni desplegable de región.
4. El esquema SQLite no cambió: `regions` y `region_id` siguen definidos, con el comentario `// legacy-upgrade` que explica la exclusión del barrido.
5. Las tres tareas dejan el árbol verde por separado: ninguna deja la app pidiendo por `getIt` algo que ya no está registrado.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/02.1-modelo-de-datos-objetivo/02.1-06-SUMMARY.md`.
</output>
