# 02.1-12: Bitácora del corte

Plan: `.planning/phases/02.1-modelo-de-datos-objetivo/02.1-12-PLAN.md`

Precondición dura verificada antes de arrancar (2026-09-22):
- `flutter test` → 104/104 verde (`All tests passed!`).
- `cd backend && npm test` → 37 pass / 0 fail / 1 skip (skip esperado: sin `DATABASE_URL` en el shell).
- Auditoría ponytail del plan 02.1-11 → completa (commits `3925e92`, `fe3b2b0`; `02.1-VALIDATION.md` cerrado).

## Task 1: rama de Neon — RESUELTO

- `backend/.env` `DATABASE_URL` host: `ep-cold-snow-awuf9dpb-pooler.c-12.us-east-1.aws.neon.tech` → rama Neon **`dev`**. Config de desarrollo local, no producción, no se usa para este corte.
- Vercel Production `DATABASE_URL` host: `ep-fancy-waterfall-aw1wz0ru-pooler.c-12.us-east-1.aws.neon.tech` → rama Neon **`main`**.
- El usuario confirmó directamente que estos dos coinciden con lo esperado (Vercel prod = `main`): sin desconfiguración.
- **Opción elegida: `rehearse-dev-cutover-main`** — ensayar en una rama descartable creada desde `main`, cortar en `main`.
- Rama de ensayo: el usuario ya creó una rama nueva llamada **`dev-2.2`** para este ensayo (después de borrar la `dev-2.1` usada en los ensayos del plan 02.1-07). Pendiente de confirmar en la Task 2 si `dev-2.2` fue creada específicamente desde `main` (el plan requiere que la rama de ensayo traiga los datos reales de `main`).

env=dev, vercel=main, opción=rehearse-dev-cutover-main

## Task 2: ensayo en `dev-2.2` — OK

Confirmado por el usuario: `dev-2.2` fue creada desde `main`. Host: `ep-curly-haze-awtyp7gx-pooler.c-12.us-east-1.aws.neon.tech/neondb` (redactado — sin credenciales). Connection string usada solo vía variable de entorno `DATABASE_URL` por comando, nunca escrita a disco.

### 1. `npm test` con `DATABASE_URL` (antes de migrar)

```
# tests 42
# pass 42
# fail 0
# skipped 0
```

Verde, 0 skipped (la suite contra Postgres corrió de verdad).

### 2. `node scripts/migrate_v2.mjs` (dry-run)

```
Conectado a: ep-curly-haze-awtyp7gx-pooler.c-12.us-east-1.aws.neon.tech/neondb

Origen (por tabla):
  geo_triggers: 440 vivos, 1 con tombstone
  audio_assets: 68 vivos, 0 con tombstone
  geo_paths: 76 vivos, 1 con tombstone
  regions: 11 vivos, 0 con tombstone
Destino (por tabla):
  audios: 68
  artistas: 0
  obras: 77
  obra_artistas: 0
  paths: 77
  triggers: 441
Omitidos: 8
  (los 8 uuid fantasma de siempre, descartados por id_overrides.json — geo_triggers:42, geo_paths:11, geo_triggers:43, geo_paths:12, 2 c/u)
Advertencias: 0

DRY-RUN: no se escribio nada. Usar --apply para aplicar.
```

El host de la línea "Conectado a" es el de `dev-2.2`, NO el de `main` — chequeo de seguridad ok. Conteos idénticos al último ensayo completo (`02.1-NEON-REHEARSAL.md`, sección "Content-overrides remediation"): 68 audios / 77 obras / 77 paths / 441 triggers, 8 omitidos, 0 advertencias. Sin ambigüedades nuevas, sin abortos de validación. Alguna deriva por pushes nuevos del celular era esperable y aceptable; no hubo ninguna.

### 3. `node scripts/migrate_v2.mjs --apply` (1ra)

```
Volcado: scripts/dump-synced-entities-2026-09-22T20-02-36-783Z.json
Aplicadas 663 filas.
En la base: { audios: 68, artistas: 0, obras: 77, obra_artistas: 0, paths: 77, triggers: 441 }
Conteos verificados.
```

(Volcado local, ignorado por `.gitignore`, patrón `backend/scripts/dump-*.json` — confirmado con `git check-ignore -v`.)

### 4. `node scripts/migrate_v2.mjs --apply` (2da, idempotencia)

```
Aplicadas 663 filas.
En la base: { audios: 68, artistas: 0, obras: 77, obra_artistas: 0, paths: 77, triggers: 441 }
Conteos verificados.
```

Mismos conteos que la 1ra corrida, mismas 663 filas reenviadas — idempotencia confirmada, cero duplicados (el guard `WHERE logical_version < EXCLUDED.logical_version` hace no-op la segunda pasada).

### 5. `npm test` con `DATABASE_URL` (después de migrar)

```
# tests 42
# pass 42
# fail 0
# skipped 0
```

Verde — la migración aplicada no rompió nada que la suite verifique.

### Resultado

**Ensayo ok.** No se corrió `--finalize` (no corresponde en esta rama de ensayo, per plan). Pendiente para el usuario, fuera de esta tarea:
- Borrar la rama `dev-2.2` en la consola de Neon (el ensayo ya cumplió su función).
- Rotar la contraseña del rol de Neon usado en esta connection string, porque se pegó dos veces en el chat.

## Task 3: migración sobre copia real, fuera del celular — OK (con una salvedad en Parte B)

### Parte A — automatizada, sobre la copia del plan 02.1-04

`CINGULA_REAL_DB` apuntando a `C:\Users\ramac\cingula-db-copia\cingula.db` (la misma copia verificada por hash del plan 02.1-04, sin cambios desde entonces).

`flutter test test/data/migration -r expanded` → 26/26 verde (`All tests passed!`). Los "error MigrationException"/"error SqfliteFfiException" que aparecen en la salida son de tests que fuerzan esos errores a propósito (verifican rollback y bloqueo); no son fallas.

El test `copia real de la base` no imprime conteos de destino (solo verifica versión final = 7), así que se corrió un chequeo aparte sobre una copia temporal para sacar los números:

```
audios: 78
obras: 72
paths total: 72 (72 route, 0 portal)
triggers: 462
```

Coincide exacto con el pre-flight del plan 02.1-04 (78 audios, 72 paths, 462 triggers) y con el pre-flight original de la Task 2 del plan 02.1-04. Nada se perdió ni se agregó.

### Parte B — app real en emulador Android (nunca el celular)

`adb devices -l` antes de cualquier comando:
```
List of devices attached
emulator-5554          device product:sdk_gphone64_x86_64 model:sdk_gphone64_x86_64 device:emu64xa transport_id:4
```
Un solo dispositivo, `emulator-5554`. Todos los comandos siguientes usaron `-s emulator-5554` explícito. El celular real nunca estuvo conectado durante esta tarea.

APK recompilado en el momento (`flutter build apk --debug`) para probar el código real del corte, no un build viejo. Instalado, abierto una vez (crea `app_flutter/`), cerrado, y su `cingula.db` reemplazado por la copia v6 (122880 bytes, hash ya verificado en plan 02.1-04) vía `/data/local/tmp` (nota técnica: copiar por `/sdcard` falló por permisos de `run-as` contra `media_rw`; `/data/local/tmp` funcionó).

Al reabrir:
- ✅ Apareció el aviso **"Datos migrados; respaldo en `cingula.db.pre-v7-20260922-204237`"**. NO apareció la pantalla de error (D-25).
- ✅ "Ver tablas de BD" → "Ver contenido BD": **Audios: 78, Obras: 72, Triggers: 462, Paths: 72** — idéntico a la Parte A.
- ✅ Tabla de Paths muestra la columna `obra` con nombre no vacío por cada path (código: `_obraNameFor(p.obraUuid)` en `data_browser_page.dart:419`; con 1 obra por path en esta migración, el nombre de obra coincide con el del path).
- ✅ Tabla de Triggers: coordenadas en progresión geográfica coherente (sin saltos), consistente con orden por `position` preservado.
- ⚠️→✅ El panel mostró "Outbox pendiente: 200" y luego "0" — **es un contador de UI que no refleja el dato real** (no se actualiza solo tras migrar/reiniciar). Verificado por SQL directo sobre una copia limpia de `app_flutter/cingula.db` (forzando `am force-stop` antes de copiar, y usando `adb exec-out` en vez de `adb shell ... > archivo` para evitar que Windows corrompiera el binario con la traducción CRLF de `adb shell`):
  ```
  sync_outbox total: 684
    audios insert: 78
    obras insert: 72
    paths insert: 72
    triggers insert: 462
  sync_state: server_cursor=null
  ```
  684 = 78+72+72+462, exacto. Cursor vacío, como corresponde. El outbox real está bien; solo el número en pantalla es engañoso — vale la pena revisarlo en algún momento, sin bloquear el corte.
- ✅ Cerrar (`am force-stop`) y reabrir: **no** volvió a mostrar el aviso de "Datos migrados" ni a respaldar — la migración es de una sola vez, confirmado.
- ⚠️ **Reproducción de un trigger real: NO verificada en vivo.** Se intentó simular GPS sobre el trigger `132713e6` ("prueba en casa", lat -42.031963, lng -71.543182) con `adb emu geo fix` / `adb emu geo gnss`; ambos comandos devuelven `OK` pero `dumpsys location` nunca reflejó la nueva posición (se quedó en la ubicación por defecto del emulador, Mountain View). Es una limitación conocida de las imágenes `google_apis_playstore` con Google Play Services: la inyección de ubicación por consola clásica no siempre llega al proveedor fusionado; la vía soportada es el panel "Extended Controls" de Android Studio, que no es accesible por `adb`/script.
  **Evidencia alternativa aceptada en su lugar:** `test/monitor_usecase_trigger_path_test.dart:240` ("trigger de un path route reproduce via playFrom") testea exactamente este camino — entrar al radio de un trigger → `playFrom` con el audio correcto — sobre el modelo uuid nuevo, y está en verde dentro de los 104 tests de la suite completa. No es la app corriendo de verdad, pero cubre la misma lógica.

### Resultado Task 3

Parte A: OK, conteos exactos. Parte B: OK en todo lo verificable por UI/SQL (migración, conteos, obra por path, orden de triggers, no-remigración); la reproducción en vivo quedó sin verificar por una limitación del emulador, cubierta por el test automatizado equivalente en su lugar.

## Task 4: copia independiente de base + audios — OK

Ruta: `C:\Users\ramac\cingula-backup-20260922\` (fechada, distinta de la copia del pre-flight del plan 02.1-04).

Contenido:
- `cingula.db` — copiado con `adb exec-out run-as ... cat` vía `cmd /c` (evita la corrupción binaria de `adb shell ... >` en PowerShell).
- `recordings.tar` — carpeta completa de grabaciones.

Conteo de archivos de audio verificado con precisión en ambos lados (el primer conteo, 74 vs 73, era un falso positivo: `tar tf` incluye la carpeta misma como una entrada más; corregido filtrando entradas que terminan en `/`):
- Tar (solo archivos, sin la entrada de carpeta): **73**
- Celular (`ls -1 | wc -l`, un nombre por línea): **73**

Coinciden. Nada perdido.

No se dejó una segunda copia en otro disco/nube en esta sesión (recomendado, no bloqueante).

## Task 5: el corte — OK

Server-side migración aplicada en `main` en dos rondas (base + `recorridos`/D-33, ver commits `5da7b0f`/`019e537`). Deploy de Vercel verificado (`/api/sync/state` → 200, push sin `schema_version` → 400).

Primer intento de instalación real en el celular falló por D-34 (carrera entre isolates, ver `02.1-CONTEXT.md`). Datos originales confirmados intactos en ambos intentos fallidos (archivo renombrado, mismo tamaño/fecha que el pre-flight). Fix real (`sqlite_header.dart`, sin lock) desplegado; tercer intento migró limpio: `cingula.db` pasó de 122880 a 667648 bytes, sin pantalla de error.

### Conteos finales

| | Celular | Neon (`main`) |
|---|---|---|
| Audios | 78 | 78 |
| Obras | 72 | 77 (72 reales + 5 fantasmas de pruebas dic-2025, ya documentados en `02.1-NEON-REHEARSAL.md`) |
| Paths | 72 | 77 (ídem) |
| Triggers | 462 | 467 (ídem) |
| Outbox pendiente | 0 | — |

Los 5 "de más" en Neon se identificaron uno por uno (uuids `f938801…`, `59d185a…`, `6a6141…`, `f81daf7…`, `6e98f49…`) — todos "prueba en casa"/"Mi ruta casa", descarte de diciembre 2025 sin tombstone (protocolo viejo, antes de esta fase). No se tocan.

Reproducción verificada en el celular real: un recorrido grabado en casa reprodujo su audio correctamente.

`--finalize` corrido: `synced_entities` → `synced_entities_legacy` en `main`, confirmado. Nada borrado.

**Corte completo.**
