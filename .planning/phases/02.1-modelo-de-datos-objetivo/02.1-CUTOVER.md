# 02.1-12: Bitácora del corte

Plan: `.planning/phases/02.1-modelo-de-datos-objetivo/02.1-12-PLAN.md`

Precondición dura verificada antes de arrancar (2026-09-22):
- `flutter test` → 104/104 verde (`All tests passed!`).
- `cd backend && npm test` → 37 pass / 0 fail / 1 skip (skip esperado: sin `DATABASE_URL` en el shell).
- Auditoría ponytail del plan 02.1-11 → completa (commits `3925e92`, `fe3b2b0`; `02.1-VALIDATION.md` cerrado).

## Task 1: rama de Neon — PENDIENTE

Sin confirmar todavía. Preguntas para el usuario (según `<how-to-verify>` de la Task 1):

1. Host de `DATABASE_URL` en `backend/.env` (solo host, sin credenciales).
2. Host de `DATABASE_URL` configurado en Vercel → proyecto Cíngula → Settings → Environment Variables → Production.
3. En la consola de Neon: qué rama corresponde a cada host.
4. Opción elegida:
   - `rehearse-dev-cutover-main`: ensayar en una rama descartable creada desde `main`, cortar en `main`.
   - `rehearse-dev-existing`: ensayar en la rama `dev` que ya existe, cortar en `main`.

**Contexto conocido (no reemplaza la confirmación):** el usuario mencionó que la rama `dev-2.1` usada en los ensayos del plan 02.1-07 ya fue borrada y se creó una rama nueva en Neon desde entonces. No se asume qué rama es esa nueva rama (¿reemplaza a `dev`? ¿es otra cosa?) — se confirma junto con el resto de esta tarea.

_(sin resolver — el ejecutor no abrió `backend/.env` ni Neon ni Vercel para esta tarea, por instrucción explícita del plan)_
