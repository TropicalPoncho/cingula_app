---
phase: 02.1
plan: 09
type: execute
wave: 5
depends_on: ["02.1-08"]
files_modified:
  - lib/domain/usecases/monitor_user_location_usecase.dart
  - lib/core/services/recorder_service.dart
  - lib/core/services/geofence_background_service.dart
  - lib/presentation/pages/home/widgets/diagnostics_panel.dart
  - lib/presentation/pages/data_browser_page.dart
  - lib/presentation/widgets/trigger_map.dart
  - lib/presentation/widgets/migration_failure_app.dart
  - lib/main.dart
  - test/monitor_usecase_trigger_path_test.dart
  - test/geo_path_test.dart
  - test/geo_trigger_model_test.dart
  - test/core/background/background_poller_test.dart
  - test/presentation/pages/home/widgets/user_mode_view_test.dart
autonomous: true
requirements: [MODEL-01, MODEL-05, MODEL-07]
must_haves:
  truths:
    - "La reproducción y la grabación se comportan igual que antes, ahora sobre uuid"
    - "Un path kind=portal arranca en 0 y no guarda progreso, como hacían los triggers sueltos"
    - "Grabar en el campo crea o usa una obra"
    - "Si la migración falla, la app muestra una pantalla bloqueante que dice que los datos están intactos, sin ninguna acción destructiva"
    - "Si la migración sale bien, un aviso descartable informa dónde quedó el respaldo"
    - "flutter analyze y flutter test completos quedan en verde"
  artifacts:
    - path: "lib/presentation/widgets/migration_failure_app.dart"
      provides: "pantalla bloqueante de fallo de migración (D-25), sin botones destructivos"
      contains: "intactos"
  key_links:
    - from: "lib/main.dart"
      to: "lib/presentation/widgets/migration_failure_app.dart"
      via: "on MigrationException / on BackupFailedException antes del catch genérico"
      pattern: "MigrationFailureApp"
    - from: "lib/domain/usecases/monitor_user_location_usecase.dart"
      to: "GeoPathRepository.fetchByUuid"
      via: "el trigger resuelve su path por uuid"
      pattern: "fetchByUuid"
---

<objective>
Levantar el árbol: adaptar a uuid los consumidores de la capa de datos (monitor, grabador, geocercas, panel, data browser, mapa), agregar la pantalla bloqueante de fallo de migración y dejar `flutter analyze` y `flutter test` completos en verde.

Purpose: el plan 02.1-08 cambió los tipos de la capa de datos; este es el otro medio del mismo cambio. Va aparte porque juntos eran 37 archivos en un solo plan, y porque acá empieza lo que el usuario ve (D-25: qué pasa si la migración falla).
Output: la app compila, corre sobre el modelo nuevo y la suite entera está verde.

**Precondición:** el plan 02.1-08 está terminado. Al empezar este plan el árbol está ROJO a propósito, exactamente en los archivos de `files_modified` — es el estado que dejó el 02.1-08, no una regresión. Este plan no cierra hasta que `flutter analyze` y `flutter test` completos estén verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-modelo-de-datos-objetivo/02.1-CONTEXT.md
@.planning/phases/02.1-modelo-de-datos-objetivo/02.1-08-SUMMARY.md
@lib/domain/entities/geo_path.dart
@lib/domain/entities/geo_trigger.dart
@lib/domain/repositories/geo_path_repository.dart
@lib/domain/repositories/obra_repository.dart
@lib/data/datasources/local/db_recovery.dart
@lib/data/migration/v7_migration.dart
@lib/data/migration/db_backup.dart

<interfaces>
<!-- Lo que el plan 02.1-08 dejó escrito y este plan consume tal cual. -->

```dart
class GeoTrigger { final String uuid, pathUuid; final int position, offsetMs;
                   final double latitude, longitude, radiusMeters; ... }
class GeoPath    { final String uuid, obraUuid, kind /* 'route' | 'portal' */, name;
                   final String? audioUuid, grabacionUuid; int savedOffsetMs; ... }
class AudioAsset { final String uuid, kind, title, description, localPath; final Duration duration; ... }

Future<GeoPath?> GeoPathRepository.fetchByUuid(String uuid);
Future<void>     GeoPathRepository.saveProgress(String pathUuid, int offsetMs);
Future<String>   GeoPathRepository.createPath({required String name, String? audioUuid,
                                               String? obraUuid, String kind, double toleranceMeters});
Future<AudioAsset?> AudioRepository.findByUuid(String uuid);
Future<String>      ObraRepository.createDraft(String name);

// lib/data/datasources/local/app_database.dart
String? AppDatabase.lastBackupPath;          // ruta del respaldo pre-v7, o null
// init() lanza MigrationException o BackupFailedException; NUNCA renombra una base sana.

// lib/data/migration/v7_migration.dart
class MigrationException implements Exception { final String message; final Object? cause;
                                                final Map<String, Object?>? report; }
// lib/data/migration/db_backup.dart
class BackupFailedException implements Exception { final String message; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: monitor, grabador y geocercas por uuid</name>
  <files>lib/domain/usecases/monitor_user_location_usecase.dart, lib/core/services/recorder_service.dart, lib/core/services/geofence_background_service.dart, test/monitor_usecase_trigger_path_test.dart, test/core/background/background_poller_test.dart, test/geo_path_test.dart, test/geo_trigger_model_test.dart</files>
  <read_first>
    - lib/domain/usecases/monitor_user_location_usecase.dart (ya sin Región tras el plan 02.1-06; la rama `match.geoPathId != null` de las líneas 253-306 y la rama de trigger suelto de 308-320)
    - lib/core/services/recorder_service.dart (`startRecording`, `_createTrigger`, `stopRecording`, `startRecordingWithMic`)
    - lib/core/services/geofence_background_service.dart (`trigger_${t.id}` y el `data: {'triggerId': ..., 'audioId': ...}`)
    - .planning/phases/02.1-modelo-de-datos-objetivo/02.1-08-SUMMARY.md (qué quedó exactamente escrito en la capa de datos)
  </read_first>
  <behavior>
    - Un trigger de un path `kind='route'` reproduce el audio del path desde el offset guardado y sigue guardando progreso, igual que hoy
    - Un trigger de un path `kind='portal'` arranca SIEMPRE en 0 y NO llama a `saveProgress` (es el comportamiento que hoy tienen los triggers sueltos, que la migración convirtió en portales)
    - Si el path resuelto tiene `audioUuid == null`, el monitor reporta por `onStatusUpdate` y vuelve, sin excepción (igual que hoy con el audio faltante)
    - `startRecordingWithMic(obraUuid: null)` termina con el path colgando de una obra draft con el nombre de la grabación
    - `startRecordingWithMic(obraUuid: 'u')` deja el path en esa obra
    - Los ids de geocerca son `trigger_<uuid>` y el `data` no lleva ningún id de audio
  </behavior>
  <action>
1. **`monitor_user_location_usecase.dart`**: ahora TODOS los triggers pertenecen a un path (los sueltos se volvieron portales en la migración), así que la rama de "trigger sin path" desaparece. El flujo queda: mejor trigger por distancia normalizada (sin cambios) → `path = await _geoPathRepository.fetchByUuid(match.pathUuid)` → `asset = await _audioRepository.findByUuid(path.audioUuid!)` (si `audioUuid` es null, reportar por `onStatusUpdate` y volver) → misma lógica de offset y de `playFrom`. Preservar el comportamiento de los que eran triggers sueltos: si `path.kind == 'portal'`, arrancar en 0 y NO llamar a `saveProgress`. Cambiar `_activeTrigger?.id` / `_activePath?.id` por los uuid. Borrar el campo `_activeTrigger` si queda sin uso después de sacar la rama, o conservarlo solo si sigue cumpliendo un rol real.

2. **`recorder_service.dart`**: `_currentPathId`/`_currentAudioAssetId` pasan a `String? _currentPathUuid` / `String? _currentAudioUuid`. `startRecording({required String audioUuid, ...})`; `startRecordingWithMic` acepta `String? obraUuid` y se lo pasa a `createPath` (null = obra draft automática, ya implementado en el repositorio). `_createTrigger` deja de mandar `audio_asset_id`/`geo_path_id` y manda `path_uuid`. `stopRecording` devuelve `fetchByPathUuid`. `updateDuration(uuid: ...)`.

3. **`geofence_background_service.dart`**: los ids de geocerca pasan a `trigger_${t.uuid}` y el `data` a `{'type': 'trigger', 'triggerUuid': t.uuid}` (ya no hay `audioId` en el trigger). Nada más cambia.

4. **Tests**: adaptar `monitor_usecase_trigger_path_test.dart` (los fakes pasan a uuid) y agregar el caso nuevo del bloque `<behavior>`: **un path con `kind='portal'` arranca en 0 y no guarda progreso**. `background_poller_test.dart`, `geo_path_test.dart` y `geo_trigger_model_test.dart`: adaptar a los tipos nuevos sin agregar casos.
  </action>
  <acceptance_criteria>
    - `grep -c "pathUuid" lib/domain/usecases/monitor_user_location_usecase.dart` >= 1 y `grep -c "geoPathId" lib/domain/usecases/monitor_user_location_usecase.dart` == 0
    - `grep -c "kind == 'portal'" lib/domain/usecases/monitor_user_location_usecase.dart` >= 1
    - `grep -rnE "\bint id\b|audioAssetId|geoPathId|regionId" lib/domain lib/core/services` devuelve 0
    - `grep -c "trigger_\${t.uuid}" lib/core/services/geofence_background_service.dart` >= 1
    - `grep -c "portal" test/monitor_usecase_trigger_path_test.dart` >= 1
    - `flutter test test/monitor_usecase_trigger_path_test.dart test/geo_path_test.dart test/geo_trigger_model_test.dart test/core/background/background_poller_test.dart` pasa
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/monitor_usecase_trigger_path_test.dart test/geo_path_test.dart test/geo_trigger_model_test.dart test/core/background/background_poller_test.dart</automated>
  </verify>
  <done>El monitor resuelve el path por uuid y respeta los portales; grabar crea o usa una obra; las geocercas identifican triggers por uuid.</done>
</task>

<task type="auto">
  <name>Task 2: presentación, pantalla de error de migración y árbol en verde</name>
  <files>lib/presentation/pages/home/widgets/diagnostics_panel.dart, lib/presentation/pages/data_browser_page.dart, lib/presentation/widgets/trigger_map.dart, lib/presentation/widgets/migration_failure_app.dart, lib/main.dart, test/presentation/pages/home/widgets/user_mode_view_test.dart</files>
  <read_first>
    - lib/presentation/pages/home/widgets/diagnostics_panel.dart (los `DropdownButton<int>` de audios, `_selectedAudioId`, las llamadas a `RecorderService`)
    - lib/presentation/pages/data_browser_page.dart (tablas de audios/paths/triggers, botones de borrar, `saveProgress(path.id, 0)`, `playFrom`)
    - lib/main.dart (el `try/catch` que hoy se come cualquier error de `AppInitializer().init()`)
    - lib/data/datasources/local/db_recovery.dart (patrón del aviso `DatabaseRecoveryEvent`, que se reutiliza para el aviso de migración exitosa)
    - test/presentation/pages/home/widgets/user_mode_view_test.dart
  </read_first>
  <action>
1. **`diagnostics_panel.dart`**: `_selectedAudioId` pasa a `String? _selectedAudioUuid` y los `DropdownButton<int>` a `DropdownButton<String>` con `value: a.uuid`. Las llamadas a `RecorderService` pasan `audioUuid:`. En el "Panel: Sincronización" no cambia nada. **No se agrega todavía la sección Obra** (es el plan 02.1-10): este plan solo deja el panel compilando y funcionando.

2. **`data_browser_page.dart`**: adaptar mecánicamente a uuid — `deleteByUuid(path.uuid)`, `saveProgress(path.uuid, 0)`, `findByUuid(path.audioUuid)`, las columnas de las tablas muestran `uuid` (recortado a 8 caracteres para que entre) en vez de `id`, y la tabla de paths muestra `obra_uuid` recortado. Agregar la fila de `obras` a los conteos que ya devuelve `getDatabaseStats`. La columna Obra con nombre legible llega en el plan 02.1-10.

3. **`trigger_map.dart`**: adaptar los ids de marcador a uuid; ningún cambio visual.

4. **Pantalla de error de migración (D-25)**, en `lib/presentation/widgets/migration_failure_app.dart` — archivo propio, no dentro de `main.dart`, para que `main.dart` quede con el mismo tamaño de hoy:
   `MigrationFailureApp` es un `MaterialApp` mínimo con un `Scaffold` que muestra, en español y sin ninguna acción destructiva: "La migración de datos falló. **Tus datos están intactos.**", el mensaje de la excepción (y su `cause` si la tiene), y la ruta del respaldo si se conoce. Es una pantalla bloqueante: no ofrece "continuar igual", porque el código nuevo no puede operar sobre una base v6. Cero botones: ni reintentar, ni borrar, ni recrear.

5. **`main.dart`**: distinguir el fallo de migración del resto:
```dart
Object? startupFailure;
try { await AppInitializer().init(); }
on MigrationException catch (e) { startupFailure = e; }
on BackupFailedException catch (e) { startupFailure = e; }
catch (e, st) { debugPrint(...); }          // igual que hoy
runApp(startupFailure == null ? const CingulaApp() : MigrationFailureApp(error: startupFailure));
```
   Para el camino feliz, mostrar el respaldo con el mismo patrón del banner de `DatabaseRecoveryEvent` que ya existe: "Datos migrados; respaldo en <archivo>", descartable con la X. Descartar el aviso ES "confirmar el respaldo" (D-26): NO construir ninguna acción de borrar el respaldo.

6. **`user_mode_view_test.dart`**: construir `AudioAsset` sin `artist` y con `uuid`.

7. Dejar el árbol en verde: `flutter analyze` sin errores y `flutter test` completo en verde antes de cerrar el plan.
  </action>
  <acceptance_criteria>
    - `grep -rnE "DropdownButton<int>|_selectedAudioId" lib/presentation` devuelve 0
    - `grep -c "MigrationFailureApp" lib/main.dart` >= 1 y `grep -c "class MigrationFailureApp" lib/presentation/widgets/migration_failure_app.dart` == 1
    - `grep -c "intactos" lib/presentation/widgets/migration_failure_app.dart` >= 1
    - `grep -cE "ElevatedButton|TextButton|OutlinedButton|IconButton" lib/presentation/widgets/migration_failure_app.dart` devuelve 0 (pantalla bloqueante, sin acciones)
    - `grep -riE "borrar respaldo|eliminar respaldo|delete.*backup" lib/` devuelve 0
    - `grep -rnE "\.id\b" lib/presentation --include=*.dart | grep -vE "outbox|ValueKey|Key\(|geofence"` devuelve 0 líneas — los únicos `.id` que quedan en presentación son los de filas de `sync_outbox` (siguen siendo enteros) y keys de widget; ninguna entidad de dominio tiene ya un campo `id`
    - `flutter analyze` 0 errores
    - `flutter test` verde (todos los archivos de test)
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze --no-fatal-warnings &amp;&amp; flutter test</automated>
  </verify>
  <done>La app compila y corre sobre el modelo nuevo, con pantalla bloqueante si la migración falla y aviso de respaldo si sale bien; toda la suite está verde.</done>
</task>

</tasks>

<verification>
- `flutter analyze` 0 errores; `flutter test` verde — **la wave 5 no cierra sin esto**: es la compuerta que levanta el rojo declarado del plan 02.1-08.
- `grep -rnE "audio_asset_id|geo_path_id|region_id|'audio_assets'|'geo_paths'|'geo_triggers'" lib/ --include=*.dart | grep -vE "lib/data/migration/|lib/data/datasources/local/app_database\.dart"` → vacío (MODEL-01).
- `cd backend && npm test` sigue verde (no se tocó el backend).
</verification>

<success_criteria>
1. `flutter analyze` y `flutter test` completos en verde, con la app entera sobre uuid.
2. Un fallo de migración muestra la pantalla bloqueante, que dice que los datos están intactos y no tiene un solo botón.
3. Un path `kind='portal'` arranca en 0 y no guarda progreso, con test propio.
4. Grabar sin elegir obra crea una obra draft con el nombre de la grabación.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/02.1-modelo-de-datos-objetivo/02.1-09-SUMMARY.md`.
</output>
