---
phase: 02.1
plan: 08
type: execute
wave: 4
depends_on: ["02.1-05", "02.1-06"]
files_modified:
  - lib/data/datasources/local/app_database.dart
  - lib/data/datasources/local/seed_data.dart
  - lib/data/datasources/local/audio_local_data_source.dart
  - lib/data/datasources/local/geo_path_local_data_source.dart
  - lib/data/datasources/local/geo_trigger_local_data_source.dart
  - lib/data/datasources/local/obra_local_data_source.dart
  - lib/domain/entities/audio_asset.dart
  - lib/domain/entities/geo_path.dart
  - lib/domain/entities/geo_trigger.dart
  - lib/domain/entities/obra.dart
  - lib/data/models/audio_asset_model.dart
  - lib/data/models/geo_path_model.dart
  - lib/data/models/geo_trigger_model.dart
  - lib/data/models/obra_model.dart
  - lib/domain/repositories/audio_repository.dart
  - lib/domain/repositories/geo_path_repository.dart
  - lib/domain/repositories/geo_trigger_repository.dart
  - lib/domain/repositories/obra_repository.dart
  - lib/data/repositories/audio_repository_impl.dart
  - lib/data/repositories/geo_path_repository_impl.dart
  - lib/data/repositories/geo_trigger_repository_impl.dart
  - lib/data/repositories/obra_repository_impl.dart
  - lib/core/di/service_locator.dart
  - test/data/datasources/local/local_data_sources_test.dart
  - test/data/migration/app_database_v7_test.dart
autonomous: true
requirements: [MODEL-01, MODEL-02, MODEL-04, MODEL-05]
must_haves:
  truths:
    - "Toda entidad de la capa de datos se identifica y se referencia por uuid: no queda ninguna clave foránea entera en entidades, modelos, data sources ni repositorios"
    - "Al abrir la app sobre una base v6 corre la migración automática, con respaldo previo"
    - "Si la migración falla por CUALQUIER motivo, la base NO se renombra ni se recrea: el error sale como MigrationException y los datos quedan intactos"
    - "Guardar progreso de reproducción no agrega ninguna fila al outbox"
    - "AppDatabase.init() se puede probar sin dispositivo: acepta una ruta y una DatabaseFactory"
  artifacts:
    - path: "lib/domain/entities/obra.dart"
      provides: "entidad Obra (unidad de publicación)"
    - path: "lib/data/datasources/local/obra_local_data_source.dart"
      provides: "acceso a la tabla obras + creación de obra draft"
    - path: "lib/data/datasources/local/app_database.dart"
      provides: "versión 7, esquema nuevo, migración cableada y guarda contra el camino de BD corrupta"
      contains: "MigrationException"
    - path: "test/data/migration/app_database_v7_test.dart"
      provides: "migración, respaldo y guarda D-25 probados A TRAVÉS de AppDatabase.init()"
      contains: "MigrationException"
  key_links:
    - from: "lib/data/datasources/local/app_database.dart"
      to: "lib/data/migration/v7_migration.dart"
      via: "onUpgrade llama a migrateToV7 y envuelve cualquier fallo en MigrationException"
      pattern: "migrateToV7"
    - from: "lib/data/datasources/local/app_database.dart"
      to: "lib/data/migration/db_backup.dart"
      via: "respaldo tomado ANTES de abrir en v7"
      pattern: "backupBeforeMigration"
    - from: "lib/data/datasources/local/geo_path_local_data_source.dart"
      to: "path_progress"
      via: "saveProgress escribe local y NO encola outbox"
      pattern: "path_progress"
---

<objective>
El cambio de identidad en la capa de datos: `int id` → `String uuid` en entidades, modelos, data sources y repositorios; tablas nuevas como esquema activo; migración cableada con su red de seguridad; y el progreso de reproducción fuera del contenido sincronizado.

Purpose: es el paso que vuelve portable el modelo (MODEL-01) y el único que no se puede hacer a medias: Dart no compila medio cambio de tipo. Se hace después de borrar Región para que atraviese menos archivos.
Output: la capa de datos corre sobre `audios`/`obras`/`paths`/`triggers` con uuid, la app migra sola al abrir con respaldo y guarda, y `flutter test test/data` queda en verde.

Regla de ponytail para todo el plan: **se conservan los nombres de clase** (`GeoPath`, `GeoTrigger`, `AudioAsset`, `GeoPathRepository`, ...). El diff es de tipos y de nombres de tabla, no de renombres masivos.

**Este plan y el 02.1-09 son UNA SOLA unidad de compilación, partida en dos por tamaño (25 + 12 archivos en vez de 37).** Cambiar el tipo de `GeoTrigger.id` rompe, por construcción, a todo el que lo lea: no existe un corte que deje el árbol entero verde en el medio. Lo que sí está garantizado:
- al terminar ESTE plan: `flutter analyze lib/domain/entities lib/domain/repositories lib/domain/usecases/run_sync_usecase.dart lib/data lib/core/di` sin errores y `flutter test test/data` verde;
- **rojo esperado entre 02.1-08 y 02.1-09**, y nada más que esto: `lib/domain/usecases/monitor_user_location_usecase.dart`, `lib/core/services/recorder_service.dart`, `lib/core/services/geofence_background_service.dart`, `lib/presentation/**`, `lib/main.dart`, y los tests `test/monitor_usecase_trigger_path_test.dart`, `test/geo_path_test.dart`, `test/geo_trigger_model_test.dart`, `test/core/background/background_poller_test.dart`, `test/presentation/pages/home/widgets/user_mode_view_test.dart`;
- el plan 02.1-09 depende SOLO de este y es el único de su wave: ningún otro plan ve el árbol en ese estado, y la wave 5 no cierra hasta que `flutter analyze` + `flutter test` completos estén verdes.
Si el ejecutor termina este plan y le sobra contexto, NO siga con el 02.1-09 en la misma corrida: el rojo intermedio es esperado, no una señal de que falta algo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-modelo-de-datos-objetivo/02.1-CONTEXT.md
@.planning/phases/02.1-modelo-de-datos-objetivo/02.1-RESEARCH.md
@lib/data/migration/v7_schema.dart
@lib/data/migration/v7_migration.dart
@lib/data/migration/db_backup.dart
@lib/data/datasources/local/app_database.dart
@lib/data/datasources/local/sync_local_data_source.dart
@lib/data/datasources/local/db_recovery.dart
@backend/api/_lib/spec.js

<interfaces>
<!-- Contratos finales. El ejecutor los implementa tal cual; no hace falta explorar el código para deducirlos. -->

```dart
// lib/domain/entities/obra.dart
class Obra {
  const Obra({required this.uuid, required this.name, this.visibility = 'draft',
              this.ownerId, this.shareToken, this.coverLat, this.coverLon,
              this.coverMinLat, this.coverMaxLat, this.coverMinLon, this.coverMaxLon,
              this.updatedAt, this.deletedAt, this.logicalVersion});
  final String uuid; final String name; final String visibility;
  final String? ownerId, shareToken;
  final double? coverLat, coverLon, coverMinLat, coverMaxLat, coverMinLon, coverMaxLon;
  final DateTime? updatedAt, deletedAt; final int? logicalVersion;
}

// lib/domain/entities/audio_asset.dart  (pierde `id` y `artist`; gana `kind`)
class AudioAsset {
  final String uuid; final String kind;        // 'grabacion' | 'final'
  final String title, description; final Duration duration;
  final String localPath;                      // viene de audio_local (solo local)
  final Uri? remoteUrl; final String? storageKey, checksum;
  final DateTime? updatedAt, deletedAt; final int? logicalVersion;
}

// lib/domain/entities/geo_path.dart
class GeoPath {
  final String uuid, obraUuid, kind /* 'route' | 'portal' */, name;
  final String? audioUuid, grabacionUuid;
  final double toleranceMeters; int savedOffsetMs;   // viene de path_progress (solo local)
  final DateTime? updatedAt, deletedAt; final int? logicalVersion;
}

// lib/domain/entities/geo_trigger.dart  (pierde audioAssetId, geoPathId, regionId; gana pathUuid y position)
class GeoTrigger {
  final String uuid, pathUuid; final int position;
  final String name, description;
  final double latitude, longitude, radiusMeters; final int offsetMs;
  final DateTime? updatedAt, deletedAt; final int? logicalVersion;
  bool contains(Coordinate c); double distanceTo(Coordinate c);   // sin cambios
}

// Repositorios
abstract class AudioRepository {
  Future<List<AudioAsset>> fetchAll();
  Future<AudioAsset?> findByUuid(String uuid);
  Future<String> insertLocalRecording({required String title, required String description,
                                       required String localPath, required Duration duration});
  Future<void> updateDuration({required String uuid, required Duration duration});
}
abstract class GeoPathRepository {
  Future<List<GeoPath>> fetchAll();
  Future<GeoPath?> fetchByUuid(String uuid);
  Future<void> saveProgress(String pathUuid, int offsetMs);       // SOLO path_progress, sin outbox
  Future<void> updateAudio({required String pathUuid, required String audioUuid});
  Future<String> createPath({required String name, String? audioUuid, String? obraUuid,
                             String kind = 'route', double toleranceMeters = 10.0});
  Future<int> deleteByAudioUuid(String audioUuid);
  Future<int> deleteByUuid(String pathUuid);
}
abstract class GeoTriggerRepository {
  Future<List<GeoTrigger>> fetchAll();
  Future<List<GeoTrigger>> fetchByPathUuid(String pathUuid);
  Future<int> deleteByPathUuid(String pathUuid);
  Future<int> deleteOrphaned();
  Future<GeoTrigger?> findMatch(Coordinate coordinate);
  Future<String> insertTrigger(Map<String, Object?> values);
}
abstract class ObraRepository {
  Future<List<Obra>> fetchAll();
  Future<Obra?> findByUuid(String uuid);
  Future<String> createDraft(String name);     // uuid v4 nuevo, visibility 'draft'
}

// lib/data/datasources/local/app_database.dart — firma nueva, con costura de test
class AppDatabase {
  String? lastBackupPath;                       // ruta del respaldo pre-v7, para el aviso (D-19/D-26)
  DatabaseRecoveryEvent? lastRecoveryEvent;     // ya existe, sin cambios
  /// [dbPath] y [factory] existen SOLO para poder probar init() sin dispositivo:
  /// por defecto son getApplicationDocumentsDirectory() + el databaseFactory global,
  /// exactamente el comportamiento de hoy.
  Future<void> init({String? dbPath, DatabaseFactory? factory});
}
```

Nombres de tabla del outbox después de este plan: `audios`, `obras`, `paths`, `triggers`.
El payload del outbox lleva EXACTAMENTE las columnas que acepta `backend/api/_lib/spec.js` para esa tabla — nada de `'id'`, `local_path`, `remote_url` ni `saved_offset_ms` (el backend rechaza el lote entero con `unknown column`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: entidades, modelos y contratos de repositorio por uuid</name>
  <files>lib/domain/entities/audio_asset.dart, lib/domain/entities/geo_path.dart, lib/domain/entities/geo_trigger.dart, lib/domain/entities/obra.dart, lib/data/models/audio_asset_model.dart, lib/data/models/geo_path_model.dart, lib/data/models/geo_trigger_model.dart, lib/data/models/obra_model.dart, lib/domain/repositories/audio_repository.dart, lib/domain/repositories/geo_path_repository.dart, lib/domain/repositories/geo_trigger_repository.dart, lib/domain/repositories/obra_repository.dart</files>
  <read_first>
    - las 3 entidades y los 3 modelos actuales (`audio_asset.dart`, `geo_path.dart`, `geo_trigger.dart` y sus `*_model.dart`)
    - los 3 repositorios abstractos actuales
    - lib/data/migration/v7_schema.dart (nombres de columna exactos de las tablas v7)
    - backend/api/_lib/spec.js (columnas exactas que el servidor acepta por tabla: `toMap()` no puede devolver ninguna otra)
  </read_first>
  <action>
Implementar exactamente las clases del bloque `<interfaces>`. Es la primera tarea del plan a propósito: son los contratos contra los que se escribe todo lo demás, acá y en el plan 02.1-09.

1. **Entidades** (`lib/domain/entities/`): las cuatro clases tal cual el bloque `<interfaces>`. `Obra` es nueva; las otras tres pierden `id` (y `artist`, `audioAssetId`, `geoPathId`, `regionId`) y ganan sus uuid. `GeoTrigger.contains` y `distanceTo` no cambian de cuerpo.

2. **Modelos** (`lib/data/models/`): los `fromMap`/`toMap` usan las columnas nuevas (`uuid`, `obra_uuid`, `path_uuid`, `audio_uuid`, `grabacion_uuid`, `position`, `kind`, `visibility`, `cover_*`).
   - `AudioAssetModel.fromMap` lee `local_path`/`remote_url` del JOIN con `audio_local`; `toMap()` devuelve SOLO las columnas de `audios` (lo que se sincroniza) y un `toLocalMap()` aparte devuelve las de `audio_local`.
   - `GeoPathModel.fromMap` lee `saved_offset_ms` del JOIN con `path_progress` (0 si no hay fila); `toMap()` NO lo incluye.
   - La columna `points` desaparece del modelo: la geometría de un recorrido son sus triggers, y la columna sobrevive intacta en `legacy_geo_paths`.
   - `ObraModel` es nuevo, con las seis columnas `cover_*`.

3. **Repositorios abstractos** (`lib/domain/repositories/`): las cuatro interfaces del bloque `<interfaces>`. `ObraRepository` es nueva. Solo las firmas; las implementaciones son la Task 3.
  </action>
  <acceptance_criteria>
    - `grep -rnE "\bint id\b|audioAssetId|geoPathId|regionId" lib/domain/entities lib/data/models lib/domain/repositories` devuelve 0
    - `grep -c "class Obra" lib/domain/entities/obra.dart` == 1
    - `grep -c "findByUuid" lib/domain/repositories/audio_repository.dart` == 1
    - `grep -c "createDraft" lib/domain/repositories/obra_repository.dart` == 1
    - `grep -c "points" lib/data/models/geo_path_model.dart` == 0
    - `grep -c "toLocalMap" lib/data/models/audio_asset_model.dart` == 1
    - `flutter analyze lib/domain/entities lib/data/models lib/domain/repositories` → 0 errores en esos archivos
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze --no-fatal-warnings lib/domain/entities lib/data/models lib/domain/repositories</automated>
  </verify>
  <done>Las cuatro entidades, los cuatro modelos y las cuatro interfaces de repositorio hablan uuid, y ningún `toMap()` devuelve columnas que el servidor no acepte.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: data sources y repositorios sobre las tablas nuevas</name>
  <files>lib/data/datasources/local/audio_local_data_source.dart, lib/data/datasources/local/geo_path_local_data_source.dart, lib/data/datasources/local/geo_trigger_local_data_source.dart, lib/data/datasources/local/obra_local_data_source.dart, lib/data/repositories/audio_repository_impl.dart, lib/data/repositories/geo_path_repository_impl.dart, lib/data/repositories/geo_trigger_repository_impl.dart, lib/data/repositories/obra_repository_impl.dart, lib/core/di/service_locator.dart, test/data/datasources/local/local_data_sources_test.dart</files>
  <read_first>
    - los cuatro data sources actuales (patrón: insert + `enqueueOutbox` con `'id': id` en el payload — ese `id` DESAPARECE)
    - lib/data/datasources/local/sync_local_data_source.dart (`withInsertMetadata`, `withUpdateMetadata`, `enqueueOutbox`, `newUuid`)
    - las cuatro implementaciones de repositorio actuales (caché en memoria y su invalidación: se conservan tal cual)
    - lib/core/di/service_locator.dart (registros actuales)
    - lib/data/migration/v7_schema.dart (las tablas contra las que se escribe)
  </read_first>
  <behavior>
    - `saveProgress(pathUuid, 12000)` escribe `path_progress` y deja el `COUNT(*)` de `sync_outbox` EXACTAMENTE igual que antes de la llamada (MODEL-04)
    - `insertRecording(...)` crea una fila en `audios` y otra en `audio_local`, y encola UNA sola fila de outbox, de tabla `audios`, cuyo payload no contiene `local_path` ni `remote_url`
    - `insertTrigger({...})` encola outbox de tabla `triggers` con payload que contiene `path_uuid` y no contiene `audio_asset_id` ni `geo_path_id`
    - El `position` del primer trigger de un path es 0 y el del segundo es 1
    - `ObraLocalDataSource.createDraft('x')` crea una obra con `visibility='draft'` y encola outbox de tabla `obras`
    - `GeoPathRepositoryImpl.createPath(name: 'y')` sin `obraUuid` deja el path colgando de una obra draft nueva llamada 'y'
    - `deleteByUuid(pathUuid)` borra el path, sus triggers y su fila de `path_progress`, y encola deletes por uuid (ninguno de `path_progress`)
    - Todas las claves de todo payload encolado están dentro de las columnas que `backend/api/_lib/spec.js` acepta para esa tabla
  </behavior>
  <action>
1. **`audio_local_data_source.dart`**: `getAll()` y `getByUuid(String)` leen `audios LEFT JOIN audio_local ON audios.uuid = audio_local.audio_uuid` y devuelven `AudioAssetModel`. `insertRecording(...)` genera el uuid con `_sync.newUuid()`, inserta en `audios` (`kind: 'grabacion'`) y en `audio_local` (`local_path`, `remote_url: null`), y encola UNA fila de outbox de tabla `audios` con payload = solo las columnas de `audios` (`toMap()`). `updateDuration({required String uuid, ...})` actualiza `audios` y encola el update. El texto `artist` desaparece del código (D-30).

2. **`geo_path_local_data_source.dart`**: opera sobre `paths` + `path_progress`. `getAll()` hace `paths LEFT JOIN path_progress ON paths.uuid = path_progress.path_uuid`. `insertPath` genera uuid, inserta y encola outbox de tabla `paths`. **`saveProgress(String pathUuid, int offsetMs)` hace SOLO** `INSERT OR REPLACE INTO path_progress (path_uuid, saved_offset_ms, updated_at) VALUES (...)` y **no llama a `enqueueOutbox` ni toca `paths`** (MODEL-04 / D-10: con varios usuarios, el progreso de uno pisaría el del otro). `updateAudio({pathUuid, audioUuid})` actualiza `paths.audio_uuid` y sí encola. `deleteByUuid` borra los triggers del path y el path (igual que hoy), encolando los deletes por uuid con `logical_version` incrementada; borrar también su fila de `path_progress` (sin outbox).

3. **`geo_trigger_local_data_source.dart`**: opera sobre `triggers`. `fetchByPathUuid(String)`, `deleteByPathUuid(String)`, `deleteOrphaned()` = `DELETE FROM triggers WHERE path_uuid NOT IN (SELECT uuid FROM paths)`. `insertTrigger(values)` genera uuid, calcula `position` como `(SELECT COALESCE(MAX(position), -1) + 1 FROM triggers WHERE path_uuid = ?)` y devuelve el uuid.

4. **`obra_local_data_source.dart`** (nuevo): `getAll()`, `findByUuid(String)`, `createDraft(String name)` → inserta en `obras` con uuid v4 nuevo, `visibility: 'draft'`, y encola outbox de tabla `obras`. Nada de `artistas` ni `obra_artistas`: existen como tablas vacías y en 2.1 no tienen repositorio, entidad ni UI.

5. **Repositorios** (`lib/data/repositories/`): adaptar las cuatro implementaciones a las firmas de la Task 1, manteniendo el caché en memoria y su invalidación tal como está hoy. `GeoPathRepositoryImpl.createPath` con `obraUuid == null` crea primero una obra draft con el nombre del path (`ObraRepository.createDraft(name)`) y usa su uuid — es la regla de D-22 "si no se eligió ninguna obra, se crea una draft con el nombre de la grabación"; el selector de obra en la UI llega en el plan 02.1-10.

6. **`service_locator.dart`**: registrar `ObraLocalDataSource` y `ObraRepository`; pasarle el `ObraRepository` al `GeoPathRepositoryImpl`; actualizar el resto de los registros a los tipos nuevos. El `MonitorUserLocationUseCase` y el `RecorderService` se siguen registrando igual: sus firmas cambian en el plan 02.1-09 y este archivo se vuelve a tocar ahí.

7. **`test/data/datasources/local/local_data_sources_test.dart`** (nuevo): con `sqflite_common_ffi`, crea una base v7 limpia llamando a `createV7Tables` directamente (NO a través de `AppDatabase`: eso es la Task 3) y cubre el bloque `<behavior>`, en especial el conteo de outbox invariante de `saveProgress` y la ausencia de `local_path` en el payload de `audios`. Para el último caso del bloque, leer las columnas permitidas desde `backend/api/_lib/spec.js` es imposible desde Dart: escribir la lista de columnas esperada por tabla a mano en el test, con un comentario que apunte a `spec.js` como fuente.
  </action>
  <acceptance_criteria>
    - `grep -c "enqueueOutbox" lib/data/datasources/local/geo_path_local_data_source.dart` >= 1 pero NO aparece dentro del cuerpo de `saveProgress`
    - `grep -rEn "'id': id|audio_asset_id|geo_path_id|region_id" lib/data/datasources/local/ --include=*.dart --exclude=app_database.dart | wc -l` devuelve 0 — `app_database.dart` queda EXCLUIDO porque conserva los bloques `oldVersion < N` históricos que nombran esas columnas (marcados `// legacy-upgrade` en el plan 02.1-06); esos bloques son el camino que lleva una base vieja hasta v6 y no se tocan
    - `grep -rEn "'audio_assets'|'geo_paths'|'geo_triggers'" lib/data/datasources/local/ --include=*.dart --exclude=app_database.dart | wc -l` devuelve 0
    - `grep -rnE "\bint id\b|audioAssetId|geoPathId" lib/data/repositories` devuelve 0
    - `flutter test test/data/datasources` pasa
    - `flutter analyze lib/domain/entities lib/domain/repositories lib/domain/usecases/run_sync_usecase.dart lib/data lib/core/di` → 0 errores
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze --no-fatal-warnings lib/domain/entities lib/domain/repositories lib/domain/usecases/run_sync_usecase.dart lib/data lib/core/di &amp;&amp; flutter test test/data/datasources</automated>
  </verify>
  <done>Los data sources y los repositorios hablan uuid sobre las tablas nuevas, el progreso no encola nada y ningún payload lleva columnas que el backend rechace.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: esquema activo v7, migración cableada y guarda D-25 probada a través de init()</name>
  <files>lib/data/datasources/local/app_database.dart, lib/data/datasources/local/seed_data.dart, test/data/migration/app_database_v7_test.dart</files>
  <read_first>
    - lib/data/datasources/local/app_database.dart (`init()`, el `catch (e)` genérico que llama a `renameCorruptDatabase`, `_onCreate`, `_onUpgrade`, `getDatabaseStats`)
    - lib/data/migration/v7_schema.dart, v7_migration.dart y db_backup.dart (lo que hay que cablear; `MigrationException` ya tiene el campo `cause`)
    - test/support/v6_fixture.dart (`buildV6`, `seedSynthetic`, `addDuplicateUuid`)
    - lib/data/datasources/local/seed_data.dart (el seed actual, ya sin regiones tras el plan 02.1-06)
  </read_first>
  <behavior>
    - Abrir con `init(dbPath: ..., factory: databaseFactoryFfi)` una base v6 sembrada deja `user_version` 7, las tablas nuevas pobladas y un archivo `*.pre-v7-*` junto a la base; `lastBackupPath` apunta a él
    - **Guarda D-25, caso discrepancia:** si la verificación de `migrateToV7` falla, `init()` lanza `MigrationException`, NO existe ningún archivo `*.corrupto-*` junto a la base, y al reabrirla sin `version:` sigue en `user_version` 6 con `audio_assets`/`geo_paths`/`geo_triggers`/`regions` completas
    - **Guarda D-25, caso error crudo de SQL:** con una base v6 preparada con `addDuplicateUuid` (la migración choca contra la PK y `migrateToV7` lanza una `DatabaseException`, NO una `MigrationException`), `init()` igual lanza **`MigrationException`**, con `cause` no nulo; NO existe ningún archivo `*.corrupto-*`; la base original sigue abriendo en `user_version` 6 con sus cuatro tablas completas. Este es el caso que existe porque el `catch (e)` genérico de hoy renombraría una base SANA para crear una vacía
    - Una instalación limpia (archivo inexistente) crea directamente el esquema v7, corre el seed, no migra y no deja respaldo (`lastBackupPath == null`)
    - Abrir dos veces seguidas la misma base ya migrada no vuelve a respaldar ni a migrar
  </behavior>
  <action>
1. **Costura de test en `init()`.** Hoy `init()` llama a `getApplicationDocumentsDirectory()` (plugin: no existe en `flutter test`) y al `openDatabase` global de sqflite: los casos del bloque `<behavior>` son imposibles de escribir. Firma nueva, con los dos parámetros opcionales y el comportamiento por defecto IDÉNTICO al de hoy:
```dart
Future<void> init({String? dbPath, DatabaseFactory? factory}) async {
  if (_database != null) return;
  final f = factory ?? databaseFactory;                       // global de sqflite
  // `??` evalúa la derecha solo si dbPath es null: en tests no se toca el plugin.
  final path = dbPath ?? p.join((await getApplicationDocumentsDirectory()).path, _dbName);
  ...
}
```
   Todas las aperturas del método pasan por `f.openDatabase(path, options: OpenDatabaseOptions(version: _dbVersion, onCreate: _onCreate, onUpgrade: _onUpgrade))`, y `backupBeforeMigration(path, DateTime.now(), factory: f)` recibe la misma factory. Ningún llamador de producción cambia: `AppInitializer` sigue llamando a `init()` sin argumentos.

2. **Orden exacto de `init()`** — es lo que evita el peor escenario de la fase:
```dart
try {
  lastBackupPath = await backupBeforeMigration(path, DateTime.now(), factory: f);  // antes de abrir en v7
  _database = await f.openDatabase(path, options: OpenDatabaseOptions(
      version: _dbVersion, onCreate: _onCreate, onUpgrade: _onUpgrade));
} on MigrationException {
  rethrow;            // NO entra al camino de "BD corrupta"
} on BackupFailedException {
  rethrow;            // sin respaldo no se migra
} catch (e) {
  ... el camino actual de renombrar-y-arrancar-en-limpio, intacto ...
}
```

3. **El envoltorio de `_onUpgrade` (D-25, lo que hace que el orden de los `catch` sirva de algo).** Atrapar `MigrationException` no alcanza: `migrateToV7` lanza de todo — violación de PK por uuid duplicado (`DatabaseException`), error de sintaxis SQL, disco lleno. Cualquiera de esas, si sale cruda, cae en el `catch (e)` genérico y renombra una base sana. Por eso el fallo se normaliza en el punto donde ocurre:
```dart
Future<void> _onUpgrade(Database db, int oldVersion, int newVersion) async {
  ... los bloques oldVersion < 2..6 tal como están hoy ...   // legacy-upgrade: no tocar
  if (oldVersion < 7) {
    try {
      await migrateToV7(db);
    } on MigrationException {
      rethrow;                                   // ya viene normalizada
    } catch (e) {
      // D-25: CUALQUIER otro fallo tiene que llegar a init() como MigrationException.
      // Si sale crudo, el catch genérico lo trata como "BD corrupta", renombra una base
      // SANA y arranca vacía: exactamente el escenario que esta fase existe para evitar.
      throw MigrationException('Falló la migración v6→v7', cause: e);
    }
  }
}
```
   NO envolver `_onUpgrade` en `db.transaction()`: sqflite ya lo corre dentro de una transacción exclusiva, y esa transacción es la que revierte todo cuando esto lanza.

4. **Resto de `app_database.dart`**: `_dbVersion = 7`; `_onCreate` pasa a `await createV7Tables(db); await SeedData.seed(db);` (borrar el DDL viejo entero; el esquema vive en `v7_schema.dart`); conservar los bloques `oldVersion < 2..6` tal como están (una base muy vieja tiene que llegar a v6 antes de entrar a v7) con su comentario `// legacy-upgrade: no tocar`; campo público nuevo `String? lastBackupPath` junto al `lastRecoveryEvent` que ya existe; `getDatabaseStats()` cuenta `audios`, `obras`, `paths`, `triggers`; `exportDatabase()` queda igual.

5. **`seed_data.dart`**: reescribir a la forma nueva, mínimo (D-32): 2 filas de `audios` (los mismos títulos y `duration_seconds` de hoy) con uuid literales fijos escritos en el archivo, sus 2 filas de `audio_local` con los mismos `local_path` de hoy, 1 `obras` draft, 1 `paths` (`kind='route'`, apuntando a esa obra y a un audio) y 1 `triggers` con el mismo lat/lon/radio de hoy y `position: 0`. Los uuid del seed son literales constantes (no generados) para que una instalación limpia y un celular migrado nunca choquen. Sin `enqueueOutbox` (el seed corre dentro de `_onCreate`, igual que hoy).

6. **`test/data/migration/app_database_v7_test.dart`** (nuevo): cubre el bloque `<behavior>` ENTERO a través de `AppDatabase.init(dbPath: ..., factory: databaseFactoryFfi)`, cada caso en su propio `Directory.systemTemp.createTempSync`. Para el caso de discrepancia, usar el mismo hook `@visibleForTesting` que ya expone `v7_migration.dart` (plan 02.1-03). Para el caso de error crudo, sembrar con `addDuplicateUuid` y afirmar las tres cosas juntas:
```dart
await expectLater(() => AppDatabase().init(dbPath: path, factory: f),
                  throwsA(isA<MigrationException>()));
expect(Directory(dir).listSync().where((f) => f.path.contains('.corrupto-')), isEmpty);
final reopened = await f.openDatabase(path);          // sin version: no migra
expect(await reopened.getVersion(), 6);
expect(Sqflite.firstIntValue(await reopened.rawQuery('SELECT COUNT(*) FROM geo_triggers')), 459);
```
  </action>
  <acceptance_criteria>
    - `grep -c "_dbVersion = 7" lib/data/datasources/local/app_database.dart` == 1
    - `grep -c "backupBeforeMigration" lib/data/datasources/local/app_database.dart` == 1
    - `grep -c "Future<void> init({String? dbPath, DatabaseFactory? factory})" lib/data/datasources/local/app_database.dart` == 1
    - `grep -n "on MigrationException" lib/data/datasources/local/app_database.dart` da al menos una línea ANTERIOR a la del `catch (e)` genérico (comparar números de línea)
    - `grep -c "throw MigrationException(" lib/data/datasources/local/app_database.dart` >= 1 y está dentro del `catch` que envuelve a `migrateToV7` — **este es el criterio que de verdad prueba D-25**: sin él, `on MigrationException` antes de `catch (e)` no atrapa nada, porque la migración casi nunca falla con esa clase
    - `grep -c "cause" lib/data/datasources/local/app_database.dart` >= 1 (la excepción original no se pierde)
    - `grep -cE "addDuplicateUuid|\.corrupto-" test/data/migration/app_database_v7_test.dart` >= 2 (el caso de error crudo existe y aserta que NO hubo renombre)
    - `flutter test test/data` pasa
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/data</automated>
  </verify>
  <done>La base activa es v7, la migración corre sola con respaldo previo, y CUALQUIER fallo de migración llega a `init()` como `MigrationException` sin que la base sana se renombre — probado a través de `init()`, no del atajo de llamar a `migrateToV7` a mano.</done>
</task>

</tasks>

<verification>
- `flutter analyze lib/domain/entities lib/domain/repositories lib/domain/usecases/run_sync_usecase.dart lib/data lib/core/di` → 0 errores.
- `flutter test test/data` → verde.
- `grep -rnE "audio_asset_id|geo_path_id|region_id|'audio_assets'|'geo_paths'|'geo_triggers'" lib/data lib/domain/entities lib/domain/repositories lib/core/di --include=*.dart | grep -vE "lib/data/migration/|lib/data/datasources/local/app_database\.dart"` → vacío (MODEL-01; `app_database.dart` excluido por su camino de upgrade histórico).
- `grep -rn "enqueueOutbox" lib/data/datasources/local/geo_path_local_data_source.dart` → ninguna aparición dentro de `saveProgress` (MODEL-04).
- `cd backend && npm test` sigue verde (no se tocó el backend).
- **Rojo esperado y aceptado al cerrar este plan** (lo levanta el 02.1-09, único plan de la wave 5): `lib/domain/usecases/`, `lib/core/services/`, `lib/presentation/`, `lib/main.dart` y los 5 tests listados en el `<objective>`. `flutter test` completo NO es una compuerta de este plan.
</verification>

<success_criteria>
1. `flutter test test/data` verde con la capa de datos entera sobre uuid.
2. Abrir una base v6 de prueba a través de `init()` deja un respaldo `*.pre-v7-*` y la base en v7 con los datos migrados.
3. Un fallo de migración —de verificación O de SQL crudo— llega como `MigrationException` y NO renombra la base sana, probado a través de `init()`.
4. `saveProgress` no agrega filas al outbox.
5. `init()` conserva su comportamiento de producción: sin argumentos, misma ruta y misma factory que hoy.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/02.1-modelo-de-datos-objetivo/02.1-08-SUMMARY.md`.
</output>
