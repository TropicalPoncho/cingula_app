# 02.1 Neon rehearsal (plan 02.1-07 Task 3)

Date: 2026-09-21. Branch: `dev-2.1` (throwaway, created from main). Host: `ep-lucky-queen-...-pooler.c-12.us-east-1.aws.neon.tech/neondb`. Credentials intentionally not recorded.

## 1. `npm test` with DATABASE_URL

Result: 36 tests, 36 pass, 0 fail, **0 skipped**. The Postgres-backed suite (`push tipado contra Neon`, 4 subtests incl. tombstone and state cursor) ran for real. Closes the sampling gap on entity_prev / deferred FKs / change_seq / idempotent upsert.

## 2. `node scripts/migrate_v2.mjs` (dry-run)

Result: FAILED (exit 1), by design. Output:

```
Conectado a: ep-lucky-queen-...-pooler.c-12.us-east-1.aws.neon.tech/neondb
ABORTA: ids ambiguos:
  geo_triggers:42 -> 65178dbc-..., 8eccb15c-..., 132713e6-...
  geo_paths:11    -> 4a9b90f1-..., e328a289-..., 38cce6ef-...
  geo_triggers:43 -> 4371c0b3-..., b09e32ea-..., 162b6aa8-...
  geo_paths:12    -> b5904bb3-..., 83a03a38-..., 28a622de-...
```

Meaning: real data has SQLite integer ids reused across different uuids (4 (table, id) pairs, 3 uuids each). The script refused to guess, as the plan requires. The report of source/target counts was not printed because the abort happens first.

## 3-4. `--apply` (x2)

NOT RUN. Blocked by the ambiguity abort above; running apply would not have written anything anyway, and no workaround was attempted.

## 5. `npm test` after migration

NOT RUN (nothing migrated). Step 1 already proves the suite is green on this branch.

## Follow-up

The ambiguity policy needs a decision before plan 02.1-12 (cutover): e.g. when one (table, id) maps to several uuids, resolve by latest `current_updated_at` / by which uuid actually is referenced by other payloads (`geo_path_id`, trigger parents), or resolve the reference per-payload instead of via a global id map. Not fixed here (architectural / semantic choice).

---

## Remediation re-run — 2026-09-21 (same session, same `dev-2.1` branch)

The user inspected the read-only phone DB copy (`C:\Users\ramac\cingula-db-copia\cingula.db`) and confirmed, for each of the 4 ambiguous keys, which uuid is the live one:

- `geo_paths:11` -> `38cce6ef-f311-4b79-abae-802a8eb76fb4` ("prueba en casa")
- `geo_paths:12` -> `28a622de-14fe-4d6f-a5ee-f15d4588abe0` ("prueba chelenko")
- `geo_triggers:42` -> `132713e6-0e9d-45c9-b786-a6176058594d`
- `geo_triggers:43` -> `162b6aa8-8025-477a-a708-bdb2c34c2787`

`buildIdMap`/`translate` now accept an explicit `{ 'table:id': uuid }` override map (`backend/scripts/id_overrides.json`, checked in). An ambiguous key WITH an override resolves to that uuid; the other live uuids for that key are excluded from the typed-table migration and reported in `skipped` with reason `id ambiguo: uuid descartado por override, ver <table>:<id>`. An ambiguous key WITHOUT an override still throws (fail-safe, unchanged behavior) — covered by tests.

### a. `npm test` with DATABASE_URL

38 tests, 38 pass, 0 fail, **0 skipped** (2 new pure tests added for the override behavior; the Postgres-backed suite is unchanged and still green).

### b. `node scripts/migrate_v2.mjs` (dry-run)

Result: the ambiguous-id abort is GONE. Output (redacted host):

```
Conectado a: ep-lucky-queen-...-pooler.c-12.us-east-1.aws.neon.tech/neondb

Origen (por tabla):
  geo_triggers: 440 vivos, 1 con tombstone
  audio_assets: 68 vivos, 0 con tombstone
  geo_paths: 76 vivos, 1 con tombstone
  regions: 11 vivos, 0 con tombstone
Destino (por tabla):
  audios: 68
  artistas: 0
  obras: 77
  obra_artistas: 0
  paths: 77
  triggers: 441
Omitidos: 8
  geo_triggers <uuid>: id ambiguo: uuid descartado por override, ver geo_triggers:42
  geo_triggers <uuid>: id ambiguo: uuid descartado por override, ver geo_triggers:42
  geo_paths <uuid>: id ambiguo: uuid descartado por override, ver geo_paths:11
  geo_paths <uuid>: id ambiguo: uuid descartado por override, ver geo_paths:11
  geo_triggers <uuid>: id ambiguo: uuid descartado por override, ver geo_triggers:43
  geo_triggers <uuid>: id ambiguo: uuid descartado por override, ver geo_triggers:43
  geo_paths <uuid>: id ambiguo: uuid descartado por override, ver geo_paths:12
  geo_paths <uuid>: id ambiguo: uuid descartado por override, ver geo_paths:12
Advertencias: 0
```

The 8 ghost uuids show up exactly as expected, and the counts look sane (77 obras/paths matches the ~626-entity context; 68 audios; 441 triggers = 440 live + 1 orphan-portal... consistent with prior runs).

**But a second, unrelated abort immediately follows**, from the same-file `validateOutboxItem` pass (which runs before `--apply` is even considered):

```
INVALIDA audios <uuid>: missing required column: title   (x63)
INVALIDA obras <uuid>: missing required column: name      (x27)
INVALIDA paths <uuid>: missing required column: name       (x27)
```

117 rows total fail validation. **This is a new, separate blocker — not the ambiguous-id issue and not fixed by this remediation.**

### Root cause (confirmed by direct query on the same branch, not guessed)

Both `current_payload` AND `previous_payload` for these rows lack `title`/`name` entirely (not null-valued, absent). Example (`audio_assets` uuid `eb3bfeb5-...`, `logical_version` 2):

```
current_payload:  { id, uuid, updated_at, logical_version, duration_seconds }
previous_payload: { id, uuid, title: "quinto escalera", artist, ..., logical_version: 1, duration_seconds: 0 }
```

Direct count: only **5 of 68** `audio_assets` rows in `synced_entities` carry a `title` in `current_payload` at all. For rows edited many times (e.g. `saved_offset_ms`/`duration_seconds`-only pushes from the old v6 client, which apparently sent partial rather than full-row payloads for updates), the base fields (`title`, `name`) fell out of `current_payload` generations ago, and the server's "current + 1 previous version" retention (SYNC-06/MODEL-03) already pruned any version that still had them. The name/title is **not recoverable from `synced_entities`** — only from the phone's local DB, which still holds the full, correct row.

This matters for the cutover design in 02.1-RESEARCH.md (line ~287): the planned full resend after migration is a **same-version no-op** for entities untouched during the no-sync window (`upsertStatement`'s guard is strict `<`, not `<=`). So if this script wrote null/placeholder names now, the phone's future full resend would **not** self-heal them — they'd stay broken permanently unless the resend bumps versions or the migration script is told the real values first.

**Not fixed here** — this needs a decision from the data owner (options include: read the true values from the phone DB copy the same way the 4 id overrides were resolved, and add them as content overrides; or skip these ~117 entities from the typed-table migration and bump their version during the 02.1-12 full resend so the phone's real values do land; or something else). Flagging for 02.1-12 planning, same as the ambiguous-id issue was flagged after Task 3's first rehearsal.

### c/d. `--apply` (x2)

NOT RUN. Blocked by the validation abort above — running `--apply` would not have written anything (validation happens before the write step), so nothing was attempted or worked around.

### e. `npm test` after migration

NOT RUN (nothing migrated). Step (a) above already re-confirms the suite is green on this branch with 0 skipped.

### Status

MODEL-06 remains **not** complete. The 4-key ambiguity from Task 3's original rehearsal is resolved and verified (step b confirms it). A second, larger, and more serious data-completeness issue was discovered one abort layer down and is unresolved. `dev-2.1` should still be deleted by the user (see reminder below) — this rehearsal does not need to keep it alive; the next real attempt (whenever the title/name issue is resolved) should use a fresh branch per plan 02.1-12.
