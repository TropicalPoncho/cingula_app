---
phase: 02.1
plan: 01
subsystem: migration-test-infra
tags: [sqflite_common_ffi, uuid-v5, cover, preflight, test-infra]
requires: []
provides:
  - sqflite_common_ffi dev dependency (SQLite in flutter test)
  - test/support/v6_fixture.dart (frozen v6 DDL, synthetic dataset, edge-case builders)
  - ids (uuid v5) and cover helpers, identical in Dart and Node
  - preflight() read-only report
affects: [02.1-03, 02.1-05, 02.1-07]
tech-stack:
  added: [sqflite_common_ffi (dev)]
  patterns: [shared cross-language vectors asserted literally in both test suites]
key-files:
  created:
    - test/support/v6_fixture.dart
    - lib/data/migration/ids.dart
    - lib/data/migration/cover.dart
    - lib/data/migration/preflight.dart
    - backend/api/_lib/ids.js
    - backend/api/_lib/cover.js
    - backend/api/_lib/vectors.test.js
    - test/data/migration/vectors_test.dart
    - test/data/migration/preflight_test.dart
  modified: [pubspec.yaml, pubspec.lock, .gitignore]
decisions:
  - Node uuid v5 implemented with node:crypto (no new backend dependency)
  - Synthetic dataset: 444 triggers with path (4 carry region_id) + 15 standalone = 459, so standaloneTriggers == 15
metrics:
  tasks: 3
  completed: 2026-09-20
---

# Phase 02.1 Plan 01: Migration test infra and shared helpers Summary

SQLite-in-`flutter test` via sqflite_common_ffi, a frozen v6 fixture with deterministic 77/70/459/20 dataset, uuid v5 and cover helpers with cross-language vectors (Dart == Node), and a read-only `preflight()` report.

## Commits
- 1bd6582: sqflite_common_ffi, v6 fixture, .gitignore (*.db, .local-db/)
- c0fd4b0: ids/cover helpers in Dart and Node with shared vectors
- 9931979: preflight report and tests

## Verification
- `flutter test`: 69/69 pass; `node --test` backend: 19 pass, 0 fail.
- `flutter analyze`: 1 pre-existing info (WillPopScope), unrelated.

## Deviations from Plan
None functionally. Interpretation note: the plan's "440 path triggers + 15 loose + 4 region" totals 459 only if the 4 region triggers are extra; I made them path triggers (444 with path total) to keep standalone == 15 as the success criteria state.

## Ponytail audit
No abstractions added beyond the plan; Node v5 reuses stdlib. Nothing to trim.

## Known Stubs
None.

## Self-Check: PASSED
